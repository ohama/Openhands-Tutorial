---
phase: 14-appendix-d-chapter-publish
plan: 14-02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "src/SUMMARY.md has exactly one new line wiring 부록 D after the 부록 C entry, in the unnested appendix format (no leading dash)"
    - "mdbook build completes with exit code 0 and only the pre-existing TD-4 <char> warning from appendix-c-comparison.md"
    - "book/appendix-d-planning-comparison.html exists after the build"
    - "Existing 1부–7부 chapters and 부록 A/B/C are not regressed — their HTML outputs still present and appendix-c-comparison.md source is byte-unchanged"
    - "appendix-d appears in the freshly-built sidebar toc-{hash}.js"
    - "The chapter file + SUMMARY.md change are committed (deploy.yml NOT touched)"
  artifacts:
    - path: "src/SUMMARY.md"
      provides: "Book table of contents with 부록 D wired after 부록 C"
      contains: "appendix-d-planning-comparison.md"
    - path: "book/appendix-d-planning-comparison.html"
      provides: "Built HTML for the new chapter (proves SUMMARY wiring + filename are correct)"
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/appendix-d-planning-comparison.md"
      via: "TOC link [부록 D: ...](appendix-d-planning-comparison.md) added after 부록 C"
      pattern: "appendix-d-planning-comparison\\.md"
    - from: "mdbook build"
      to: "book/appendix-d-planning-comparison.html + book/toc-{hash}.js"
      via: "html backend renders the wired chapter and regenerates the sidebar TOC"
      pattern: "appendix-d"
---

<objective>
Wire 부록 D into `src/SUMMARY.md` (one-line append after 부록 C), run a clean `mdbook build`, confirm the new chapter builds with no new warnings and no regression to existing content, then commit the chapter file (from 14-01) plus the SUMMARY.md edit. This plan does NOT push or deploy (that is plan 14-03).

Purpose: Satisfies DPUB-01 — the book builds clean with 부록 D included and existing 1부–7부 + 부록 A/B/C un-regressed.
Output: Updated src/SUMMARY.md, a clean local build under book/, and a commit containing only the chapter + SUMMARY change.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-appendix-d-chapter-publish/14-RESEARCH.md
@src/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 부록 D into src/SUMMARY.md</name>
  <files>src/SUMMARY.md</files>
  <action>
Add EXACTLY ONE line to `src/SUMMARY.md`, immediately after the existing 부록 C entry, using the unnested appendix format (no leading `-`, matching the existing 부록 A/B/C lines):

```
[부록 D: 계획 방식 비교 — Claude 계획 vs OpenHands 자체 계획](appendix-d-planning-comparison.md)
```

After the edit, the last four appendix lines must read:
```
[부록 A: 재현 가이드](appendix-a-repro.md)
[부록 B: 트러블슈팅](appendix-b-troubleshooting.md)
[부록 C: 모델 비교 — 35B vs 122B](appendix-c-comparison.md)
[부록 D: 계획 방식 비교 — Claude 계획 vs OpenHands 자체 계획](appendix-d-planning-comparison.md)
```

Do NOT change any other line in SUMMARY.md. Do NOT nest 부록 D under another entry. The link target filename must EXACTLY match the file created in 14-01 (`appendix-d-planning-comparison.md`).
  </action>
  <verify>`tail -4 src/SUMMARY.md` shows 부록 A/B/C/D in order; `git diff src/SUMMARY.md` shows exactly one added line and no other changes.</verify>
  <done>SUMMARY.md wires 부록 D after 부록 C with a single unnested line pointing at appendix-d-planning-comparison.md; no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Run mdbook build and assert clean output + no regression</name>
  <files></files>
  <action>
From `/Users/ohama/projs/OpenHandsTests/`, run `mdbook build` (mdbook v0.5.3 at /opt/homebrew/bin/mdbook).

Assert ALL of the following:
1. Exit code 0.
2. The ONLY warning is the pre-existing TD-4 `<char>` warning from `appendix-c-comparison.md`:
   `WARN unclosed HTML tag \`<char>\` found in \`appendix-c-comparison.md\``.
   Any NEW warning — especially a broken-link or missing-file warning mentioning `appendix-d-planning-comparison` — means the SUMMARY wiring or filename is wrong. Fix and rebuild.
3. `book/appendix-d-planning-comparison.html` exists:
   `test -f book/appendix-d-planning-comparison.html`
4. The sidebar TOC includes appendix-d (the toc-{hash}.js hash changes after the SUMMARY.md edit):
   `grep -l "appendix-d" book/toc-*.js`
5. No regression — these existing outputs still present:
   `book/ch01-agentic-ai/ book/ch02-openhands/ book/ch03-setup/ book/ch04-calculator/ book/ch05-wrap-up/ book/ch06-rust-server/ book/ch07-scala-calc/ book/appendix-a-repro.html book/appendix-b-troubleshooting.html book/appendix-c-comparison.html`
6. The 부록 C source is byte-unchanged: `git diff HEAD -- src/appendix-c-comparison.md` must be empty (the only src/ changes this milestone are the new appendix-d file and the one-line SUMMARY edit).
  </action>
  <verify>mdbook build exits 0 with only the `<char>` warning; book/appendix-d-planning-comparison.html exists; `grep -l "appendix-d" book/toc-*.js` finds it; all listed existing HTML outputs present; `git diff HEAD -- src/appendix-c-comparison.md` empty.</verify>
  <done>Clean build (exit 0, only TD-4 warning) with 부록 D present in book/ and in the sidebar TOC, and zero regression to existing chapters or 부록 C source. DPUB-01 satisfied locally.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the chapter + SUMMARY.md change</name>
  <files></files>
  <action>
Stage ONLY the two source files and commit. Do NOT stage book/ (it is built output, regenerated by the Actions deploy), and do NOT touch .github/workflows/deploy.yml.

```bash
git add src/appendix-d-planning-comparison.md src/SUMMARY.md
git status   # confirm ONLY these two files are staged
git commit -m "feat(14): add 부록 D planning comparison chapter from captured evidence

Arm A (Claude-authored plan) vs Arm B (35B self-plans) across F#/Rust/Scala
on the local 35B, n=3 per cell. Sourced verbatim from committed
captured-planning/ artifacts (CAPTURE-MANIFEST + comparison.json +
PLAN-COMPARISON-QUALITATIVE). Wired after 부록 C in SUMMARY.md.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

If on a non-default branch unexpectedly, note it — but `main` is the deploy branch here, matching the prior publish phases (5/7/9/11). Do NOT push in this plan (push + deploy is 14-03).
  </action>
  <verify>`git show --stat HEAD` lists exactly src/appendix-d-planning-comparison.md and src/SUMMARY.md; no book/ files and no deploy.yml in the commit; `git diff -- .github/workflows/deploy.yml` empty.</verify>
  <done>A single commit contains only the new chapter and the SUMMARY.md edit; deploy.yml untouched; ready for push in 14-03.</done>
</task>

</tasks>

<verification>
- src/SUMMARY.md: one new unnested line wiring 부록 D after 부록 C; no other changes.
- mdbook build: exit 0, only the TD-4 `<char>` warning; book/appendix-d-planning-comparison.html present; appendix-d in toc-{hash}.js.
- No regression: all 1부–7부 + 부록 A/B/C HTML present; appendix-c-comparison.md source byte-unchanged.
- Commit contains only the chapter + SUMMARY.md; deploy.yml unchanged.
</verification>

<success_criteria>
DPUB-01 satisfied: clean mdbook build with 부록 D wired and built, existing content un-regressed, and a focused commit (chapter + SUMMARY only) ready to push. deploy.yml byte-identical.
</success_criteria>

<output>
After completion, create .planning/phases/14-appendix-d-chapter-publish/14-02-SUMMARY.md
</output>
