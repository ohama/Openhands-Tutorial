Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee, or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor / str_replace tool — it errors in this setup (it requires a security_risk field that fails validation).

The directory is empty of any calculator project. Your task: scaffold the F# FsLexYacc calculator project.

Step 1: Create the project skeleton.

  cd /Users/ohama/projs/OpenHandsTests/oh-workdir
  dotnet new console -lang F# -o calc

This creates calc/ with calc.fsproj and Program.fs.

Step 2: Replace the entire contents of calc/calc.fsproj with the following exact content (use cat, tee, or any file-writing method — the important thing is the content is exactly as shown):

<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <FsYacc Include="Parser.fsy">
      <OtherFlags>--module Parser</OtherFlags>
    </FsYacc>
    <FsLex Include="Lexer.fsl">
      <OtherFlags>--unicode --module Lexer</OtherFlags>
    </FsLex>
  </ItemGroup>

  <!-- REQUIRED WORKAROUND for .NET 10 + FsLexYacc 11.3.0:
       fsyacc generates "# 0 """" line directives that F# 10 compiler rejects.
       Strip them with a post-generation sed step. -->
  <Target Name="FixLineDirectives" BeforeTargets="CoreCompile" DependsOnTargets="CallFsYacc;CallFsLex">
    <Exec Command="sed -i '' '/^# 0/d' Parser.fs" Condition="Exists('Parser.fs')" />
    <Exec Command="sed -i '' '/^# 0/d' Lexer.fs" Condition="Exists('Lexer.fs')" />
  </Target>

  <ItemGroup>
    <!-- Compile order matters in F#: Parser.fsi/.fs must precede Lexer.fs (Lexer opens Parser) -->
    <Compile Include="Parser.fsi" />
    <Compile Include="Parser.fs" />
    <Compile Include="Lexer.fs" />
    <Compile Include="Program.fs" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FsLexYacc" Version="11.3.0" />
  </ItemGroup>
</Project>

This .fsproj is pre-wired for FsLexYacc 11.3.0 on .NET 10. The FixLineDirectives target works around a known compiler-incompatibility in the files generated by fsyacc/fslex — use it exactly as given.

Step 3: Create an empty placeholder file for the parser source.

  cd calc
  touch Parser.fsy

Do NOT create Lexer.fsl — a later task (task 2) creates it with provided content. Leave Program.fs as-is for now (subsequent tasks will fill in Parser.fsy and Program.fs).

Step 4: Verify the scaffold looks correct.

  ls calc/
  cat calc/calc.fsproj

The directory should contain: calc.fsproj, Program.fs, Parser.fsy. (Lexer.fsl will be added in task 2.)

Note: FsLexYacc 11.3.0 is already in the local NuGet cache — dotnet restore will be fast and does NOT require network access. Do NOT attempt to build yet (the lexer and parser sources are empty placeholders); just set up the scaffold.
