---
phase: 12-harness-rust-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/claude-plan.md
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt
  - .planning/phases/12-harness-rust-pilot/task-prompts/CONTROL-BLOCK-rust.txt
  - .planning/phases/12-harness-rust-pilot/task-prompts/PROMPT-DIFF-rust.txt
  - .planning/phases/12-harness-rust-pilot/metrics_extractor.py
autonomous: true

must_haves:
  truths:
    - "The Arm A Rust prompt and Arm B Rust prompt share a byte-identical control block (goal + std-only + port 8080 + respond hello + curl canonical test); the ONLY difference is Arm A embeds Claude's numbered task plan and Arm B says 'plan your own steps using the task tracker' (METH-01)"
    - "A literal diff of the two prompt files (with the plan/self-plan block masked out) shows NO other differences, and that diff is saved to disk as evidence (METH-01, Pitfall 1)"
    - "metrics_extractor.py parses a JSONL and emits a metrics.json with TerminalAction count, event count, wall-clock, LLM-call-gap stats, error-fix cycles, AgentErrorEvent count, canonical curl_hello pass/fail, and a source=agent honesty_gate result, using 1-based event numbering (METH-03, Pitfall 8)"
    - "oh-workdir-planning/ is gitignored and arm-isolated empty workspaces exist for arm-a/rust and arm-b/rust (METH-02, Pitfall 6/9)"
  artifacts:
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt"
      provides: "Arm A single-session Rust prompt = control block + Claude's numbered task plan"
      min_lines: 20
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt"
      provides: "Arm B single-session Rust prompt = control block + self-plan-via-task-tracker instruction"
      min_lines: 15
    - path: ".planning/phases/12-harness-rust-pilot/task-prompts/PROMPT-DIFF-rust.txt"
      provides: "Saved literal diff proving only the plan input differs"
      min_lines: 1
    - path: ".planning/phases/12-harness-rust-pilot/metrics_extractor.py"
      provides: "JSONL -> metrics.json extractor per research ARCHITECTURE schema"
      min_lines: 120
  key_links:
    - from: "arm-a/oh-prompt.txt and arm-b/oh-goal-prompt.txt"
      to: "CONTROL-BLOCK-rust.txt"
      via: "both prompts are control block + (plan | self-plan); identical control block"
      pattern: "hello"
    - from: "metrics_extractor.py"
      to: "honesty_gate + canonical_tests.curl_hello"
      via: "source=agent gate over ActionEvents; curl observation -> pass/fail"
      pattern: "source.*agent"
---

<objective>
Build the Phase 12 harness for the Rust planning A/B pilot: the two symmetric prompt templates (Arm A = Claude's numbered plan embedded; Arm B = bare goal + self-plan-via-task-tracker), the mandatory side-by-side symmetry diff, the gitignored arm-isolated workspaces, and metrics_extractor.py. No live runs happen in this plan — this is preflight only.

Purpose: METH-01 (prompt symmetry) and METH-03 (metrics_extractor.py) are the foundation the whole study rests on. If the prompts are asymmetric or the extractor is wrong, every downstream capture is invalid. Pitfall 1 (prompt-wording asymmetry) is the single most dangerous failure mode and is killed here by a literal diff before any model is invoked.
Output: Two Rust prompt files sharing an identical control block, a saved symmetry diff, gitignored empty workspaces, and a validated metrics_extractor.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

# The proven v1.2 Rust goal + Claude's task decomposition that Arm A reuses (mechanical embed only):
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task1-scaffold.txt
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md
</context>

<honesty_constraints>
These bind every task in this plan and all Phase 12 plans:
- This plan writes prompt files and a python script ONLY. It runs NO openhands invocation and writes NO agent-authored source. Do not fabricate any run output.
- The Arm A conversion is MECHANICAL: each Claude v1.2 subtask (scaffold / write-server / build-test) becomes a numbered step with its text preserved in meaning; NO new detail, NO restructuring, NO clarifications beyond format normalization and single-session state-handoff notes (Pitfall 7). Do NOT paste scaffolded Rust source into the prompt (that would violate the unaided discipline).
- The `~14–32s/call` figure is a v1 PREDICTION and must never appear as a measurement anywhere.
- Do NOT modify .github/workflows/deploy.yml.
- Event numbering is 1-based everywhere (enumerate(events, start=1)); JSONL line N = event #N (Pitfall 8/11).
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — gitignore + arm-isolated empty workspaces</name>
  <files>.gitignore</files>
  <action>
Add `oh-workdir-planning/` to /Users/ohama/projs/OpenHandsTests/.gitignore (it already contains oh-workdir/, oh-workdir-122b/, oh-workdir-rust/, oh-workdir-scala/ — append the new entry in the same block, do not duplicate).

Then create the arm-isolated workspace directories and verify they are EMPTY (Pitfall 6/9 — workspace-state leakage between arms):
  mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust
  mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-b/rust
  ls -la /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust   # must be empty
  ls -la /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-b/rust   # must be empty

Also create the committed artifact tree skeleton so later plans have somewhere to write:
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/{logs,planning-artifact,final-source}
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/{logs,planning-artifact,final-source}
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/task-prompts

Confirm `oh-workdir-planning/` is gitignored:
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-planning/ && echo "GITIGNORED OK"
  </action>
  <verify>git -C /Users/ohama/projs/OpenHandsTests check-ignore -q oh-workdir-planning/ && echo PASS; test -d /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust && test -d /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-b/rust && [ -z "$(ls -A /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust)" ] && echo "WORKSPACES EMPTY OK"</verify>
  <done>oh-workdir-planning/ is gitignored; oh-workdir-planning/arm-a/rust and arm-b/rust exist and are empty; the captured-planning/rust artifact tree skeleton exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write both Rust prompts (identical control block) + mandatory symmetry diff</name>
  <files>.planning/phases/12-harness-rust-pilot/task-prompts/CONTROL-BLOCK-rust.txt, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/claude-plan.md, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt, .planning/phases/12-harness-rust-pilot/task-prompts/PROMPT-DIFF-rust.txt</files>
  <action>
Write all files with bash heredocs (`cat > FILE <<'EOF'` quoted). These are AUTHORED prompt files, not agent output.

STEP 1 — Write CONTROL-BLOCK-rust.txt. This is the shared block that appears BYTE-IDENTICAL in both arm prompts. It is the v1.2 Rust goal + constraints + canonical test, reframed for a single session. Content (preserve meaning from the v1.2 task2-server.txt + task3-buildtest.txt goal):
  - Working directory line: "Working directory: <WORKDIR>" where <WORKDIR> is a literal placeholder token both arms substitute (arm-a uses /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust, arm-b uses .../arm-b/rust). To keep the control block byte-identical for the diff, write the control block with the token `__WORKDIR__` and have each arm prompt substitute it; the diff in STEP 4 compares the control-block portion which is identical.
  - IMPORTANT bash-only constraint VERBATIM from v1.2: "Create and edit ALL files using ONLY bash shell commands (printf, tee, or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor / str_replace tool — it errors in this setup."
  - Goal: build a Rust HTTP server that listens on TCP port 8080 and responds to any HTTP request with the text `hello` followed by a newline, looping (does not exit after first request).
  - Constraints: Rust standard library ONLY, no external crates (Cargo.toml [dependencies] stays empty); port 8080.
  - Canonical test (must pass): `curl -s http://localhost:8080/` prints `hello` and a newline, curl exits 0.
  - Closing line VERBATIM in both arms: "Run everything in this one session. Do not stop until the canonical test passes or you have documented why it cannot be fixed. Report the EXACT curl output and exit code; an honest failure is more valuable than a fabricated success."

STEP 2 — Write claude-plan.md (Arm A INPUT, the mechanical record of Claude's decomposition). Three numbered tasks, meaning preserved from the v1.2 task1/task2/task3 prompts, single-session reframed:
  1. Scaffold: `cargo new rust-server`; confirm Cargo.toml + src/main.rs exist; do not modify yet.
  2. Write server: rewrite rust-server/src/main.rs to bind 8080, respond "hello\n" to any request, loop; std-only.
  3. Build + test: `cargo build` (fix compiler errors at hand, std-only, report each error+fix); run `cargo run &`, `sleep 2`, `curl -s http://localhost:8080/`, capture exact output + exit code; `kill %1`.
  NO scaffolded source code in this file (Pitfall 7 + unaided discipline).

STEP 3 — Write the two arm prompts:
  - arm-a/oh-prompt.txt = the control block (with __WORKDIR__ substituted to the arm-a path) + a section header "Complete the following numbered steps in order, in this one session:" + the three numbered steps from claude-plan.md (verbatim meaning). The numbered steps include single-session state-handoff phrasing where needed (e.g. "After Step 1 the rust-server/ directory exists; in Step 2 rewrite its src/main.rs") — this is the ONLY allowed addition and is format/handoff normalization, not new planning content.
  - arm-b/oh-goal-prompt.txt = the SAME control block (with __WORKDIR__ substituted to the arm-b path) + the single sentence: "Plan your own implementation steps using the task tracker, then execute each step. Do not ask for confirmation between steps." (this exact phrasing is chosen to nudge TaskTrackerAction emission — the open unknown 12-02 resolves).

STEP 4 — MANDATORY symmetry diff (METH-01, Pitfall 1). This is a REQUIRED, LOGGED step. Build two normalized temp copies in which (a) the __WORKDIR__ substitution is reverted to the token so the path difference is not counted, and (b) the plan-vs-self-plan block is removed from each, leaving only the control block. Then diff:
  # normalize: strip arm-specific workdir back to token, then remove the variable block
  # arm-a variable block = everything from "Complete the following numbered steps" to end
  # arm-b variable block = the single "Plan your own implementation steps..." line
  Produce the diff and SAVE it to task-prompts/PROMPT-DIFF-rust.txt with a header documenting: what was masked (the plan block and the workdir token) and the result. The control-block portions MUST diff clean (no differences). If the diff shows ANY non-plan, non-workdir difference, FIX the prompts until the control block diffs clean, and record the final clean diff. Write into PROMPT-DIFF-rust.txt the literal command used and its output, ending with a line "CONTROL-BLOCK SYMMETRY: PASS" only if clean.
  </action>
  <verify>test -s /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt && test -s /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt && grep -q "CONTROL-BLOCK SYMMETRY: PASS" /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/task-prompts/PROMPT-DIFF-rust.txt && grep -q "hello" /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/task-prompts/CONTROL-BLOCK-rust.txt && grep -q "task tracker" /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt</verify>
  <done>Both arm prompts exist; both contain the identical control block (curl hello canonical test, std-only, port 8080); Arm A embeds the 3 numbered steps; Arm B embeds the "plan your own steps using the task tracker" instruction; PROMPT-DIFF-rust.txt records the literal diff and ends with "CONTROL-BLOCK SYMMETRY: PASS"; no non-plan/non-workdir asymmetry remains.</done>
</task>

<task type="auto">
  <name>Task 3: Write and self-validate metrics_extractor.py</name>
  <files>.planning/phases/12-harness-rust-pilot/metrics_extractor.py</files>
  <action>
Write metrics_extractor.py to /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/metrics_extractor.py, implementing the schema in research/ARCHITECTURE.md (§Metrics Extractor) EXACTLY. Required behavior:
  - CLI args: --jsonl, --example (choices fsharp/rust/scala), --arm (choices arm-a/arm-b), --prompt-file, --out.
  - load_events: parse JSONL lines starting with "{".
  - 1-BASED EVENT NUMBERING: any event index emitted (error_fix_cycles error_event_index/fix_event_index, canonical_tests event_index, honesty offending event_index) uses 1-based positions (event #1 = first JSONL line). Use enumerate(events, start=1) — NOT 0-based (Pitfall 8/11). Note: the ARCHITECTURE.md reference snippet uses 0-based events.index(); you MUST convert all emitted indices to 1-based.
  - event_counts: total + per-kind counts.
  - terminal_actions: count + command excerpts where kind==ActionEvent, source==agent, tool_name=="terminal".
  - finish: has_finish_action (any action.kind==FinishAction), has_conversation_error, settled_by.
  - timing: first/last ts, wall_clock_seconds, llm_call_gaps_seconds (ObservationEvent.ts -> next ActionEvent.ts), avg/min/max gap.
  - error_fix_cycles: nonzero-exit ObservationEvent -> next agent ActionEvent pairs; nonzero_exit_count, cycle_count, cycles[] with 1-based indices.
  - canonical_tests: for example=="rust", curl_hello = {expected:"hello", actual, pass, event_index(1-based)} — scan ObservationEvents whose command contains "curl"; pass = ("hello" in content and exit_code==0). For fsharp/scala include the 2+3*4 / (2+3)*4 / 10-3-2 tests per the schema (so the extractor is reusable in Phase 13).
  - honesty_gate: result PASS/FAIL where every ActionEvent must have source==agent; offending_events list (1-based indices); total_action_events_checked. The initial source=user MessageEvent is NOT an ActionEvent so it is correctly NOT flagged (Pitfall 13).
  - usage detection (resolves open unknown #2 for 12-02): also scan events for a `usage` field (top-level, or under observation, or under an `llm_response`/`metadata` key) and emit a top-level field "usage_present": true|false plus "usage_sample": <first usage dict found or null>. This lets 12-02 record whether P3 token metrics are feasible without re-parsing.
  - task_tracker detection (resolves open unknown #1 for 12-02): emit "task_tracker_observation_count": <count of events where kind=="ObservationEvent" and (observation.kind=="TaskTrackerObservation" or any field/tool_name mentions "task_tracker"/"TaskTracker")> and "task_tracker_action_count" likewise for ActionEvents. This lets 12-02 record whether Arm B emitted TaskTrackerObservation events.
  - main(): write metrics.json (json.dump indent=2 ensure_ascii=False), print a summary including honesty gate, curl_hello pass/fail, usage_present, task_tracker_observation_count.

SELF-VALIDATE with a synthetic fixture (do NOT use any real run — none exists yet). Create /tmp/fixture-rust.jsonl with a few JSON lines exercising the code paths: one MessageEvent source=user; one ActionEvent source=agent tool_name=terminal action.kind=TerminalAction action.command="cargo build"; one ObservationEvent observation.exit_code=0; one ObservationEvent observation.command="curl -s http://localhost:8080/" observation.content=[{"type":"text","text":"hello\n"}] observation.exit_code=0; one ActionEvent action.kind=FinishAction source=agent; include ISO timestamps so timing computes. Run:
  python3 /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/metrics_extractor.py --jsonl /tmp/fixture-rust.jsonl --example rust --arm arm-a --prompt-file dummy.txt --out /tmp/fixture-metrics.json
Confirm /tmp/fixture-metrics.json has honesty_gate.result=="PASS", canonical_tests.curl_hello.pass==true, canonical_tests.curl_hello.event_index is an integer >=1 (1-based), usage_present==false, and task_tracker_observation_count==0. Also add a NEGATIVE check: a fixture line with an ActionEvent source=user must make honesty_gate.result=="FAIL". Print the fixture metrics so the validation is visible in the run log. Delete /tmp fixtures after (they are scratch).
  </action>
  <verify>cd /Users/ohama/projs/OpenHandsTests && python3 -c "import ast,sys; src=open('.planning/phases/12-harness-rust-pilot/metrics_extractor.py').read(); ast.parse(src); assert 'enumerate' in src and 'start=1' in src, 'must use 1-based enumerate'; assert 'curl' in src and 'hello' in src; assert 'usage_present' in src; assert 'task_tracker' in src.lower(); print('EXTRACTOR STATIC OK')"</verify>
  <done>metrics_extractor.py exists (>=120 lines), parses JSONL into the ARCHITECTURE.md schema with 1-based event indices, computes the source=agent honesty gate, rust curl_hello pass/fail, usage_present, and task_tracker_observation_count; the synthetic-fixture self-validation passed (PASS gate on clean fixture, FAIL gate on a source=user ActionEvent fixture) and was shown in the run log.</done>
</task>

</tasks>

<verification>
- `git -C /Users/ohama/projs/OpenHandsTests check-ignore -q oh-workdir-planning/` exits 0.
- oh-workdir-planning/arm-a/rust and arm-b/rust exist and are empty.
- The Arm A and Arm B Rust prompts both contain the identical control block; PROMPT-DIFF-rust.txt records the literal diff and ends "CONTROL-BLOCK SYMMETRY: PASS".
- metrics_extractor.py passes its synthetic-fixture self-validation (clean fixture -> gate PASS + curl_hello.pass=true; source=user ActionEvent fixture -> gate FAIL) and uses enumerate(start=1).
- No openhands invocation was run; no agent source was authored; deploy.yml untouched.
</verification>

<success_criteria>
METH-01 prompt symmetry foundation laid (both Rust prompts + saved literal diff proving only the plan input differs) and METH-03 metrics_extractor.py written and self-validated. Workspaces gitignored and empty. Ready for 12-02 to run both arms live.
</success_criteria>

<output>
After completion, create `.planning/phases/12-harness-rust-pilot/12-01-SUMMARY.md`.
</output>
