---
phase: 08-capture-the-35b-rust-http-server-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task1-scaffold.txt
  - .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  - .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server-scaffold.txt
  - .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt
  - .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md
  - .planning/phases/08-capture-the-35b-rust-http-server-run/08-01-PREFLIGHT.md
  - .gitignore
  - oh-workdir-rust/
autonomous: true

must_haves:
  truths:
    - "task2-server.txt instructs the agent to write src/main.rs HIMSELF — it contains NO Rust source body, NO mention of TcpListener/BufReader/read/write_all/accept/bind, NO HTTP framing details (status line, headers, CRLF)"
    - "task2-server.txt names ONLY the goal: port 8080, GET / returns the literal text 'hello' followed by a newline, server runs in an accept loop, std-only (no external crates)"
    - "task2-server-scaffold.txt (fallback only) contains the verbatim verified-working src/main.rs from 08-RESEARCH.md §5 — used ONLY if 35B fails per scaffold-fallback policy"
    - "All task prompts point at /Users/ohama/projs/OpenHandsTests/oh-workdir-rust and carry the bash-only file-write constraint (no file_editor / str_replace — errors due to security_risk field)"
    - "task1-scaffold.txt explicitly tells the agent to run `cargo new rust-server` (this is infrastructure setup, parallel to v1's `dotnet new console`; the agent runs it, satisfying RUN unaided-first criterion #2)"
    - "task3-buildtest.txt tells the agent to background `cargo run &` before curl, defends against the read_to_string hang trap (no mention of read_to_string itself — only 'if curl hangs, the server may be blocking on read; consider reading what's available rather than to EOF'), and demands honest reporting of the exact curl output"
    - "oh-workdir-rust/ exists, is empty, and is gitignored (git check-ignore returns oh-workdir-rust); v1's oh-workdir/ and v1.1's oh-workdir-122b/ remain ignored and untouched"
    - "Port 8080 is free on the host (lsof -ti :8080 returns empty); rustc/cargo 1.95.0 still on host; litellm proxy at 127.0.0.1:4000 responds and lists openai/qwen-35b"
  artifacts:
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task1-scaffold.txt"
      provides: "Scaffold prompt — agent runs `cargo new rust-server` itself in oh-workdir-rust/"
      contains: "cargo new rust-server"
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt"
      provides: "Unaided server prompt — goal only (port 8080, returns hello\\n, std-only), zero implementation hints"
      contains: "port 8080"
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server-scaffold.txt"
      provides: "Fallback server prompt — verbatim verified-working src/main.rs; used only on disclosed fallback"
      contains: "TcpListener::bind"
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt"
      provides: "Build + run + curl + report prompt; backgrounds cargo run; demands honest reporting"
      contains: "curl -s http://localhost:8080/"
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md"
      provides: "openhands headless invocation reference for qwen-35b + per-task JSONL filename table"
      contains: "openai/qwen-35b"
    - path: ".planning/phases/08-capture-the-35b-rust-http-server-run/08-01-PREFLIGHT.md"
      provides: "Live preflight evidence (rustc version, port 8080 free, proxy lists qwen-35b, gitignore confirmed)"
      contains: "PREFLIGHT GREEN"
    - path: ".gitignore"
      provides: "oh-workdir-rust/ ignore entry (alongside existing oh-workdir/ and oh-workdir-122b/)"
      contains: "oh-workdir-rust"
  key_links:
    - from: "task1-scaffold.txt cargo new command"
      to: "oh-workdir-rust/rust-server/ directory created by agent"
      via: "agent executes cargo new via bash TerminalAction"
      pattern: "cargo new rust-server"
    - from: "task2-server.txt goal description"
      to: "task3-buildtest.txt curl test"
      via: "agreed contract — server returns literal 'hello\\n' on port 8080"
      pattern: "hello"
    - from: "all task prompts"
      to: "oh-workdir-rust scratch dir"
      via: "Working directory line at top of each prompt"
      pattern: "oh-workdir-rust"
---

<objective>
Prepare the 35B Rust task-prompt set and run preflight so the execute plan (08-02) can launch a clean, honest capture of a minimal Rust HTTP server.

This plan does NOT run OpenHands. It only: (1) creates the `task-prompts-rust/` prompt files — task1 (scaffold via `cargo new`), task2 (write server unaided), task2-scaffold (fallback only), task3 (build + curl + report); (2) ensures `oh-workdir-rust/` exists, is empty, and is gitignored without touching the existing v1 / v1.1 scratch dirs; (3) confirms preflight: rustc/cargo still on host, port 8080 free, litellm proxy serves qwen-35b. Writes 08-01-PREFLIGHT.md as live evidence the launchpad is green.

Purpose: The unaided-first protocol is the milestone's whole point (RUST-01). task2-server.txt must hand the agent the GOAL (port 8080, GET / returns "hello\n") and NOTHING else — no `TcpListener`, no `BufReader`, no `read_to_string`/`read`/`write_all`, no HTTP framing details, no crate hints. If the prompt leaks the answer, the captured run is no longer a real test of 35B's unaided Rust capability and the chapter loses its honesty.

Output: 4 prompt files + 00-INVOCATION.md under `task-prompts-rust/`, a clean gitignored `oh-workdir-rust/`, a green preflight report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-capture-the-35b-rust-http-server-run/08-RESEARCH.md

# v1.1 prompts to mirror in shape (NOT content — Rust is a different language):
@.planning/milestones/v1.1-phases/06-capture-the-122b-openhands-run/task-prompts-122b/00-INVOCATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 task prompts (task1-scaffold, task2-server unaided, task2-server-scaffold fallback, task3-buildtest)</name>
  <files>
    .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task1-scaffold.txt
    .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
    .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server-scaffold.txt
    .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt
  </files>
  <action>
First create the directory:
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust

Write each prompt using a quoted heredoc (`cat > FILE <<'EOF' ... EOF`) so $variables and backslashes are preserved literally.

---

**task1-scaffold.txt** — agent runs `cargo new` itself (this is infrastructure setup; satisfies RUN unaided-first criterion #2 because the operator does NOT provide Cargo.toml or src/main.rs). Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-rust

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

The directory /Users/ohama/projs/OpenHandsTests/oh-workdir-rust is empty. Your task:
create a new Rust project here named rust-server.

  cd /Users/ohama/projs/OpenHandsTests/oh-workdir-rust
  cargo new rust-server

This creates rust-server/ with Cargo.toml and src/main.rs filled in with the default
template by cargo itself.

After running cargo new, confirm the project was created:
  ls rust-server/
  cat rust-server/Cargo.toml
  cat rust-server/src/main.rs

Do NOT modify any files yet — a later task fills in the source code.
```

Rationale: `cargo new` is infrastructure (parallel to v1's `dotnet new console`). The agent ITSELF runs it, so RUST-01's "agent runs cargo new" requirement is captured in a TerminalAction in task1's JSONL. The Cargo.toml will have no dependencies (std only by default).

---

**task2-server.txt** — THE UNAIDED PROMPT. This is the milestone's core test of whether 35B can write a Rust HTTP server from goal alone. Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-rust

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

A Rust project scaffold exists in rust-server/. Start by reviewing it:

  cd rust-server
  ls
  cat Cargo.toml
  cat src/main.rs

Your task: rewrite src/main.rs so that when the program runs, it listens on TCP
port 8080 and responds to any incoming HTTP request with the text:

  hello

followed by a newline character, then closes the connection.

Requirements:
- Port: 8080 (bind to 0.0.0.0:8080 or 127.0.0.1:8080 — either is fine)
- Response: the literal characters "hello" followed by a newline (\n)
- The program must continue running, accepting connections in a loop (not exit
  after the first request)
- Use ONLY the Rust standard library — no external crates (Cargo.toml stays as-is,
  with an empty [dependencies] section)

After writing src/main.rs, show its contents:
  cat src/main.rs

Do NOT build yet — a later task handles build and testing.
```

**CRITICAL — what task2-server.txt must NOT contain (fully unaided per the milestone's design):**
  - NO mention of `TcpListener`, `TcpStream`, `BufReader`, `accept`, `bind`, `read`, `read_to_string`, `write_all`, `incoming` — these name the implementation
  - NO mention of HTTP framing details: "status line", "Content-Length", "Connection: close", "\r\n", "headers", "RFC 7230" — leak format
  - NO mention of `unwrap()` vs `?` vs `Result<(), Box<dyn Error>>` — leak idiom
  - NO mention of borrow checker / ownership / move / reference — defensive hint = leak
  - NO mention of any external crate name (`hyper`, `axum`, `actix-web`, `tokio`) — even "do not use X" leaks that X exists
  - NO code sample, NO snippet, NO "here's the shape"

(The agent is EXPECTED to potentially hit one of the 35B Rust blind spots — BufReader-move E0382, read_to_string hang, missing Write import — and self-correct in task3. That self-correction IS the desired material for the 6부 chapter.)

---

**task2-server-scaffold.txt** — FALLBACK ONLY (used ONLY if the scaffold-fallback policy in 08-02 triggers; its mere use is a disclosed deviation in CAPTURE-MANIFEST). Use this exact body — it embeds the verified-working src/main.rs from 08-RESEARCH.md §5:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-rust

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool.

A Rust project scaffold exists in rust-server/. Replace its src/main.rs with the
following exact contents using a heredoc:

  cd rust-server
  cat > src/main.rs <<'RUST_EOF'
  use std::net::TcpListener;
  use std::io::{Read, Write};

  fn main() {
      let listener = TcpListener::bind("0.0.0.0:8080").unwrap();
      for stream in listener.incoming() {
          let mut stream = stream.unwrap();
          let mut buf = [0u8; 4096];
          let _ = stream.read(&mut buf);
          let response = "HTTP/1.1 200 OK\r\nContent-Length: 6\r\nConnection: close\r\n\r\nhello\n";
          stream.write_all(response.as_bytes()).unwrap();
      }
  }
  RUST_EOF

After writing, confirm the contents:
  cat src/main.rs

Do NOT modify Cargo.toml (no external crates). Do NOT build yet — a later task
handles build and testing.
```

(Note the heredoc inside a heredoc when the operator writes THIS file from the planner: outer is `'EOF'`, inner is `'RUST_EOF'`. The agent, when given THIS prompt, runs the inner heredoc as part of its own bash session.)

---

**task3-buildtest.txt** — build, run in background, curl, report honestly. Critical: this prompt must teach the background-process pattern (`cargo run &` then `sleep`) WITHOUT teaching the server implementation, and must softly defend against the `read_to_string` hang (08-RESEARCH.md Blind Spot 2) by saying "if curl hangs, the server may be blocking" — NOT by naming `read_to_string` itself. Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-rust

IMPORTANT: Create and edit ALL files using ONLY bash shell commands. Do NOT use
the file_editor tool.

A Rust HTTP server source exists in rust-server/src/main.rs. Your task: build it,
run it, test it with curl, and report the REAL outcome.

Step 1: Build.

  cd rust-server
  cargo build 2>&1

If the build fails, read the compiler error carefully, edit src/main.rs to fix the
error at hand (using heredoc/printf/tee — NOT file_editor), and rebuild. Repeat
until the build succeeds. Report each error you saw and what you changed to fix it.

Step 2: Run the server in the background.

  cargo run &
  sleep 2

(The `&` is REQUIRED — `cargo run` blocks the shell while the server accepts
connections, so it MUST run in the background or the next command never runs.
The `sleep 2` lets the server finish binding before curl tries to connect.)

Step 3: Test with curl and capture the exact output.

  curl -s http://localhost:8080/

Required behavior: curl prints `hello` and a newline (so the shell prompt appears
on the next line, not run together with "hello"). The curl command should exit 0.

If curl hangs (no output, no return) for more than ~5 seconds, the server is
probably blocking trying to read from the socket. Kill the server (`kill %1`),
adjust src/main.rs (consider whether the read operation might block waiting for
data that the client never sends), and re-run from Step 1.

Step 4: Kill the server.

  kill %1
  wait %1 2>/dev/null

Step 5: Report results explicitly.

State the EXACT output curl printed (verbatim — copy from the terminal), the curl
exit code (`echo $?`), and whether the required output ("hello\n") was produced.

Constraints:
- Do NOT modify Cargo.toml (no external crates).
- If you fix src/main.rs, fix only the error at hand — and report what you changed
  and WHY.
- Report every build error you encountered and how you fixed it — this is the
  point of the exercise.
- DO NOT modify results to appear successful. If the server did not respond, if
  curl returned an error, or if the output was wrong, state that clearly. An
  honest failure is more valuable than a fabricated success.
```

Rationale: tells the agent HOW to test (curl command, background process pattern, kill %1) but not HOW the server should be written. The "if curl hangs" hint softly defends against the read_to_string blind spot WITHOUT naming the API. "Do not modify results to appear successful" is the honesty rule (parallel to v1.1's "do not silently rewrite the grammar").
  </action>
  <verify>
Verify task2-server.txt has ZERO implementation leaks:
  cd /Users/ohama/projs/OpenHandsTests
  # All of these must print 0:
  grep -c -iE "TcpListener|TcpStream|BufReader|read_to_string|write_all|incoming|accept\(|bind\(" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  grep -c -iE "Content-Length|Connection: close|status line|RFC 7230|\\\\r\\\\n|CRLF" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  grep -c -iE "hyper|actix|tokio|axum" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  grep -c -iE "borrow checker|ownership|move|E0382" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt

# These must be >=1 (goal is stated):
  grep -c "8080" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  grep -c -iE "standard library|no external crates" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt
  grep -c "oh-workdir-rust" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server.txt

# Scaffold fallback DOES contain the impl (it's the verbatim working src/main.rs):
  grep -c "TcpListener::bind" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task2-server-scaffold.txt

# task1 has the cargo new instruction:
  grep -c "cargo new rust-server" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task1-scaffold.txt

# task3 has the background+curl pattern but NOT the implementation:
  grep -c "cargo run &" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt
  grep -c "curl -s http://localhost:8080/" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt
  grep -c -iE "TcpListener|read_to_string" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/task3-buildtest.txt   # must be 0
  </verify>
  <done>
4 prompt files exist in task-prompts-rust/. task2-server.txt names the goal (port 8080, hello\n, std-only) and has ZERO implementation leaks (no TcpListener, no HTTP framing, no crate names, no borrow-hints). task2-server-scaffold.txt has the verbatim working src/main.rs for fallback. task1-scaffold.txt tells the agent to run `cargo new rust-server` itself. task3-buildtest.txt teaches the background+curl pattern + honesty rule without leaking the server implementation. All prompts reference /Users/ohama/projs/OpenHandsTests/oh-workdir-rust and carry the bash-only file-write constraint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 00-INVOCATION.md (qwen-35b headless invocation reference + JSONL filename table)</name>
  <files>
    .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md
  </files>
  <action>
Write a fresh 00-INVOCATION.md (not a copy — the v1.1 version refers to qwen-122b and oh-workdir-122b). Use this exact body:

```
# 35B Rust Run — Invocation Reference

## Per-task command pattern

  OPENHANDS_SUPPRESS_BANNER=1 \
    LLM_MODEL="openai/qwen-35b" \
    LLM_BASE_URL="http://127.0.0.1:4000/v1" \
    LLM_API_KEY="dummy" \
    OPENHANDS_WORK_DIR="/Users/ohama/projs/OpenHandsTests/oh-workdir-rust" \
    openhands --headless --json --yolo --override-with-envs \
    -t "$(cat task-prompts-rust/<prompt-file>)" \
    2>oh-workdir-rust/<task>.stderr.log \
    | tee oh-workdir-rust/<task>.jsonl

## Key notes

- `--override-with-envs` is REQUIRED (configures the LLM from env vars).
- `--yolo` runs autonomously (no confirmation prompts).
- `--headless --json` emits JSONL events to stdout — captured by `tee` into a per-task .jsonl file.
- Each invocation is a fresh ZERO-MEMORY conversation: the agent does not remember
  prior tasks. Files persist on disk (so task2 can `cat` what task1 produced); the
  agent rediscovers state via `ls`/`cat`.
- Bash-only file writes are enforced via the per-prompt IMPORTANT note (file_editor
  errors in this OpenHands setup — it requires a security_risk field that fails
  validation).
- The CLI exits 0 REGARDLESS of task success — DO NOT judge outcome from exit code.
  Judge ONLY from the JSONL contents (FinishAction present, exit codes inside
  TerminalObservations, actual command output).
- Run each invocation in the BACKGROUND (run_in_background=true) and POLL the JSONL
  until the run settles (FinishAction event appears, or no new events for ~60s of
  sustained idle, or ConversationErrorEvent).
- 35B per-LLM-call timing on this hardware ≈ 5.3s (from v1 + v1.1 captured runs).
  Budget ~4–8 minutes per complex task; up to ~15 minutes if the agent gets stuck
  on borrow-checker iterations or runtime hangs. Full run estimate: 15–40 minutes.

## Per-task JSONL filenames

| Task | Prompt file | JSONL filename |
|------|-------------|----------------|
| task1-scaffold       | task-prompts-rust/task1-scaffold.txt       | oh-workdir-rust/task1-scaffold.jsonl |
| task2-server         | task-prompts-rust/task2-server.txt         | oh-workdir-rust/task2-server.jsonl |
| task2-server-scaffold (fallback only) | task-prompts-rust/task2-server-scaffold.txt | oh-workdir-rust/task2-server-scaffold.jsonl |
| task3-buildtest      | task-prompts-rust/task3-buildtest.txt      | oh-workdir-rust/task3-buildtest.jsonl |

Stderr is captured in parallel as oh-workdir-rust/<task>.stderr.log for each.

## Scaffold-fallback policy summary (full rules in 08-RESEARCH.md §5)

The scaffold (task2-server-scaffold.txt) is invoked ONLY if ALL the following are
true after the unaided task2 attempt:

1. The agent's cargo build (in task3) fails on the SAME root error class across
   3+ consecutive build attempts within task3 (e.g., always E0382 borrow conflict,
   or always the same syntax error).
2. The error variations show no syntactic progress — the agent is cycling on
   identical fix attempts with no new diagnostic reasoning.
3. The JSONL shows the agent is stuck (repeated identical commands).

Do NOT trigger scaffold fallback if:
- The agent is making diverse fix attempts (even if all failing) — that is genuine
  error-and-fix and is the desired chapter material.
- The build error changes between attempts — the agent is making progress.
- The agent hits a runtime hang (curl never returns) — give it one more attempt;
  task3 prompt already softly hints "if curl hangs, the server may be blocking".

If fallback IS triggered, that fact MUST be disclosed in CAPTURE-MANIFEST.md with
the failure reasons documented. did-write-server-unaided = NO.
```
  </action>
  <verify>
  cd /Users/ohama/projs/OpenHandsTests
  test -s .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md && echo "INVOCATION present"
  grep -c "openai/qwen-35b" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md   # >=1
  grep -c "oh-workdir-rust" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md   # >=4 (each task row)
  grep -c "scaffold" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md          # >=1
  grep -c "DO NOT judge outcome from exit code" .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md  # >=1
  # confirm directory listing has exactly 5 files (4 prompts + INVOCATION):
  ls .planning/phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/ | wc -l   # 5
  </verify>
  <done>
00-INVOCATION.md exists with the qwen-35b invocation pattern, polling guidance, JSONL filename table, and a summary of the scaffold-fallback policy. The task-prompts-rust/ directory contains exactly 5 files (task1-scaffold.txt, task2-server.txt, task2-server-scaffold.txt, task3-buildtest.txt, 00-INVOCATION.md).
  </done>
</task>

<task type="auto">
  <name>Task 3: Preflight — gitignore oh-workdir-rust, create clean scratch dir, confirm rustc/cargo/port/proxy; write 08-01-PREFLIGHT.md</name>
  <files>
    .gitignore
    oh-workdir-rust/
    .planning/phases/08-capture-the-35b-rust-http-server-run/08-01-PREFLIGHT.md
  </files>
  <action>
**STEP 1 — gitignore.** Append `oh-workdir-rust/` to /Users/ohama/projs/OpenHandsTests/.gitignore IF not already present. The existing entries (`book/`, `oh-workdir/`, `oh-workdir-122b/`) MUST remain untouched.

  cd /Users/ohama/projs/OpenHandsTests
  grep -q '^oh-workdir-rust/$' .gitignore || echo 'oh-workdir-rust/' >> .gitignore
  cat .gitignore   # confirm all 4 entries present: book/, oh-workdir/, oh-workdir-122b/, oh-workdir-rust/

**STEP 2 — scratch dir.** Create empty scratch dir. If it already exists, list contents; if any contents are stale capture artifacts from a prior aborted attempt, remove them (NEVER touch oh-workdir/ or oh-workdir-122b/).

  mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-rust
  ls -la /Users/ohama/projs/OpenHandsTests/oh-workdir-rust/
  # If anything other than . and .. is present and it looks like prior capture artifacts (rust-server/, *.jsonl, *.stderr.log), remove:
  #   rm -rf /Users/ohama/projs/OpenHandsTests/oh-workdir-rust/{rust-server,*.jsonl,*.stderr.log}
  # ONLY if those entries exist; do not blindly rm.

**STEP 3 — gitignore verification.** Confirm all 4 scratch dirs are ignored:

  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-rust    # must print: oh-workdir-rust
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-122b    # must print: oh-workdir-122b  (v1.1 still ignored)
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir         # must print: oh-workdir       (v1 still ignored)

**STEP 4 — toolchain check.** Confirm rustc/cargo 1.95.0 still on host:

  rustc --version    # must print: rustc 1.95.0 (...)  — minor patch differences acceptable; major.minor must be 1.95
  cargo --version    # must print: cargo 1.95.0 (...)

**STEP 5 — port check.** Confirm port 8080 is free:

  lsof -ti :8080 || echo "PORT_8080_FREE"   # must print PORT_8080_FREE (lsof returns nothing -> non-zero exit -> our echo fires)

If something is listening on 8080, identify it (`lsof -i :8080`) and HALT — 08-02 cannot run with the port occupied. Report what's holding it; do not auto-kill arbitrary processes.

**STEP 6 — proxy check.** Confirm litellm proxy serves qwen-35b:

  curl -s -m 5 http://127.0.0.1:4000/v1/models | grep -c "qwen-35b"   # must be >=1

If the curl times out or qwen-35b is absent, HALT — 08-02 cannot run without the proxy serving the model. Report the symptom; do not guess at fixes.

**STEP 7 — write 08-01-PREFLIGHT.md.** Capture every check above with its actual output. Use this structure:

```
# Phase 8 Preflight Report

Date: <date from `date -u +%FT%TZ`>

## Toolchain
- rustc: <output of rustc --version>
- cargo: <output of cargo --version>

## Port 8080
- lsof -ti :8080: <output or "(empty)">
- status: FREE | OCCUPIED

## Scratch dir
- /Users/ohama/projs/OpenHandsTests/oh-workdir-rust/ exists: YES/NO
- empty: YES/NO (or list contents)

## Gitignore
- oh-workdir-rust ignored: YES (git check-ignore output: <path>)
- oh-workdir-122b ignored: YES (...)
- oh-workdir ignored: YES (...)

## Proxy
- curl http://127.0.0.1:4000/v1/models response: <truncated or full>
- qwen-35b present: YES/NO

## Verdict
PREFLIGHT GREEN — 08-02 can launch.    # or RED with the reason
```

If any check fails, write `PREFLIGHT RED` with the specific failure and HALT (do not proceed; 08-02 depends on green preflight).
  </action>
  <verify>
  cd /Users/ohama/projs/OpenHandsTests

  # gitignore has all 4 entries:
  grep -c '^oh-workdir-rust/$' .gitignore       # 1
  grep -c '^oh-workdir-122b/$' .gitignore       # 1 (still there)
  grep -c '^oh-workdir/$' .gitignore            # 1 (still there)
  grep -c '^book/$' .gitignore                  # 1 (still there)

  # All 3 prior scratch dirs still ignored + new one ignored:
  git check-ignore oh-workdir-rust              # prints oh-workdir-rust
  git check-ignore oh-workdir-122b              # prints oh-workdir-122b
  git check-ignore oh-workdir                   # prints oh-workdir

  # Scratch dir exists:
  test -d oh-workdir-rust && echo "scratch dir ready"

  # Toolchain on path:
  rustc --version | grep -c "^rustc 1\."        # >=1
  cargo --version | grep -c "^cargo 1\."        # >=1

  # Port free:
  lsof -ti :8080 ; test $? -ne 0 && echo "PORT_FREE"   # prints PORT_FREE if nothing listening

  # Proxy live:
  curl -s -m 5 http://127.0.0.1:4000/v1/models | grep -c "qwen-35b"   # >=1

  # Preflight report exists and is GREEN:
  test -s .planning/phases/08-capture-the-35b-rust-http-server-run/08-01-PREFLIGHT.md && echo "PREFLIGHT report present"
  grep -c "PREFLIGHT GREEN" .planning/phases/08-capture-the-35b-rust-http-server-run/08-01-PREFLIGHT.md   # 1 (HALT if 0 — preflight failed)
  </verify>
  <done>
oh-workdir-rust/ exists, is empty (or only contains stale-artifacts-just-cleaned), and is gitignored. v1's oh-workdir/ and v1.1's oh-workdir-122b/ are still ignored and untouched (audit-verifiable by git check-ignore). rustc/cargo 1.95.x on host. Port 8080 free. litellm proxy responds and lists qwen-35b. 08-01-PREFLIGHT.md records all evidence with verdict PREFLIGHT GREEN. 08-02 may launch.
  </done>
</task>

</tasks>

<verification>
- task-prompts-rust/ contains exactly 5 files (task1-scaffold.txt, task2-server.txt, task2-server-scaffold.txt, task3-buildtest.txt, 00-INVOCATION.md).
- task2-server.txt has ZERO implementation leaks (greps for TcpListener / BufReader / read_to_string / Content-Length / hyper / borrow checker all return 0) and DOES name the goal (8080, "no external crates", oh-workdir-rust).
- task2-server-scaffold.txt contains the verbatim working src/main.rs (grep `TcpListener::bind` >= 1) — staged but unused unless fallback triggers.
- task1-scaffold.txt contains `cargo new rust-server` (>=1).
- task3-buildtest.txt contains `cargo run &` and `curl -s http://localhost:8080/` (>=1 each) and ZERO impl leaks (>=0 grep on TcpListener/read_to_string).
- 00-INVOCATION.md names openai/qwen-35b, includes the JSONL filename table, polling guidance, and exit-code warning.
- .gitignore has all 4 entries (`book/`, `oh-workdir/`, `oh-workdir-122b/`, `oh-workdir-rust/`); git check-ignore returns each scratch-dir name.
- 08-01-PREFLIGHT.md records rustc/cargo version, port 8080 status, proxy response, and the verdict `PREFLIGHT GREEN`.
- oh-workdir-rust/ exists on disk and is empty.
</verification>

<success_criteria>
The unaided-first protocol is encoded in task2-server.txt (RUST-01 made achievable: the prompt asks for a server matching a goal contract without leaking any implementation detail). The cargo new step is explicitly assigned to the agent in task1 (RUST-01 "agent runs cargo new itself" criterion #2). The fallback prompt is staged but unused. The scratch dir is clean, gitignored, and does not touch prior milestones' scratch dirs. rustc/cargo + port 8080 + litellm proxy are all confirmed live in 08-01-PREFLIGHT.md with verdict GREEN — so 08-02 can launch a clean, honest capture.
</success_criteria>

<output>
After completion, create `.planning/phases/08-capture-the-35b-rust-http-server-run/08-01-SUMMARY.md` summarizing: which files were created, the preflight verdict, and any notable observations (e.g., whether port 8080 was free first try, whether the proxy responded normally).
</output>
