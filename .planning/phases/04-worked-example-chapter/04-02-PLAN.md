---
phase: 04-worked-example-chapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ch04-calculator/writing.md
  - src/ch04-calculator/build-test.md
autonomous: true

must_haves:
  truths:
    - "Reader sees the agent write Parser.fsy with correct %left precedence from the start (WALK-03 source)"
    - "Reader sees the error-and-fix cycle narrated as observed→decided→corrected for each of 4 real build failures (WALK-02)"
    - "Every quoted command and error is verbatim (trimmed) from the captured transcript — no idealized output (WALK-02)"
    - "Reader sees concept↔action callouts for tool calling and the agent loop (self-correction), tied to ch01 vocabulary"
  artifacts:
    - path: "src/ch04-calculator/writing.md"
      provides: "코드 작성 단계: agent writes Parser.fsy (with %left) + Program.fs; tool calling + memory callouts; embedded Parser.fsy source"
      min_lines: 60
    - path: "src/ch04-calculator/build-test.md"
      provides: "빌드와 테스트 단계: 4-failure error-and-fix narration (observed→decided→corrected, verbatim) + agent-loop self-correction callout"
      min_lines: 80
  key_links:
    - from: "src/ch04-calculator/writing.md"
      to: "ch01 tool calling vocabulary (Callout A)"
      via: "CmdRunAction running dotnet build = tool calling"
      pattern: "CmdRunAction|tool calling|툴 호출"
    - from: "src/ch04-calculator/build-test.md"
      to: "ch01 agent loop / self-correction vocabulary (Callout B)"
      via: "build-error→fix→rebuild cycle = agent loop observation, not a separate feature"
      pattern: "agent loop|self-correction|자가 수정|관찰 루프"
    - from: "src/ch04-calculator/build-test.md"
      to: "the 4 real build failures"
      via: "verbatim FSY000 / parse error / FS0039 error text from transcript.md"
      pattern: "FSY000|parse error|FS0039"
---

<objective>
Write the heart of 4부: the code-writing step (writing.md — the agent authoring Parser.fsy with correct %left precedence and Program.fs) and the build-and-test step (build-test.md — the 4 real build-failure self-correction cycles narrated observed→decided→corrected). This is where WALK-02, WALK-03 (source), and the agentic-AI payoff live.

Purpose: This is the most important content in the book — the genuine, real demonstration of an agent self-correcting. It must be evidence-faithful (verbatim quotes) and must narrate the REAL build errors, not the precedence bug the roadmap originally guessed.
Output: src/ch04-calculator/writing.md and src/ch04-calculator/build-test.md (Korean prose, English technical terms, ASCII art only).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-worked-example-chapter/04-RESEARCH.md
@src/ch01-agentic-ai/concepts.md
@src/ch02-openhands/actions-observations.md

# Verbatim sources — read these for exact embed/quote content:
@.planning/phases/03-capture-the-openhands-run/captured/transcript.md
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Parser.fsy
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Program.fs
</context>

<honesty_constraints>
NON-NEGOTIABLE (from 04-RESEARCH.md §2, §3, §8, §10):
1. The error-and-fix is FOUR FsYacc/F# BUILD FAILURES, NOT a precedence bug. The agent wrote correct `%left PLUS MINUS` / `%left STAR SLASH` itself, on the first try. NEVER narrate a precedence bug.
2. The 4 failures, in order (RESEARCH §3, §10):
   - F1: FSY000 missing %start → agent adds `%start <int> start` (WRONG form)
   - F2: Parser.fsy(16,7) parse error → agent removes %type, keeps `%start <int> start` (STILL wrong)
   - F3: SAME parse error again → agent separates into `%start start` + `%type <int> start` on separate lines (CORRECT). NO ThinkAction recorded for F3 — do NOT invent reasoning; narrate per RESEARCH §8.1 exact wording.
   - F4: FS0039 LexBuffer.FromText does not exist → agent changes to `LexBuffer<char>.FromString` + curried `Parser.start Lexer.tokenize lexbuf`
   Then SUCCESS build.
3. ThinkAction (decided) text is available VERBATIM for F1, F2, F4 only (RESEARCH §3, §10). Quote them verbatim. For F3, state explicitly no reasoning was recorded.
4. All build-error text quoted must be VERBATIM (trimmed) from transcript.md (RESEARCH §3 gives the exact blocks). No fabrication.
5. task3 also wrote Program.fs out-of-scope (RESEARCH §3 additional context, §8.4) — note this as observed genuine behavior; it's what introduced F4.
6. Do NOT reproduce "~240s/call".
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write writing.md (코드 작성 단계) — Parser.fsy + Program.fs authoring, tool-calling callout, source embed</name>
  <files>src/ch04-calculator/writing.md</files>
  <action>
Read transcript.md (Task 3 section) and final-source/Parser.fsy + Program.fs first. Write writing.md in Korean. Sections:

1. **task1 스캐폴딩 (요약)** — Briefly (one short paragraph) note that task1 set up the project skeleton with calc.fsproj. The agent's 27 TerminalActions were mostly file-writing struggles (heredoc corruption, base64, Python-script workarounds). Per RESEARCH §8.2, do NOT narrate this in full detail — it's verbose and not the interesting part. One or two sentences acknowledging the friction, then move on.

2. **task2 렉서 투입 (요약)** — One sentence: Lexer.fsl was copied in (provided, not written). Reuse the scaffolding disclosure from intro/planning; do not re-derive.

3. **task3 — 에이전트가 파서를 작성하다** — The core of this file. Narrate (from transcript.md Task 3): the agent writes Parser.fsy. Highlight that the agent wrote correct precedence declarations `%left PLUS MINUS` and `%left STAR SLASH` ITSELF on the first attempt (RESEARCH §2B — this is the honesty point: NO precedence bug). Then note the agent ALSO wrote Program.fs out-of-scope to verify the build (RESEARCH §3 additional context) — observed genuine agent behavior that exceeded task scope.

4. **CONCEPT CALLOUT A — tool calling** — Blockquote callout (`> **개념 ↔ 행동: 툴 호출 (tool calling)**`). Use RESEARCH §4 Callout A text: the agent running `dotnet build` is tool calling in the concrete — it emits a CmdRunAction(`command: dotnet build`) rather than writing "I will build". Name the ch01 term (1부에서 배운 tool calling). Reference that file creation uses FileWriteAction/FileEditAction (ch01 concepts.md says 4부 코드 작성 단계 is exactly where tool calling is seen — honor that pointer).

5. **CONCEPT CALLOUT C — memory / context (filesystem persistence)** — Blockquote callout (`> **개념 ↔ 행동: 메모리와 컨텍스트**`). Use RESEARCH §4 Callout C text: each of the 5 tasks is a SEPARATE OpenHands invocation; task3 can still read the Lexer.fsl that task2 wrote, and task5 can build all earlier files. This cross-invocation "memory" lives in the filesystem (LocalWorkspace), NOT the LLM context window / EventLog (which is per-session). This shows the boundary of the ch01 memory concept. (This is the 2nd and 3rd of the ≥3 callouts requirement — A and C both here; D is in 04-01; B is in build-test.md, giving 4 total.)

6. **Parser.fsy 전체 (WALK-03 source embed)** — Embed the COMPLETE final Parser.fsy verbatim from captured/final-source/Parser.fsy inside a fenced code block. It MUST include the `%left PLUS MINUS` / `%left STAR SLASH` lines (lines 13-14) and the two-part `%start start` / `%type <int> start` form (lines 16, 18). Add a one-line caption that the arithmetic happens in the grammar actions (`$1 + $3` etc.) — reinforcing "no separate evaluator". Do NOT show Program.fs in full here (build-test.md / final.md own the Program.fs evolution); a forward-reference in prose is fine.

Prose-only forward refs; NO Markdown links to unwritten files.
  </action>
  <verify>Read back. Confirm: (a) embedded Parser.fsy matches captured/final-source/Parser.fsy verbatim including `%left PLUS MINUS` + `%left STAR SLASH` and `%start start`/`%type <int> start`; (b) explicit statement that the agent wrote correct `%left` itself (no precedence bug); (c) two concept callouts present (tool calling = CmdRunAction dotnet build; memory = filesystem persistence across separate invocations), each naming the ch01 term; (d) task1 file-writing struggle is summarized, not over-narrated; (e) no "240" string; (f) no dead Markdown links.</verify>
  <done>writing.md exists, ≥60 lines, embeds the verbatim final Parser.fsy (with %left), states the agent wrote correct precedence, and carries Callouts A (tool calling) and C (memory), each tied to ch01 vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Write build-test.md (빌드와 테스트 단계) — 4-failure error-and-fix narration + agent-loop callout</name>
  <files>src/ch04-calculator/build-test.md</files>
  <action>
Read transcript.md (Task 3 build section, Steps 5-15) and RESEARCH §3 (which gives the exact verbatim error blocks). Write build-test.md in Korean. Structure:

1. **들어가며** — One paragraph: task3's parser compiled only after 4 build failures the agent self-corrected without human help. Frame this as the strongest agentic demonstration in the book (RUN-03). State the failures are real build errors (NOT a precedence bug — the precedence was already correct).

2. **네 번의 빌드 실패와 자가 수정** — Narrate each of the 4 failures in the observed→decided→corrected format. For EACH failure use three labeled sub-parts: **관찰(observed)**, **판단(decided)**, **수정(corrected)**. Quote VERBATIM (trimmed) from RESEARCH §3 / transcript.md:

   - **실패 1 — %start 선언 누락**: observed = the FSY000 block (`FSYACC : error FSY000: at least one %start declaration is required`, verbatim from RESEARCH §3 Failure 1). decided = ThinkAction verbatim: "The `%start` declaration is missing — I need to add it alongside `%type`:". corrected = agent adds `%start <int> start` above `%type <int> start` — and NOTE this was the wrong syntax that caused failure 2.

   - **실패 2 — %start <int> start 잘못된 구문 (parse error)**: observed = the `Parser.fsy(16,7): error parse error` block (verbatim, RESEARCH §3 Failure 2). decided = ThinkAction verbatim: "The issue is having both `%start` and `%type` for the same rule. FsYacc only needs `%start` for the entry point — `%type` is for non-start rules. Let me fix that:". corrected = agent removes %type, keeps only `%start <int> start` — STILL invalid.

   - **실패 3 — 같은 parse error, 두 번째 시도**: observed = same `Parser.fsy(16,7): error parse error` block. decided = **NO ThinkAction recorded** — per RESEARCH §8.1, narrate exactly: "에이전트는 세 번째 빌드에서도 같은 오류를 받았습니다. 이번에는 추론 기록 없이 곧바로 다른 방법을 시도했습니다: `%start start`와 `%type <int> start`를 별도 줄로 분리하는 방식이었습니다." Do NOT invent reasoning. corrected = separated declarations onto individual lines — CORRECT; the parser now compiled, but a new error surfaced in Program.fs.

   - **실패 4 — LexBuffer<_>.FromText 가 존재하지 않음 (FS0039)**: observed = the FS0039 block (`Program.fs(11,38): error FS0039: 'LexBuffer<_>' 형식은 'FromText' ...` verbatim, RESEARCH §3 Failure 4). decided = ThinkAction verbatim: "The parser compiled successfully! The errors are in `Program.fs` — let me fix the API calls:". corrected = `LexBuffer<char>.FromText(...)` → `LexBuffer<char>.FromString input`, and `Parser.start(Lexer.tokenize, lexbuf)` → curried `Parser.start Lexer.tokenize lexbuf`. Note this error came from the Program.fs the agent wrote out-of-scope in task3.

3. **빌드 성공** — Embed the verbatim success block (RESEARCH §3 SUCCESS): `calc net10.0 성공 (0.7초) → bin/Debug/net10.0/calc.dll` / `성공 빌드(1.0초)`. Then note the agent's own in-task validation: it immediately ran 8 expressions (1+2*3=7, 10-3-2=5, (10-3)-2=5, 10-(3-2)=9, 2*3+4=10, (2+3)*4=20, 100/10/2=5, 1+2+3+4+5=15), all correct (RESEARCH §3 SUCCESS post-fix validation). Clarify this is the AGENT's self-check, distinct from the formal host verification (which lives in 완성된 계산기).

4. **CONCEPT CALLOUT B — agent loop / self-correction**: Blockquote callout (`> **개념 ↔ 행동: 에이전트 루프와 자가 수정**`). Use RESEARCH §4 Callout B text: the 4× build-fail→fix→rebuild cycle is NOT a separately-designed "self-correction feature" — the compiler error in CmdOutputObservation becomes part of the next loop iteration's LLM input, the LLM reads it and emits a fix ActionEvent. This is the natural result of the agent loop (cite ch01: "루프가 자율적", ch02 actions-observations: "자가 수정은 ... 관찰 루프의 자연스러운 부수효과"). Name the ch01 term agent loop.

5. **다음으로** — One sentence forward-reference (prose, no link) that '완성된 계산기' shows the full final source and the official 14/20/5 verification block plus the honest performance note.

Prose-only forward refs; NO Markdown links to unwritten files.
  </action>
  <verify>Read back. Confirm: (a) exactly 4 failures, each with 관찰/판단/수정 sub-parts; (b) verbatim ThinkAction quotes for F1, F2, F4 and EXPLICIT "no reasoning recorded" for F3 (no invented rationale); (c) verbatim error strings FSY000, `Parser.fsy(16,7): error parse error`, FS0039 present; (d) NO precedence-bug narration anywhere; (e) success block + agent's 8-expression self-check; (f) Callout B (agent loop self-correction) naming ch01 term; (g) no "240" string; (h) no dead Markdown links. Run `grep -E "FSY000|parse error|FS0039|성공 빌드" src/ch04-calculator/build-test.md` and confirm all four appear.</verify>
  <done>build-test.md exists, ≥80 lines, narrates the 4 real build failures observed→decided→corrected with verbatim quotes (and honest "no ThinkAction" for F3), embeds the success block, and carries Callout B tied to ch01 agent loop. VERIFY-01 forward-referenced to final.md. RUN-03 / WALK-02 error-and-fix satisfied.</done>
</task>

</tasks>

<verification>
- Both files exist under src/ch04-calculator/.
- writing.md embeds verbatim final Parser.fsy WITH `%left PLUS MINUS` / `%left STAR SLASH` (WALK-03).
- build-test.md narrates exactly 4 build failures observed→decided→corrected, all quotes verbatim from transcript/RESEARCH §3; F3 explicitly has no invented reasoning.
- No precedence-bug narration in either file.
- `grep -ri "240" src/ch04-calculator/writing.md src/ch04-calculator/build-test.md` returns nothing.
- ≥2 concept callouts across these two files (A tool calling, C memory in writing.md; B agent loop in build-test.md) — combined with Callout D in 04-01 gives 4 total (≥3 requirement met).
- No Markdown links to not-yet-created files.
</verification>

<success_criteria>
- WALK-02 satisfied: real captured output at every key step; error-and-fix narrated observed→decided→corrected.
- WALK-03 (source) satisfied for Parser.fsy embed with %left.
- Honesty constraints #1 (real build errors not precedence bug), #2 (verbatim quotes, no invented F3 reasoning) enforced.
- 3 of the 4 total concept↔action callouts land here (A, C, B), each tied to ch01 vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/04-worked-example-chapter/04-02-SUMMARY.md`
</output>
