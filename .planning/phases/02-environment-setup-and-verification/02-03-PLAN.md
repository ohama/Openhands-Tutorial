---
phase: 02-environment-setup-and-verification
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/ch03-setup/installation.md
  - src/ch03-setup/qwen-connection.md
  - src/ch03-setup/first-run.md
  - src/SUMMARY.md
  - .planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "The 3부 setup chapter documents every reader step matching the verified config exactly"
    - "Reader can install OpenHands via uv and verify it (openhands --version → 1.16.0)"
    - "Reader can configure OpenHands to use openai/qwen-local @ 127.0.0.1:4000 via env vars + --override-with-envs"
    - "Reader can run the headless tool-call + dotnet checklist and know what success looks like"
    - "mdbook build exits 0 with the three new 3부 files wired into SUMMARY.md; 4부/5부 stay drafts"
  artifacts:
    - path: src/ch03-setup/installation.md
      provides: "Install + base-stack verification chapter (uv tool install, openhands --version, PATH, litellm preflight)"
      min_lines: 25
    - path: src/ch03-setup/qwen-connection.md
      provides: "LLM connection chapter (env-var config, openai/qwen-local, 127.0.0.1:4000, --override-with-envs, ~240s note)"
      min_lines: 25
    - path: src/ch03-setup/first-run.md
      provides: "First headless run + pre-run checklist chapter (echo ping, dotnet --version, reading JSONL)"
      min_lines: 25
    - path: src/SUMMARY.md
      provides: "3부 draft () entries replaced with the three real ch03-setup paths"
      contains: "ch03-setup/installation.md"
  key_links:
    - from: "src/SUMMARY.md 3부 entries"
      to: "src/ch03-setup/*.md"
      via: "Markdown links to existing files"
      pattern: "ch03-setup/"
    - from: "first-run.md checklist"
      to: "02-VERIFICATION-EVIDENCE.md (real outputs)"
      via: "documented commands match captured invocation exactly"
      pattern: "override-with-envs"
---

<objective>
Write 3부 "환경 설정" of the mdBook from the captured verification evidence, in Korean, matching the
verified headless path EXACTLY. Three chapters map to the three existing 3부 draft entries:
installation (OpenHands install + base-stack verify), qwen-connection (env-var LLM config), and
first-run (headless tool-call ping + pre-run checklist incl. dotnet). Convert ONLY the 3부 `()`
draft entries in SUMMARY.md to real paths; leave 4부/5부 as drafts. mdbook build must exit 0.

Purpose: SETUP-01/02/03/04 documentation deliverable — the reader-facing setup chapter. It must
reflect reality (headless CLI + LocalWorkspace + env-var config + qwen-local@4000 + host dotnet),
note Docker is only for the GUI/DockerWorkspace alternative, and be honest about ~240s/call.
Output: three src/ch03-setup/*.md files, updated SUMMARY.md, green mdbook build.

DO NOT FABRICATE: quote commands and expected outputs from 02-VERIFICATION-EVIDENCE.md. The
chapter's commands must be copy-pasteable and match the verified invocation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-environment-setup-and-verification/02-VERIFICATION-EVIDENCE.md
@.planning/phases/02-environment-setup-and-verification/02-02-SUMMARY.md
@src/SUMMARY.md
@src/ch02-openhands/overview.md

CONSTRAINTS:
- Korean prose; English for technical terms (agent loop, tool calling, LocalWorkspace, headless, env var).
- mdbook at /opt/homebrew/bin/mdbook. book.toml has `create-missing = false` → every SUMMARY.md
  link MUST point to a file that already exists, or the build fails. Therefore CREATE the three files
  BEFORE editing SUMMARY.md, and only link the three real 3부 files (4부/5부 stay `()` drafts).
- Phase 1 established: ASCII art for diagrams (no preprocessor deps); forward references to unwritten
  chapters are PROSE ONLY (no Markdown links to draft `()` entries).
- VERIFIED path to document (NOT the stale Docker-Desktop/config.toml/serve approach):
  * Install: `uv tool install openhands --python 3.12`; verify `openhands --version` → 1.16.0; ensure
    `~/.local/bin` on PATH. Headless CLI on macOS — no browser. (`openhands serve` GUI needs Docker and
    is the alternative path only.)
  * LLM config via ENV VARS: LLM_MODEL=openai/qwen-local, LLM_BASE_URL=http://127.0.0.1:4000/v1,
    LLM_API_KEY=dummy, applied with `--override-with-envs`. No ~/.openhands/config.toml.
  * Run shape: `openhands --headless --json --yolo --override-with-envs -t "<task>"` → JSONL events.
  * dotnet: already on host (10.0.x); LocalWorkspace = host, so the agent uses host dotnet. If the
    evidence recorded a PATH fallback, document using `/opt/homebrew/bin/dotnet` or exporting PATH.
  * Docker NOTE: only needed for the GUI / DockerWorkspace alternative — not for this headless path.
  * Performance honesty: ~240s per tool-call cycle on the local 35B model.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write installation.md and qwen-connection.md (3부 ch1–2)</name>
  <files>src/ch03-setup/installation.md, src/ch03-setup/qwen-connection.md</files>
  <action>
  Create the directory `src/ch03-setup/` and write two Korean chapters.

  installation.md — "OpenHands 설치":
  - Premise: headless SSH Mac (Apple Silicon), no browser. Explain why the headless CLI path is used.
  - Steps: install uv (if needed), `uv tool install openhands --python 3.12`, ensure `~/.local/bin`
    on PATH, verify with `openhands --version` (expected 1.16.0). Quote the real version from evidence.
  - Base-stack note: Python 3.12 toolchain managed by uv.
  - A clearly-marked aside: the GUI (`openhands serve`) needs Docker (a /var/run/docker.sock mount)
    and is NOT used in this tutorial's headless path; mention it only as the alternative. Do not put
    Docker on the reader's critical path.

  qwen-connection.md — "로컬 Qwen 서버 연결":
  - Explain the env-var configuration model (Mode A, no persisted config): LLM_MODEL=openai/qwen-local,
    LLM_BASE_URL=http://127.0.0.1:4000/v1, LLM_API_KEY=dummy, and that `--override-with-envs` is REQUIRED
    or the env vars are ignored. Explain why model uses the `openai/` prefix and base_url is loopback
    (the host process calls the LLM; not host.docker.internal in this headless setup).
  - Show the litellm preflight: `curl http://127.0.0.1:4000/v1/models` and the expected qwen-local
    listing (quote real output from evidence/02-01).
  - Honesty note: ~240s per tool-call cycle on the local 35B model; set reader expectations.
  - Forward references to first-run.md are fine (same dir, file will exist).

  Use ASCII art only if a diagram helps; otherwise prose. Match commands to the evidence file exactly.
  </action>
  <verify>Both files exist with >=25 lines; `grep -q "uv tool install openhands" src/ch03-setup/installation.md` and `grep -q "override-with-envs" src/ch03-setup/qwen-connection.md`.</verify>
  <done>installation.md and qwen-connection.md exist in Korean, documenting the verified install + env-var LLM config, with Docker correctly framed as the GUI-only alternative and the ~240s note present.</done>
</task>

<task type="auto">
  <name>Task 2: Write first-run.md (3부 ch3) — headless run + pre-run checklist</name>
  <files>src/ch03-setup/first-run.md</files>
  <action>
  Write first-run.md — "첫 실행 테스트" in Korean, built from 02-VERIFICATION-EVIDENCE.md.

  - Show the full verified headless invocation (the env-var block + `openhands --headless --json --yolo
    --override-with-envs -t '...'`), copy-pasteable. Recommend a dedicated working dir via OPENHANDS_WORK_DIR.
  - The echo tool-call ping: task string `Run the bash command: echo OPENHANDS_PING_OK`; explain how to
    read the JSONL — an action event (the tool call) and an observation event (output contains the
    sentinel). Quote the real action/observation lines from the evidence (trimmed).
  - dotnet check: task `Run the bash command: dotnet --version`, expected 10.0.x. If the evidence recorded
    a PATH fallback, document `/opt/homebrew/bin/dotnet` (or `export PATH="/opt/homebrew/bin:$PATH"`) and
    say why (LocalWorkspace = host; PTY may not source the full login profile).
  - The PRE-RUN VERIFICATION CHECKLIST (reader-facing), each with the command and the success signal:
    1) `openhands --version` → 1.16.0
    2) `curl 127.0.0.1:4000/v1/models` → qwen-local present
    3) headless echo ping → action + observation with OPENHANDS_PING_OK
    4) headless `dotnet --version` → 10.0.x
  - Honesty note again: each headless run blocks ~240s+; no --timeout flag exists.
  - Forward reference to 4부 (the calculator run) as PROSE ONLY — no Markdown link to a draft entry.
  </action>
  <verify>`test -f src/ch03-setup/first-run.md` and >=25 lines and `grep -q "OPENHANDS_PING_OK" src/ch03-setup/first-run.md` and `grep -q "dotnet --version" src/ch03-setup/first-run.md`.</verify>
  <done>first-run.md documents the verified headless invocation, the echo+dotnet checks with real JSONL evidence, the four-item pre-run checklist, and the ~240s honesty note. Forward references are prose-only.</done>
</task>

<task type="auto">
  <name>Task 3: Wire 3부 into SUMMARY.md and build; write 02-03-SUMMARY.md</name>
  <files>src/SUMMARY.md, .planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md</files>
  <action>
  Now that the three files exist, edit src/SUMMARY.md: replace ONLY the three 3부 draft lines

      - [OpenHands 설치]()
      - [로컬 Qwen 서버 연결]()
      - [첫 실행 테스트]()

  with the real paths:

      - [OpenHands 설치](ch03-setup/installation.md)
      - [로컬 Qwen 서버 연결](ch03-setup/qwen-connection.md)
      - [첫 실행 테스트](ch03-setup/first-run.md)

  Leave ALL 4부 and 5부 and appendix entries as `()` drafts — do not touch them.

  Build and confirm green:
    `/opt/homebrew/bin/mdbook build /Users/ohama/projs/OpenHandsTests`
  Expected: exit code 0, no "Summary parsing failed" / no broken-link errors. (book.toml has
  create-missing=false, so a wrong path would fail the build — the three files now exist.)

  Then write 02-03-SUMMARY.md (GSD template): subsystem: tutorial/mdbook, what was written, mapping to
  SETUP-01/02/03/04 documentation, confirmation that mdbook build exits 0 and 4부/5부 remain drafts.
  </action>
  <verify>`/opt/homebrew/bin/mdbook build /Users/ohama/projs/OpenHandsTests` exits 0; `grep -c "ch03-setup/" src/SUMMARY.md` returns 3; the 4부 drafts are untouched — `grep -q '\[예제 프로젝트 소개\]()' src/SUMMARY.md` succeeds (a representative 4부 entry is still a draft); and the total count of remaining `()` draft entries is unchanged from before the edit (`grep -c '()' src/SUMMARY.md` returns 9 — the three 3부 drafts converted, all 4부/5부/appendix drafts intact).</verify>
  <done>SUMMARY.md links the three real 3부 files (and only those new ones); mdbook build exits 0; 4부/5부 remain drafts; 02-03-SUMMARY.md written.</done>
</task>

</tasks>

<verification>
- src/ch03-setup/{installation,qwen-connection,first-run}.md exist, Korean, each >=25 lines
- SUMMARY.md 3부 entries point to the three real files; 4부/5부 remain `()` drafts
- `/opt/homebrew/bin/mdbook build` exits 0 (no broken links; create-missing=false respected)
- Chapter commands match 02-VERIFICATION-EVIDENCE.md exactly (env-var invocation, echo ping, dotnet)
- Docker framed only as GUI/DockerWorkspace alternative; ~240s honesty note present
</verification>

<success_criteria>
3부 documents every reader step matching the verified headless config exactly: uv install + version
check, env-var LLM config to openai/qwen-local@127.0.0.1:4000 with --override-with-envs, the headless
tool-call ping, host dotnet via the agent, and a four-item pre-run checklist — all sourced from real
captured evidence. mdbook build is green and 4부/5부 stay drafts. Completes the SETUP-01..04
documentation (criterion 5).
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md`
</output>
