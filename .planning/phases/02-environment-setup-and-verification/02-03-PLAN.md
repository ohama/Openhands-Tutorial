---
phase: 02-environment-setup-and-verification
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - "/Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet"
  - "~/.openhands/config.toml"

autonomous: true

must_haves:
  truths:
    - "sandbox/Dockerfile.openhands-dotnet exists, extends nikolaik/python-nodejs:python3.12-nodejs22, and installs .NET 10 via dotnet-install.sh"
    - "`docker build` of the image succeeds and tags openhands-dotnet-sandbox"
    - "`docker run --rm openhands-dotnet-sandbox dotnet --version` prints a 10.0.x version"
    - "~/.openhands/config.toml has a [sandbox] section with base_container_image = openhands-dotnet-sandbox"
  artifacts:
    - path: "/Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet"
      provides: "Custom OpenHands sandbox base image definition with .NET 10 SDK"
      contains: "FROM nikolaik/python-nodejs:python3.12-nodejs22"
    - path: "~/.openhands/config.toml"
      provides: "[sandbox] base_container_image pointing at the built image"
      contains: "base_container_image"
  key_links:
    - from: "~/.openhands/config.toml [sandbox].base_container_image"
      to: "docker image openhands-dotnet-sandbox"
      via: "OpenHands builds its runtime layer on top of this base at first session"
      pattern: "openhands-dotnet-sandbox"
---

<objective>
Create the custom sandbox Dockerfile (Debian base + .NET 10 SDK via dotnet-install.sh), build it as `openhands-dotnet-sandbox` for linux/arm64, verify `dotnet --version` works in the bare image, and register it in `~/.openhands/config.toml` under [sandbox]. Pre-building avoids the known first-session build hang.

Purpose: Satisfies Phase 2 SC#3 (build portion) — the calculator (Phase 3) is F#/.NET, so the sandbox MUST have the .NET SDK. The in-OpenHands-session `dotnet --version` (SC#3 runtime portion) is verified in plan 02-04.
Output: A built `openhands-dotnet-sandbox` image and a [sandbox] config entry pointing at it.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-environment-setup-and-verification/02-RESEARCH.md

# Facts:
# - Base image MUST be Debian-based; nikolaik/python-nodejs:python3.12-nodejs22 is required.
# - .NET 10 is ARM64-native on Debian; dotnet-install.sh auto-detects arch.
# - Build with --platform linux/arm64 (Apple Silicon native; explicit is safer).
# - OpenHands injects its own agent server on top — do NOT install it in the Dockerfile.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write sandbox/Dockerfile.openhands-dotnet</name>
  <files>/Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet</files>
  <action>
    Create the `sandbox/` directory and write `sandbox/Dockerfile.openhands-dotnet` with EXACTLY this content (from RESEARCH "Complete Dockerfile"):

      # File: sandbox/Dockerfile.openhands-dotnet
      FROM nikolaik/python-nodejs:python3.12-nodejs22

      # Install prerequisites for dotnet-install.sh
      RUN apt-get update && apt-get install -y --no-install-recommends \
          wget \
          ca-certificates \
          libicu-dev \
          && rm -rf /var/lib/apt/lists/*

      # Install .NET 10 SDK (official script; ARM64-aware)
      RUN wget -qO /tmp/dotnet-install.sh https://dot.net/v1/dotnet-install.sh \
          && chmod +x /tmp/dotnet-install.sh \
          && /tmp/dotnet-install.sh --channel 10.0 --install-dir /usr/local/share/dotnet \
          && rm /tmp/dotnet-install.sh

      ENV DOTNET_ROOT=/usr/local/share/dotnet
      ENV PATH=$PATH:/usr/local/share/dotnet
      ENV DOTNET_CLI_TELEMETRY_OPTOUT=1
      ENV DOTNET_NOLOGO=1

      # Sanity check baked into image
      RUN dotnet --version
  </action>
  <verify>`test -f /Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet` succeeds and the file contains `FROM nikolaik/python-nodejs:python3.12-nodejs22` and `dotnet-install.sh`.</verify>
  <done>The Dockerfile exists with the exact base image, dotnet-install.sh install, PATH/DOTNET_ROOT env, and a baked-in `dotnet --version` sanity step.</done>
</task>

<task type="auto">
  <name>Task 2: Build the image and verify dotnet in the bare container</name>
  <files>(builds docker image openhands-dotnet-sandbox; no repo files)</files>
  <action>
    Build from the repo root (the `.` build context), explicitly ARM64:

      docker build --platform linux/arm64 \
        -f /Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet \
        -t openhands-dotnet-sandbox \
        /Users/ohama/projs/OpenHandsTests

    Expected final lines include a successful build and tagging of openhands-dotnet-sandbox; the baked `RUN dotnet --version` step prints a `10.0.x` version during build.

    Then verify the bare image runs dotnet (RESEARCH Check-3 precondition):
      docker run --rm openhands-dotnet-sandbox dotnet --version
      -> expected: `10.0.x` (e.g. 10.0.300)

    If the build HANGS (RESEARCH Pitfall 4 — "DOCKER BUILD STARTED" with no progress >5 min): kill it, run `docker system prune -f`, and rebuild. Record the build duration in the SUMMARY (the chapter warns readers about first-build time).
  </action>
  <verify>`docker run --rm openhands-dotnet-sandbox dotnet --version` prints a version starting with `10.0.`.</verify>
  <done>The openhands-dotnet-sandbox image is built and `dotnet --version` runs inside it, printing 10.0.x.</done>
</task>

<task type="auto">
  <name>Task 3: Register the image in config.toml [sandbox]</name>
  <files>~/.openhands/config.toml</files>
  <action>
    Append (or update) a [sandbox] section in `~/.openhands/config.toml`, preserving the existing [llm] section from plan 02-02:

      [sandbox]
      base_container_image = "openhands-dotnet-sandbox"

    Idempotent: if [sandbox] already exists, ensure base_container_image equals "openhands-dotnet-sandbox". Do not modify the [llm] section.

    Note for SUMMARY: plan 02-04 will ALSO pass `SANDBOX_BASE_CONTAINER_IMAGE=openhands-dotnet-sandbox` as an env var when launching for verification (the more explicit, env-override path per RESEARCH); both should agree on the same image name.
  </action>
  <verify>`grep -A2 '^\[sandbox\]' ~/.openhands/config.toml` shows `base_container_image = "openhands-dotnet-sandbox"`, and `grep -c '^\[llm\]' ~/.openhands/config.toml` returns 1 (the [llm] section is intact).</verify>
  <done>config.toml has a [sandbox] section pointing at openhands-dotnet-sandbox, with the [llm] section preserved.</done>
</task>

</tasks>

<verification>
- `test -f /Users/ohama/projs/OpenHandsTests/sandbox/Dockerfile.openhands-dotnet` -> exists
- `docker run --rm openhands-dotnet-sandbox dotnet --version` -> 10.0.x
- `grep -A2 '^\[sandbox\]' ~/.openhands/config.toml` -> base_container_image = "openhands-dotnet-sandbox"
- [llm] section still present and unchanged
</verification>

<success_criteria>
Phase 2 SC#3 (build portion) met: a custom sandbox image with the .NET SDK is built and `dotnet --version` runs inside the bare image. (Running it from within an OpenHands sandbox SESSION completes SC#3 in plan 02-04.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md` recording: the exact dotnet version printed, the build duration (for the chapter's first-build warning), and the final [sandbox] config block.
</output>
