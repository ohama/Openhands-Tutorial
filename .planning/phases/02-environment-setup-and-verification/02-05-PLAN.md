---
phase: 02-environment-setup-and-verification
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - "/Users/ohama/projs/OpenHandsTests/src/SUMMARY.md"
  - "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/install.md"
  - "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/local-llm.md"
  - "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/sandbox-dotnet.md"
  - "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/first-run.md"

autonomous: true

must_haves:
  truths:
    - "The 3부 setup chapter covers every reader step: Docker+OpenHands install, LLM config, .NET sandbox build, and the four-check pre-run verification"
    - "Every command and output shown matches the VERIFIED configuration (from 02-VERIFICATION-EVIDENCE.md) — no invented outputs"
    - "SUMMARY.md 3부 entries are converted from draft `()` to real ch03-setup/*.md paths and a fourth (sandbox) entry is added"
    - "`mdbook build` exits 0 with no broken links"
  artifacts:
    - path: "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/install.md"
      provides: "Reader steps: Docker Desktop + socket, uv python 3.12, openhands install, openhands serve"
      min_lines: 30
    - path: "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/local-llm.md"
      provides: "host.docker.internal vs 127.0.0.1, openai/ double-slash model string, full config.toml [llm], timeout rationale"
      min_lines: 30
    - path: "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/sandbox-dotnet.md"
      provides: "Dockerfile, build command, [sandbox] key, first-build warning, dotnet --version verification"
      min_lines: 25
    - path: "/Users/ohama/projs/OpenHandsTests/src/ch03-setup/first-run.md"
      provides: "The four pre-run checks in order, expected outputs, JSONL interpretation, ~240s patience callout"
      min_lines: 30
  key_links:
    - from: "src/SUMMARY.md 3부 entries"
      to: "src/ch03-setup/*.md files"
      via: "real markdown paths (no longer draft `()`)"
      pattern: "ch03-setup/"
---

<objective>
Write the 3부 "환경 설정" chapter (four files under src/ch03-setup/) in Korean, documenting the VERIFIED setup exactly as proven in plan 02-04's evidence file. Convert the three draft SUMMARY.md entries to real paths and add a fourth (sandbox) entry. Keep `mdbook build` green.

Purpose: Satisfies Phase 2 SC#5 — the setup chapter covers every reader step and matches the verified configuration exactly. This plan depends on 02-04 so it documents reality, not assumptions (no fabricated outputs — a hard project rule).
Output: Four Korean chapter files, an updated SUMMARY.md (draft->real for 3부), and a clean mdbook build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-environment-setup-and-verification/02-RESEARCH.md
@.planning/phases/02-environment-setup-and-verification/02-VERIFICATION-EVIDENCE.md
@.planning/phases/02-environment-setup-and-verification/02-02-SUMMARY.md
@.planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md
@.planning/phases/02-environment-setup-and-verification/02-04-SUMMARY.md
@src/SUMMARY.md
@src/ch02-openhands/runtime.md

# Style rules (from Phase 1, observed in ch02 files):
# - Korean prose; keep English for technical terms (agent loop, tool calling, host.docker.internal, sandbox, LiteLLM, etc.)
# - Use tables and fenced ASCII/code blocks; mdbook 0.5.3, no preprocessors.
# - Only link to files that EXIST. 3부 may link within ch03-setup/ (all four files exist after this plan)
#   and back to existing ch01/ch02 files. Do NOT link to 4부/5부 (still drafts) — use prose forward pointers.
# - mdbook binary: /opt/homebrew/bin/mdbook
# - Content rule: every command/output shown must come from 02-VERIFICATION-EVIDENCE.md (verified). No invented outputs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write install.md and local-llm.md</name>
  <files>
    /Users/ohama/projs/OpenHandsTests/src/ch03-setup/install.md
    /Users/ohama/projs/OpenHandsTests/src/ch03-setup/local-llm.md
  </files>
  <action>
    Create `src/ch03-setup/` and write the first two chapter files in Korean (English for technical terms), all commands/outputs sourced from 02-VERIFICATION-EVIDENCE.md and the 02-01/02-02 SUMMARYs.

    install.md (maps to SUMMARY "OpenHands 설치"):
    - Why Docker Desktop is required (every agent session runs in a Docker sandbox); install for macOS Apple Silicon; enable "Allow the default Docker socket to be used" (Settings -> Advanced -> Apply & Restart).
    - `uv python install 3.12` (why exactly 3.12); `uv tool install openhands --python 3.12`; PATH note (~/.local/bin).
    - `openhands serve` -> UI at http://localhost:3000; mention first-run agent-server image pull.
    - Show the verified `openhands --version` and `docker run --rm hello-world` confirmation.
    - Opening sentence may reference the architecture chapter, e.g. link back to [런타임과 샌드박스](../ch02-openhands/runtime.md) (that file exists).

    local-llm.md (maps to SUMMARY "로컬 Qwen 서버 연결"):
    - Explain host.docker.internal vs 127.0.0.1 (containers can't reach host loopback).
    - The openai/ prefix and the double-slash model string `openai//Users/...` and WHY (the model id is itself a path) — use whichever string 02-04 CONFIRMED (double-slash or alias); state the confirmed one.
    - Show the full verified `~/.openhands/config.toml` [llm] block.
    - timeout rationale: ~240s/call -> timeout=600, num_retries=2, retry_min_wait=300.
    - UI alternative (Settings -> LLM -> Advanced) noted briefly; config.toml preferred.
    - Forward pointer to the verification chapter (prose or link to first-run.md, which exists after Task 2).
  </action>
  <verify>Both files exist under src/ch03-setup/; each ≥30 lines; local-llm.md contains the verified config.toml [llm] block and the confirmed model string; no links to 4부/5부 draft files.</verify>
  <done>install.md and local-llm.md document the verified install and LLM config in Korean, matching the evidence file.</done>
</task>

<task type="auto">
  <name>Task 2: Write sandbox-dotnet.md and first-run.md</name>
  <files>
    /Users/ohama/projs/OpenHandsTests/src/ch03-setup/sandbox-dotnet.md
    /Users/ohama/projs/OpenHandsTests/src/ch03-setup/first-run.md
  </files>
  <action>
    Write the remaining two files, sourcing all commands/outputs/durations from 02-VERIFICATION-EVIDENCE.md and the 02-03/02-04 SUMMARYs.

    sandbox-dotnet.md (NEW SUMMARY entry — ".NET 샌드박스 이미지" or similar):
    - Why a custom sandbox image (the calculator is F#/.NET; the default sandbox has no .NET SDK).
    - The full Dockerfile.openhands-dotnet content (FROM nikolaik/python-nodejs:python3.12-nodejs22 + dotnet-install.sh --channel 10.0).
    - The build command (`docker build --platform linux/arm64 -f sandbox/Dockerfile.openhands-dotnet -t openhands-dotnet-sandbox .`).
    - The [sandbox] config key (base_container_image) AND the SANDBOX_BASE_CONTAINER_IMAGE env-var alternative.
    - First-build / first-session warning with the REAL measured duration from the evidence file.
    - Verification: `docker run --rm openhands-dotnet-sandbox dotnet --version` -> the real 10.0.x version observed.

    first-run.md (maps to SUMMARY "첫 실행 테스트"):
    - The four pre-run checks IN ORDER (MLX up; Docker->host; dotnet in sandbox session; tool-call ping), each with the exact command, the real expected output (from evidence), and one line on what it proves.
    - How to read the JSONL: the action(run) line then the observation line (show the real lines from captured/toolcall-ping.jsonl).
    - A "patience" callout: ~240s/call on the local 35B; budget ~10 min for the ping.
    - If 02-04 needed AGENT_SERVER_EXTRA_HOSTS or the alias fallback, document it as a troubleshooting note (matches reality).
  </action>
  <verify>Both files exist; sandbox-dotnet.md ≥25 lines and contains the Dockerfile + build command; first-run.md ≥30 lines and lists all four checks with verified outputs; intra-ch03-setup links resolve.</verify>
  <done>sandbox-dotnet.md and first-run.md document the verified sandbox build and the four-check checklist in Korean.</done>
</task>

<task type="auto">
  <name>Task 3: Convert SUMMARY.md 3부 entries to real paths and build</name>
  <files>/Users/ohama/projs/OpenHandsTests/src/SUMMARY.md</files>
  <action>
    In src/SUMMARY.md, under "# 3부: 환경 설정", convert the three draft entries to real paths and ADD a fourth for the sandbox file (the verified setup has four reader steps; research prescribes four files):

      - [OpenHands 설치](ch03-setup/install.md)
      - [로컬 Qwen 서버 연결](ch03-setup/local-llm.md)
      - [.NET 샌드박스 이미지](ch03-setup/sandbox-dotnet.md)
      - [첫 실행 테스트](ch03-setup/first-run.md)

    Leave 4부 and 5부 entries as DRAFT `()` (Phase 4-5 own them — same draft/real discipline as Phase 1). Do NOT touch 1부/2부 entries.

    Then build and confirm green:
      /opt/homebrew/bin/mdbook build /Users/ohama/projs/OpenHandsTests
    Expect exit code 0 and no broken-link errors. If a link error appears, fix the offending link (only link to files that exist).
  </action>
  <verify>`/opt/homebrew/bin/mdbook build /Users/ohama/projs/OpenHandsTests` exits 0 with no error/broken-link output; SUMMARY.md 3부 has four real ch03-setup/ paths; 4부/5부 remain `()` drafts.</verify>
  <done>SUMMARY.md 3부 points to the four real files, 4부/5부 stay drafts, and mdbook build is green.</done>
</task>

</tasks>

<verification>
- `/opt/homebrew/bin/mdbook build /Users/ohama/projs/OpenHandsTests` -> exit 0, no broken links
- src/ch03-setup/{install,local-llm,sandbox-dotnet,first-run}.md all exist with Korean prose
- SUMMARY.md 3부 entries are real paths; 4부/5부 still draft `()`
- Spot-check: a command/output in the chapter matches 02-VERIFICATION-EVIDENCE.md
</verification>

<success_criteria>
Phase 2 SC#5 met: the 3부 setup chapter covers every reader step and matches the verified configuration exactly; mdbook build stays green with proper draft->real path discipline.
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-05-SUMMARY.md` recording: the four files written, the SUMMARY.md entries converted/added, and the mdbook build result.
</output>
