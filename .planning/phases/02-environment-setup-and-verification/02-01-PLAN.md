---
phase: 02-environment-setup-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "openhands --version returns 1.16.0 (CLI installed via uv tool)"
    - "openhands binary is reachable on PATH (~/.local/bin)"
    - "Python toolchain is 3.12.x under uv"
    - "litellm proxy at 127.0.0.1:4000 lists the qwen-local model with no auth"
  artifacts:
    - path: .planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md
      provides: "Recorded base-stack preflight outputs (openhands version, PATH, python, /v1/models)"
      contains: "qwen-local"
  key_links:
    - from: "openhands CLI"
      to: "~/.local/bin on PATH"
      via: "uv tool install shim"
      pattern: "\\.local/bin"
    - from: "litellm proxy"
      to: "qwen-local model alias"
      via: "GET /v1/models"
      pattern: "qwen-local"
---

<objective>
Confirm and record the base stack the OpenHands run depends on: the uv-installed OpenHands
CLI (1.16.0), Python 3.12, the binary on PATH, and the litellm proxy serving `qwen-local`
at 127.0.0.1:4000. This is a verification/recording plan — install only if a component is
missing (it is already installed per research). It satisfies SETUP-01 (install side) and
the LLM-reachability half of SETUP-02.

Purpose: Prove the prerequisites are present before the slow end-to-end agent run in 02-02,
so failures are localized to fast preflight checks rather than a 240s agent invocation.
Output: 02-01-SUMMARY.md containing the verbatim outputs of each check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-environment-setup-and-verification/02-RESEARCH.md

VERIFIED ENVIRONMENT (do not redo — VERIFY and RECORD):
- Headless SSH Mac, Apple Silicon, no browser.
- OpenHands SDK 1.21.0 / CLI 1.16.0 installed via `uv tool install openhands --python 3.12`;
  binaries at ~/.local/bin (openhands, openhands-acp). Python 3.12.13 via uv.
- litellm proxy (launchd com.ohama.litellm) on 127.0.0.1:4000, model alias `qwen-local`,
  NO auth on /v1/models.
- The run uses the HEADLESS CLI with LocalWorkspace (agent runs on the host). Docker is NOT
  on the critical path. `openhands serve` (GUI) is unusable here. Do not start/require Docker.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify OpenHands CLI install + PATH + Python toolchain</name>
  <files>(no source files — produces evidence captured into the SUMMARY in Task 3)</files>
  <action>
  Run the following checks and capture their exact stdout for the SUMMARY. Do NOT fabricate.

  1. CLI present and version:
     `openhands --version`
     Expected: `1.16.0` (the CLI version). If `openhands: command not found`, the binary is
     not on PATH — try the absolute path `~/.local/bin/openhands --version`, and if THAT works
     note that PATH must include `~/.local/bin` (add `export PATH="$HOME/.local/bin:$PATH"`).
     Only if the binary does not exist at all, install it:
       `uv tool install openhands --python 3.12`
     then re-run `openhands --version`.

  2. Binary location / PATH:
     `command -v openhands`
     Expected: `/Users/ohama/.local/bin/openhands` (or a path under the user's home `.local/bin`).

  3. Python toolchain (the uv-managed interpreter behind the tool):
     `uv tool list | grep -i openhands`  (expect an `openhands v1.16.0` entry)
     `~/.local/share/uv/tools/openhands/bin/python --version 2>/dev/null || python3 --version`
     Expected: `Python 3.12.x`.

  Record all four command lines and their outputs verbatim.
  </action>
  <verify>`openhands --version` prints `1.16.0`; `command -v openhands` resolves to a `.local/bin` path.</verify>
  <done>OpenHands CLI is confirmed installed at version 1.16.0, reachable on PATH, backed by Python 3.12.x. Outputs captured.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight the litellm proxy (qwen-local reachable, no auth)</name>
  <files>(no source files — produces evidence captured into the SUMMARY in Task 3)</files>
  <action>
  Verify the LLM gateway is up and exposes the `qwen-local` alias. Capture exact outputs.

  1. launchd agent running:
     `launchctl list | grep com.ohama.litellm`
     Expected: a line whose first column (PID) is a number, not `-`. If it shows `-` or is
     missing, restart with `launchctl start com.ohama.litellm` then re-check.

  2. Model listing (NO auth header — proxy allows it):
     `curl -s http://127.0.0.1:4000/v1/models | python3 -c "import sys,json; d=json.load(sys.stdin); ids=[m['id'] for m in d['data']]; print('MODELS:', ids); assert 'qwen-local' in ids, 'qwen-local MISSING'; print('PASS')"`
     Expected: `MODELS: ['qwen-local', ...]` then `PASS`.

  FALLBACK (only if curl to 127.0.0.1:4000 is refused from this shell): retry the same curl
  against `http://host.docker.internal:4000/v1/models`. If THAT works, note in the SUMMARY that
  the call origin cannot reach the loopback proxy directly and the headless agent (which runs on
  the host) should still use `127.0.0.1:4000` — only document the discrepancy; do not change the
  agent config. (Per research the host process reaches 127.0.0.1:4000 fine.)

  Do NOT run a tool-call chat ping here — that takes ~240s and is exercised end-to-end in 02-02.
  </action>
  <verify>`curl http://127.0.0.1:4000/v1/models` returns JSON containing `"id": "qwen-local"` and the assertion prints `PASS`.</verify>
  <done>litellm proxy confirmed running and serving `qwen-local` with no auth. Outputs captured.</done>
</task>

<task type="auto">
  <name>Task 3: Write 02-01-SUMMARY.md with the recorded preflight evidence</name>
  <files>.planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md</files>
  <action>
  Write the SUMMARY following the GSD summary template. Include a "Preflight Evidence" section
  with the VERBATIM command + output pairs from Task 1 and Task 2 (no fabrication — paste real
  output). Record: openhands version, `command -v openhands` path, uv tool list entry, python
  version, launchctl litellm line, and the /v1/models output showing `qwen-local`.

  In the frontmatter set the standard summary fields (phase, plan, subsystem: environment,
  affects: [02-02, 02-03]). Note any deviations found (e.g., PATH needed exporting, or the
  host.docker.internal fallback was required). State explicitly that Docker was NOT touched and
  is not on the critical path for the headless LocalWorkspace run.
  </action>
  <verify>`test -f .planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md && grep -q qwen-local .planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md`</verify>
  <done>02-01-SUMMARY.md exists, contains real preflight outputs including the qwen-local model listing and openhands 1.16.0 version.</done>
</task>

</tasks>

<verification>
- `openhands --version` → 1.16.0
- `command -v openhands` → path under `~/.local/bin`
- `curl -s http://127.0.0.1:4000/v1/models` → JSON listing including `qwen-local`
- 02-01-SUMMARY.md exists with verbatim evidence (no fabricated output)
</verification>

<success_criteria>
The base stack is confirmed present and recorded: OpenHands CLI 1.16.0 on PATH, Python 3.12.x,
litellm proxy serving qwen-local with no auth. Evidence captured for downstream plans. No Docker
action taken. (Satisfies SETUP-01 install-side and SETUP-02 LLM-reachability-side.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-01-SUMMARY.md`
</output>
