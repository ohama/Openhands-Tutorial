---
phase: 02-environment-setup-and-verification
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - "/Users/ohama/projs/OpenHandsTests/captured/.gitkeep"
  - "/Users/ohama/projs/OpenHandsTests/.planning/phases/02-environment-setup-and-verification/02-VERIFICATION-EVIDENCE.md"

autonomous: false

must_haves:
  truths:
    - "With SANDBOX_BASE_CONTAINER_IMAGE=openhands-dotnet-sandbox, an OpenHands session runs `dotnet --version` and observes a 10.0.x version (SC#3 runtime)"
    - "A tool-call ping produces an action (run) followed by an observation in the JSONL log — proving the LLM->tool-call->sandbox loop works (SC#2 / SC#4)"
    - "The four pre-run checks (MLX up, Docker->host, dotnet-in-sandbox, tool-call ping) all pass and their real outputs are recorded"
    - "If the sandbox sub-container cannot resolve host.docker.internal, the AGENT_SERVER_EXTRA_HOSTS fallback was applied and the result recorded"
  artifacts:
    - path: "/Users/ohama/projs/OpenHandsTests/.planning/phases/02-environment-setup-and-verification/02-VERIFICATION-EVIDENCE.md"
      provides: "The verified, real outputs of all four checks — the source of truth the 3부 chapter (02-05) documents"
      contains: "Check 4"
  key_links:
    - from: "OpenHands sandbox sub-container"
      to: "host MLX server at :8000"
      via: "host.docker.internal (with AGENT_SERVER_EXTRA_HOSTS fallback if needed)"
      pattern: "host.docker.internal"
---

<objective>
Run the full pre-run verification checklist against the live, configured stack: re-confirm Checks 1-2, then run Check 3 (`dotnet --version` from inside an OpenHands sandbox session using the custom image) and Check 4 (a tool-call ping proving the full LLM->tool->sandbox loop). Apply the documented fallbacks if the double-slash model string or host.docker.internal-from-sub-container fail. Capture every real output to an evidence file that plan 02-05 documents verbatim.

Purpose: This is the capture gate. It closes Phase 2 SC#2 (in-sandbox tool-call ping), SC#3 (dotnet in a real session), and SC#4 (full checklist passes). Phase 3 must not begin until this passes. The two runtime unknowns (double-slash model string; sub-container host.docker.internal) are only knowable by running this.
Output: 02-VERIFICATION-EVIDENCE.md with all four checks' real outputs, and JSONL capture logs under captured/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-environment-setup-and-verification/02-RESEARCH.md
@.planning/phases/02-environment-setup-and-verification/02-02-SUMMARY.md
@.planning/phases/02-environment-setup-and-verification/02-03-SUMMARY.md

# Reminders:
# - ~240s per tool call on the 35B model — Check 4 alone budgets ~10 minutes.
# - Confirmed model string and build details come from 02-02 / 02-03 SUMMARYs.
# - First sandbox session builds a runtime layer on top of openhands-dotnet-sandbox (5-15 min one-time).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Launch serve with the custom sandbox; confirm dotnet --version in a real session (Check 3)</name>
  <what-built>config.toml [llm]+[sandbox] (02-02/02-03) and the built openhands-dotnet-sandbox image (02-03). This checkpoint exercises a real OpenHands sandbox session end to end.</what-built>
  <how-to-verify>
    Claude launches the server pinned to the custom image (env override is the explicit path per RESEARCH):
      `SANDBOX_BASE_CONTAINER_IMAGE=openhands-dotnet-sandbox openhands serve`
    NOTE: the FIRST session builds a runtime layer on top of the .NET base image — expect 5-15 minutes one-time (RESEARCH Open Question 4). Watch Docker Desktop for build activity; if it hangs >5 min at "DOCKER BUILD STARTED", run `docker system prune -f` and restart (RESEARCH Pitfall 4).

    The user verifies Check 3 in the UI (http://localhost:3000):
    1. New Conversation
    2. Send: `Run this command: dotnet --version`
    3. The agent should execute it via a command action and the observation should show a `10.0.x` version.

    Claude records the observed version into the evidence file. If `dotnet: command not found`: the custom image wasn't used (SANDBOX_BASE_CONTAINER_IMAGE not applied / overridden by UI settings) or PATH is wrong — confirm `docker run --rm openhands-dotnet-sandbox dotnet --version` still works (02-03) and that serve was launched with the env var.
  </how-to-verify>
  <resume-signal>Type "dotnet ok" with the version the agent reported, or paste the error / observation content.</resume-signal>
  <verify>The OpenHands session observation for `dotnet --version` contains `10.0.`; recorded in 02-VERIFICATION-EVIDENCE.md.</verify>
  <done>An OpenHands sandbox session (custom image) runs `dotnet --version` and observes a 10.0.x version.</done>
</task>

<task type="auto">
  <name>Task 2: Tool-call ping (Check 4) with host.docker.internal sub-container fallback</name>
  <files>/Users/ohama/projs/OpenHandsTests/captured/.gitkeep</files>
  <action>
    Create the capture dir: `mkdir -p /Users/ohama/projs/OpenHandsTests/captured && touch /Users/ohama/projs/OpenHandsTests/captured/.gitkeep`.

    Run the headless tool-call ping (proves the full LLM->tool-call->sandbox loop). Budget ~10 min (one ~240s call):
      SANDBOX_BASE_CONTAINER_IMAGE=openhands-dotnet-sandbox \
        openhands --headless -t "Run: echo hello && echo world" --json 2>&1 \
        | tee /Users/ohama/projs/OpenHandsTests/captured/toolcall-ping.jsonl

    PASS criteria in the JSONL:
    - a line with `"type": "action"` and `"action": "run"` (the LLM produced a dispatched tool call), FOLLOWED BY
    - a line with `"type": "observation"` whose content shows `hello` and `world`.
    - NO `APIConnectionError`, `APITimeoutError`, or `LiteLLM.BadRequestError` lines.

    FALLBACK A — model string rejected (`LiteLLM.BadRequestError: model not found`): the double-slash string failed (RESEARCH Pitfall 2). Switch to the MLX alias per 02-02 Task 3 fallback (`mlx_lm.server --help | grep -i alias`; set config.toml model to `openai/<alias>`), then re-run. Record which string worked.

    FALLBACK B — connection error to :8000 from the sandbox while Check 2 (02-02) passed: this is the sub-container host.docker.internal bug (RESEARCH Pitfall 5, issues #12229/#12500). Set the extra-hosts env var and relaunch:
      export AGENT_SERVER_EXTRA_HOSTS="host.docker.internal:host-gateway"
    then re-run the ping. Record whether the env var exists in 1.7 and whether it resolved the failure.

    Record the final PASS (or the applied fallback + result) and the model string actually used in 02-VERIFICATION-EVIDENCE.md.
  </action>
  <verify>captured/toolcall-ping.jsonl contains an `"action": "run"` line followed by an `"type": "observation"` line showing hello/world, with no API error lines.</verify>
  <done>The tool-call ping proves the full LLM->tool->sandbox loop; any required fallback (model alias or AGENT_SERVER_EXTRA_HOSTS) is applied and documented.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the verification evidence file (all four checks)</name>
  <files>/Users/ohama/projs/OpenHandsTests/.planning/phases/02-environment-setup-and-verification/02-VERIFICATION-EVIDENCE.md</files>
  <action>
    Write 02-VERIFICATION-EVIDENCE.md capturing the REAL outputs (no invented values — this file is the source of truth the chapter in 02-05 quotes). Include, in this order:
    - Check 1 (MLX up): `curl http://127.0.0.1:8000/health` output + `/v1/models` model id.
    - Check 2 (Docker->host): the `docker run ... curl host.docker.internal:8000/v1/models` output.
    - Check 3 (dotnet in sandbox session): the version the agent observed (from Task 1).
    - Check 4 (tool-call ping): the relevant action+observation JSONL lines from captured/toolcall-ping.jsonl.
    - CONFIRMED model string actually used (double-slash vs alias).
    - Whether AGENT_SERVER_EXTRA_HOSTS was needed (and if so, that it exists/works in 1.7).
    - First-sandbox-session runtime build duration (for the chapter's patience callout).
    Mark each check PASS/FAIL. All four must be PASS for the phase gate to open.
  </action>
  <verify>02-VERIFICATION-EVIDENCE.md exists and contains sections for Check 1, Check 2, Check 3, Check 4, each marked PASS, plus the confirmed model string.</verify>
  <done>A single evidence file records all four checks' real outputs and the resolved runtime unknowns, ready for the chapter.</done>
</task>

</tasks>

<verification>
- captured/toolcall-ping.jsonl: action run -> observation hello/world, no API errors
- OpenHands session observed dotnet --version = 10.0.x
- 02-VERIFICATION-EVIDENCE.md: all four checks PASS, model string confirmed, fallbacks documented
</verification>

<success_criteria>
Phase 2 SC#2, SC#3 (runtime), SC#4 met: in-sandbox tool-call ping succeeds, `dotnet --version` runs in an OpenHands session, and the four-check checklist passes — the capture gate for Phase 3 is open.
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-04-SUMMARY.md` summarizing pass/fail of each check, the confirmed model string, fallback outcomes, and the one-time runtime build duration. (02-VERIFICATION-EVIDENCE.md holds the verbatim outputs.)
</output>
