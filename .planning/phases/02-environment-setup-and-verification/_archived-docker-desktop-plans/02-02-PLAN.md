---
phase: 02-environment-setup-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["~/.openhands/config.toml"]
autonomous: true

must_haves:
  truths:
    - "`~/.openhands/config.toml` exists with a [llm] section using the openai/ model prefix, host.docker.internal:8000/v1 base_url, dummy api_key, and timeout=600"
    - "MLX server still advertises model id /Users/ohama/llm-system/models/qwen36-35b at /v1/models"
    - "A Docker container can reach the host MLX endpoint via host.docker.internal (preflight curl returns {\"status\": \"ok\"})"
    - "The working model string (double-slash or alias fallback) is recorded as confirmed for use by later plans"
  artifacts:
    - path: "~/.openhands/config.toml"
      provides: "OpenHands [llm] configuration pointing at the local Qwen MLX endpoint"
      contains: "[llm]"
  key_links:
    - from: "~/.openhands/config.toml [llm].base_url"
      to: "host MLX server at :8000"
      via: "host.docker.internal resolved by Docker Desktop"
      pattern: "host\\.docker\\.internal:8000/v1"
---

<objective>
Write `~/.openhands/config.toml` with the [llm] section pointing OpenHands at the local Qwen MLX endpoint, then preflight-verify that (a) the MLX server still advertises the expected model id and (b) a Docker container can reach the host LLM via `host.docker.internal`. Confirm the LiteLLM model string that actually works (double-slash, with MLX-alias fallback).

Purpose: Establishes and de-risks the LLM connection (Phase 2 SC#2) before the slow full tool-call ping in plan 02-04. host.docker.internal-from-Docker is the most commonly misconfigured step; proving it here isolates failures.
Output: A correct, reproducible `~/.openhands/config.toml` and a confirmed working model string recorded in the SUMMARY.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-environment-setup-and-verification/02-RESEARCH.md

# Confirmed facts:
# - MLX model id (from /v1/models): /Users/ohama/llm-system/models/qwen36-35b
# - LiteLLM model string: openai//Users/ohama/llm-system/models/qwen36-35b (double slash, confirmed valid)
# - base_url MUST be host.docker.internal (NOT 127.0.0.1) — Docker containers can't reach host loopback
# - ~35s..240s per call -> timeout=600, num_retries=2, retry_min_wait=300
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm MLX model id and write ~/.openhands/config.toml [llm] section</name>
  <files>~/.openhands/config.toml</files>
  <action>
    1. Reconfirm the advertised model id (it is a path and load-bearing for the model string):
       `curl -s http://127.0.0.1:8000/v1/models | python3 -m json.tool`
       Expected: `data[0].id` == `/Users/ohama/llm-system/models/qwen36-35b`. If it differs, use the actual advertised id (still prefixing `openai/`) and note the change in the SUMMARY.

    2. Create `~/.openhands/` if absent (`mkdir -p ~/.openhands`) and write `~/.openhands/config.toml` with EXACTLY this [llm] section (use the confirmed model id from step 1 after the `openai/` prefix — note the double slash because the id itself begins with `/`):

       [llm]
       model = "openai//Users/ohama/llm-system/models/qwen36-35b"
       api_key = "dummy"
       base_url = "http://host.docker.internal:8000/v1"
       timeout = 600
       num_retries = 2
       retry_min_wait = 300
       retry_max_wait = 600

       Do NOT add the [sandbox] section here — that is plan 02-03 (which owns the sandbox image and appends [sandbox]).
    3. This write is idempotent: if config.toml already exists, ensure the [llm] section matches exactly (overwrite the [llm] block, preserve any [sandbox] block if present).
  </action>
  <verify>`grep -A7 '^\[llm\]' ~/.openhands/config.toml` shows model with the `openai/` prefix, api_key="dummy", base_url with `host.docker.internal:8000/v1`, timeout=600, num_retries=2.</verify>
  <done>~/.openhands/config.toml contains a correct [llm] section with the confirmed model string and host.docker.internal base_url.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight — MLX up (Check 1) and Docker reaches host LLM (Check 2)</name>
  <files>(no files; verification commands only)</files>
  <action>
    Run the two non-OpenHands preflight checks from RESEARCH (these prove the network path without the slow LLM loop):

    Check 1 — MLX server up and model advertised:
      `curl -s http://127.0.0.1:8000/health`  -> expected `{"status": "ok"}`
      `curl -s http://127.0.0.1:8000/v1/models | python3 -m json.tool` -> data[0].id == /Users/ohama/llm-system/models/qwen36-35b

    Check 2 — Docker container reaches the host LLM endpoint:
      `docker run --rm --add-host host.docker.internal:host-gateway curlimages/curl curl -s http://host.docker.internal:8000/v1/models`
      -> expected: the SAME JSON as Check 1 (model id present).

    If Check 2 fails: confirm Docker Desktop's "Allow the default Docker socket to be used" is on (plan 02-01 Task 1) and that the `--add-host host.docker.internal:host-gateway` flag is present. Record the outcome. (Note: this proves the MAIN container path; the sandbox SUB-container path is separately validated in plan 02-04 — RESEARCH Pitfall 5.)
  </action>
  <verify>Check 1: `curl -s http://127.0.0.1:8000/health` returns `{"status": "ok"}`. Check 2: the `docker run ... curl host.docker.internal:8000/v1/models` command prints JSON containing the model id.</verify>
  <done>Both Check 1 and Check 2 pass; Docker can reach the host MLX endpoint via host.docker.internal.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the LiteLLM model string resolves (double-slash, with alias fallback ready)</name>
  <files>(no files; records the confirmed string in SUMMARY)</files>
  <action>
    Sanity-check that the double-slash model string is accepted by the OpenAI-compatible endpoint directly (this isolates the model-string question from the full OpenHands loop, which is slow and tested in 02-04). Send a minimal chat completion using the exact model string and base_url, from the host:

      curl -s http://127.0.0.1:8000/v1/chat/completions \
        -H "Content-Type: application/json" \
        -d '{"model":"/Users/ohama/llm-system/models/qwen36-35b","messages":[{"role":"user","content":"say hi"}],"max_tokens":8}'

    Expected: a JSON chat completion (a `choices[0].message.content`), NOT a "model not found" error. (Note: the raw server takes the bare path as `model`; LiteLLM prepends `openai/` for routing. The double slash only appears in the LiteLLM `model` string.)

    FALLBACK (only if the full ping in 02-04 later returns `LiteLLM.BadRequestError: model not found` for the double-slash string — RESEARCH Pitfall 2): the MLX server can expose a shorter alias. Check the flag with `mlx_lm.server --help 2>&1 | grep -i alias` (exact flag only knowable by running it). If an alias like `qwen35b` is configured, change config.toml `model` to `openai/qwen35b`. Record in the SUMMARY which model string is confirmed working: either `openai//Users/ohama/llm-system/models/qwen36-35b` (default) or the alias form.
  </action>
  <verify>The chat-completions curl returns a JSON response with `choices[0].message.content` populated (no "model not found"). The confirmed model string is recorded for plan 02-04.</verify>
  <done>The model string is confirmed to resolve at the endpoint; config.toml holds the confirmed string and the alias fallback path is documented.</done>
</task>

</tasks>

<verification>
- `grep -A7 '^\[llm\]' ~/.openhands/config.toml` shows the correct [llm] keys/values
- `curl -s http://127.0.0.1:8000/health` -> {"status": "ok"}
- `docker run --rm --add-host host.docker.internal:host-gateway curlimages/curl curl -s http://host.docker.internal:8000/v1/models` -> model id JSON
- chat-completions curl returns a populated choices[0].message.content
</verification>

<success_criteria>
Phase 2 SC#2 (config portion) met: ~/.openhands/config.toml uses the openai/ prefix, host.docker.internal:8000/v1 base_url, dummy api_key, timeout=600; Docker can reach the host LLM; the working model string is confirmed. (The in-sandbox tool-call ping that fully closes SC#2 runs in plan 02-04.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment-setup-and-verification/02-02-SUMMARY.md` recording: the exact [llm] section written, Check 1/Check 2 outputs, and the CONFIRMED working model string (double-slash vs alias) for 02-04 and the chapter.
</output>
