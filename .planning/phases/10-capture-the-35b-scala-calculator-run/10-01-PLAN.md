---
phase: 10-capture-the-35b-scala-calculator-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task1-scaffold.txt
  - .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-write-calc.txt
  - .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-calc-scaffold.txt
  - .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task3-buildtest.txt
  - .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/00-INVOCATION.md
  - .planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md
  - .gitignore
  - oh-workdir-scala/

autonomous: true

must_haves:
  truths:
    - "task2-write-calc.txt instructs the agent to write Calc.scala HIMSELF — it contains NO Scala source body and NONE of the zero-leak words: `sealed trait`, `case class`, `enum`, `recursive descent`/`recursive-descent`, `top-down parser`, `expr()`/`term()`/`factor()`, `precedence level`/`precedence layer`, `left-associativity`/`left-assoc`, `Tokenizer`/`tokenize`/`Token`, `ListBuffer`, `pattern matching`, `@main`, `//> using scala 3`"
    - "task2-write-calc.txt names ONLY the goal/behavior: arithmetic expression string in → integer out; `*` and `/` bind more tightly than `+` and `-`; parentheses override precedence; integer arithmetic only; std-only (no external libraries / no build dependencies)"
    - "The `10-3-2 → 5` left-associativity test is ABSENT from task2-write-calc.txt and PRESENT in task3-buildtest.txt (revealing it in task2 would leak the left-associativity requirement)"
    - "task2-calc-scaffold.txt (fallback only) contains the verbatim std-only Calc.scala from 10-RESEARCH.md §3 (sealed trait Expr ADT + char tokenizer + expr/term/factor recursive descent + pattern-matching eval + @main) — used ONLY if 35B fails per scaffold-fallback policy"
    - "All task prompts point at /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc and carry the bash-only file-write constraint (no file_editor / str_replace — errors due to security_risk field)"
    - "task1-scaffold.txt explicitly tells the agent to verify scala-cli works and run a trivial `@main def hello() = println(\"SCALA_OK\")` via `scala-cli run` (this is infrastructure setup, parallel to v1.2's `cargo new`; the agent runs it, satisfying SCAL-01 unaided-first criterion that the agent sets up scala-cli itself); it warns the first run may download artifacts for 30-60s"
    - "task3-buildtest.txt runs all THREE canonical tests (`2+3*4 → 14`, `(2+3)*4 → 20`, `10-3-2 → 5`) via `scala-cli run Calc.scala -- \"<expr>\"`, forbids adding any parser library/build.sbt/scala-parser-combinators/fastparse, and demands honest reporting of the exact output of each test"
    - "oh-workdir-scala/ exists, is empty, and is gitignored (git check-ignore returns oh-workdir-scala); v1's oh-workdir/, v1.1's oh-workdir-122b/, and v1.2's oh-workdir-rust/ remain ignored and untouched"
    - "scala-cli is INSTALLED on the host (it is NOT present at plan start) via `brew install scala-cli`; the trivial `scala-cli run` PRE-WARMS the artifact cache (first run downloads 30-120s) so the agent's task1 is fast; JDK 17 present; litellm proxy at 127.0.0.1:4000 responds and lists openai/qwen-35b — all recorded as PREFLIGHT GREEN"
  artifacts:
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task1-scaffold.txt"
      provides: "Scaffold prompt — agent verifies scala-cli + runs a trivial Scala file itself in oh-workdir-scala/calc"
      contains: "scala-cli run"
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-write-calc.txt"
      provides: "Unaided calculator prompt — behavior only (precedence, parentheses, std-only), zero implementation/technique hints, NO 10-3-2 test"
      contains: "arithmetic expression"
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-calc-scaffold.txt"
      provides: "Fallback calculator prompt — verbatim verified-working Calc.scala from research §3; used only on disclosed fallback"
      contains: "sealed trait Expr"
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task3-buildtest.txt"
      provides: "Run + 3-canonical-test + report prompt; reveals the 10-3-2 left-assoc test; demands honest reporting"
      contains: "10-3-2"
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/00-INVOCATION.md"
      provides: "openhands headless invocation reference for qwen-35b + per-task JSONL filename table + scaffold-fallback policy summary"
      contains: "openai/qwen-35b"
    - path: ".planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md"
      provides: "Live preflight evidence (scala-cli installed + version, trivial run pre-warmed cache prints SCALA/PREFLIGHT_OK, JDK 17, proxy lists qwen-35b, gitignore confirmed)"
      contains: "PREFLIGHT GREEN"
    - path: ".gitignore"
      provides: "oh-workdir-scala/ ignore entry (alongside existing oh-workdir/, oh-workdir-122b/, oh-workdir-rust/)"
      contains: "oh-workdir-scala"
  key_links:
    - from: "task1-scaffold.txt trivial scala-cli run"
      to: "oh-workdir-scala/calc/ project dir created by agent + warm artifact cache"
      via: "agent executes scala-cli run via bash TerminalAction"
      pattern: "scala-cli run"
    - from: "task2-write-calc.txt behavior description"
      to: "task3-buildtest.txt 3-canonical-test contract"
      via: "agreed contract — Calc.scala prints integer result of the expression"
      pattern: "14"
    - from: "all task prompts"
      to: "oh-workdir-scala scratch dir"
      via: "Working directory line at top of each prompt"
      pattern: "oh-workdir-scala"
---

<objective>
Prepare the 35B Scala task-prompt set and run the gating host preflight so the execute plan (10-02) can launch a clean, honest capture of a minimal Scala 3 arithmetic calculator.

This plan does NOT run OpenHands. It: (1) creates the `task-prompts-scala/` prompt files — task1 (scaffold: verify scala-cli + trivial run), task2 (write calculator unaided), task2-scaffold (fallback only), task3 (run all three canonical tests + report); (2) ensures `oh-workdir-scala/` exists, is empty, and is gitignored without touching the existing v1 / v1.1 / v1.2 scratch dirs; (3) runs the gating preflight: scala-cli is NOT installed on this host — `brew install scala-cli` first, then a real `scala-cli run` to PRE-WARM the artifact cache (the first run downloads compiler artifacts for 30-120s; pre-warming makes the agent's task1 fast), confirm JDK 17, confirm litellm proxy serves qwen-35b. Writes 10-01-PREFLIGHT.md as live evidence the launchpad is green.

Purpose: The unaided-first protocol is the milestone's whole point (SCAL-01). task2-write-calc.txt must hand the agent the GOAL/BEHAVIOR (expression string → integer; `*`/`/` bind tighter than `+`/`-`; parentheses override; std-only) and NOTHING about technique — no `sealed trait`, no `recursive descent`, no `expr()`/`term()`/`factor()`, no `Tokenizer`, no `pattern matching`, no `@main`, no `//> using scala 3`. If the prompt leaks the parser technique or the left-associativity requirement, the captured run is no longer a real test of 35B's unaided Scala 3 capability and the 7부 chapter loses its honesty. The `10-3-2 → 5` test is reserved for task3 precisely because it reveals left-associativity — surfacing it in task2 would leak that requirement.

Output: 4 prompt files + 00-INVOCATION.md under `task-prompts-scala/`, a clean gitignored `oh-workdir-scala/`, scala-cli installed + cache pre-warmed, a green preflight report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-capture-the-35b-scala-calculator-run/10-RESEARCH.md

# v1.2 prompts to mirror in SHAPE (NOT content — Scala is a different language; the reference impl lives in 10-RESEARCH.md §3):
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/task-prompts-rust/00-INVOCATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 task prompts (task1-scaffold, task2-write-calc unaided, task2-calc-scaffold fallback, task3-buildtest)</name>
  <files>
    .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task1-scaffold.txt
    .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-write-calc.txt
    .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-calc-scaffold.txt
    .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task3-buildtest.txt
  </files>
  <action>
First create the directory:
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala

Write each prompt using a quoted heredoc (`cat > FILE <<'EOF' ... EOF`) so $variables and backslashes are preserved literally.

---

**task1-scaffold.txt** — agent verifies scala-cli works and runs a trivial Scala file itself (this is infrastructure setup; satisfies SCAL-01's "agent sets up scala-cli itself" criterion — the operator does NOT pre-write Calc.scala). The `@main def hello()` here is acceptable because it is the scaffold convention, NOT the calculator. Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-scala

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

The directory /Users/ohama/projs/OpenHandsTests/oh-workdir-scala is empty. Your task:
verify that scala-cli is installed and set up a minimal Scala project here.

Step 1: Confirm scala-cli works.
  scala-cli --version

Step 2: Create a project subdirectory and write a minimal Scala file.
  mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc
  cd /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc

  Write a file named Hello.scala with this exact content (use a heredoc):
    @main def hello() = println("SCALA_OK")

Step 3: Run it.
  scala-cli run Hello.scala

  Note: The first run may download compiler artifacts and take 30-60 seconds. That
  is normal. Wait for the program output at the END — do not treat the download
  progress lines as an error and do not retry prematurely.
  Expected output: SCALA_OK

Step 4: Confirm the output and report the scala-cli version and whether the run
succeeded (whether SCALA_OK appeared).

Do NOT write any calculator code yet — a later task handles that.
```

Rationale: Tells the agent the exact directory and a trivial program to run. `@main` is acceptable here because this is infrastructure setup, not the calculator — the agent sees the scaffold convention but must still DESIGN the calculator itself in task2. The "first run may take 30-60 seconds" note prevents the agent from treating a slow first compile / artifact download as an error and panic-retrying (10-RESEARCH.md Pitfall 2). The cache will already be warm from the preflight, so the agent's task1 should be fast — but the note is kept as a safety net.

---

**task2-write-calc.txt** — THE UNAIDED PROMPT. This is the milestone's core test of whether 35B can write a Scala 3 arithmetic calculator from goal/behavior alone. Describe BEHAVIOR, not technique. Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

A scala-cli project directory exists here (you set it up in a previous step). Start
by reviewing it:

  cd /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc
  ls

Your task: write a Scala source file named Calc.scala that, when given an arithmetic
expression string as a command-line argument, evaluates it and prints the integer
result.

Requirements:
- Input: a single command-line argument, e.g. "2+3*4"
- Output: the integer result on its own line, e.g. 14
- Operators supported: + - * / where * and / bind more tightly than + and -
  (for example, 2+3*4 should evaluate to 14, not 20)
- Parentheses override the normal precedence (for example, (2+3)*4 should evaluate
  to 20)
- Integer arithmetic only (no floating point)
- Use ONLY the Scala standard library — no external libraries and no build
  dependencies

After writing Calc.scala, show its contents:
  cat Calc.scala

Do NOT run or compile yet — a later task handles running and testing.
```

**CRITICAL — what task2-write-calc.txt must NOT contain (fully unaided per the milestone's design). NONE of these words/phrases may appear:**
  - `sealed trait` / `case class` / `enum` — leak the ADT representation choice
  - `recursive descent` / `recursive-descent` / `top-down parser` — leak the parsing technique
  - `expr()` / `term()` / `factor()` — leak the function decomposition
  - `precedence level` / `precedence layer` — leak the grammar structure
  - `left-associativity` / `left-assoc` / "associate left-to-right" — leak how to handle 10-3-2
  - `Tokenizer` / `tokenize` / `Token` / `lexer` — leak the lexer
  - `ListBuffer` — leak an implementation detail
  - `pattern matching` / `match` — leak the evaluator technique
  - `@main` — leak the entry-point annotation (the agent saw `@main` in task1's scaffold, which is fine; do NOT re-name it here as the required calculator entry point)
  - `//> using scala 3` — leak the directive (let the agent discover it or not; scala-cli defaults to Scala 3 anyway)
  - NO code sample, NO snippet, NO "here's the shape"
  - NO `10-3-2` test case (that test reveals the left-associativity requirement — it belongs in task3 ONLY)

(The agent is EXPECTED to potentially hit one of the 35B Scala 3 blind spots — precedence/associativity bug, Scala 3-vs-2 syntax slip such as `object extends App`, exhaustiveness warning, multi-digit tokenizer slip — and self-correct in task3. That self-correction IS the desired material for the 7부 chapter.)

---

**task2-calc-scaffold.txt** — FALLBACK ONLY (used ONLY if the scaffold-fallback policy in 10-02 triggers; its mere use is a disclosed deviation in CAPTURE-MANIFEST). Use this exact body — it embeds the verbatim verified-working Calc.scala from 10-RESEARCH.md §3 (copy it CHARACTER-FOR-CHARACTER from the research file: `//> using scala 3` directive + `sealed trait Expr` ADT with Num/Add/Sub/Mul/Div + the `Token` enum + char tokenizer + the expr/term/factor recursive-descent `Parser` class + the pattern-matching `eval` + `@main def calc(expression: String)`):

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool.

A scala-cli project directory exists here. Write a file named Calc.scala with the
following EXACT contents using a heredoc:

  cd /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc
  cat > Calc.scala <<'SCALA_EOF'
  <<< PASTE THE VERBATIM Calc.scala FROM 10-RESEARCH.md §3 HERE, UNCHANGED >>>
  SCALA_EOF

After writing, confirm the contents:
  cat Calc.scala

Do NOT add any external libraries or build files. A later task runs and tests it.
```

When writing THIS file from the planner: the outer heredoc that creates task2-calc-scaffold.txt must use a DIFFERENT delimiter than the inner one. Use outer `'SCAFFOLD_EOF'` and inner `'SCALA_EOF'`. Read the verbatim Calc.scala body straight from 10-RESEARCH.md §3 (lines beginning `//> using scala 3` through the final `println(eval(ast))`) so it is byte-identical to the verified reference. The agent, when given THIS prompt, runs the inner heredoc as part of its own bash session.

---

**task3-buildtest.txt** — run all THREE canonical tests, fix on error, report honestly. This is where the `10-3-2 → 5` left-associativity test is FIRST revealed (it was withheld from task2 to avoid leaking left-associativity). Critical: this prompt teaches HOW to run + test (the `scala-cli run Calc.scala -- "<expr>"` command and the expected outputs) but NOT how the calculator should be written. Use this exact body:

```
Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/calc

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
tool.

A Scala source file Calc.scala exists in this directory. Your task: run it with
scala-cli and verify it produces the correct output for these three test cases:

  scala-cli run Calc.scala -- "2+3*4"    -> should print: 14
  scala-cli run Calc.scala -- "(2+3)*4"  -> should print: 20
  scala-cli run Calc.scala -- "10-3-2"   -> should print: 5

(The first run may download compiler artifacts and take 30-60 seconds — that is
normal; wait for the program output at the end.)

Step 1: Compile and run the first test.
  scala-cli run Calc.scala -- "2+3*4"

If compilation fails, read the compiler error carefully, edit Calc.scala to fix the
error at hand (using heredoc/printf/tee — NOT file_editor), and retry. Repeat until
the compilation succeeds. Report each error you saw and what you changed to fix it.

Step 2: Once compilation succeeds, run ALL THREE tests and capture the exact output
of each:
  scala-cli run Calc.scala -- "2+3*4"
  scala-cli run Calc.scala -- "(2+3)*4"
  scala-cli run Calc.scala -- "10-3-2"

Step 3: Report results explicitly. For EACH of the three test cases, state the EXACT
output the program printed (verbatim — copy from the terminal), whether it matched
the expected value (14, 20, 5 respectively), and — if you made any fixes — what
compiler or runtime errors you encountered and exactly what you changed in
Calc.scala.

Constraints:
- Do NOT add external library dependencies. No build.sbt, no project/plugins.sbt,
  no scala-parser-combinators, no fastparse, no cats-parse. The whole program must
  use only the Scala standard library.
- If a test produces the WRONG number (for example 10-3-2 prints 9 instead of 5),
  that is a real result — investigate and fix Calc.scala if you can, but report the
  wrong output honestly first. Fix only the error at hand and report what you changed
  and WHY.
- Report every error you encountered and how you fixed it — this is the point of the
  exercise.
- DO NOT modify results to appear successful. If a test printed the wrong number, if
  compilation never succeeded, or if the program errored, state that clearly. An
  honest failure is more valuable than a fabricated success.
```

Rationale: tells the agent HOW to test (the exact `scala-cli run ... -- "<expr>"` command and the three expected values) but not HOW the calculator should be written. The `10-3-2 → 5` case is the left-associativity probe (10-RESEARCH.md §5 Failure Mode 1: a right-recursive parser gives `10-(3-2)=9`; `2+3*4` may still print 14 with that bug, so the third test is ESSENTIAL). "Do not modify results to appear successful" is the honesty rule carried from v1/v1.1/v1.2.
  </action>
  <verify>
Verify task2-write-calc.txt has ZERO technique/implementation leaks and no left-assoc leak:
  cd /Users/ohama/projs/OpenHandsTests
  P=.planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala
  # All of these must print 0 (zero-leak word list from 10-RESEARCH.md §4.2):
  grep -c -iE "sealed trait|case class|enum|recursive[ -]descent|top-down parser" "$P/task2-write-calc.txt"
  grep -c -iE "expr\(\)|term\(\)|factor\(\)|precedence level|precedence layer" "$P/task2-write-calc.txt"
  grep -c -iE "left-assoc|left-associativity|associate left-to-right" "$P/task2-write-calc.txt"
  grep -c -iE "tokeniz|token|lexer|ListBuffer|pattern match" "$P/task2-write-calc.txt"
  grep -c -iE "@main|//> using scala|scala-parser-combinators|fastparse" "$P/task2-write-calc.txt"
  grep -c "10-3-2" "$P/task2-write-calc.txt"   # MUST be 0 — left-assoc test is withheld until task3

# These must be >=1 (goal/behavior is stated):
  grep -c -iE "arithmetic expression" "$P/task2-write-calc.txt"
  grep -c -iE "standard library|no external libr" "$P/task2-write-calc.txt"
  grep -c "oh-workdir-scala" "$P/task2-write-calc.txt"
  grep -c -iE "2\+3\*4" "$P/task2-write-calc.txt"   # the precedence test IS allowed in task2

# Scaffold fallback DOES contain the verbatim impl:
  grep -c "sealed trait Expr" "$P/task2-calc-scaffold.txt"   # >=1
  grep -c "@main def calc" "$P/task2-calc-scaffold.txt"      # >=1
  grep -c "//> using scala 3" "$P/task2-calc-scaffold.txt"   # >=1

# task1 verifies scala-cli + runs trivial file:
  grep -c "scala-cli --version" "$P/task1-scaffold.txt"      # >=1
  grep -c "scala-cli run Hello.scala" "$P/task1-scaffold.txt" # >=1
  grep -c "SCALA_OK" "$P/task1-scaffold.txt"                 # >=1

# task3 reveals all three canonical tests (including 10-3-2) and bans parser libs:
  grep -c "10-3-2" "$P/task3-buildtest.txt"                  # >=1 (left-assoc test lives here)
  grep -c -E "2\+3\*4|\(2\+3\)\*4" "$P/task3-buildtest.txt"  # >=1
  grep -c -iE "scala-parser-combinators|fastparse" "$P/task3-buildtest.txt"  # >=1 (named as BANNED)
  grep -c -iE "sealed trait|recursive descent|expr\(\)" "$P/task3-buildtest.txt"  # MUST be 0 (no technique leak in task3 either)
  </verify>
  <done>
4 prompt files exist in task-prompts-scala/. task2-write-calc.txt names the goal/behavior (expression→integer, `*`/`/` bind tighter, parentheses override, std-only) and has ZERO technique leaks (no sealed trait/case class/enum, no recursive descent, no expr/term/factor, no tokenizer/token, no pattern matching, no @main, no //> using scala 3) and ZERO `10-3-2` left-assoc leak. task2-calc-scaffold.txt embeds the verbatim verified-working Calc.scala from research §3 for fallback. task1-scaffold.txt tells the agent to verify scala-cli and run a trivial Scala file itself (with the first-run download warning). task3-buildtest.txt reveals all three canonical tests (including the withheld `10-3-2 → 5`), bans parser libraries, and demands honest reporting. All prompts reference /Users/ohama/projs/OpenHandsTests/oh-workdir-scala and carry the bash-only file-write constraint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 00-INVOCATION.md (qwen-35b headless invocation reference + JSONL filename table + scaffold-fallback policy)</name>
  <files>
    .planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/00-INVOCATION.md
  </files>
  <action>
Write a fresh 00-INVOCATION.md (not a copy — the v1.2 version refers to oh-workdir-rust and a server). Use this exact body:

```
# 35B Scala Calculator Run — Invocation Reference

## Per-task command pattern

  OPENHANDS_SUPPRESS_BANNER=1 \
    LLM_MODEL="openai/qwen-35b" \
    LLM_BASE_URL="http://127.0.0.1:4000/v1" \
    LLM_API_KEY="dummy" \
    OPENHANDS_WORK_DIR="/Users/ohama/projs/OpenHandsTests/oh-workdir-scala" \
    openhands --headless --json --yolo --override-with-envs \
    -t "$(cat task-prompts-scala/<prompt-file>)" \
    2>oh-workdir-scala/<task>.stderr.log \
    | tee oh-workdir-scala/<task>.jsonl

## Key notes

- `--override-with-envs` is REQUIRED (configures the LLM from env vars).
- `--yolo` runs autonomously (no confirmation prompts).
- `--headless --json` emits JSONL events to stdout — captured by `tee` into a per-task .jsonl file.
- Each invocation is a fresh ZERO-MEMORY conversation: the agent does not remember
  prior tasks. Files persist on disk (so task2 can `cat` what task1 produced; task3
  can `cat` Calc.scala); the agent rediscovers state via `ls`/`cat`.
- Bash-only file writes are enforced via the per-prompt IMPORTANT note (file_editor
  errors in this OpenHands setup — it requires a security_risk field that fails
  validation).
- The CLI exits 0 REGARDLESS of task success — DO NOT judge outcome from exit code.
  Judge ONLY from the JSONL contents (FinishAction present, exit codes inside
  TerminalObservations, actual command output).
- Run each invocation in the BACKGROUND (run_in_background=true) and POLL the JSONL
  until the run settles (FinishAction event appears, or no new events for ~60s of
  sustained idle, or ConversationErrorEvent).
- The scala-cli artifact cache is PRE-WARMED by the 10-01 preflight (the trivial
  `scala-cli run` already downloaded the compiler), so the agent's runs should NOT
  pay the 30-120s first-run download. If a run still appears to stall on artifact
  download, that is the cache miss — wait it out; it is not an error.
- TIMING NOTE: 35B measured per-LLM-call timing on this hardware is ~5.3s (v1 F#) /
  ~3.8s (v1.2 Rust). Budget ~4-10 minutes per task. DO NOT cite the legacy
  "~14-32s/call" figure as a measurement — it was a v1 pre-run PREDICTION, never
  measured (carried as a standing honesty rule from the v1.2 audit / STATE.md).
  Full run estimate: 15-40 minutes.

## Per-task JSONL filenames

| Task | Prompt file | JSONL filename |
|------|-------------|----------------|
| task1-scaffold     | task-prompts-scala/task1-scaffold.txt     | oh-workdir-scala/task1-scaffold.jsonl |
| task2-write-calc   | task-prompts-scala/task2-write-calc.txt   | oh-workdir-scala/task2-write-calc.jsonl |
| task2-calc-scaffold (fallback only) | task-prompts-scala/task2-calc-scaffold.txt | oh-workdir-scala/task2-calc-scaffold.jsonl |
| task3-buildtest    | task-prompts-scala/task3-buildtest.txt    | oh-workdir-scala/task3-buildtest.jsonl |

Stderr is captured in parallel as oh-workdir-scala/<task>.stderr.log for each.

## Scaffold-fallback policy summary (full rules in 10-RESEARCH.md §3)

The scaffold (task2-calc-scaffold.txt) is invoked ONLY if ALL the following are true
after the unaided task2 attempt:

1. The agent's `scala-cli run` (in task3) fails on the SAME root error class across
   3+ consecutive compile attempts within task3 (e.g., always the same type error,
   or always the same syntax error), with no syntactic variation in the fix attempts.
2. The fix attempts show no new diagnostic reasoning — the agent is cycling on
   identical fixes with the same result.
3. The JSONL shows the agent is stuck (repeated identical commands).

Do NOT trigger scaffold fallback if:
- The agent is making diverse fix attempts (even if all failing) — that is genuine
  error-and-fix and is the desired chapter material.
- The compile error changes between attempts — the agent is making progress.
- A test prints the WRONG number (e.g., 10-3-2 -> 9, the left-assoc bug) but the
  program compiles and runs — that is a genuine, valuable result; give the agent the
  chance to discover and self-correct it. A buggy-but-running calculator is a
  SUCCESSFUL unaided attempt at the task2 stage.

Scala is more in-distribution for 35B than FsLex was (v1); the bar to scaffold is
high. Only invoke if the agent demonstrably cannot produce compiling Scala at all.

If fallback IS triggered, that fact MUST be disclosed in CAPTURE-MANIFEST.md with
the failure reasons documented. did-write-calc-unaided = NO.
```
  </action>
  <verify>
  cd /Users/ohama/projs/OpenHandsTests
  P=.planning/phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala
  test -s "$P/00-INVOCATION.md" && echo "INVOCATION present"
  grep -c "openai/qwen-35b" "$P/00-INVOCATION.md"          # >=1
  grep -c "oh-workdir-scala" "$P/00-INVOCATION.md"         # >=4 (each task row + pattern)
  grep -c -i "scaffold" "$P/00-INVOCATION.md"              # >=1
  grep -c "DO NOT judge outcome from exit code" "$P/00-INVOCATION.md"  # >=1
  grep -c "14-32s/call" "$P/00-INVOCATION.md"              # >=1 (named as the FORBIDDEN figure)
  # exactly 5 files in the prompts dir (4 prompts + INVOCATION):
  ls "$P/" | wc -l   # 5
  </verify>
  <done>
00-INVOCATION.md exists with the qwen-35b invocation pattern (OPENHANDS_WORK_DIR=oh-workdir-scala), polling guidance, JSONL filename table, the cache-pre-warmed note, the measured-timing note that explicitly forbids citing the ~14-32s/call prediction as a measurement, and a summary of the scaffold-fallback policy. The task-prompts-scala/ directory contains exactly 5 files (task1-scaffold.txt, task2-write-calc.txt, task2-calc-scaffold.txt, task3-buildtest.txt, 00-INVOCATION.md).
  </done>
</task>

<task type="auto">
  <name>Task 3: Preflight — install scala-cli, pre-warm cache, gitignore oh-workdir-scala, confirm JDK/proxy; write 10-01-PREFLIGHT.md</name>
  <files>
    .gitignore
    oh-workdir-scala/
    .planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md
  </files>
  <action>
**STEP 1 — install scala-cli (THE GATING STEP — it is NOT installed on this host).**

  which scala-cli || echo "NOT_INSTALLED"
  # If NOT_INSTALLED:
  brew install scala-cli
  # Then confirm it landed on PATH (the brew formula may need a link / shellenv):
  which scala-cli || brew link scala-cli
  scala-cli --version   # must print: Scala CLI version: 1.14.0 (or newer)

If `brew install scala-cli` fails (network, formula issue), HALT and record the
failure in 10-01-PREFLIGHT.md as PREFLIGHT RED — 10-02 cannot run without scala-cli.

**STEP 2 — JDK check.** scala-cli needs a JDK; Java 17 is already present on this host:

  java -version 2>&1   # must show 17.x or newer (e.g., openjdk version "17.0.19")

If no JDK >= 17, record RED and HALT (scala-cli's brew formula build-depends on
openjdk@17, so this should already be satisfied).

**STEP 3 — trivial run to PRE-WARM the artifact cache (CRITICAL).** The first
`scala-cli run` after a fresh install downloads the Scala compiler + Zinc/Bloop
artifacts (30-120s). Doing it HERE in preflight pre-warms the cache so the agent's
task1 is fast:

  printf '@main def preflight() = println("PREFLIGHT_OK")\n' > /tmp/Preflight.scala
  scala-cli run /tmp/Preflight.scala 2>&1 | tail -5
  # Expected (after any download output): PREFLIGHT_OK, exit 0.

Confirm PREFLIGHT_OK appears. This both verifies the toolchain end-to-end AND warms
the cache. If it does not print PREFLIGHT_OK, record RED and HALT.

**STEP 4 — gitignore.** Append `oh-workdir-scala/` to /Users/ohama/projs/OpenHandsTests/.gitignore IF not already present. The existing entries (`book/`, `oh-workdir/`, `oh-workdir-122b/`, `oh-workdir-rust/`) MUST remain untouched.

  cd /Users/ohama/projs/OpenHandsTests
  grep -q '^oh-workdir-scala/$' .gitignore || echo 'oh-workdir-scala/' >> .gitignore
  cat .gitignore   # confirm all 5 entries present

**STEP 5 — scratch dir.** Create empty scratch dir. If it already exists with stale capture artifacts from a prior aborted attempt (calc/, *.jsonl, *.stderr.log), remove ONLY those (NEVER touch oh-workdir/, oh-workdir-122b/, or oh-workdir-rust/).

  mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-scala
  ls -la /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/
  # If stale artifacts present, e.g.:
  #   rm -rf /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/{calc,*.jsonl,*.stderr.log}
  # ONLY if those entries exist; do not blindly rm.

**STEP 6 — gitignore verification.** Confirm all 4 scratch dirs are ignored:

  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-scala   # must print: oh-workdir-scala
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-rust    # must print: oh-workdir-rust   (v1.2 still ignored)
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-122b    # must print: oh-workdir-122b   (v1.1 still ignored)
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir         # must print: oh-workdir        (v1 still ignored)

**STEP 7 — proxy check.** Confirm litellm proxy serves qwen-35b:

  curl -s -m 5 http://127.0.0.1:4000/v1/models | grep -c "qwen-35b"   # must be >=1

If the curl times out or qwen-35b is absent, HALT — 10-02 cannot run without the
proxy serving the model. Report the symptom; do not guess at fixes.

**STEP 8 — write 10-01-PREFLIGHT.md.** Capture every check above with its actual output. Use this structure:

```
# Phase 10 Preflight Report

Date: <date from `date -u +%FT%TZ`>

## Toolchain
- scala-cli installed by preflight: YES/NO (was not present at start)
- scala-cli: <output of scala-cli --version>
- JDK: <output of java -version, first line>

## Cache pre-warm (trivial run)
- command: scala-cli run /tmp/Preflight.scala
- output (tail): <...>
- PREFLIGHT_OK printed: YES/NO
- artifact cache warm: YES (first-run download completed in preflight)

## Scratch dir
- /Users/ohama/projs/OpenHandsTests/oh-workdir-scala/ exists: YES/NO
- empty: YES/NO (or list contents)

## Gitignore
- oh-workdir-scala ignored: YES (git check-ignore output: <path>)
- oh-workdir-rust ignored: YES (...)
- oh-workdir-122b ignored: YES (...)
- oh-workdir ignored: YES (...)

## Proxy
- curl http://127.0.0.1:4000/v1/models response: <truncated>
- qwen-35b present: YES/NO

## Verdict
PREFLIGHT GREEN — 10-02 can launch.    # or PREFLIGHT RED with the reason
```

If any check fails, write `PREFLIGHT RED` with the specific failure and HALT (do not proceed; 10-02 depends on a green preflight with a warm scala-cli cache).
  </action>
  <verify>
  cd /Users/ohama/projs/OpenHandsTests

  # scala-cli installed and on PATH:
  which scala-cli && echo "scala-cli on PATH"
  scala-cli --version | grep -c -E "Scala CLI version" # >=1

  # JDK present:
  java -version 2>&1 | grep -c -E "version \"(17|18|19|2[0-9])" # >=1

  # gitignore has all 5 entries:
  grep -c '^oh-workdir-scala/$' .gitignore      # 1
  grep -c '^oh-workdir-rust/$' .gitignore       # 1 (still there)
  grep -c '^oh-workdir-122b/$' .gitignore       # 1 (still there)
  grep -c '^oh-workdir/$' .gitignore            # 1 (still there)
  grep -c '^book/$' .gitignore                  # 1 (still there)

  # All 4 scratch dirs ignored:
  git check-ignore oh-workdir-scala             # prints oh-workdir-scala
  git check-ignore oh-workdir-rust              # prints oh-workdir-rust
  git check-ignore oh-workdir-122b              # prints oh-workdir-122b
  git check-ignore oh-workdir                   # prints oh-workdir

  # Scratch dir exists:
  test -d oh-workdir-scala && echo "scratch dir ready"

  # Proxy live:
  curl -s -m 5 http://127.0.0.1:4000/v1/models | grep -c "qwen-35b"   # >=1

  # Preflight report exists and is GREEN:
  test -s .planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md && echo "PREFLIGHT report present"
  grep -c "PREFLIGHT GREEN" .planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md   # 1 (HALT if 0 — preflight failed)
  grep -c -i "PREFLIGHT_OK" .planning/phases/10-capture-the-35b-scala-calculator-run/10-01-PREFLIGHT.md   # >=1 (cache pre-warm evidence)
  </verify>
  <done>
scala-cli is installed (it was NOT present at start) and on PATH at version >= 1.14.0; the trivial `scala-cli run` printed PREFLIGHT_OK and pre-warmed the artifact cache (so the agent's task1 will not pay the first-run download). JDK 17 confirmed. oh-workdir-scala/ exists, is empty, and is gitignored; v1's oh-workdir/, v1.1's oh-workdir-122b/, and v1.2's oh-workdir-rust/ are still ignored and untouched (audit-verifiable by git check-ignore). litellm proxy responds and lists qwen-35b. 10-01-PREFLIGHT.md records all evidence with verdict PREFLIGHT GREEN. 10-02 may launch.
  </done>
</task>

</tasks>

<verification>
- task-prompts-scala/ contains exactly 5 files (task1-scaffold.txt, task2-write-calc.txt, task2-calc-scaffold.txt, task3-buildtest.txt, 00-INVOCATION.md).
- task2-write-calc.txt has ZERO technique/implementation leaks (greps for sealed trait / case class / enum / recursive descent / expr() / tokenizer / pattern matching / @main / //> using scala 3 all return 0), ZERO `10-3-2` (left-assoc withheld), and DOES name the behavior (arithmetic expression, `*`/`/` bind tighter, std-only, oh-workdir-scala, the 2+3*4 precedence test).
- task2-calc-scaffold.txt embeds the verbatim verified Calc.scala (grep `sealed trait Expr` >= 1, `@main def calc` >= 1, `//> using scala 3` >= 1) — staged but unused unless fallback triggers.
- task1-scaffold.txt verifies scala-cli (`scala-cli --version`) and runs a trivial file (`scala-cli run Hello.scala`, SCALA_OK) with the first-run download warning.
- task3-buildtest.txt reveals all three canonical tests including `10-3-2`, bans parser libraries (scala-parser-combinators/fastparse named as banned), and has ZERO technique leaks.
- 00-INVOCATION.md names openai/qwen-35b, includes the JSONL filename table, polling guidance, cache-pre-warm note, the forbidden-timing-figure note, and the scaffold-fallback policy.
- scala-cli is installed (was NOT at start), on PATH, >= 1.14.0; the trivial run pre-warmed the cache (PREFLIGHT_OK captured). JDK 17 present.
- .gitignore has all 5 entries; git check-ignore returns each scratch-dir name.
- 10-01-PREFLIGHT.md records scala-cli version, JDK, cache pre-warm, proxy response, gitignore, and the verdict `PREFLIGHT GREEN`.
- oh-workdir-scala/ exists on disk and is empty.
</verification>

<success_criteria>
The unaided-first protocol is encoded in task2-write-calc.txt (SCAL-01 made achievable: the prompt asks for a calculator matching a behavior contract without leaking the parser technique or the left-associativity requirement). The scala-cli self-setup is assigned to the agent in task1 (SCAL-01 "agent sets up scala-cli itself" criterion). The fallback prompt is staged but unused. The scratch dir is clean, gitignored, and does not touch prior milestones' scratch dirs. scala-cli is installed AND its cache pre-warmed by the preflight (the gating host step), JDK 17 + litellm proxy confirmed live in 10-01-PREFLIGHT.md with verdict GREEN — so 10-02 can launch a clean, honest capture without paying the first-run download.
</success_criteria>

<output>
After completion, create `.planning/phases/10-capture-the-35b-scala-calculator-run/10-01-SUMMARY.md` summarizing: which files were created, the preflight verdict, whether scala-cli had to be installed (it did) and the version, whether the cache pre-warm printed PREFLIGHT_OK, and any notable observations (e.g., whether the proxy responded normally).
</output>
