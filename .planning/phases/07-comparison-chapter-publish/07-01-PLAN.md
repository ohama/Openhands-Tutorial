---
phase: 07-comparison-chapter-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/appendix-c-comparison.md
autonomous: true

must_haves:
  truths:
    - "A new Korean appendix file exists at src/appendix-c-comparison.md, ready for SUMMARY wiring (plan 07-02)."
    - "The chapter discloses the setup asymmetry up front: 35B's Lexer.fsl was scaffolded (attempt 2, after attempt-1 FsLex failure); 122B wrote Lexer.fsl unaided."
    - "The chapter answers CMP-01 (a): did 122B write the .fsl lexer unaided? YES, with verbatim event-9 quote from task2-lexer-unaided.jsonl."
    - "The chapter answers CMP-01 (b): error-and-fix cycle comparison — 35B 4 iterations (task3 events 10–30); 122B 9 iterations (task5 events 12–74) with cross-task context (task3 events 24/42/46)."
    - "The chapter answers CMP-01 (c): measured speed — 35B implied ~5.3s/call (wall-time ÷ TerminalAction, derived from 03-02-RUN-NOTES.md task durations); 122B 6.3s/call avg (directly measured from JSONL gaps in CAPTURE-MANIFEST.md). Both models in the same ~5–7s/call range; total wall-clock differs because of iteration count (67 vs 150 TerminalActions), not per-call latency."
    - "Every speed number, error count, and capability claim has an inline citation pointing to a specific captured file + event/line range (CMP-02)."
    - "No fabricated or idealized figures — the stale '~14–32s/call' pre-run prediction is NOT used; the derived 35B figure is explicitly labeled as approximation (wall-time ÷ TerminalAction)."
    - "Source diffs are accurate: 122B Parser.fsy uses `| MINUS factor { -$2 }` for unary minus (NOT a `%right NEG` token); 122B Lexer.fsl uses `INT (int (new string(lexbuf.Lexeme)))`; 35B's scaffolded lexer uses `LexBuffer<_>.LexemeString lexbuf`."
    - "Korean prose with English technical terms inline (matching existing chapter style: 'OpenHands', 'FsLex', 'TerminalAction', etc.)."
  artifacts:
    - path: "src/appendix-c-comparison.md"
      provides: "부록 C: 35B vs 122B 모델 비교 chapter content"
      contains: "부록 C"
      min_lines: 150
  key_links:
    - from: "src/appendix-c-comparison.md"
      to: "captured-122b/logs/task2-lexer-unaided.jsonl"
      via: "verbatim citation of event 9 (Lexer.fsl write) and event 12 (cat exit_code=0)"
      pattern: "task2-lexer-unaided.jsonl"
    - from: "src/appendix-c-comparison.md"
      to: "captured-122b/logs/task5-buildtest.jsonl"
      via: "9-iteration error-fix citation (events 12–74)"
      pattern: "task5-buildtest.jsonl"
    - from: "src/appendix-c-comparison.md"
      to: "captured-122b/CAPTURE-MANIFEST.md"
      via: "timing table (6.3s/call avg, 150 TA, 1229.2s total)"
      pattern: "6\\.3s|1229|150 TerminalAction"
    - from: "src/appendix-c-comparison.md"
      to: "captured/CAPTURE-MANIFEST.md (35B v1)"
      via: "scaffolded lexer disclosure + attempt-1 failure (94+27+16 TerminalActions)"
      pattern: "94\\+27\\+16|scaffold"
    - from: "src/appendix-c-comparison.md"
      to: "03-02-RUN-NOTES.md (35B attempt 2)"
      via: "task duration table (186s/27TA, 16s/2TA, 77s/15TA, 45s/14TA, 32s/9TA → 356s/67TA)"
      pattern: "356s|67 TerminalAction"
---

<objective>
Draft the new Korean comparison chapter `src/appendix-c-comparison.md` (부록 C: 35B vs 122B 모델 비교) using verbatim evidence from both the v1 35B captured run and the v1.1 122B captured run. The chapter must honestly disclose the setup asymmetry (scaffolded vs unaided lexer), use only measured/derived figures from real captured artifacts, and provide inline citations for every comparison claim.

Purpose: Satisfies CMP-01 (chapter exists, addresses capability + error-fix + measured speed) and CMP-02 (every claim traces to real captured evidence). Produces the artifact that plan 07-02 will wire into SUMMARY.md.

Output: A single new Markdown file at `src/appendix-c-comparison.md` — Korean prose, ASCII tables, fenced code blocks for verbatim source quotes, inline citations in parentheses.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-comparison-chapter-publish/07-RESEARCH.md

# Source-of-truth files — quote verbatim, cite by file + event/line range
@.planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md
@.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
@.planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES.md
@.planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES-attempt1.md
@.planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Lexer.fsl
@.planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Parser.fsy
@.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/final-source/Lexer.fsl
@.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/final-source/Parser.fsy

# Existing book style references (Korean prose, ASCII tables, code fences)
@src/SUMMARY.md
@src/appendix-a-repro.md
@src/appendix-b-troubleshooting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 부록 C — 35B vs 122B 비교 chapter from captured evidence</name>
  <files>src/appendix-c-comparison.md</files>
  <action>
Create a NEW file `src/appendix-c-comparison.md`. The file must NOT yet be referenced from `src/SUMMARY.md` (plan 07-02 handles wiring; mdBook `create-missing = false` requires file-first).

**Read these source-of-truth files first** (do not paraphrase numbers without re-checking):
- `.planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md` — 122B timing table (line ~125–143), Lexer Outcome (line ~33–63), Error-and-Fix Record (line ~65–104), Comparison Hook (line ~147–154)
- `.planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES.md` — 35B attempt-2 Per-Task Outcome Table (line ~25–34, durations 186s/16s/77s/45s/32s with 27/2/15/14/9 TerminalActions; total 356s/67TA), Error-and-Fix Cycle (line ~48–100, events 9–30 of task3-parser.jsonl)
- `.planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES-attempt1.md` — 35B attempt-1 failure (3 agents, 94+27+16 TerminalActions, all `%%` confusion)
- `.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md` § "On the lexer and .fsproj (scaffolded, not agent-authored)" (line ~104) — confirms Lexer.fsl was provided verbatim in 35B attempt 2
- `.planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Lexer.fsl` and `Parser.fsy` — quote verbatim
- `.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/final-source/Lexer.fsl` and `Parser.fsy` — quote verbatim

**Chapter structure** (Korean headers; match existing 부록 A/B style — `#` for title, `##` for sections):

```
# 부록 C: 모델 비교 — Qwen2.5-35B vs 122B

## 실험 설계와 공정성 전제
[2–4 paragraphs. Disclose UP FRONT: v1의 35B 실행은 시도 1이 FsLex 형식(`%%` 혼동)에서 실패한 뒤
Lexer.fsl을 task2 프롬프트에 그대로 제공받아 진행한 attempt 2 결과다. v1.1의 122B 실행은
Lexer.fsl을 처음부터 스스로 작성하도록 했다. 즉 이 비교는 의도된 설계 차이 — 35B가 못한 일을
122B가 할 수 있는지 보는 능력 테스트 — 위에서 이루어진다. 동일 조건의 head-to-head 비교가
아니라는 점을 명시한다. (인용: 03-02-RUN-NOTES-attempt1.md § "Root Cause Analysis: FsLex Issues";
captured/CAPTURE-MANIFEST.md § "On the lexer and .fsproj (scaffolded, not agent-authored)";
captured-122b/CAPTURE-MANIFEST.md § "Lexer Outcome (RUN122-01/02)")]

## 1. 렉서(.fsl) 작성 능력
[Subsection: 35B 시도 1의 실패]
- attempt 1: 3 OpenHands agents (94+27+16 TerminalActions = 137+) 모두 `%%` separator를 사용한
  비유효 FsLex 파일 생성. FsYacc/yacc 스타일 친숙도 편향이 원인.
- 인용: 03-02-RUN-NOTES-attempt1.md § "Root Cause Analysis: FsLex Issues" / Problem 1 (line 76–79);
  captured/CAPTURE-MANIFEST.md § "Run attempts" (line ~128).

[Subsection: 35B attempt 2 — 스캐폴딩 적용]
- Lexer.fsl 내용을 task2 프롬프트에 그대로 제공. 에이전트는 6 events / 2 TerminalActions로 파일 복사.
- 인용: 03-02-RUN-NOTES.md Per-Task Outcome Table line ~28 (task2 lexer 16s).

[Subsection: 122B — 무지원 첫 시도 성공]
- task2-lexer-unaided.jsonl event 9에서 cat heredoc로 Lexer.fsl 작성.
- event 12에서 `cat Lexer.fsl` exit_code=0 확인.
- `rule tokenize = parse` 형식 사용, `%%` 혼동 없음.
- 인용: captured-122b/CAPTURE-MANIFEST.md § "RUN122-01" (line 159–165);
  captured-122b/logs/task2-lexer-unaided.jsonl event 9, 12.

[VERBATIM CODE BLOCK — quote 122B's final Lexer.fsl from
 .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Lexer.fsl —
 wrap in ```fsharp fence; show the full ~19 lines including the `new string(lexbuf.Lexeme)` line.]

## 2. 오류-수정 사이클 비교

[Subsection: 35B — task3 Parser.fsy의 4회 반복]
- Event 10: `%start` 빠짐 → FSY000 오류 → 추가
- Event 16: 잘못된 결합형 `%start <int> start` → parse error
- Event 20: 동일 parse error 재발 → 다시 시도
- Event 26: `LexBuffer<_>.FromText` 존재하지 않는 API → FS0039 → `LexBuffer<char>.FromString`로 수정
- Event 30: 빌드 성공 (`calc net10.0 성공 (0.7초)`)
- 인용: 03-02-RUN-NOTES.md § "Error-and-Fix Cycle (Branch A)" (line 48–100);
  captured/logs/task3-parser.jsonl events 9–30.

[Subsection: 122B — task5 Lexer API의 9회 반복]
[ASCII table: iteration | event | agent's INT line | error
 0 | 12 | `as s { INT (int s) }`              | FS0001, FS0039
 1 | 18 | `rule tokenize lexbuf = parse` + `Lexing.matched` | FS0038, FS0001
 2 | 30 | `Lexing.matched lexbuf`             | FS0001, FS0039
 3 | 40 | `tokenize lexbuf` + `Lexing.matched lexbuf` | FS0039
 4 | 50 | `FSharp.Text.Lexing.matched`        | FS0039 (non-existent)
 5 | 56 | `Lexing.matchedText`                | FS0039 (non-existent)
 6 | 60 | full namespace + `matchedText`      | FS0039
 7 | 66 | `lexbuf.ToString()`                  | exit_code=134 runtime crash
 8 | 70 | `lexbuf.Lexeme`                      | FS0193 (char array → int)
 9 | 74 | `new string(lexbuf.Lexeme)`         | Build SUCCESS]
- 추가: 동일 lexer 오류가 task3-parser.jsonl events 24, 42, 46에서 먼저 등장 (parser 작업 중 빌드 시
  나타남). 에이전트는 task5로 수정을 미뤘다.
- 인용: captured-122b/CAPTURE-MANIFEST.md § "Error-and-Fix Record (RUN122-03)" (line 65–104);
  captured-122b/logs/task5-buildtest.jsonl events 12–74.

[질적 차이 정리: 35B는 4 iterations / 20 events / FsYacc 선언 문법 + 1 가짜 API. 122B는
 9 iterations / 62 events / FSharp.Text.Lexing API 자체에 대한 추측 (runtime crash 포함).
 둘 다 진짜 자율 복구. 122B의 영역(FsLex API)이 객관적으로 더 어려운 탐색 공간이었다.]

## 3. 처리 속도 측정 (정직한 보고)

[CRITICAL HONESTY SECTION — must not use the stale "~14–32s/call" pre-run prediction.]

[Subsection: 122B 직접 측정 (JSONL 타임스탬프)]
[ASCII table from captured-122b/CAPTURE-MANIFEST.md line 130–137:
 Task              | Total       | TerminalActions | Avg LLM-call gap
 task1-scaffold    | 167.5s      | 20              | 6.6s
 task2-lexer-unaided| 57.7s       | 7               | 3.2s
 task3-parser      | 252.3s      | 37              | 5.5s
 task4-evaluator   | 362.7s      | 47              | 6.6s
 task5-buildtest   | 389.0s      | 39              | 7.0s
 합계              | 1229.2s     | 150             | 6.3s avg]
- 측정 방법: ObservationEvent timestamp → 다음 ActionEvent timestamp (순수 모델 사고 시간, bash 실행 제외).
- 범위: 1.8s (최소) – 46.6s (최대, 오류 진단 중 긴 추론).
- 인용: captured-122b/CAPTURE-MANIFEST.md § "Timing Summary (CMP-01)" line 125–141.

[Subsection: 35B attempt 2 — 파생 추정값 (직접 측정 아님)]
[ASCII table from 03-02-RUN-NOTES.md Per-Task Outcome Table line 27–31:
 Task             | Duration | TerminalActions | 함의 평균 (wall ÷ TA)
 task1-scaffold   | 186s     | 27              | 6.9s
 task2-lexer      | 16s      | 2               | 8.0s
 task3-parser     | 77s      | 15              | 5.1s
 task4-evaluator  | 45s      | 14              | 3.2s
 task5-buildtest  | 32s      | 9               | 3.6s
 합계             | 356s     | 67              | ~5.3s avg]
- 측정 한계: 이 수치는 task wall-time ÷ TerminalAction count로 파생된 근사값이다. bash 실행
  시간도 포함되며, 122B와 같은 방식 (LLM-call gap = ObservationEvent → 다음 ActionEvent)으로
  순수 모델 사고 시간만 측정한 값이 아니다. 03-02-RUN-NOTES.md는 task별 시작/종료 타임스탬프만
  기록하며 LLM-call 단위 timestamp를 따로 집계하지 않았다.
- 인용: 03-02-RUN-NOTES.md § "Per-Task Outcome Table" (line 25–34).

[Subsection: 정직한 비교 결론]
- 두 모델 모두 이 하드웨어에서 호출당 약 5–7초 범위로, 모델 크기로부터 예상되는 큰 격차는
  관찰되지 않았다.
- 총 경과시간 차이 (35B 356s vs 122B 1229s)는 **호출당 지연이 아니라 호출 횟수**가 만든 것이다.
  35B는 67 TerminalActions, 122B는 150 TerminalActions — 122B의 9회 lexer 오류-수정 사이클이
  주된 추가 비용.
- 명시할 점: 122B CAPTURE-MANIFEST.md의 vs-35B-speed 코멘트는 35B에 대해 "~14–32s/call"
  수치를 인용하지만, 이 수치는 06-RESEARCH.md §1.3의 사전 예측 (pre-run probe)이지 attempt 2의
  실제 측정값이 아니다. 본 부록은 실제 attempt 2 데이터 (파생 ~5.3s/call)를 사용한다.

## 4. 최종 소스 코드 차이

[Subsection: Lexer.fsl INT 토큰 라인]
[Table:
 모델       | 라인                                                         | 출처
 35B (스캐폴딩)| `let s = LexBuffer<_>.LexemeString lexbuf` / `INT (System.Int32.Parse s)` | captured/final-source/Lexer.fsl
 122B (자체 작성)| `INT (int (new string(lexbuf.Lexeme)))`                   | captured-122b/final-source/Lexer.fsl]
- 두 형식 모두 동일 결과. 35B 스캐폴딩은 상위 API (`LexBuffer<_>.LexemeString`) 사용; 122B는
  9회 시도 끝에 하위 형식 (`lexbuf.Lexeme` char array → `new string(...)` → `int`)으로 수렴.

[Subsection: Parser.fsy 차이]
[Table:
 항목         | 35B                                  | 122B
 문법 구조     | flat `expr` (term/factor 없음)         | `expr → term → factor` 명시적 계층
 단항 마이너스   | 없음                                 | `factor: | MINUS factor { -$2 }` (별도 `%right NEG` 토큰 아님)
 `%token<int>` | `%token <int> INT` (공백 있음)         | `%token<int> INT` (공백 없음)
 `%start`/`%type`| 별도 라인 (올바른 FsYacc 형식)         | 동일]
- 122B의 문법이 더 구조화되어 있고 단항 마이너스 규칙을 추가하지만, **정답성** 차이는 없다
  (둘 다 14/20/5 통과). 스타일 차이일 뿐 "더 나은" 코드라고 주장하지 않는다.
- 출처: captured/final-source/Parser.fsy (35B); captured-122b/final-source/Parser.fsy (122B).

[OPTIONAL VERBATIM CODE BLOCKS for both Parser.fsy files in ```ocaml or ```text fences,
 if space permits. Quote actual file contents verbatim.]

## 5. 결론

[3–5 short paragraphs.]
- 두 모델 모두 `2+3*4=14`, `(2+3)*4=20`, `10-3-2=5` 세 테스트 케이스를 통과했다.
  - 35B: captured/logs/task5-buildtest.jsonl (FinalMsg, 모든 케이스 정답);
    03-02-RUN-NOTES.md Per-Task Outcome Table line 31.
  - 122B: captured-122b/test-output.txt (호스트 재실행); captured-122b/logs/task5-buildtest.jsonl
    events 76/78/80.
- 핵심 차별점은 **렉서 작성 능력**이다. 35B는 attempt 1에서 137+ TerminalActions로 실패했고
  Lexer.fsl 스캐폴딩이 필요했다. 122B는 무지원으로 한 번에 구조적으로 유효한 FsLex 파일을
  생성했다.
- 속도는 **모델 크기와 비례하는 큰 차이가 관찰되지 않았다**. 두 모델 모두 호출당 약 5–7초.
  총 시간 차이는 호출 횟수 (67 vs 150) — 즉 오류-수정 사이클의 길이 — 에서 비롯된다.
- 한계: 35B 데이터는 attempt 2 기준이며 lexer가 스캐폴딩된 설정이다. 더 큰 모델이 작은 모델보다
  "더 낫다"는 일반화는 이 단일 사례에서 지지되지 않으며, 이 부록의 범위를 넘는다.

## 출처 (Sources)

[Final list of all cited files with absolute-relative paths from project root:
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Lexer.fsl
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Parser.fsy
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/task2-lexer-unaided.jsonl
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/task5-buildtest.jsonl
 - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/test-output.txt
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/captured/final-source/Lexer.fsl
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/captured/final-source/Parser.fsy
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES.md
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/03-02-RUN-NOTES-attempt1.md
 - .planning/milestones/v1-phases/03-capture-the-openhands-run/captured/logs/task3-parser.jsonl]
```

**Style and discipline rules (HARD):**
1. Korean prose; English technical terms inline (OpenHands, FsLex, FsYacc, TerminalAction, Lexer.fsl, Parser.fsy, etc.) matching existing chapter style.
2. ASCII tables only — pipe-and-dash format. No HTML, no Mermaid.
3. Code blocks in triple-backtick fences with language identifier (```fsharp, ```text, ```ocaml).
4. NO markdown links to draft entries or external URLs in the body (citations are inline in parentheses by file path + event/line reference).
5. NEVER use the stale "~14–32s/call" pre-run prediction for 35B. Use the derived ~5.3s/call from 03-02-RUN-NOTES.md Per-Task Outcome Table, with explicit "파생 추정값 (wall-time ÷ TerminalAction)" disclaimer.
6. NEVER claim "122B is slower per call." The data shows the opposite framing: both ~5–7s/call; the 122B total wall-clock is longer because of 150 vs 67 TerminalActions (iteration count).
7. NEVER claim "122B is faster per call than 35B" either — the measurements are not apples-to-apples (one is direct JSONL gap, the other is wall-time ÷ TA approximation including bash time). Use the honest framing: "두 모델 모두 약 5–7s/call 범위."
8. Quote source code VERBATIM from the captured `final-source/` files (do not retype from memory). Verify the 122B Lexer.fsl actually has `INT (int (new string(lexbuf.Lexeme)))`, the 122B Parser.fsy actually has `| MINUS factor { -$2 }`, and the 35B's scaffolded Lexer.fsl actually uses `LexBuffer<_>.LexemeString lexbuf`. If the file content differs from this plan's description, trust the file.
9. Setup-asymmetry disclosure is in the OPENING section, not buried.
10. The file MUST exist and have content; do NOT yet add an entry in `src/SUMMARY.md` (that is plan 07-02's job, and adding it now will fail any local `mdbook build` attempt).
  </action>
  <verify>
1. File exists: `test -f src/appendix-c-comparison.md && wc -l src/appendix-c-comparison.md` — should print line count >= 150.
2. Title present: `head -1 src/appendix-c-comparison.md` — should output a line containing `부록 C`.
3. Setup asymmetry in opening section: `grep -n "스캐폴딩\|scaffold\|attempt 1\|attempt 2" src/appendix-c-comparison.md | head -5` — should match in the first ~40 lines.
4. Citations to all four primary sources present (one grep each):
   - `grep -c "task2-lexer-unaided" src/appendix-c-comparison.md` >= 1
   - `grep -c "task5-buildtest" src/appendix-c-comparison.md` >= 1
   - `grep -c "CAPTURE-MANIFEST" src/appendix-c-comparison.md` >= 2
   - `grep -c "03-02-RUN-NOTES" src/appendix-c-comparison.md` >= 2
   - `grep -c "94+27+16\|137" src/appendix-c-comparison.md` >= 1 (attempt-1 failure citation)
5. Real timing figures present, stale figure absent:
   - `grep -c "6.3s\|1229" src/appendix-c-comparison.md` >= 1 (122B direct measurement)
   - `grep -c "5.3s\|356s\|67 TerminalAction\|wall-time" src/appendix-c-comparison.md` >= 1 (35B derived)
   - `grep -c "14[–-]32" src/appendix-c-comparison.md` == 0 (stale figure NOT used as a claim about measured 35B speed; OK if it appears once explicitly labeled as a pre-run prediction being corrected)
6. Source-code accuracy:
   - `grep -c "new string(lexbuf.Lexeme)" src/appendix-c-comparison.md` >= 1
   - `grep -c "LexBuffer<_>.LexemeString\|LexBuffer<_>\\.LexemeString" src/appendix-c-comparison.md` >= 1
   - `grep -c "MINUS factor" src/appendix-c-comparison.md` >= 1
   - `grep -c "%right NEG\|NEG token" src/appendix-c-comparison.md` == 0 (must NOT claim 122B used a separate NEG token)
7. SUMMARY.md NOT yet modified by this plan: `grep -c "appendix-c" src/SUMMARY.md` == 0.
  </verify>
  <done>
- `src/appendix-c-comparison.md` exists with >=150 lines of Korean prose, ASCII tables, and verbatim-quoted code blocks.
- Setup asymmetry (35B scaffolded vs 122B unaided) is disclosed in the opening section.
- Sections cover: 렉서 작성 능력 (capability), 오류-수정 사이클 (error-fix), 처리 속도 (measured speed, honestly framed), 최종 소스 차이 (verbatim code diffs), 결론.
- Every speed/iteration/capability claim has an inline citation pointing to a specific captured file + event/line range.
- The 35B per-call figure is labeled as derived (wall-time ÷ TerminalAction) with explicit measurement-asymmetry note vs the 122B direct JSONL measurement.
- No stale "~14–32s/call" claim about measured 35B speed. No "122B is slower per call" or "122B is faster per call" overclaim — both models are ~5–7s/call range.
- Source code quotes match the actual `captured/final-source/` and `captured-122b/final-source/` files verbatim.
- `src/SUMMARY.md` is unchanged (plan 07-02 owns that edit).
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

1. File exists and is non-trivial in size:
   `wc -l src/appendix-c-comparison.md` returns >= 150.

2. All 4 mandated sections present (capability, error-fix, speed, source diffs):
   `grep -cE "^##? (실험 설계|1\\. 렉서|2\\. 오류|3\\. 처리 속도|4\\. 최종 소스|5\\. 결론)" src/appendix-c-comparison.md` >= 5.

3. CMP-01 coverage (capability + error-fix + speed) verifiable:
   - Capability claim present with citation: `grep -B1 -A2 "task2-lexer-unaided" src/appendix-c-comparison.md`
   - Error-fix iteration counts present: `grep -E "9 iteration|9회|4 iteration|4회" src/appendix-c-comparison.md`
   - Speed numbers (6.3s, ~5.3s, 150, 67) all appear: `grep -cE "6\\.3|5\\.3|150|67" src/appendix-c-comparison.md` >= 4.

4. CMP-02 coverage (every claim traceable):
   - Inline citations to JSONL files: `grep -c "\\.jsonl" src/appendix-c-comparison.md` >= 4.
   - Citations to MANIFEST and RUN-NOTES: `grep -c "CAPTURE-MANIFEST\\|RUN-NOTES" src/appendix-c-comparison.md` >= 4.

5. Honesty checks pass:
   - No stale prediction used as fact: `grep -c "14[–-]32" src/appendix-c-comparison.md` == 0, OR the only match is in a context explicitly disclaiming it as a pre-run prediction.
   - Setup asymmetry disclosed early: `grep -n "스캐폴딩\\|attempt 1" src/appendix-c-comparison.md | head -1` → line number <= 50.

6. SUMMARY.md NOT touched: `git diff --name-only src/SUMMARY.md` returns empty.

7. mdBook would still build (sanity — chapter file alone doesn't break anything because it isn't referenced yet):
   `mdbook build 2>&1 | grep -cE "ERROR|error:" || echo 0` returns 0.
</verification>

<success_criteria>
- `src/appendix-c-comparison.md` exists with the structure above.
- All 6 honesty constraints (setup asymmetry, real timing, no fabrication, accurate source diffs, Korean prose, ASCII art) satisfied.
- All `must_haves.truths` verifiable from the file content via the grep commands in <verify>.
- Ready for plan 07-02 to wire into SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/07-comparison-chapter-publish/07-01-SUMMARY.md` recording:
- Final file path and line count.
- Section headings created.
- Notable verbatim quotes used (1–2 lines from 122B Lexer.fsl, 1 line from 122B Parser.fsy unary-minus rule).
- Any deviation from the planned structure with reason.
- Confirmation that SUMMARY.md was not modified.
</output>
