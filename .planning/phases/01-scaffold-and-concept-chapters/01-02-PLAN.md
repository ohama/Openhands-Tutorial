---
phase: 01-scaffold-and-concept-chapters
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/ch01-agentic-ai/overview.md
  - src/ch01-agentic-ai/concepts.md
autonomous: true

must_haves:
  truths:
    - "A reader new to the topic can, after reading overview.md, restate in their own words how agentic AI differs from a reactive chatbot"
    - "concepts.md defines all four core terms: tool/function calling, agent loop, plan→write→test→run, memory/context"
    - "Each of the four vocabulary terms has a PROSE forward pointer (no Markdown link) to where it appears in the run (4부)"
    - "All prose is Korean; technical terms (agent loop, tool calling, EventLog, ActionEvent) stay in English"
  artifacts:
    - path: "src/ch01-agentic-ai/overview.md"
      provides: "CONCEPT-01: defines agentic AI and contrasts it with a reactive chatbot"
      min_lines: 40
      contains: "챗봇"
    - path: "src/ch01-agentic-ai/concepts.md"
      provides: "CONCEPT-02: four core vocabulary terms, each with a prose forward pointer to 4부"
      min_lines: 60
      contains: "tool calling"
  key_links:
    - from: "src/ch01-agentic-ai/concepts.md"
      to: "4부 walkthrough (Phase 4 draft chapters)"
      via: "PROSE forward pointer per term (e.g. '4부 빌드와 테스트 단계에서 ... 볼 수 있습니다'), NOT a Markdown link"
      pattern: "4부"
    - from: "src/ch01-agentic-ai/overview.md"
      to: "reactive chatbot contrast"
      via: "explicit comparison framing agentic loop vs single request-response"
      pattern: "반응형|reactive|챗봇"
---

<objective>
Write the 1부 concept chapter: overview.md (what agentic AI is, contrasted with a reactive chatbot — CONCEPT-01) and concepts.md (the four core vocabulary terms, each with a prose forward pointer to the run — CONCEPT-02). These overwrite the H1-only stubs created in plan 01-01.

Purpose: Grounds the reader in agentic AI before any OpenHands or F# material; every vocabulary term is anchored forward to where the reader will see it in the real run (4부), so the chapter is not an isolated glossary.
Output: Full Korean prose in src/ch01-agentic-ai/overview.md and src/ch01-agentic-ai/concepts.md.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-and-concept-chapters/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md

Depends on plan 01-01 (book.toml, SUMMARY.md, and the two ch01 stub files must already exist).
Key inputs:
- 01-RESEARCH.md Pitfall 4 (vocabulary needs forward pointers) and "Concept ↔ Component Mapping" table + the authoring instruction with the example sentence.
- ARCHITECTURE.md Part 3 mapping table and section 2.6 (CodeActAgent's plan→write→test→run methodology).
Constraint (01-RESEARCH.md Code Examples / forward-pointer guidance): forward references are PROSE ONLY (e.g. "4부 '빌드와 테스트 단계'에서 ... 볼 수 있습니다"). Do NOT write Markdown links to Phase 4 chapter files — they are drafts and would 404 in the deployed Phase 1 book.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write overview.md (agentic AI vs reactive chatbot)</name>
  <files>src/ch01-agentic-ai/overview.md</files>
  <action>
Overwrite /Users/ohama/projs/OpenHandsTests/src/ch01-agentic-ai/overview.md (currently an H1-only stub). Keep the H1 `# 에이전틱 AI 개요`. Write in KOREAN prose; keep technical terms in English (agent loop, tool calling, LLM).

Content (CONCEPT-01 — must let a newcomer restate the difference):
1. 반응형 챗봇(reactive chatbot)의 동작: 사용자 입력 → LLM이 한 번 응답 → 끝. 외부 세계에 행동하지 않고, 스스로 다음 단계를 정하지 않음. 텍스트 생성기.
2. 에이전틱 AI(agentic AI)의 동작: 목표를 받고 스스로 계획을 세워, 툴을 호출해 행동(action)하고, 그 결과(observation)를 관찰하며, 끝날 때까지 반복하는 루프(agent loop)로 동작. LLM은 "무엇을 할지 결정하는 두뇌"이고, 실제 행동은 툴 호출로 환경에 영향을 준다.
3. 핵심 차이를 명시적으로 대조: (a) 한 번 응답 vs 반복 루프, (b) 텍스트만 생성 vs 환경에 행동, (c) 사람이 매 단계 지시 vs 스스로 다음 단계 결정, (d) 상태 없음 vs 진행 상황을 기억(memory/context). 가능하면 짧은 대조 표 또는 불릿으로.
4. 왜 중요한가: 코드를 짜고, 빌드하고, 실패를 보고, 스스로 고치는 일을 사람 개입 없이 수행할 수 있다 — 이 튜토리얼에서 OpenHands가 F# 계산기를 만드는 것이 그 실제 사례임을 한 문장으로 예고(프로즈, 링크 금지).
5. 4개 핵심 용어(tool calling, agent loop, plan→write→test→run, memory/context)는 다음 장 '핵심 개념과 용어'에서 자세히 다룬다고 안내 — 이 링크는 같은 디렉터리 내 실제 파일이므로 Markdown 링크 허용: `[핵심 개념과 용어](concepts.md)`.

Aim for ~40-70 lines of substantive prose. Do NOT teach F#. Do NOT link to Phase 2-5 draft chapters.
  </action>
  <verify>File begins with `# 에이전틱 AI 개요`; contains an explicit chatbot-vs-agent contrast (grep for 챗봇 and 루프); >= 40 non-blank lines; the only Markdown chapter link present is to concepts.md (no `()`-style or ../ch0[2345] links).</verify>
  <done>overview.md defines agentic AI, contrasts it point-by-point with a reactive chatbot, and a newcomer could restate the difference; Korean prose, English technical terms.</done>
</task>

<task type="auto">
  <name>Task 2: Write concepts.md (four vocabulary terms with prose forward pointers)</name>
  <files>src/ch01-agentic-ai/concepts.md</files>
  <action>
Overwrite /Users/ohama/projs/OpenHandsTests/src/ch01-agentic-ai/concepts.md (currently an H1-only stub). Keep the H1 `# 핵심 개념과 용어`. Write in KOREAN prose; technical terms in English.

Define exactly these four core terms (CONCEPT-02), each as its own `## ` section. After each definition, add ONE prose forward-pointer sentence (no Markdown link) to where it appears in 4부, drawn from the mapping table in 01-RESEARCH.md:

1. `## 툴 호출 (tool / function calling)` — LLM이 미리 정의된 툴(함수)의 스키마를 받고, 구조화된 호출(이름+인자)을 출력해 환경에 행동을 지시하는 메커니즘. OpenHands에서는 ActionEvent(CmdRunAction, FileEditAction 등)로 나타남. 포워드 포인터: "4부 '코드 작성 단계'에서 OpenHands가 FileWriteAction / FileEditAction으로 .fsl·.fsy 파일을 만드는 장면에서 tool calling을 직접 볼 수 있습니다."
2. `## 에이전트 루프 (agent loop)` — 목표가 끝날 때까지 '프롬프트 준비 → LLM 호출 → 행동 분류·실행 → 관찰 추가'를 반복하는 while-not-finished 루프. OpenHands의 Conversation.step(). 포워드 포인터: "4부 전체(계획 → 코드 작성 → 빌드와 테스트)가 이 루프가 한 바퀴씩 도는 모습입니다."
3. `## 계획 → 작성 → 테스트 → 실행 (plan → write → test → run)` — 에이전트가 목표를 작은 단계로 나눠 탐색·분석·구현·검증하는 방법론(OpenHands CodeActAgent의 4단계: Explore → Analyze → Implement → Verify). 포워드 포인터: "4부의 각 장이 이 단계 하나씩에 대응합니다."
4. `## 메모리와 컨텍스트 (memory / context window)` — 에이전트가 지금까지 한 일을 기억하는 방식. OpenHands는 모든 상호작용을 append-only EventLog에 기록하고, 길어지면 condensation(CondensationSummaryEvent)으로 압축. 포워드 포인터: "4부의 긴 다단계 실행에서 EventLog가 쌓이고 필요하면 context가 압축되는 상황을 볼 수 있습니다."

Also add a short intro paragraph and, optionally, a closing concept→component mapping table (개념 ↔ OpenHands 컴포넌트 ↔ 4부 등장 위치) consistent with 01-RESEARCH.md's table — this reinforces the forward pointers. Each forward pointer MUST be prose (the word "4부" present), NOT a Markdown link to a draft file.

Aim for ~60-100 lines. Do NOT link to Phase 2-5 draft chapters.
  </action>
  <verify>File begins with `# 핵심 개념과 용어`; contains all four term headings (grep for "tool", "에이전트 루프", "plan", "메모리"); the string "4부" appears at least 4 times (one forward pointer per term); no Markdown links of the form `](../ch0[345]` or `]()` ; >= 60 non-blank lines.</verify>
  <done>concepts.md defines tool calling, agent loop, plan→write→test→run, and memory/context, each with a prose forward pointer to 4부; Korean prose, English technical terms, no dead links.</done>
</task>

</tasks>

<verification>
- Both files are full Korean prose (not stubs) and begin with their correct H1.
- overview.md (CONCEPT-01): explicit agentic-AI-vs-reactive-chatbot contrast a newcomer can restate.
- concepts.md (CONCEPT-02): four terms defined, each with a PROSE forward pointer to 4부.
- No Markdown links to unwritten Phase 2-5 draft chapters anywhere (prose forward pointers only); the only allowed chapter link is overview.md → concepts.md.
- `mdbook build` still exits 0 after these files are written.
</verification>

<success_criteria>
Maps to ROADMAP Phase 1 success criteria #2 (concept chapter defines agentic AI and distinguishes it from a reactive chatbot), #3 (vocabulary chapter defines the four terms each with a forward pointer), and #5 (Korean prose, English technical terms). Implements REQUIREMENTS CONCEPT-01 and CONCEPT-02.
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffold-and-concept-chapters/01-02-SUMMARY.md
</output>
