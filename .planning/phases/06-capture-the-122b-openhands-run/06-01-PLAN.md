---
phase: 06-capture-the-122b-openhands-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task1-scaffold.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided-retry.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-scaffold.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task3-parser.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task4-evaluator.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task5-buildtest.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task6-fix.txt
  - .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/00-INVOCATION.md
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "task2-lexer-unaided.txt instructs the agent to WRITE Lexer.fsl itself — it contains NO lexer body, NO `rule tokenize = parse` block, NO action code"
    - "task2-lexer-unaided.txt names the 8 token names (INT, PLUS, MINUS, STAR, SLASH, LPAREN, RPAREN, EOF) for parser agreement and nothing more revealing"
    - "task2-lexer-scaffold.txt (fallback only) contains the verbatim v1 Lexer.fsl body, used ONLY if 2 unaided attempts fail"
    - "All task prompts point at oh-workdir-122b and instruct bash-only file writes (no file_editor)"
    - "oh-workdir-122b/ is gitignored (git check-ignore returns it)"
    - "The litellm proxy serves qwen-122b at http://127.0.0.1:4000/v1"
  artifacts:
    - path: ".planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt"
      provides: "The unaided lexer prompt — token names only, no lexer source"
      contains: "Write calc/Lexer.fsl"
    - path: ".planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task1-scaffold.txt"
      provides: "Scaffold prompt with the known-good .fsproj (FixLineDirectives), workdir = oh-workdir-122b"
      contains: "FixLineDirectives"
    - path: ".planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-scaffold.txt"
      provides: "Fallback lexer prompt (verbatim v1 Lexer.fsl) — used only on disclosed fallback"
      contains: "rule tokenize = parse"
    - path: ".gitignore"
      provides: "oh-workdir-122b/ ignore entry"
      contains: "oh-workdir-122b"
  key_links:
    - from: "task2-lexer-unaided.txt token names"
      to: "task3-parser.txt token declarations"
      via: "identical names INT/PLUS/MINUS/STAR/SLASH/LPAREN/RPAREN/EOF"
      pattern: "INT.*PLUS.*MINUS.*STAR.*SLASH.*LPAREN.*RPAREN.*EOF"
    - from: "all task prompts"
      to: "oh-workdir-122b scratch dir"
      via: "Working directory line"
      pattern: "oh-workdir-122b"
---

<objective>
Prepare the 122B task-prompt set and run preflight so the execute plan (06-02) can launch a clean, honest capture.

This plan does NOT run OpenHands. It only: (1) creates the `task-prompts-122b/` prompt files — reusing v1's task1/3/4/5/6 with the workdir swapped to `oh-workdir-122b`, and authoring the NEW unaided task2 (the milestone's whole point); (2) ensures `oh-workdir-122b/` exists, is empty, and is gitignored; (3) confirms the litellm proxy is serving `qwen-122b`.

Purpose: The unaided-first lexer is what distinguishes this run from v1. The task2 prompt must hand the agent the token-name contract (for parser agreement) and nothing else — no lexer body, no format hints, no API hints — so the JSONL can prove the lexer was submitted unaided (Phase 6 success criterion #2).

Output: 8 prompt files + invocation reference under `task-prompts-122b/`, a clean gitignored `oh-workdir-122b/`, and a confirmed-live proxy.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-capture-the-122b-openhands-run/06-RESEARCH.md

# v1 prompts to reuse (read these to copy verbatim, then swap workdir):
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task1-scaffold.txt
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task2-lexer.txt
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task3-parser.txt
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task4-evaluator.txt
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task5-buildtest.txt
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task6-fix.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the unaided task2 prompt + the fallback scaffold prompt</name>
  <files>
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided-retry.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-scaffold.txt
  </files>
  <action>
First create the directory:
  mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b

**task2-lexer-unaided.txt** — THE UNAIDED PROMPT (the milestone's core test). Write it with a quoted heredoc (`cat > FILE <<'EOF' ... EOF`). It MUST:
  - Set Working directory to /Users/ohama/projs/OpenHandsTests/oh-workdir-122b
  - Carry the bash-only file-write constraint verbatim (no file_editor / str_replace — errors due to missing security_risk field).
  - Tell the agent the scaffold exists: instruct `cd calc`, `ls`, `cat calc.fsproj`, and that calc.fsproj is wired for FsLex/FsYacc (--unicode --module Lexer) and that the parser (task 3) will declare and use these EXACT token names: INT (of int), PLUS, MINUS, STAR, SLASH, LPAREN, RPAREN, EOF.
  - State the task: "Write calc/Lexer.fsl yourself — the FsLex lexer source — that tokenizes integer arithmetic expressions."
  - Give the behavioral requirements ONLY (not the code): skip whitespace; match one or more digits and return INT carrying the parsed integer value; match the six operator/paren characters returning PLUS/MINUS/STAR/SLASH/LPAREN/RPAREN; match eof returning EOF; error on any unexpected character. State that the FsLex rule entry point must be named `tokenize` (so the parser's start calls Lexer.tokenize).
  - Constraints: do NOT create or modify calc.fsproj; do NOT write Parser.fsy yet (only Lexer.fsl); do NOT attempt to build yet (parser source not written). After writing, `cat Lexer.fsl` to confirm.

**CRITICAL — what task2-lexer-unaided.txt must NOT contain (fully unaided per the user's choice):**
  - NO lexer source/body, NO `rule tokenize = parse`, NO sample patterns like `['0'-'9']+`, NO action bodies.
  - NO FsLex `%%` note / "does not use %% separators" hint.
  - NO `LexBuffer<_>.LexemeString` API hint, NO `System.Int32.Parse` hint, NO header-open hints (do not tell it which modules to open).
  (NOTE: this deliberately omits even the API hint and %% note that 06-RESEARCH.md's "Recommended" variant suggested — the key decision for this phase is fully unaided. The agent is EXPECTED to hit lexer build errors and self-correct in task5; that self-correction is the desired genuine error-and-fix.)

**task2-lexer-unaided-retry.txt** — used only if attempt 1 gets stuck/produces no parseable Lexer.fsl. Same as the unaided prompt but slightly more explicit on STRUCTURE only (not content): add one line stating "FsLex (.fsl) files are structured as a `{ ... }` header block followed by `rule <name> = parse` with one `| pattern { action }` line per token — this is NOT FsYacc grammar syntax." Still NO action-code bodies, NO API names, NO lexeme-extraction hint. This raises the structural floor without handing over the lexer.

**task2-lexer-scaffold.txt** — FALLBACK ONLY (used only if BOTH unaided attempts fail; its mere use is a disclosed deviation). This is the v1 task2-lexer.txt content, with the Working directory line changed to oh-workdir-122b. Read /Users/ohama/projs/OpenHandsTests/.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task2-lexer.txt and reproduce it verbatim (it provides the exact Lexer.fsl body), changing only `oh-workdir` -> `oh-workdir-122b` on the Working directory line. This file exists on disk now but is NOT used unless the fallback is triggered in 06-02.
  </action>
  <verify>
Confirm unaided prompt has NO lexer body and NO hints, but DOES name the tokens and the rule name:
  grep -c -E "rule tokenize = parse|LexemeString|%%|\['0'-'9'\]|Int32.Parse" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt   # must print 0
  grep -c -E "INT|PLUS|MINUS|STAR|SLASH|LPAREN|RPAREN|EOF" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt   # must be >= 1
  grep -c "tokenize" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt   # must be >= 1 (rule name allowed)
  grep -c "oh-workdir-122b" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-unaided.txt   # must be >= 1
Confirm the fallback DOES contain the lexer body:
  grep -c "rule tokenize = parse" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task2-lexer-scaffold.txt   # must be >= 1
  </verify>
  <done>
task2-lexer-unaided.txt exists with token names + rule name `tokenize` + behavioral requirements but ZERO lexer body / format / API hints. task2-lexer-unaided-retry.txt adds structural floor only. task2-lexer-scaffold.txt holds the verbatim v1 lexer for fallback. All three reference oh-workdir-122b.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reuse v1 prompts (task1/3/4/5/6) with workdir swapped + write 00-INVOCATION.md</name>
  <files>
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task1-scaffold.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task3-parser.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task4-evaluator.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task5-buildtest.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task6-fix.txt
    .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/00-INVOCATION.md
  </files>
  <action>
Copy each v1 prompt to the new dir, then replace EVERY occurrence of the path `oh-workdir` with `oh-workdir-122b` (use sed with a word-safe pattern so `oh-workdir-122b` is not double-replaced — replace `oh-workdir` only where NOT already followed by `-122b`). Concretely, since the v1 files contain only `oh-workdir` (never `oh-workdir-122b`), a plain `s|oh-workdir|oh-workdir-122b|g` is safe on a fresh copy.

  SRC=/Users/ohama/projs/OpenHandsTests/.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts
  DST=/Users/ohama/projs/OpenHandsTests/.planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b
  for t in task1-scaffold task3-parser task4-evaluator task5-buildtest task6-fix; do
    sed 's|oh-workdir|oh-workdir-122b|g' "$SRC/$t.txt" > "$DST/$t.txt"
  done

DO NOT alter task1's .fsproj content (FixLineDirectives target + ItemGroups must stay verbatim — it is a .NET10/FsLexYacc toolchain workaround, not a model-capability question; research confirms keeping it). task1 says "Do NOT create Lexer.fsl — a later task (task 2) creates it" — that line is fine and stays (task2 still creates the lexer, just unaided now).

Use task4-evaluator.txt (the bash-only variant), NOT task4-evaluator-adjusted.txt.

For **00-INVOCATION.md**: write the 122B invocation reference (a fresh file, NOT a copy). Include:
  - The exact per-task command pattern:
    OPENHANDS_SUPPRESS_BANNER=1 LLM_MODEL="openai/qwen-122b" LLM_BASE_URL="http://127.0.0.1:4000/v1" LLM_API_KEY="dummy" OPENHANDS_WORK_DIR="/Users/ohama/projs/OpenHandsTests/oh-workdir-122b" openhands --headless --json --yolo --override-with-envs -t "$(cat <prompt-file>)" 2>oh-workdir-122b/<task>.stderr.log | tee oh-workdir-122b/<task>.jsonl
  - Key notes: --override-with-envs REQUIRED; --yolo autonomous; --headless --json emits JSONL; each invocation is a fresh zero-memory conversation (files persist on disk, agent rediscovers via ls/cat); bash-only writes; CLI exits 0 regardless of outcome so judge from JSONL; run in background + poll; 122B ~25-30% slower than 35B (budget ~6-8 min/complex task, up to ~15 if stuck on lexer).
  - The per-task JSONL filename table (from 06-RESEARCH.md section 4.3): task1-scaffold, task2-lexer-unaided, task2-lexer-unaided-retry (if needed), task2-lexer-scaffold (fallback only), task3-parser, task4-evaluator, task5-buildtest, task6-fix (conditional).
  </action>
  <verify>
  ls .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/   # 8 .txt prompts + 00-INVOCATION.md
  grep -rL "oh-workdir-122b" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task*.txt   # prints nothing (every task file references the new workdir)
  grep -c "oh-workdir-122b/oh-workdir-122b\|oh-workdir-122b-122b" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task1-scaffold.txt   # must be 0 (no double-replacement)
  grep -c "FixLineDirectives" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/task1-scaffold.txt   # must be >= 1 (.fsproj preserved)
  grep -c "qwen-122b" .planning/phases/06-capture-the-122b-openhands-run/task-prompts-122b/00-INVOCATION.md   # >= 1
  </verify>
  <done>
task1/3/4/5/6 prompts exist, all reference oh-workdir-122b (no double-replacement artifacts), task1 retains the FixLineDirectives .fsproj verbatim. 00-INVOCATION.md documents the qwen-122b invocation, background+poll guidance, and JSONL filename convention.
  </done>
</task>

<task type="auto">
  <name>Task 3: Preflight — gitignore oh-workdir-122b, clean scratch dir, confirm proxy serves qwen-122b</name>
  <files>.gitignore</files>
  <action>
1. Add `oh-workdir-122b/` to /Users/ohama/projs/OpenHandsTests/.gitignore (it currently contains `book/` and `oh-workdir/`). Append the line only if not already present. Do NOT remove existing entries — v1's `oh-workdir/` MUST stay ignored and untouched.

2. Create a clean, empty scratch dir:
   mkdir -p /Users/ohama/projs/OpenHandsTests/oh-workdir-122b
   It must be empty before the run (no stale calc/). If it already exists with contents, list them; only remove if they are stale capture artifacts from a prior aborted attempt (NOT v1's oh-workdir/ — never touch that).

3. Confirm gitignore works:
   git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-122b   # must print oh-workdir-122b

4. Confirm the litellm proxy serves qwen-122b:
   curl -s http://127.0.0.1:4000/v1/models   # response must list "qwen-122b"
   If the proxy is down or qwen-122b is absent, HALT and report — 06-02 cannot run without it. (Per 06-RESEARCH.md the proxy was live with qwen-35b, qwen-122b, qwen-local.)
  </action>
  <verify>
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-122b   # prints oh-workdir-122b
  git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir        # prints oh-workdir (v1 still ignored)
  test -d /Users/ohama/projs/OpenHandsTests/oh-workdir-122b && echo "scratch dir ready"
  curl -s http://127.0.0.1:4000/v1/models | grep -c "qwen-122b"   # >= 1
  </verify>
  <done>
oh-workdir-122b/ exists, is empty/clean, and is gitignored (and v1's oh-workdir/ remains ignored & untouched). The litellm proxy responds and lists qwen-122b. Preflight green — execute plan can launch.
  </done>
</task>

</tasks>

<verification>
- task-prompts-122b/ contains 8 prompt .txt files + 00-INVOCATION.md.
- task2-lexer-unaided.txt has ZERO lexer body/format/API hints (grep for `rule tokenize = parse`, `LexemeString`, `%%`, `['0'-'9']`, `Int32.Parse` all return 0) yet names the 8 tokens and the `tokenize` rule name.
- task2-lexer-scaffold.txt holds the verbatim v1 lexer body (fallback).
- All task*.txt reference oh-workdir-122b with no double-replacement artifacts; task1 keeps the FixLineDirectives .fsproj.
- oh-workdir-122b/ gitignored and clean; v1 oh-workdir/ untouched; proxy serves qwen-122b.
</verification>

<success_criteria>
The unaided-first protocol is encoded in the prompts (criterion #2 made achievable), the .fsproj is still provided (fair comparison), the fallback prompt is staged but unused, the scratch dir is clean+gitignored, and the proxy is confirmed live — so 06-02 can execute a clean, honest capture.
</success_criteria>

<output>
After completion, create `.planning/phases/06-capture-the-122b-openhands-run/06-01-SUMMARY.md`
</output>
