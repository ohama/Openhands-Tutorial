---
phase: 06-capture-the-122b-openhands-run
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md
  - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/
  - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/
  - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/test-output.txt
  - .planning/phases/06-capture-the-122b-openhands-run/captured-122b/transcript.md
autonomous: true

must_haves:
  truths:
    - "A committed per-task JSONL set exists under captured-122b/logs/ — each file non-empty with ActionEvent + ObservationEvent records (criterion #1)"
    - "CAPTURE-MANIFEST.md states did-lexer-unaided (YES/NO) with attempt count, and discloses fallback honestly if NO (criterion #3)"
    - "The 3-case final outcome (2+3*4, (2+3)*4, 10-3-2) is recorded and traceable to a JSONL terminal observation, plus a fresh host re-run in test-output.txt (criterion #4)"
    - "CAPTURE-MANIFEST records timing (per-task total + per-LLM-call avg) and the agent's own error-and-fix for Phase 7 consumption (CMP-01)"
    - "Committed artifacts live under captured-122b/ (tracked); oh-workdir-122b/ stays gitignored and uncommitted"
  artifacts:
    - path: ".planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md"
      provides: "Artifact-to-requirement map for the 122B run, parallel to the v1 35B manifest"
      contains: "did-lexer-unaided"
    - path: ".planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/task2-lexer-unaided.jsonl"
      provides: "Committed proof the lexer was attempted unaided first"
      min_lines: 1
    - path: ".planning/phases/06-capture-the-122b-openhands-run/captured-122b/test-output.txt"
      provides: "Fresh host re-run of the 3 cases (or honest failure record)"
      min_lines: 1
    - path: ".planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/Lexer.fsl"
      provides: "Final Lexer.fsl (agent-written or scaffold) — final-state evidence"
      min_lines: 1
  key_links:
    - from: "CAPTURE-MANIFEST.md claims"
      to: "captured-122b/logs/*.jsonl events"
      via: "event-range citations (task file + event numbers), same style as v1 manifest"
      pattern: "task[0-9].*jsonl"
    - from: "captured-122b/ committed tree"
      to: "git index"
      via: "git add of captured-122b/ only (oh-workdir-122b/ excluded by .gitignore)"
      pattern: "captured-122b"
---

<objective>
Verify the captured 122B JSONL is intact and honest, snapshot the final source + a fresh host test run, write CAPTURE-MANIFEST.md (parallel to the v1 35B manifest with all Phase-7 fields), and commit the captured artifacts to a tracked location.

Purpose: This closes the Phase 6 capture gate. Phase 7's comparison chapter cannot start until these artifacts are committed and every comparison hook (did-lexer-unaided, error-and-fix, timing) is recorded from real evidence — no fabrication.

Output: `captured-122b/` committed (CAPTURE-MANIFEST.md + logs/ + final-source/ + test-output.txt + transcript.md). oh-workdir-122b/ stays gitignored.
</objective>

<context>
@.planning/phases/06-capture-the-122b-openhands-run/06-RESEARCH.md
@.planning/phases/06-capture-the-122b-openhands-run/06-02-RUN-NOTES.md
@.planning/phases/06-capture-the-122b-openhands-run/06-02-SUMMARY.md

# Mirror this manifest's structure and citation style:
@.planning/milestones/v1-phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
</context>

<jsonl_schema_reference>
Same confirmed schema as 06-02. For timing (06-RESEARCH.md 4.5): per-task total = last event timestamp − first event timestamp in each JSONL; per-LLM-call ≈ gap from an ObservationEvent timestamp to the NEXT ActionEvent timestamp (pure model time, excludes bash exec). Use dateutil or datetime.fromisoformat on the ISO8601 `timestamp` fields.
</jsonl_schema_reference>

<tasks>

<task type="auto">
  <name>Task 1: Verify JSONL integrity + extract timing, outcome, and error-and-fix from the captured logs</name>
  <files>.planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/</files>
  <action>
1. Confirm each expected JSONL in oh-workdir-122b/ is non-empty and well-formed (every line that starts with `{` parses as JSON) and has at least one ActionEvent and one ObservationEvent. Expected always-present: task1-scaffold, task2-lexer-unaided, task3-parser, task4-evaluator, task5-buildtest. Conditionally present (verify only if they exist): task2-lexer-unaided-retry, task2-lexer-scaffold, task6-fix.
   For each file print: kinds counter, TerminalAction count, FinishAction present?, ConversationErrorEvent present? (reuse the 06-02 check snippet).
   If any always-present file is empty or malformed, HALT and report — capture is incomplete; do not paper over it.

2. Copy the JSONL set into the committed location:
   mkdir -p /Users/ohama/projs/OpenHandsTests/.planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs
   cp /Users/ohama/projs/OpenHandsTests/oh-workdir-122b/*.jsonl /Users/ohama/projs/OpenHandsTests/.planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/
   (Copy the .stderr.log files too if present, for completeness.)

3. Extract TIMING per task (per 06-RESEARCH.md 4.5) — for each JSONL compute total wall clock (last−first timestamp) and the average ObservationEvent→next-ActionEvent gap (per-LLM-call). Record numbers for the manifest.

4. Extract the 3-CASE OUTCOME from task5 (and task6 if it exists): the dotnet run commands and their TerminalObservation content/exit codes. Record 2+3*4 -> X, (2+3)*4 -> Y, 10-3-2 -> Z and all-pass YES|NO.

5. Identify the AGENT'S OWN ERROR-AND-FIX: scan task2/task3/task5/task6 JSONL for TerminalObservation with non-zero exit_code (build error) followed by a corrective ActionEvent. Record the error text (quoted), the fix command, and the location (task file + event indices). Cross-check with 06-02-RUN-NOTES.md (which captured this live).
   Confirm the unaided signal: in task2-lexer-unaided.jsonl, find the ActionEvent whose action.command writes Lexer.fsl (heredoc/printf/tee). This is the proof the agent authored the lexer (or, if fallback, confirm task2-lexer-scaffold.jsonl exists instead and note did-lexer-unaided=NO).
  </action>
  <verify>
  ls .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/*.jsonl   # >=5 files
  for f in .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/*.jsonl; do test -s "$f" || echo "EMPTY: $f"; done   # prints nothing
  python3 -c "import json;e=[json.loads(l) for l in open('.planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/task2-lexer-unaided.jsonl') if l.strip().startswith('{')];print('task2 A/O=',sum(1 for x in e if x.get('kind')=='ActionEvent'),sum(1 for x in e if x.get('kind')=='ObservationEvent'))"
  </verify>
  <done>
All always-present JSONL files are non-empty, parse cleanly, and have Action+Observation events; they are copied into captured-122b/logs/. Timing (per-task total + per-call avg), the 3-case outcome, and the agent's own error-and-fix (with event-range citations) are extracted and cross-checked against RUN-NOTES — ready to write into the manifest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Snapshot final source + fresh host test run; write CAPTURE-MANIFEST.md + transcript.md</name>
  <files>
    .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/
    .planning/phases/06-capture-the-122b-openhands-run/captured-122b/test-output.txt
    .planning/phases/06-capture-the-122b-openhands-run/captured-122b/transcript.md
    .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md
  </files>
  <action>
1. Snapshot the final agent-produced source:
   mkdir -p .../captured-122b/final-source
   cp /Users/ohama/projs/OpenHandsTests/oh-workdir-122b/calc/{calc.fsproj,Lexer.fsl,Parser.fsy,Program.fs} .../captured-122b/final-source/
   (If a file is missing because the run failed before producing it, note that in the manifest rather than inventing one.)

2. Fresh host re-run of the 3 cases into test-output.txt (authoritative final outcome, independent of the agent loop):
   dotnet build /Users/ohama/projs/OpenHandsTests/oh-workdir-122b/calc 2>&1 | tail -5
   then for each: dotnet run --project /Users/ohama/projs/OpenHandsTests/oh-workdir-122b/calc -- "2+3*4" ; "(2+3)*4" ; "10-3-2"
   Capture command + actual output for each into test-output.txt. If the build fails or a case is wrong, RECORD THE REAL OUTPUT — do not fix the source to make it pass (honesty over green). This is the host-side mirror of the JSONL outcome.

3. Write transcript.md — a human-readable per-task summary: for each task, the key commands the agent ran and the notable observations (build errors, fixes, final outputs), distilled from the JSONL. Mirror the v1 transcript.md style (per-task sections). Keep it factual and quoted.

4. Write CAPTURE-MANIFEST.md mirroring the v1 manifest structure and including ALL Phase-7 fields (from 06-RESEARCH.md 4.7):
   - **Run Metadata:** run date; Model = openai/qwen-122b (Qwen2.5-122B via litellm @ http://127.0.0.1:4000/v1, :8001); OpenHands SDK v1.21.0 / CLI 1.16.0; Workspace = oh-workdir-122b/ (LocalWorkspace, host PTY, gitignored).
   - **Lexer Outcome (RUN122-01/02):** `did-lexer-unaided: YES|NO`; `unaided-attempts: N`; lexer-description (what the agent wrote, or "v1 scaffold used"); fallback-disclosure (if NO: why + what was provided). If YES, RUN122-01 PASS; if NO, RUN122-02 PARTIAL with disclosure.
   - **Error-and-Fix Record (RUN122-03):** error-description (agent-seen messages, quoted); fix-description (what the agent changed); location-in-jsonl (task file + event range). If no error occurred, say so honestly.
   - **Test Results (RUN122-03):** 2+3*4, (2+3)*4, 10-3-2 actual outputs; `all-pass: YES|NO`. Cite the JSONL terminal observation AND test-output.txt.
   - **Timing Summary (CMP-01):** per-task totals + TerminalAction counts; per-LLM-call avg; total-run.
   - **Comparison Hook (for Phase 7):** vs-35B-lexer (35B couldn't write FsLex unaided; 122B did/did not); vs-35B-error-fix; vs-35B-speed (122B ~Xs/call vs 35B ~14-32s/call from v1 RUN-NOTES).
   - **Artifact-to-Requirement Map + Artifact Index** table (like v1): each captured file -> requirement(s) it evidences (RUN122-01/02/03).
   - **Deviations:** retries taken, fallback (if any), whether Lexer.fsl was touched in a later task — all honestly listed. Explicitly state: zero manual edits to agent files; no fabricated success.

   CRITICAL: every claim/number in the manifest must trace to a JSONL event range or test-output.txt line. If the run was an honest failure, the manifest records the failure plainly (criteria #1/#2/#4 are still satisfied by a real captured run + unaided-first + traceable terminal outcome).
  </action>
  <verify>
  ls .planning/phases/06-capture-the-122b-openhands-run/captured-122b/final-source/   # calc.fsproj, Lexer.fsl, Parser.fsy, Program.fs (or noted-missing)
  test -s .planning/phases/06-capture-the-122b-openhands-run/captured-122b/test-output.txt && echo "test-output present"
  grep -c "did-lexer-unaided" .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md   # >=1
  grep -c -E "Timing|per-LLM-call|all-pass|RUN122" .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md   # >=1 (Phase-7 fields present)
  </verify>
  <done>
final-source/ holds the final agent source; test-output.txt holds the fresh host 3-case run (real outputs, not fixed-to-pass); transcript.md is a factual per-task readable summary; CAPTURE-MANIFEST.md records all Phase-7 fields (did-lexer-unaided + attempts, error-and-fix, 3-case outcome, timing, comparison hooks, deviations) with every claim traceable to JSONL/test-output. Honest failure recorded plainly if that is the outcome.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit captured-122b/ artifacts; confirm oh-workdir-122b/ stays gitignored</name>
  <files>.planning/phases/06-capture-the-122b-openhands-run/captured-122b/</files>
  <action>
1. Confirm oh-workdir-122b/ is NOT staged and remains ignored:
   git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir-122b   # prints oh-workdir-122b
   git -C /Users/ohama/projs/OpenHandsTests status --porcelain | grep -c "oh-workdir-122b/"   # must be 0 (nothing from the scratch dir staged)

2. Stage and commit ONLY the planning artifacts (captured-122b/, the PLAN/SUMMARY/RUN-NOTES files, task-prompts-122b/, and the .gitignore change from 06-01). Branch first if on the default branch (this repo's working tree is not the blueCode repo — operate only in /Users/ohama/projs/OpenHandsTests). Then:
   git -C /Users/ohama/projs/OpenHandsTests add .planning/phases/06-capture-the-122b-openhands-run/ .gitignore
   git -C /Users/ohama/projs/OpenHandsTests commit -m "feat(06): capture honest 122B OpenHands run (unaided-first lexer)

Per-task JSONL captured under captured-122b/logs/; CAPTURE-MANIFEST records
did-lexer-unaided, the agent's own error-and-fix, 3-case outcome, and timing
for Phase 7. oh-workdir-122b/ remains gitignored.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

3. Confirm the commit landed and the captured tree is tracked:
   git -C /Users/ohama/projs/OpenHandsTests log --oneline -1
   git -C /Users/ohama/projs/OpenHandsTests ls-files .planning/phases/06-capture-the-122b-openhands-run/captured-122b/ | head
  </action>
  <verify>
  git -C /Users/ohama/projs/OpenHandsTests ls-files .planning/phases/06-capture-the-122b-openhands-run/captured-122b/CAPTURE-MANIFEST.md   # prints the path (tracked)
  git -C /Users/ohama/projs/OpenHandsTests ls-files .planning/phases/06-capture-the-122b-openhands-run/captured-122b/logs/ | wc -l   # >=5 (logs tracked)
  git -C /Users/ohama/projs/OpenHandsTests ls-files oh-workdir-122b/ | wc -l   # 0 (scratch dir NOT tracked)
  </verify>
  <done>
captured-122b/ (manifest + logs + final-source + test-output + transcript) is committed and tracked. oh-workdir-122b/ is confirmed gitignored and untracked. The Phase 6 capture gate is closed — Phase 7 can consume the committed evidence.
  </done>
</task>

</tasks>

<verification>
- captured-122b/logs/ has >=5 committed non-empty JSONL files (task1, task2-lexer-unaided, task3, task4, task5; plus retry/scaffold/task6 iff taken).
- CAPTURE-MANIFEST.md records did-lexer-unaided + attempts, the agent's own error-and-fix, the 3-case outcome (all-pass YES|NO), timing, and Phase-7 comparison hooks — all traceable to JSONL/test-output.
- final-source/ and test-output.txt are present; honest failure (if any) recorded plainly.
- captured-122b/ is git-tracked; oh-workdir-122b/ is gitignored and untracked.
</verification>

<success_criteria>
All 5 Phase 6 success criteria are met from committed evidence: per-task JSONL committed (#1), unaided-first confirmed in task2-lexer-unaided.jsonl (#2), honest lexer-outcome disclosure in the manifest (#3), 3-case terminal outcome traceable (#4), and CAPTURE-MANIFEST records invocation + lexer attempt + deviations parallel to v1 (#5). RUN122-01/02/03 evidenced and indexed for Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/06-capture-the-122b-openhands-run/06-03-SUMMARY.md`
</output>
