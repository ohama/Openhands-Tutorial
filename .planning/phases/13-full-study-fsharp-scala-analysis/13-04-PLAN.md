---
phase: 13-full-study-fsharp-scala-analysis
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md
autonomous: false

must_haves:
  truths:
    - "CAPTURE-MANIFEST.md summarizes ALL SIX (example × arm) outcomes — F# + Rust + Scala — with each cell's n, canonical pass/fail (incl. honest F# FAILs), honesty-gate result, and the median(min–max) headline metrics (PCAP-03)"
    - "The manifest records the run conditions and counterbalanced run order + proxy state per example, and states the timing cache/run-order caveat (PCAP-03, METH-02)"
    - "The manifest cites 1-based event numbers and does NOT present ~14–32s/call as a measurement (METH-03 / Pitfalls 8, 9)"
    - "A gate status line declares the capture gate CLOSED only when all six metrics.json have honesty_gate=PASS and all canonical_tests are populated (not null) — this is the gate that unblocks Phase 14 (PCAP-03)"
    - "A human-verify checkpoint approves the capture before the gate-closing commit; on approval all captured-planning/ artifacts (runs JSONL, metrics, comparisons, planning artifacts, manifest) are committed"
  artifacts:
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md"
      provides: "Top-level study capture manifest: all six example × arm outcomes, run conditions/order, honesty results, both arms' canonical outcomes (incl. honest F# failures); GATE CLOSED line"
      min_lines: 70
  key_links:
    - from: "CAPTURE-MANIFEST.md GATE CLOSED line"
      to: "all six captured-planning/{lang}/arm-{a,b}/metrics.json"
      via: "manifest asserts honesty_gate=PASS + canonical_tests populated for every cell before declaring CLOSED"
      pattern: "CAPTURE GATE: CLOSED"
---

<objective>
Assemble the top-level CAPTURE-MANIFEST.md summarizing all six (example × arm) captures, verify the whole study capture with a human-verify checkpoint, and on approval commit every captured-planning/ artifact — closing the Phase 13 capture gate that unblocks Phase 14 (부록 D chapter).

Purpose: PCAP-03. The committed manifest is THE gate. It must honestly summarize all six cells (incl. F# FAILs), the counterbalanced run conditions, the honesty-gate results, and the median/range headline metrics, citing 1-based event numbers and never presenting ~14–32s/call as a measurement. The human-verify checkpoint exists because this commit authorizes the rest of the milestone (mirrors Phase-12's 12-03 gate).
Output: captured-planning/CAPTURE-MANIFEST.md + a single commit of all Phase 13 capture artifacts; gate CLOSED.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md
@.planning/phases/13-full-study-fsharp-scala-analysis/13-03-SUMMARY.md

# Mirror the Phase-12 manifest structure + 1-based convention:
@.planning/phases/12-harness-rust-pilot/captured-planning/CAPTURE-MANIFEST.md
</context>

<honesty_constraints>
- The manifest reports the captured reality exactly — including F# FAILs in either/both arms (valid data). Do NOT soften, omit, or retro-fit any result. Mixed/inconclusive cells are reported as such.
- The gate is declared CLOSED ONLY IF every one of the six metrics.json has honesty_gate=PASS AND all canonical_tests are populated (PASS or FAIL, not null). A canonical FAIL does NOT block the gate (it is valid data); a null canonical_test or an honesty-gate FAIL DOES block it.
- Cite 1-based event numbers throughout (JSONL line N = event #N). Do NOT present `~14–32s/call` as a measurement — if per-call timing appears, label it "derived from JSONL" with the cache/run-order caveat.
- No manual edits to any agent-written file or any captured JSONL. The commit captures artifacts as produced.
- Do NOT modify .github/workflows/deploy.yml; do NOT touch existing 1부–7부 / 부록 content.
- Commit ONLY on human approval at the checkpoint (this plan is autonomous:false).
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write CAPTURE-MANIFEST.md (all six cells, run conditions, honesty, gate line)</name>
  <files>.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md</files>
  <action>
Write captured-planning/CAPTURE-MANIFEST.md mirroring the Phase-12 manifest structure, scaled to the full study. Include:
  1. Header: study name (v1.4 planning comparison), date, model (openai/qwen-35b), the 1-based event-numbering convention statement, and the framing note ("the question is whether an expert-authored plan helps the 35B execute, not whether Claude plans better").
  2. Run conditions block: host toolchains, CLI/SDK versions, the counterbalanced run order per example + proxy state (from 13-02 RUN-NOTES), and the timing caveat ("wall-clock/per-call timing is derived from JSONL and carries a cache-warmth/run-order confound — not a clean planning-quality signal"; explicitly note `~14–32s/call` is a legacy pre-run PREDICTION, not a measurement).
  3. The six-cell outcome table: rows = {F#, Rust, Scala} × {Arm A, Arm B}; columns = n, canonical pass count (e.g. "0/3" for honest F# FAILs, "3/3" for Scala), honesty_gate, and headline median(min–max) for terminal_actions + total_events + wall_clock_seconds + error_fix_cycle_count. Pull values from the committed metrics.json / comparison.json. Label any n=1 cell "(단일 실행)".
  4. Per-example one-line takeaway (neutral, hedged; mixed/inconclusive stated honestly).
  5. Honesty results block: confirm the source=agent gate PASS on all runs (count of runs checked; note Arm A's excepted initial source=user MessageEvent); confirm no manual edits; confirm no cherry-picking (all reps kept).
  6. Pointers to the planning artifacts (claude-plan.md + oh-self-plan.md per example) and PLAN-COMPARISON-QUALITATIVE.md (ANAL-02).
  7. GATE STATUS line: print "PHASE 13 CAPTURE GATE: CLOSED" ONLY if the assertion below passes; otherwise "PHASE 13 CAPTURE GATE: OPEN — <reason>".

Before writing the GATE line, run the assertion:
  python3 -c "
import json,glob,sys
cells=glob.glob('.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics.json')
assert len(cells)==6, f'expected 6 metrics.json, got {len(cells)}'
bad=[]
for f in cells:
    m=json.load(open(f)); g=m.get('honesty_gate'); g=g.get('result') if isinstance(g,dict) else g
    if g!='PASS': bad.append((f,'honesty',g))
    ct=m.get('canonical_tests') or {}
    # canonical must be populated (pass True/False, not null) for every test
    for k,v in (ct.items() if isinstance(ct,dict) else []):
        pv=v.get('pass') if isinstance(v,dict) else v
        if pv is None: bad.append((f,'canonical-null',k))
if bad: print('GATE OPEN:',bad); sys.exit(1)
print('GATE CLOSED: 6/6 honesty=PASS, all canonical populated')
"
  Use its result to write the correct GATE line. Do NOT write CLOSED if it exits non-zero.
  </action>
  <verify>
test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md &&
grep -q "CAPTURE GATE:" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md &&
! grep -q "~14.32s/call.*measured\|measured.*~14" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md &&
python3 -c "
import json,glob,sys
cells=glob.glob('.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics.json')
assert len(cells)==6, f'expected 6, got {len(cells)}'
for f in cells:
    m=json.load(open(f)); g=m.get('honesty_gate'); g=g.get('result') if isinstance(g,dict) else g
    assert g=='PASS', f'honesty FAIL {f}'
print('6/6 cells honesty=PASS')
" &&
echo "MANIFEST OK"
  </verify>
  <done>CAPTURE-MANIFEST.md exists with the six-cell outcome table (incl. honest F# FAILs as canonical pass counts), run conditions + counterbalanced order + timing caveat, 1-based convention, honesty-results block, planning-artifact pointers, and a GATE line that reads CLOSED only when all 6 metrics.json are honesty_gate=PASS with canonical populated; ~14–32s/call not cited as a measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full Phase 13 capture: all six (example × arm) cells captured at n=3 where feasible (F# + Scala fresh n=3; Rust topped to n=3) with real per-run JSONL; metrics_extractor.py (reused) over every run; blocking source=agent honesty gate PASS on all runs; per-arm metrics.json (median/range) + per-example comparison.json (Arm A vs Arm B + fairness caveats); both planning artifacts per example (claude-plan.md + verbatim oh-self-plan.md); the ANAL-02 qualitative plan comparison; and CAPTURE-MANIFEST.md summarizing all six cells with the GATE CLOSED line. This commit is the capture gate that unblocks Phase 14.
  </what-built>
  <how-to-verify>
1. Read captured-planning/CAPTURE-MANIFEST.md — confirm the six-cell table covers F#/Rust/Scala × Arm A/B with n, canonical pass counts, honesty_gate, and median(min–max) headline metrics; confirm honest F# FAILs (if any) are shown, not hidden.
2. Confirm the GATE line reads "PHASE 13 CAPTURE GATE: CLOSED" (all 6 metrics.json honesty_gate=PASS, canonical populated). If it reads OPEN, the gate is not closed — review the reason.
3. Spot-check one comparison.json (e.g. captured-planning/fsharp/comparison.json) — confirm median(min–max), arm_a/arm_b, deltas, and the timing cache/run-order caveat are present.
4. Open captured-planning/fsharp/arm-b/planning-artifact/oh-self-plan.md and claude-plan.md — confirm both planning artifacts exist and the self-plan is verbatim from the tracker.
5. Skim PLAN-COMPARISON-QUALITATIVE.md — confirm per-example task count / granularity / ordering / scaffold→write→build→test comparison.
6. Confirm timing is labelled "derived from JSONL" with the cache/run-order caveat and ~14–32s/call is not cited as a measurement.
  </how-to-verify>
  <resume-signal>Type "approved" to commit all Phase 13 capture artifacts and close the gate, or describe issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit all Phase 13 capture artifacts (closes the capture gate)</name>
  <files>.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md</files>
  <action>
On approval, branch if on the default branch (do not commit study artifacts directly to main without a branch unless the repo convention is trunk-based — check `git rev-parse --abbrev-ref HEAD`; this repo's recent history commits docs to main, so committing to main is acceptable here). Stage and commit ALL Phase 13 capture artifacts:
  git add .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/ \
          .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/ \
          .planning/phases/13-full-study-fsharp-scala-analysis/aggregate_metrics.py \
          .planning/phases/13-full-study-fsharp-scala-analysis/*-RUN-NOTES.md
  Verify oh-workdir-planning/ scratch is NOT staged (it is gitignored): `git status --porcelain | grep oh-workdir-planning` must be empty.
  Commit message:
    feat(13): full F#/Scala/Rust planning A/B capture — gate CLOSED

    - 6 example×arm cells captured (F#+Scala n=3 fresh; Rust topped to n=3)
    - metrics_extractor.py over every run; source=agent honesty gate PASS on all
    - per-arm metrics.json (median/min–max) + per-example comparison.json + caveats
    - both planning artifacts per example (claude-plan.md + verbatim oh-self-plan.md)
    - ANAL-02 qualitative plan comparison; CAPTURE-MANIFEST gate CLOSED
    - honest F# FAILs preserved; no cherry-picking; deploy.yml untouched

    Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  Confirm the commit landed: `git log --oneline -1`.
  </action>
  <verify>
git -C /Users/ohama/projs/OpenHandsTests log --oneline -1 | grep -qi "gate CLOSED\|planning A/B" &&
test -z "$(git -C /Users/ohama/projs/OpenHandsTests status --porcelain .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/ 2>/dev/null)" &&
test -z "$(git -C /Users/ohama/projs/OpenHandsTests status --porcelain | grep oh-workdir-planning)" &&
echo "COMMIT OK; scratch not staged"
  </verify>
  <done>All Phase 13 capture artifacts (runs JSONL, per-run + per-arm metrics, comparisons, planning artifacts, RUN-NOTES, manifest, aggregate_metrics.py) committed in one commit; gitignored oh-workdir-planning/ scratch not staged; CAPTURE-MANIFEST gate CLOSED; Phase 14 unblocked.</done>
</task>

</tasks>

<verification>
- CAPTURE-MANIFEST.md committed with the six-cell table (F#/Rust/Scala × Arm A/B), n + canonical pass counts (incl. honest F# FAILs) + honesty_gate + median(min–max) headline metrics, run conditions + counterbalanced order + timing caveat, 1-based convention, honesty-results block, and the GATE CLOSED line.
- All six metrics.json have honesty_gate=PASS and all canonical_tests populated (not null) — gate CLOSED assertion passed.
- Human-verify checkpoint approved before commit; all capture artifacts committed in one commit; gitignored scratch not staged.
- ~14–32s/call not cited as a measurement; deploy.yml + existing chapters untouched.
</verification>

<success_criteria>
PCAP-03 met: the committed CAPTURE-MANIFEST.md closes the Phase 13 capture gate — all six example×arm cells summarized with run conditions, honesty results, and both arms' canonical outcomes (incl. honest F# failures). Phase 14 (부록 D) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-study-fsharp-scala-analysis/13-04-SUMMARY.md`.
</output>
