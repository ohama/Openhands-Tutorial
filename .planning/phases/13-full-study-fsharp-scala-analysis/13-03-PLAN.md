---
phase: 13-full-study-fsharp-scala-analysis
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-a/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-b/metrics.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/comparison.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/comparison.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/comparison.json
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md
  - .planning/phases/13-full-study-fsharp-scala-analysis/aggregate_metrics.py
autonomous: true

must_haves:
  truths:
    - "metrics_extractor.py (reused as-is) ran over EVERY captured run JSONL (all reps, all six example × arm cells), emitting per-run metrics-run-N.json with honesty_gate=PASS and canonical_tests populated (not null) (ANAL-01, METH-03)"
    - "The BLOCKING source=agent honesty gate passed on EVERY run JSONL (Arm A's lone source=user plan-delivery MessageEvent excepted); any FAIL blocks the commit (METH-03 / Pitfall 7)"
    - "Per (example × arm) an aggregated metrics.json reports median(min–max) for n≥2 metrics, with n recorded and '(단일 실행)' labelling for any n=1 cell (ANAL-01, REPETITION POLICY)"
    - "Per example a comparison.json gives Arm A vs Arm B on all P1/P2 metrics as median(min–max) with the timing/cache fairness caveat attached (ANAL-01)"
    - "Both planning artifacts are saved per example: Claude's plan (Arm A claude-plan.md, already present) + the Arm B self-plan extracted verbatim from the TaskTrackerObservation events (oh-self-plan.md) (PCAP-02)"
    - "A qualitative plan comparison (PLAN-COMPARISON-QUALITATIVE.md) covers per example: task count, granularity, ordering, and structural match to scaffold→write→build→test for Claude's plan vs the self-plan (ANAL-02)"
  artifacts:
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/aggregate_metrics.py"
      provides: "Aggregates per-run metrics-run-N.json into per-arm metrics.json (median/min/max, n, single-run labelling) and per-example comparison.json (Arm A vs Arm B + caveats)"
      min_lines: 40
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/comparison.json"
      provides: "F# Arm A vs Arm B on all P1/P2 metrics, median(min–max), with caveats"
      contains: "median"
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md"
      provides: "ANAL-02 qualitative plan comparison per example (task count, granularity, ordering, scaffold→write→build→test match)"
      min_lines: 40
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-self-plan.md"
      provides: "F# Arm B self-plan extracted verbatim from TaskTrackerObservation events (PCAP-02)"
      min_lines: 3
  key_links:
    - from: "metrics_extractor.py + aggregate_metrics.py"
      to: "captured-planning/{lang}/arm-{a,b}/metrics.json + {lang}/comparison.json"
      via: "per-run extraction then median/range aggregation; honesty_gate carried through"
      pattern: "honesty_gate"
---

<objective>
Turn the raw captured JSONL (from 13-02) into the analysis artifacts: run metrics_extractor.py (REUSED AS-IS) over every run, run the blocking source=agent honesty gate over every run, aggregate per-run metrics into per-arm metrics.json with median(min–max), build per-example comparison.json (Arm A vs Arm B + fairness caveats), extract both planning artifacts per example, and write the ANAL-02 qualitative plan comparison.

Purpose: ANAL-01 + ANAL-02 + PCAP-02 + METH-03. metrics_extractor.py is the validated Phase-12 tool (1-based, supports fsharp/rust/scala canonical sets, curl-detection fix) — do NOT rewrite it; call it per run. A thin aggregate_metrics.py turns the per-run outputs into the median/range shape required for n=3 reporting. Honest F# FAILs flow through unchanged — canonical_tests populated (PASS or FAIL, never null). The honesty gate is BLOCKING: any run with a source!=agent ActionEvent (other than Arm A's plan-delivery MessageEvent) blocks the commit in 13-04.
Output: 6 per-arm metrics.json (median/range) + 18+ metrics-run-N.json + 3 comparison.json + 3 oh-self-plan.md + PLAN-COMPARISON-QUALITATIVE.md + aggregate_metrics.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/13-full-study-fsharp-scala-analysis/13-02-SUMMARY.md

# REUSE AS-IS — the validated metrics extractor (CLI: --jsonl --example {fsharp,rust,scala} --arm --prompt-file --out):
@.planning/phases/12-harness-rust-pilot/metrics_extractor.py
# Reuse the Phase-12 comparison.json shape (extend for median/range):
@.planning/phases/12-harness-rust-pilot/captured-planning/rust/comparison.json
# Reuse the Phase-12 Arm A claude-plan as the qualitative reference for Arm A plan shape:
@.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/claude-plan.md
</context>

<honesty_constraints>
- metrics_extractor.py is REUSED AS-IS — do NOT rewrite or alter its logic. Only the new aggregate_metrics.py is created here.
- The source=agent honesty gate is BLOCKING and runs over EVERY run JSONL. Arm A's single initial source=user MessageEvent (plan delivery) is the ONLY excepted non-agent event. Any other source!=agent ActionEvent is a FAIL → record it and do NOT proceed to commit (13-04 stays blocked until fixed by a clean re-capture, never by editing the JSONL).
- canonical_tests must be populated for every run (PASS or FAIL — NEVER null). Honest F# FAILs are kept and reported, not hidden.
- ALL reps are aggregated — no cherry-picking. n is recorded per cell. At n=1, the aggregated value is labelled "(단일 실행)". At n≥2, report median(min–max). Never compute mean/SD (implies false normality at small n). Never use "significantly/consistently/reliably".
- Timing metrics carry the cache/run-order caveat (label "derived from JSONL; cache-warmth/run-order confound — not a clean planning-quality signal"). `~14–32s/call` is never cited as a measurement.
- The Arm B self-plan (oh-self-plan.md) is extracted VERBATIM from the TaskTrackerObservation events (the proven Phase-12 source) — no editorial rewriting. If a given Arm B run emitted no tracker events, fall back to the first agent MessageEvent/AgentThinkAction text and note the fallback.
- Event numbers in any cited index are 1-based (METH-03 / Pitfall 8).
- Do NOT modify deploy.yml or existing chapters.
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run metrics_extractor.py + BLOCKING honesty gate over every run; write aggregate_metrics.py; emit per-arm metrics.json + per-example comparison.json</name>
  <files>
.planning/phases/13-full-study-fsharp-scala-analysis/aggregate_metrics.py
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-a/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-b/metrics.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/comparison.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/comparison.json
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/comparison.json
  </files>
  <action>
STEP A — Per-run extraction. For each (LANG ∈ {fsharp,scala,rust}, ARM ∈ {arm-a,arm-b}, REP) with a run-REP.jsonl, run the reused extractor:
  python3 .planning/phases/12-harness-rust-pilot/metrics_extractor.py \
    --jsonl captured-planning/<LANG>/<ARM>/runs/run-<REP>.jsonl \
    --example <LANG> --arm <ARM> \
    --prompt-file <the arm's prompt file> \
    --out captured-planning/<LANG>/<ARM>/metrics-run-<REP>.json
  This writes one metrics-run-N.json per run. Confirm each has honesty_gate="PASS" and canonical_tests populated (not null). (For fsharp/scala the canonical set is 2+3*4 / (2+3)*4 / 10-3-2; for rust it is curl_hello.)

STEP B — BLOCKING honesty gate sweep over EVERY run JSONL. Each metrics-run-N.json already carries honesty_gate from the extractor; additionally assert the gate result programmatically across all runs:
  python3 -c "
import json,glob,sys
bad=[]
for f in glob.glob('.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics-run-*.json'):
    m=json.load(open(f))
    g=m.get('honesty_gate'); g=g.get('result') if isinstance(g,dict) else g
    if g!='PASS': bad.append((f,g,m.get('honesty_gate')))
print('runs checked:',len(glob.glob('.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics-run-*.json')))
if bad:
    print('HONESTY GATE FAIL:',bad); sys.exit(1)
print('HONESTY GATE: PASS on all runs')
"
  If this FAILS, STOP — do not aggregate, do not proceed; the offending run must be re-captured in 13-02 (never edited). Record the failure in the SUMMARY.

STEP C — Write aggregate_metrics.py: reads all metrics-run-N.json for a given (LANG, ARM), computes for each numeric P1/P2 metric (terminal_actions, total_events, wall_clock_seconds, avg_llm_call_gap_seconds, error_fix_cycle_count, agent_error_event_count) the median, min, max, and n; carries canonical_tests as a per-test pass count across reps (e.g. "2/3 reps PASS"); records honesty_gate (PASS only if all reps PASS); and writes captured-planning/<LANG>/<ARM>/metrics.json with a "n" field and, when n==1, a "single_run_label":"(단일 실행)" field. Then it builds captured-planning/<LANG>/comparison.json: arm_a + arm_b blocks each as {metric: {median,min,max,n}} plus a deltas_a_minus_b block on medians, plus a "caveats" field documenting the timing cache/run-order confound and any n=1 single-run hedges, plus per-test canonical pass counts per arm. Mirror the Phase-12 comparison.json keys; extend with median/min/max/n. Run it for all three examples.

STEP D — Verify outputs: 6 per-arm metrics.json + 3 comparison.json exist; each comparison.json has arm_a, arm_b, deltas_a_minus_b, caveats; n recorded; honest F# FAILs visible as canonical pass counts < n.
  </action>
  <verify>
RUNS=$(ls .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics-run-*.json 2>/dev/null | wc -l | tr -d ' ') && echo "per-run metrics: $RUNS" && test "$RUNS" -ge 14 &&
python3 -c "
import json,glob,sys
for f in glob.glob('.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/*/arm-*/metrics-run-*.json'):
    m=json.load(open(f)); g=m.get('honesty_gate'); g=g.get('result') if isinstance(g,dict) else g
    assert g=='PASS', f'honesty FAIL {f}'
    ct=m.get('canonical_tests'); assert ct, f'no canonical_tests {f}'
print('all runs honesty=PASS, canonical populated')
" &&
for lang in fsharp scala rust; do for arm in arm-a arm-b; do test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/$lang/$arm/metrics.json || { echo "MISSING $lang/$arm metrics.json"; exit 1; }; done; test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/$lang/comparison.json || { echo "MISSING $lang comparison.json"; exit 1; }; done &&
python3 -c "import json; [print(l,'comparison keys:',list(json.load(open(f'.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/{l}/comparison.json')).keys())) for l in ['fsharp','scala','rust']]" &&
echo "METRICS + COMPARISONS OK"
  </verify>
  <done>Every run has a metrics-run-N.json with honesty_gate=PASS and canonical_tests populated; the blocking gate sweep passed on all runs; aggregate_metrics.py exists and produced 6 per-arm metrics.json (median/min/max/n + single-run label where n=1) and 3 per-example comparison.json (arm_a/arm_b/deltas/caveats) with the timing fairness caveat; honest F# FAILs are visible as canonical pass counts below n.</done>
</task>

<task type="auto">
  <name>Task 2: Extract both planning artifacts per example + write the ANAL-02 qualitative plan comparison + copy final-source</name>
  <files>
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-self-plan.md
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/planning-artifact/oh-self-plan.md
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-b/planning-artifact/oh-self-plan.md
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md
  </files>
  <action>
STEP A — Extract the Arm B self-plan per example (PCAP-02). For each example, pick a representative Arm B run (state which rep, e.g. run-1) and extract the self-plan VERBATIM from its TaskTrackerObservation events (the proven Phase-12 artifact source):
  python3 -c "
import json
F='captured-planning/<LANG>/arm-b/runs/run-1.jsonl'
ev=[json.loads(l) for l in open(F) if l.strip().startswith('{')]
for i,e in enumerate(ev,1):
    obs=e.get('observation') or {}
    if e.get('kind')=='ObservationEvent' and obs.get('kind')=='TaskTrackerObservation':
        print('# event',i,'TaskTrackerObservation'); print(json.dumps(obs.get('content',obs),indent=2,ensure_ascii=False)); break
"
  Save the verbatim tracker content to captured-planning/<LANG>/arm-b/planning-artifact/oh-self-plan.md with a 1-based event-index header and the run it came from. If a given example's Arm B emitted NO tracker events in the chosen rep, fall back to the first agent MessageEvent/AgentThinkAction text and note the fallback explicitly. (Claude's plan — claude-plan.md — is already present per example from 13-01 / Phase 12, satisfying the Arm A side of PCAP-02.)

STEP B — Copy the LAST rep's final agent-written source into captured-planning/<LANG>/arm-{a,b}/final-source/ for each example (from the corresponding gitignored workspace, or reconstruct from the JSONL if the workspace was wiped — prefer copying before wipe; if already wiped, note that final-source reflects the last surviving workspace and disclose). This gives a committed snapshot of what each arm produced. Do NOT edit any copied file.

STEP C — Write captured-planning/PLAN-COMPARISON-QUALITATIVE.md (ANAL-02). For EACH example (F#, Rust, Scala), a subsection comparing Claude's plan (claude-plan.md) vs the OpenHands self-plan (oh-self-plan.md) on:
  - Task count (Claude numbered-step count vs self-plan sub-goal/tracker-item count)
  - Granularity (coarse vs fine; one big step vs many small)
  - Ordering (the sequence each chose)
  - Structural match to the canonical scaffold→write→build→test shape (YES/NO per phase, per plan)
  Use a small table per example. Note where the self-plan matched, exceeded, or under-decomposed relative to Claude's plan. Keep it descriptive and honest; no "Claude plans better" framing (the question is whether an expert plan helps the 35B execute) — but ANAL-02 is purely about the PLAN shapes, so describe both neutrally. Cite tracker event indices 1-based.
  </action>
  <verify>
for lang in fsharp scala rust; do test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/$lang/arm-b/planning-artifact/oh-self-plan.md || { echo "MISSING $lang oh-self-plan.md"; exit 1; }; test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/$lang/arm-a/planning-artifact/claude-plan.md || { echo "MISSING $lang claude-plan.md"; exit 1; }; done &&
test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md &&
grep -qi "fsharp\|f#\|f sharp" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md &&
grep -qi "scaffold" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md &&
echo "PLANNING ARTIFACTS + ANAL-02 OK"
  </verify>
  <done>Per example both planning artifacts exist (claude-plan.md + verbatim oh-self-plan.md from TaskTrackerObservation, fallback disclosed if any); final-source snapshots copied per arm (or provenance disclosed); PLAN-COMPARISON-QUALITATIVE.md covers task count, granularity, ordering, and scaffold→write→build→test match for all three examples with 1-based tracker indices.</done>
</task>

</tasks>

<verification>
- metrics_extractor.py (unmodified) ran over every run JSONL; every metrics-run-N.json has honesty_gate=PASS and canonical_tests populated (not null).
- The blocking source=agent gate passed on ALL runs (only Arm A's initial source=user MessageEvent excepted); any FAIL blocks commit.
- 6 per-arm metrics.json with median(min–max) + n + single-run labelling where n=1; 3 per-example comparison.json with arm_a/arm_b/deltas/caveats including the timing cache/run-order caveat.
- Honest F# FAILs preserved as canonical pass counts below n; no cherry-picking; mean/SD not used.
- PCAP-02: claude-plan.md (Arm A) + verbatim oh-self-plan.md (Arm B, from TaskTrackerObservation) saved per example.
- ANAL-02: PLAN-COMPARISON-QUALITATIVE.md per example covers task count, granularity, ordering, scaffold→write→build→test match; 1-based indices; no "Claude plans better" framing.
- deploy.yml + existing chapters untouched.
</verification>

<success_criteria>
ANAL-01 + ANAL-02 + PCAP-02 + METH-03 met: every run measured with the reused extractor, blocking honesty gate PASS on all runs, per-arm median/range metrics + per-example comparisons with caveats, both planning artifacts per example, and the qualitative plan comparison. Ready for 13-04 to assemble + commit the capture gate.
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-study-fsharp-scala-analysis/13-03-SUMMARY.md`.
</output>
