Working directory: __WORKDIR__

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

Goal: Build a Scala 3 calculator (Calc.scala) that evaluates an integer arithmetic
expression passed as a command-line argument and prints the integer result. Operators:
+ - * / where * and / bind more tightly than + and -; parentheses override
precedence; integer arithmetic only. Use ONLY the Scala standard library — no
external dependencies.

Constraints:
- scala-cli 1.14.0 + Scala 3 is available on this host (verified).
- All file creation and editing must use bash commands only (heredoc/printf/tee).
- The calculator must read exactly one command-line argument (the expression string).
- Use only the Scala standard library — no external libraries, no build.sbt,
  no scala-parser-combinators, no fastparse.

Canonical tests (must pass):
  scala-cli run Calc.scala -- "2+3*4"    → 14   (precedence: * binds tighter than +)
  scala-cli run Calc.scala -- "(2+3)*4"  → 20   (parentheses override)
  scala-cli run Calc.scala -- "10-3-2"   → 5    (left-associative subtraction)

Run everything in this one session. Do not stop until all three canonical tests pass
or you have documented why they cannot be fixed. Report the EXACT output and exit
code for each test; an honest failure is more valuable than a fabricated success.

Complete the following numbered steps in order, in this one session:

Step 1 — Scaffold:
Confirm scala-cli is installed: scala-cli --version
Create a project subdirectory: mkdir -p calc && cd calc
Write a smoke-test file Hello.scala (using a bash heredoc):
  @main def hello() = println("SCALA_OK")
Run it: scala-cli run Hello.scala
The first run may download compiler artifacts and take 30–60 seconds — wait for the
output; do not retry prematurely.
Expected output: SCALA_OK
Report the scala-cli version and whether SCALA_OK appeared.
Success criterion: scala-cli --version runs; scala-cli run Hello.scala prints SCALA_OK.
Do not write any calculator code yet.

Step 2 — Write Calc.scala:
After Step 1, calc/ exists and scala-cli is confirmed working. Write Calc.scala in
calc/ that evaluates an arithmetic expression from a single command-line argument and
prints the integer result.
Requirements:
- Input: a single command-line argument, e.g. "2+3*4"
- Output: the integer result on its own line, e.g. 14
- Operators: + - * / where * and / bind more tightly than + and - (2+3*4 = 14)
- Parentheses override precedence ((2+3)*4 = 20)
- Integer arithmetic only (no floating point)
- Use ONLY the Scala standard library — no external libraries, no build dependencies
After writing, show: cat Calc.scala
Do NOT run or compile yet — Step 3 handles running and testing.
Success criterion: Calc.scala exists with correct precedence and parentheses handling.

Step 3 — Build and test:
After Step 2, Calc.scala exists in calc/. Run and verify all canonical tests.
Run the first test (compilation happens here; fix any compile errors before
continuing):
  scala-cli run Calc.scala -- "2+3*4"
If compilation fails, read the error, fix Calc.scala (heredoc/printf/tee only — NOT
file_editor), and retry. Report each compiler error and what was changed to fix it.
Once compilation succeeds, run all three canonical tests and capture exact output:
  scala-cli run Calc.scala -- "2+3*4"
  scala-cli run Calc.scala -- "(2+3)*4"
  scala-cli run Calc.scala -- "10-3-2"
Required outputs: 14 / 20 / 5 respectively.
Report the EXACT output for each test. If a test prints the wrong number, investigate
and fix Calc.scala if possible, but report the wrong output honestly first.
Constraints: no external library dependencies; standard library only.
Success criterion: all three canonical tests print the correct integer and exit 0.
An honest failure is more valuable than a fabricated success.
