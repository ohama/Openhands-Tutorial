# Requirements Archive: v1.1 Model Comparison (35B vs 122B)

**Archived:** 2026-05-28
**Status:** ✅ SHIPPED

This is the archived requirements specification for v1.1.
For the cumulative validated set, see `.planning/PROJECT.md` (Validated section).
For current requirements, run `/gsd:new-milestone` (creates a fresh REQUIREMENTS.md).

---

## v1.1 Requirements (all complete)

### 122B run capture

- [x] **RUN122-01**: A real captured 122B OpenHands run of the F# FsLex/FsYacc calculator exists (per-task JSONL on disk), with the `.fsl` lexer attempted **unaided first** (no provided lexer).
- [x] **RUN122-02**: The run honestly records whether 122B wrote the `.fsl` lexer itself; if scaffolding was used as a fallback, that is disclosed. *(Outcome: 122B succeeded unaided; scaffold fallback never triggered.)*
- [x] **RUN122-03**: The run's real outcome on `2+3*4=14`, `(2+3)*4=20`, `10-3-2=5` is captured, along with any genuine error-and-fix cycle. *(Outcome: 14/20/5 PASS; 9 agent-driven FsLex API fix iterations in task5 events 12–74.)*

### Comparison content

- [x] **CMP-01**: A comparison chapter/appendix contrasts 35B vs 122B on this task — capability (did 122B write the lexer unaided?), error-and-fix differences, and **measured speed** (real per-call timing from both runs' JSONL — the original "~14–32s/cycle" estimate was a pre-run prediction, not measured data).
- [x] **CMP-02**: Every comparison claim is backed by real captured evidence from both runs (verbatim, traceable to the JSONL/logs); no fabrication or idealized numbers. *(Outcome: audit spot-checked 6 numeric claims, all matched primary sources verbatim.)*

### Publish

- [x] **PUB-01**: The updated book builds clean with `mdbook build` (no broken links), the new comparison chapter wired into `src/SUMMARY.md`. *(Outcome: build exits 0; one cosmetic WARN on `<char>` HTML tag inside a code span — does not affect HTML rendering.)*
- [x] **PUB-02**: The updated book is re-deployed live to GitHub Pages (the existing GitHub Actions workflow on push to `main`). *(Outcome: live at https://ohama.github.io/Openhands-Tutorial/appendix-c-comparison.html, HTTP 200.)*

## Out of Scope (v1.1) — confirmed held

| Feature | Reason | Held? |
|---------|--------|-------|
| Rewriting v1 chapters | v1.1 is additive — v1 content stays; only a comparison chapter is added | ✓ (v1 chapters untouched except for additive UX callouts in 3 files) |
| Cloud/hosted models | The comparison is local-only (35B vs 122B); cloud comparison stays deferred | ✓ |
| Re-running the 35B | The 35B run is already captured in v1; v1.1 reuses it as baseline | ✓ |
| Fabricated/idealized comparison numbers | Honesty is the core value | ✓ (audit confirmed) |

## Traceability (all Complete)

| Requirement | Phase | Final Status |
|-------------|-------|-------------|
| RUN122-01 | 6 | ✓ Complete |
| RUN122-02 | 6 | ✓ Complete (unaided succeeded; no fallback needed) |
| RUN122-03 | 6 | ✓ Complete (14/20/5; 9 fix iterations captured) |
| CMP-01 | 7 | ✓ Complete (capability + error-fix + real timing) |
| CMP-02 | 7 | ✓ Complete (all claims verbatim-traced) |
| PUB-01 | 7 | ✓ Complete (mdbook EXIT 0) |
| PUB-02 | 7 | ✓ Complete (HTTP 200 live) |

---

## Milestone Summary

**Shipped:** 7 of 7 v1.1 requirements
**Adjusted:** CMP-01 wording re-framed mid-milestone — the original draft made `~14–32s/cycle` sound like 35B measured baseline; revised to explicitly call out that it was a pre-run prediction and to derive real per-call averages from JSONL. (Commit `ec73397`.)
**Dropped:** None.

**Additive (out of scope of original requirements):**

- Tutorial UX callouts (📨/⚙️/✅ blockquote pattern) added to 4 chapters in response to direct user request after Phase 7 shipped (commits `296711e`, `0439c85`, `7aba9d6`, `84053be`).
- README.md updated to reflect 35B/122B both running, 부록 C addition, callout pattern, and honest measured per-call timing replacing stale pre-run estimate (commit `c2032ac`).

**Future Requirements (deferred to a later milestone, carried from v1's v2 backlog):**

- **EXT-01**: Additional worked examples beyond the calculator
- **EXT-02**: English translation of the tutorial
- **EXT-03**: A "build your own minimal agent in F#" appendix
- **EXT-04** *(suggested by v1.1 audit, optional)*: Local-vs-cloud model comparison

---

*Archived: 2026-05-28 during /gsd:complete-milestone v1.1*
