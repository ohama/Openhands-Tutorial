---
phase: 05-troubleshooting-reproducibility-publish
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - .github/workflows/deploy.yml
  - book.toml
autonomous: false
user_setup:
  - service: github-pages
    why: "Publishing a PUBLIC GitHub repo and enabling Pages is an irreversible publishing action requiring user authorization"
    dashboard_config:
      - task: "Confirm repo name (OpenHandsTests) + visibility (public), then authorize the push, OR run the gh commands themselves"
        location: "GitHub — repo create + Settings → Pages (Source = GitHub Actions)"

must_haves:
  truths:
    - "A valid .github/workflows/deploy.yml builds the mdbook and deploys to GitHub Pages via actions/deploy-pages@v4"
    - "The workflow has permissions pages:write + id-token:write and triggers on push to main"
    - "book.toml carries the git-repository-url / edit-url-template fields"
    - "AFTER the user authorizes the publish (checkpoint), the live URL https://ohama.github.io/OpenHandsTests/ serves all chapters with correct assets"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "mdBook→GitHub Pages deploy workflow (build + deploy jobs)"
      contains: "actions/deploy-pages@v4"
      min_lines: 40
    - path: "book.toml"
      provides: "git-repository-url + edit-url-template for edit-on-GitHub links"
      contains: "git-repository-url"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/upload-pages-artifact@v3 (path: book) → actions/deploy-pages@v4"
      pattern: "upload-pages-artifact@v3"
    - from: "git push origin main (user-authorized)"
      to: "live site"
      via: "workflow run triggered by push, Pages source = GitHub Actions"
      pattern: "ohama.github.io/OpenHandsTests"
---

<objective>
Ship the book. Autonomously create the GitHub Actions deploy workflow and add the optional
book.toml repo fields, with a final green local build. Then STOP at a user-gated checkpoint:
the repo does not exist yet (local-only `main`, no remote) and creating/pushing a PUBLIC repo
+ enabling Pages is a publishing action the user must authorize. Provide the exact gh/git/Pages
commands in the checkpoint, and verify the live site only AFTER the user authorizes.

Purpose: Satisfies BOOK-03 (Actions workflow deploys to Pages; live URL serves all chapters).
SC#4's live-site verification depends on the user-authorized publish step (the checkpoint).
Output: .github/workflows/deploy.yml, updated book.toml; then (post-authorization) a live
GitHub Pages site at https://ohama.github.io/OpenHandsTests/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-troubleshooting-reproducibility-publish/05-RESEARCH.md
@book.toml
</context>

<constraints>
- The workflow runs on ubuntu-latest and builds ONLY the mdbook (it does NOT build the F#
  project), so no .NET/sed concerns in CI.
- Use the EXACT action versions from 05-RESEARCH §3.2 (all HIGH-confidence, stable as of
  2026-05-28): actions/checkout@v4, actions/configure-pages@v4,
  actions/upload-pages-artifact@v3, actions/deploy-pages@v4.
- Repo name MUST be OpenHandsTests to match `site-url = "/OpenHandsTests/"` in book.toml.
- DO NOT create or push the GitHub repo, and DO NOT enable Pages, without explicit user
  authorization (the checkpoint). gh is logged in as `ohama` with repo+workflow scopes.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create the GitHub Actions deploy workflow + book.toml repo fields</name>
  <files>.github/workflows/deploy.yml, book.toml</files>
  <action>
Create .github/workflows/deploy.yml with the EXACT YAML from 05-RESEARCH §3.2 (verbatim):

```yaml
name: Deploy mdBook to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install mdBook
        run: |
          tag=$(curl -s 'https://api.github.com/repos/rust-lang/mdBook/releases/latest' \
            | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/')
          curl -sSL \
            "https://github.com/rust-lang/mdBook/releases/download/${tag}/mdbook-${tag}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar -xz --directory="$HOME/.cargo/bin"

      - name: Build book
        run: mdbook build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

Then add the two recommended fields to book.toml under [output.html] (after the existing
preferred-dark-theme line), per §3.4:
```
git-repository-url = "https://github.com/ohama/OpenHandsTests"
edit-url-template = "https://github.com/ohama/OpenHandsTests/edit/main/{path}"
```
Do not remove or change the existing site-url / default-theme / preferred-dark-theme lines.

Finally run a clean local build to confirm book.toml is still valid:
  /opt/homebrew/bin/mdbook build
  </action>
  <verify>
    test -f .github/workflows/deploy.yml;
    grep -c "actions/deploy-pages@v4" .github/workflows/deploy.yml (==1);
    grep -c "pages: write" .github/workflows/deploy.yml (==1) && grep -c "id-token: write" .github/workflows/deploy.yml (==1);
    python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy.yml'))" exits 0 (valid YAML);
    grep -c "git-repository-url" book.toml (==1);
    /opt/homebrew/bin/mdbook build exits 0.
  </verify>
  <done>
    .github/workflows/deploy.yml exists, is valid YAML, has the build+deploy jobs with
    pages:write + id-token:write and the four pinned action versions; book.toml has
    git-repository-url + edit-url-template; local mdbook build still green.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    The deploy workflow (.github/workflows/deploy.yml) and book.toml repo fields are in place
    and the book builds clean locally. The repository does NOT yet exist on GitHub — `main` is
    local-only with no remote. Creating + pushing a PUBLIC repo and enabling Pages publishes the
    book to the internet, which is irreversible (the content becomes publicly visible).
  </what-built>
  <decision>
    Confirm before the agent publishes:
    1. Repo name: `OpenHandsTests` (MUST match site-url "/OpenHandsTests/" — changing it means
       editing book.toml and the two book.toml URLs first).
    2. Visibility: `public` (free GitHub Pages requires public; private needs GitHub Pro).
    3. Authorize the agent to run `gh repo create ... --push`, enable Pages, and verify the live
       site — OR you will run the commands yourself.
  </decision>
  <how-to-verify>
    Once you authorize (or after you run these yourself), these are the EXACT commands
    (from 05-RESEARCH §3.5). Run from the repo root:

    1) Create the remote repo (public) and push main:
       gh repo create OpenHandsTests \
         --public \
         --description "에이전틱 AI 튜토리얼: OpenHands로 배우는 AI 에이전트" \
         --source . \
         --remote origin \
         --push

    2) Enable GitHub Pages with source = GitHub Actions:
       gh api repos/ohama/OpenHandsTests/pages --method POST -f build_type=workflow

    3) Confirm Pages is enabled:
       gh api repos/ohama/OpenHandsTests/pages | python3 -m json.tool | grep '"status"'

    4) Watch the deploy workflow finish (the push in step 1 triggers it):
       gh run watch --exit-status   (or: gh run list --workflow=deploy.yml)

    5) Verify the LIVE site serves all chapters with correct assets (SC#4):
       curl -sSI https://ohama.github.io/OpenHandsTests/ | head -1          # expect 200
       curl -sS  https://ohama.github.io/OpenHandsTests/appendix-a-repro.html | grep -q "재현" && echo OK
       curl -sS  https://ohama.github.io/OpenHandsTests/appendix-b-troubleshooting.html | grep -q "트러블슈팅" && echo OK
       # spot-check a CSS asset returns 200 (no asset 404s):
       curl -sSI https://ohama.github.io/OpenHandsTests/css/general.css | head -1

    Alternative: enable Pages manually in the web UI (Settings → Pages → Source = "GitHub Actions").

    Note: GitHub Pages may take 1–2 minutes after the first workflow run before the URL serves
    content; re-run the curl checks if the first attempt 404s.
  </how-to-verify>
  <resume-signal>
    Reply "authorize: public OpenHandsTests" to have the agent run the commands, or "I'll publish
    myself" (then paste the live-site curl results), or specify a different repo name/visibility.
  </resume-signal>
</task>

</tasks>

<verification>
- .github/workflows/deploy.yml is valid YAML with build+deploy jobs, pages:write + id-token:write,
  and the four pinned action versions; book.toml has the repo URL fields; local build green.
- AFTER user authorization (checkpoint): repo OpenHandsTests exists, pushed; Pages enabled with
  source=GitHub Actions; deploy workflow run succeeds; https://ohama.github.io/OpenHandsTests/
  returns 200 and serves 부록 A, 부록 B and assets without 404s.
</verification>

<success_criteria>
- BOOK-03 met: a GitHub Actions workflow deploys the mdbook to GitHub Pages.
- SC#4 met (post-checkpoint): the live URL serves all chapters with correct CSS/JS — verified
  only after the user authorizes the publish.
</success_criteria>

<output>
After completion, create
`.planning/phases/05-troubleshooting-reproducibility-publish/05-04-SUMMARY.md`
</output>
