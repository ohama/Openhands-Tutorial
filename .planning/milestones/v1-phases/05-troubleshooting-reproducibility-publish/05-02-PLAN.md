---
phase: 05-troubleshooting-reproducibility-publish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/appendix-a-repro.md
autonomous: true

must_haves:
  truths:
    - "Reader has the exact prerequisites (Colima/Docker, uv+OpenHands 1.16, dotnet 10, litellm@127.0.0.1:4000 serving qwen-local) to reproduce the run"
    - "Reader has the exact env-var headless invocation and the per-task prompt files to run"
    - "Reader knows the expected outputs (14/20/5; build 성공) verbatim from the captured run"
    - "All content is verbatim-from-artifacts — nothing fabricated"
  artifacts:
    - path: "src/appendix-a-repro.md"
      provides: "부록 A 재현 가이드 (REPRO-01) — prereqs + verify commands + exact invocation + task-prompt pointers + expected outputs"
      contains: "openai/qwen-local"
      min_lines: 90
  key_links:
    - from: "src/appendix-a-repro.md"
      to: "captured/test-output.txt expected results"
      via: "verbatim test cases 2+3*4=14, (2+3)*4=20, 10-3-2=5"
      pattern: "2\\+3\\*4 = 14"
---

<objective>
Write 부록 A — a reproducibility guide (REPRO-01). It must give a reader EXACTLY what they
need to reproduce the captured run: prerequisites with versions, stack-verification commands,
the verbatim env-var headless invocation, pointers to the committed task-prompt files (with
the key design rationale), and the verbatim expected outputs (14/20/5 and the build summary).

Purpose: Satisfies REPRO-01 with the project's no-fabrication constraint. All values come
from 05-RESEARCH.md §2 and the committed artifacts.
Output: src/appendix-a-repro.md (file only — SUMMARY wiring + the 부록 A title rename to
"재현 가이드" happens in 05-03).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-troubleshooting-reproducibility-publish/05-RESEARCH.md

# Verbatim sources — read these to copy exact values, do not paraphrase numbers:
@.planning/phases/03-capture-the-openhands-run/captured/test-output.txt
@.planning/phases/03-capture-the-openhands-run/task-prompts/00-INVOCATION.md
@.planning/phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
</context>

<constraints>
- Korean prose; English/code for technical terms, commands, env vars, file names.
- NO fabrication. Every version, command, and output value is copied verbatim from
  05-RESEARCH.md §2 or the captured artifacts (test-output.txt, 00-INVOCATION.md).
- The canonical task prompts live at
  `.planning/phases/03-capture-the-openhands-run/task-prompts/` — the book's `src/` does NOT
  contain them. Point readers there by RELATIVE-IN-REPO path and quote the key prompt design
  choices; you may quote short representative snippets (the IMPORTANT bash-only block) but do
  not paste entire multi-hundred-line prompt files into the appendix.
- Do NOT add Markdown links to unwired SUMMARY draft entries. Prose references only.
- Real timings are 14–32s/cycle, total ~6 min for the 5-task attempt-2 run (§2.5). Never 240s.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the prerequisites + stack-verification section of 부록 A</name>
  <files>src/appendix-a-repro.md</files>
  <action>
Create src/appendix-a-repro.md. Start with an H1-less intro paragraph (mdBook uses the
SUMMARY title) explaining this appendix lets the reader reproduce the exact captured run.

## 전제 조건 (Prerequisites):
Reproduce the table from §2.2 faithfully (component / version / notes):
  - macOS (Apple Silicon) — run was on a headless SSH Mac; adapts to Intel/Linux.
  - Colima OR Docker Desktop — `colima start --cpu 4 --memory 8 --disk 60` before running;
    Docker Desktop users omit DOCKER_HOST.
  - uv — `curl -LsSf https://astral.sh/uv/install.sh | sh`.
  - OpenHands CLI v1.16.0 (SDK v1.21.0) — `uv tool install openhands --python 3.12`; PATH via ~/.local/bin.
  - .NET SDK 10.0.203+ — host PATH (LocalWorkspace).
  - litellm proxy at 127.0.0.1:4000 exposing model alias `qwen-local`.
  - Qwen2.5-35B or larger with tool calling; proxy routes via `openai/qwen-local`.
  - FsLexYacc 11.3.0 in the NuGet cache (`~/.nuget/packages/fslexyacc/11.3.0/`).

## 스택 점검 (Verify the stack before running):
Include the verbatim command block from §2.2 in a fenced bash block: `docker run hello-world`;
`openhands --version` (SDK v1.21.0 / CLI 1.16.0); the `curl .../v1/models | ... grep '"id"'`
that should show `"id": "qwen-local"`; the chat/completions tool-calling smoke curl; and
`dotnet --version` (10.0.203+). Add one Korean sentence per command saying what a passing
result looks like.
  </action>
  <verify>
    grep -c "qwen-local" src/appendix-a-repro.md (>=2);
    grep -c "10.0.203\|fslexyacc/11.3.0\|--python 3.12" src/appendix-a-repro.md (>=1);
    /opt/homebrew/bin/mdbook build exits 0.
  </verify>
  <done>
    Prereqs table (all 8 components with versions) and the verify-stack command block present,
    matching §2.2 verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the invocation + task-prompt pointers + expected-outputs section</name>
  <files>src/appendix-a-repro.md</files>
  <action>
Append to src/appendix-a-repro.md (same file; Task 1 created it).

## 실행 명령 (Exact invocation):
Reproduce the verbatim env-var headless invocation from §2.3 in a fenced bash block:
```
OPENHANDS_SUPPRESS_BANNER=1 \
LLM_MODEL="openai/qwen-local" \
LLM_BASE_URL="http://127.0.0.1:4000/v1" \
LLM_API_KEY="dummy" \
OPENHANDS_WORK_DIR="/path/to/your/oh-workdir" \
openhands --headless --json --yolo --override-with-envs \
  -t "$(cat task1-scaffold.txt)" \
  2>task1-scaffold.stderr.log | tee task1-scaffold.jsonl
```
Explain (Korean): repeat for each task, substituting prompt file + log name. Colima users
prepend `DOCKER_HOST="unix:///Users/$(whoami)/.colima/default/docker.sock" \` to the env block.
Note `--override-with-envs` is required and `openai/` prefix is required (cross-reference
부록 B in prose).

## 태스크 프롬프트 (Task prompts — verbatim sources):
List the canonical prompt files at the in-repo path
`.planning/phases/03-capture-the-openhands-run/task-prompts/`:
00-INVOCATION.md, task1-scaffold.txt, task2-lexer.txt, task3-parser.txt,
task4-evaluator.txt, task5-buildtest.txt. Then state the key design choices from §2.4:
  - task1 provides verbatim calc.fsproj with FixLineDirectives (bypasses the .NET 10 bug).
  - task2 provides verbatim Lexer.fsl (FsLex is OOD for the 35B — attempt 1 proved it with
    137 failed TerminalActions).
  - task3 specifies behavioral outcomes (10-3-2=5, 2+3*4=14) WITHOUT revealing %left.
  - every prompt includes the bash-only file-editing instruction. Quote that IMPORTANT block
    verbatim (the "Create and edit ALL files using ONLY bash shell commands ... Do NOT use the
    file_editor / str_replace tool" text from §2.4 / §1.1 REAL-04).

## 예상 출력 (Expected outputs):
Reproduce VERBATIM the captured test output from §2.5 / captured/test-output.txt in a fenced
block (the `=== Calculator Correctness Test ===` block ending with `2+3*4 = 14`,
`(2+3)*4 = 20`, `10-3-2 = 5`). Then the run summary from §2.5: 5 tasks / 0 retries in
attempt 2; 146 total events; 67 TerminalActions; timings task1=3m6s, task2=16s, task3=1m17s,
task4=45s, task5=32s → ~6 min total. Then the error-and-fix cycle list (task3 events 9–30):
the 5-line Attempt 1..5 summary from §2.5 ending in `calc net10.0 성공 (0.7초)`.
Korean framing sentences around each block.
  </action>
  <verify>
    grep -c "openai/qwen-local" src/appendix-a-repro.md (>=1, total file now >=2 from Task 1);
    grep -F "2+3*4 = 14" src/appendix-a-repro.md (match);
    grep -F "10-3-2 = 5" src/appendix-a-repro.md (match);
    grep -c "override-with-envs\|file_editor\|FixLineDirectives" src/appendix-a-repro.md (>=2);
    grep -c "240" src/appendix-a-repro.md == 0 (or only as "unmeasured estimate" framing);
    /opt/homebrew/bin/mdbook build exits 0.
  </verify>
  <done>
    src/appendix-a-repro.md (>=90 lines total) contains the verbatim invocation, the task-prompt
    file pointers + design rationale + the bash-only IMPORTANT block, and the verbatim expected
    outputs (14/20/5, build 성공, ~6 min, error-and-fix list). No fabricated values; no 240s as real.
  </done>
</task>

</tasks>

<verification>
- src/appendix-a-repro.md exists, >=90 lines.
- Verbatim 14/20/5 outputs and the exact env-var invocation present.
- `/opt/homebrew/bin/mdbook build` exits 0 (file unwired; build must still pass).
- No fabricated numbers; timings are real (~6 min, 14–32s/cycle), never 240s as a real figure.
</verification>

<success_criteria>
- REPRO-01 covered: exact prereqs + commands + config + task-prompt pointers + expected outputs,
  all verbatim from the captured artifacts.
</success_criteria>

<output>
After completion, create
`.planning/phases/05-troubleshooting-reproducibility-publish/05-02-SUMMARY.md`
</output>
