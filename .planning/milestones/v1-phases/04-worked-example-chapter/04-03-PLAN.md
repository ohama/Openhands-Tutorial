---
phase: 04-worked-example-chapter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ch04-calculator/final.md
autonomous: true

must_haves:
  truths:
    - "Reader sees the complete final source of all 4 files, with provided files labeled as not agent-authored (WALK-03)"
    - "Reader sees the calculator correctly evaluate 2+3*4 → 14, a parenthesized case (2+3)*4 → 20, and 10-3-2 → 5 from real captured output (VERIFY-01)"
    - "Reader gets an honest performance note: real attempt-2 timings (~10 min) and the attempt-1 failure story (~150 min, FsLex capability boundary) — NOT ~240s/call (VERIFY-02)"
  artifacts:
    - path: "src/ch04-calculator/final.md"
      provides: "Final source recap (4 files, scaffolding labeled) + verbatim verification block (14/20/5) + honest performance/limits note"
      min_lines: 70
  key_links:
    - from: "src/ch04-calculator/final.md"
      to: "captured/test-output.txt verification block"
      via: "verbatim 2+3*4 = 14 / (2+3)*4 = 20 / 10-3-2 = 5"
      pattern: "2\\+3\\*4 = 14"
    - from: "src/ch04-calculator/final.md"
      to: "honest performance numbers from RESEARCH §7"
      via: "real attempt-2 (~10 min) + attempt-1 failure (~150 min, FsLex)"
      pattern: "10\\s*분|150\\s*분|FsLex"
---

<objective>
Write the closing file of 4부: final.md (완성된 계산기). It recaps the complete final source of all four files (with provided files clearly labeled), shows the authoritative verification block from captured/test-output.txt (2+3*4 → 14, (2+3)*4 → 20, 10-3-2 → 5), and ends with an HONEST performance-and-limits note using the REAL numbers (not the stale ~240s/call).

Purpose: Delivers WALK-03 (final source incl. %left), VERIFY-01 (14 + parenthesized + precedence), and VERIFY-02 (honest perf) — the chapter's verifiable payoff.
Output: src/ch04-calculator/final.md (Korean prose, English technical terms, ASCII art only).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-worked-example-chapter/04-RESEARCH.md

# Verbatim sources — embed/quote exactly from these:
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Parser.fsy
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Program.fs
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Lexer.fsl
@.planning/phases/03-capture-the-openhands-run/captured/final-source/calc.fsproj
@.planning/phases/03-capture-the-openhands-run/captured/test-output.txt
</context>

<honesty_constraints>
NON-NEGOTIABLE (from 04-RESEARCH.md §2D, §5, §6, §7, §10):
1. Embed ALL FOUR final files VERBATIM from captured/final-source/. Label Lexer.fsl and calc.fsproj as provided (NOT agent-authored), with the exact labels from RESEARCH §5:
   - Lexer.fsl: "이 파일은 태스크 프롬프트에 전달된 내용을 그대로 복사한 것입니다 (에이전트가 작성하지 않음)."
   - calc.fsproj: "이 파일도 태스크 프롬프트에 제공됐습니다. FixLineDirectives 타겟은 .NET 10 + FsLexYacc 11.3.0 호환성 문제를 해결하는 비직관적인 워크어라운드입니다."
2. Parser.fsy MUST show `%left PLUS MINUS` / `%left STAR SLASH` and the two-part `%start start` / `%type <int> start` form (the form the agent reached after 3 failed attempts).
3. Program.fs final form uses `LexBuffer<char>.FromString` (not FromText) and curried `Parser.start Lexer.tokenize lexbuf`. Note (without editorializing) that `open System` is present but unused — leftover from an earlier draft (RESEARCH §5).
4. Verification block: quote test-output.txt VERBATIM (build summary + the three test cases 2+3*4 = 14, (2+3)*4 = 20, 10-3-2 = 5).
5. Performance note: use the REAL numbers from RESEARCH §7. NEVER write "~240s/call" (RESEARCH §2D: that figure is wrong and absent from evidence). Use: attempt-2 simple tasks fast (task2 16s, task5 32s), complex tasks (task1 3m 6s, task3 1m 17s), full 5-task run ~10 min; attempt 1 ~150 min FAILED — Qwen2.5-35B could not write valid FsLex (.fsl out-of-distribution), 94+27+16 TerminalActions across 3 invocations.
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write final.md (완성된 계산기) — source recap + verification + honest perf note</name>
  <files>src/ch04-calculator/final.md</files>
  <action>
Read all four captured/final-source/ files and test-output.txt first. Write final.md in Korean. Sections:

1. **완성된 소스 (final source, WALK-03)** — Embed all four files verbatim, each in a fenced code block with a heading and an authorship label:
   - **Parser.fsy (에이전트 작성)** — full verbatim content; one-line caption noting `%left PLUS MINUS` / `%left STAR SLASH` give precedence and the grammar actions (`$1 + $3`) do the evaluation.
   - **Program.fs (에이전트 작성)** — full verbatim content; note `LexBuffer<char>.FromString` + curried `Parser.start Lexer.tokenize lexbuf` were the fixes from 빌드와 테스트 단계; note plainly that `open System` is present but unused (leftover draft) — no editorializing.
   - **Lexer.fsl (제공됨)** — full verbatim content with the exact label from RESEARCH §5: "이 파일은 태스크 프롬프트에 전달된 내용을 그대로 복사한 것입니다 (에이전트가 작성하지 않음)."
   - **calc.fsproj (제공됨)** — full verbatim content with the exact label from RESEARCH §5: "이 파일도 태스크 프롬프트에 제공됐습니다. FixLineDirectives 타겟은 .NET 10 + FsLexYacc 11.3.0 호환성 문제를 해결하는 비직관적인 워크어라운드입니다." Add a sentence explaining the FixLineDirectives target strips fsyacc-generated `# 0` line directives that the F# 10 compiler rejects.

2. **검증 (verification, VERIFY-01)** — Embed the verbatim verification block from test-output.txt (the `=== Calculator Correctness Test ===` header, the Build summary with 오류 0개 / 경과 시간, and the three Test cases). Then explain each case in prose (RESEARCH §6):
   - `2+3*4 = 14`: STAR binds tighter than PLUS (`%left STAR SLASH` declared after `%left PLUS MINUS` = higher precedence). Without `%left` this would be 20 (wrong). This is the headline proof that operator precedence works.
   - `(2+3)*4 = 20`: explicit grouping via the `LPAREN expr RPAREN` rule overrides precedence.
   - `10-3-2 = 5`: left-associativity ((10-3)-2 = 5, not 9), enforced by `%left`.

3. **성능과 한계에 대한 솔직한 기록 (VERIFY-02)** — Use the draft paragraph from RESEARCH §7 (adapt lightly, keep all numbers exact). Three parts:
   - **시도 2의 실제 속도**: task2 16초, task5 32초; task1 3분 6초, task3 1분 17초; 전체 5개 태스크 ~10분.
   - **시도 1의 실패**: ~150분 진행 후 실패. 원인은 속도가 아니라 능력의 경계 — Qwen2.5-35B가 FsLex .fsl 파일을 올바르게 작성하지 못함 (3회 실행, 94+27+16 TerminalActions, `%%` 오삽입 등). 그래서 시도 2에서 Lexer.fsl을 직접 제공.
   - **결론**: 35B 로컬 모델은 F# 컴파일러 오류를 스스로 진단·수정할 수 있다 (task3의 4회 자가 수정이 증거). 그러나 훈련 데이터에서 드문 DSL(.fsl)은 생성하지 못한다. 현재 로컬 LLM의 현실적 능력 지도.
   ABSOLUTELY NO "~240s/call". Use "약" (approximately) for the 150-minute figure per RESEARCH §8.3 (it's an approximation).

4. **마무리** — One short closing paragraph tying back: the reader watched a real local-model agent plan, write a parser, self-correct four build errors, and produce a correct calculator — the agentic-AI concepts from 1부 made concrete. Prose-only forward-reference (no link) that 5부/부록 cover troubleshooting and reproducibility.

Prose-only forward refs; NO Markdown links to unwritten 5부/부록 files.
  </action>
  <verify>Read back. Confirm: (a) all 4 files embedded verbatim (Parser.fsy with %left + %start start/%type; Program.fs with FromString + curried + unused open System noted; Lexer.fsl + calc.fsproj with EXACT provided-labels from RESEARCH §5); (b) test-output.txt verification block verbatim with 2+3*4 = 14, (2+3)*4 = 20, 10-3-2 = 5; (c) each case explained (precedence/parens/left-assoc); (d) honest perf note with real numbers (16초/32초/3분 6초/1분 17초/~10분/~150분/FsLex) and NO "240" string; (e) no dead Markdown links. Run `grep -E "2\+3\*4 = 14|%left|FromString|150" src/ch04-calculator/final.md` and confirm matches; run `grep -i 240 src/ch04-calculator/final.md` and confirm NO output.</verify>
  <done>final.md exists, ≥70 lines, embeds all 4 final files verbatim (provided files labeled), shows the verbatim 14/20/5 verification block with per-case explanation, and ends with an honest performance/limits note using real numbers (no 240s). WALK-03, VERIFY-01, VERIFY-02 all satisfied.</done>
</task>

</tasks>

<verification>
- final.md exists under src/ch04-calculator/.
- All 4 final-source files embedded verbatim; Lexer.fsl + calc.fsproj carry the exact provided-not-agent labels.
- Parser.fsy embed includes `%left PLUS MINUS` / `%left STAR SLASH` (WALK-03).
- Verification block matches test-output.txt verbatim; 2+3*4 = 14 plus parenthesized + left-assoc cases explained (VERIFY-01).
- `grep -i "240" src/ch04-calculator/final.md` returns nothing; real perf numbers present (VERIFY-02).
- No Markdown links to not-yet-created files.
</verification>

<success_criteria>
- WALK-03 satisfied: final F# source (lexer .fsl, parser .fsy with %left, Program.fs) embedded.
- VERIFY-01 satisfied: 2+3*4 → 14 + parenthesized + precedence cases from real captured output.
- VERIFY-02 satisfied honestly: real attempt-2 (~10 min) and attempt-1 failure (~150 min, FsLex boundary) — not 240s.
- Honesty constraints #1 (scaffolding labeled), #4 (verbatim quotes) enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-worked-example-chapter/04-03-SUMMARY.md`
</output>
