---
phase: 04-worked-example-chapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ch04-calculator/intro.md
  - src/ch04-calculator/planning.md
autonomous: true

must_haves:
  truths:
    - "Reader learns what the calculator does (tokenize → parse → evaluate) without an F# language tutorial (WALK-01)"
    - "Reader understands the 4-file architecture (Lexer.fsl, Parser.fsy, Program.fs, calc.fsproj) and which files the agent actually wrote"
    - "Reader is told up front that Lexer.fsl AND calc.fsproj were PROVIDED to the agent, not written by it"
    - "Reader sees the run was decomposed into 5 scoped OpenHands tasks, mapped to the plan→write→test→run methodology"
  artifacts:
    - path: "src/ch04-calculator/intro.md"
      provides: "WALK-01 calculator intro: tokenize→parse→evaluate pipeline + 4-file architecture + scaffolding disclosure"
      min_lines: 40
    - path: "src/ch04-calculator/planning.md"
      provides: "5-task decomposition + honest scaffolding rationale (FsLex out-of-distribution) + plan→write→test→run callout"
      min_lines: 40
  key_links:
    - from: "src/ch04-calculator/intro.md"
      to: "tokenize → parse → evaluate pipeline"
      via: "prose explanation, no F# syntax tutorial"
      pattern: "토큰|tokenize|파싱|parse|평가|evaluate"
    - from: "src/ch04-calculator/planning.md"
      to: "ch01 plan→write→test→run vocabulary"
      via: "concept↔action callout (Callout D)"
      pattern: "Explore|탐색|Implement|구현|Verify|검증"
---

<objective>
Write the first two files of 4부 (the worked-example chapter): the project intro (WALK-01) and the task-planning narrative. These set up the calculator's purpose, its 4-file architecture, and — critically — the honest disclosure that the lexer and project file were scaffolded (provided to the agent), not written by it.

Purpose: Establishes the honest framing the entire chapter depends on. If the reader does not understand up front that Lexer.fsl/calc.fsproj were provided, the rest of the walkthrough would overstate the agent's work.
Output: src/ch04-calculator/intro.md and src/ch04-calculator/planning.md (Korean prose, English technical terms, ASCII art only).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-worked-example-chapter/04-RESEARCH.md
@src/ch01-agentic-ai/concepts.md

# Verbatim source files for embedding (read these for exact content):
@.planning/phases/03-capture-the-openhands-run/captured/final-source/Lexer.fsl
@.planning/phases/03-capture-the-openhands-run/captured/final-source/calc.fsproj
</context>

<honesty_constraints>
NON-NEGOTIABLE (from 04-RESEARCH.md §2, §10):
1. Lexer.fsl AND calc.fsproj were PROVIDED to the agent in task prompts (task1-scaffold.txt, task2-lexer.txt). Do NOT imply the agent wrote them. The agent's genuine work = Parser.fsy + Program.fs + self-correcting build errors.
2. Reason FsLex was provided: FsLex .fsl format is out-of-distribution for the Qwen2.5-35B model (attempt 1 failed — 3 agent invocations all produced invalid .fsl). State this honestly as a capability boundary, not a speed problem.
3. Do NOT narrate a precedence bug. The agent wrote correct `%left` from the start. (This file does not narrate the build errors at all — that's 04-02 — but must not preview a precedence bug.)
4. The "evaluate" step is NOT a separate evaluator module — the arithmetic happens in the parser grammar actions (`$1 + $3` etc.). Program.fs is the CLI entry point + lexer/parser wiring. Do not imply a standalone evaluator (RESEARCH §8.5).
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write intro.md (예제 프로젝트 소개, WALK-01)</name>
  <files>src/ch04-calculator/intro.md</files>
  <action>
Create the directory src/ch04-calculator/ if needed, then write intro.md in Korean (English for technical terms: tokenize, parse, FsLex, FsYacc, etc.). Sections:

1. **무엇을 만드는가** — A command-line F# calculator that evaluates integer arithmetic expressions like `2+3*4` with correct operator precedence and parentheses. Frame: this is the artifact OpenHands builds across 4부; the point is watching the agent work, not learning F#. State explicitly we do NOT teach F# from scratch (RESEARCH §10, WALK-01; matches REQUIREMENTS Out-of-Scope "Teaching F# language fundamentals").

2. **tokenize → parse → evaluate 파이프라인** — Explain the three conceptual stages in prose (no F# syntax tutorial):
   - tokenize (렉싱): raw text `2+3*4` → token stream INT(2) PLUS INT(3) STAR INT(4) EOF. Handled by the lexer.
   - parse (파싱): token stream → an expression structure honoring precedence (STAR binds tighter than PLUS). Handled by the parser grammar.
   - evaluate (평가): the result is computed AS the parse happens — the grammar actions (`$1 + $3`, `$1 * $3`) ARE the evaluation. CRITICAL: explicitly say there is no separate evaluator module; the arithmetic lives in the parser's grammar actions (RESEARCH §8.5). Program.fs only wires lexer→parser and prints the result.

3. **네 개의 파일 (4-file architecture)** — Present the 4 files with a small ASCII-art pipeline diagram (ASCII only, no mermaid). For each file state its role AND who wrote it:
   | 파일 | 역할 | 작성 주체 |
   - Lexer.fsl — tokenizer (FsLex DSL) — **제공됨 (에이전트 아님)**
   - Parser.fsy — grammar + precedence + arithmetic actions (FsYacc DSL) — **에이전트 작성**
   - Program.fs — CLI 진입점 + lexer/parser 연결 — **에이전트 작성**
   - calc.fsproj — 빌드 설정 (FsLexYacc, FixLineDirectives 워크어라운드) — **제공됨 (에이전트 아님)**

4. **솔직한 공개 (scaffolding disclosure)** — One short paragraph stating clearly: Lexer.fsl and calc.fsproj were handed to the agent verbatim in the task prompts; the agent's genuine work was Parser.fsy, Program.fs, and self-correcting four build failures. Forward-reference (prose only, no Markdown link) that '태스크 계획 단계' explains why the lexer was provided.

5. **1부 개념 미리보기** — Brief prose pointer: 4부 will surface the ch01 vocabulary (tool calling, agent loop, plan→write→test→run, memory) as concept↔action callouts. Prose only — NO Markdown links to other 4부/5부 files (mdBook create-missing discipline; matches 01-02 decision "prose-only forward pointers").

Do NOT embed the full file contents here (intro stays conceptual). Full source goes in 04-02 and 04-03.
  </action>
  <verify>Read back the file. Confirm: (a) tokenize/parse/evaluate all explained; (b) the 4-file table marks Lexer.fsl and calc.fsproj as 제공됨/provided; (c) explicit statement that there is no separate evaluator; (d) no Markdown links to unwritten files (grep for `](` pointing at .md files other than none — should find none); (e) Korean prose with English technical terms.</verify>
  <done>intro.md exists, ≥40 lines, explains the pipeline without an F# tutorial, discloses scaffolding (Lexer.fsl + calc.fsproj provided), and contains no dead Markdown links.</done>
</task>

<task type="auto">
  <name>Task 2: Write planning.md (태스크 계획 단계) with scaffolding rationale + plan→write→test→run callout</name>
  <files>src/ch04-calculator/planning.md</files>
  <action>
Write planning.md in Korean. Sections:

1. **다섯 개의 태스크로 분해** — We (the authors) decomposed the build into 5 scoped OpenHands headless invocations, not one mega-prompt (matches RUN-02). List them with the real attempt-2 outcomes from RESEARCH §2D:
   - task1-scaffold — project skeleton + calc.fsproj written in (3m 6s, 27 TerminalActions)
   - task2-lexer — Lexer.fsl copied in (16s, 2 TerminalActions)
   - task3-parser — agent writes Parser.fsy (+ Program.fs out-of-scope), self-corrects 4 build errors (1m 17s, 15 TerminalActions)
   - task4-evaluator — confirms/refines Program.fs CLI wiring (45s, 14 TerminalActions)
   - task5-buildtest — final build + test (32s, 9 TerminalActions)
   Use these numbers verbatim from RESEARCH §2D. Note task4 did NOT build a separate evaluator — Program.fs was largely already correct from task3; task4 read the parser interface and wrote a cleaner Program.fs (RESEARCH §8.4, §8.5). Do not call it "the evaluator" without this clarification.

2. **왜 렉서와 프로젝트 파일을 제공했는가 (honest scaffolding rationale)** — The core honesty section. State:
   - calc.fsproj and Lexer.fsl were provided verbatim in task1/task2 prompts.
   - Reason: FsLex .fsl DSL is out-of-distribution for Qwen2.5-35B. In attempt 1, three separate agent invocations (94+27+16 TerminalActions) ALL produced invalid .fsl files (inserting FsYacc's `%%` separator, wrong lexeme-extraction patterns). The model could NOT synthesize a valid FsLex file. (RESEARCH §2A, §7.)
   - Framing: this is a capability boundary, not a failure of the tutorial. The agent's genuine, demonstrable work is the parser grammar, the CLI wiring, and self-correcting F# build errors. Forward-reference (prose only) that '완성된 계산기' has the full honest performance/limits note.
   - Do NOT reproduce "~240s/call" anywhere. Do NOT narrate a precedence bug.

3. **CONCEPT CALLOUT D — plan → write → test → run** — Insert a visually distinct callout box (use a Markdown blockquote `>` with a bold label like `> **개념 ↔ 행동: 계획 → 작성 → 테스트 → 실행**`). Tie the 5-task decomposition to the ch01 methodology using ch01's exact vocabulary (탐색 Explore / 분석 Analyze / 구현 Implement / 검증 Verify). Use the suggested text from RESEARCH §4 Callout D: task1(탐색·스캐폴딩)=Explore, task3(파서 작성)=Implement, task5(빌드+테스트)=Verify; task3 내부의 4회 빌드 실패=Verify→Implement 자가 수정 사이클. The callout must name the ch01 term "plan → write → test → run" / "Explore → Analyze → Implement → Verify" so a reader can trace it back to ch01.

Prose-only forward references; NO Markdown links to unwritten 4부/5부 files.
  </action>
  <verify>Read back. Confirm: (a) all 5 tasks listed with the verbatim durations from RESEARCH §2D (3m 6s / 16s / 1m 17s / 45s / 32s); (b) scaffolding rationale present and names FsLex out-of-distribution + 94+27+16 attempt-1 failures; (c) NO "240s" string anywhere (grep -i "240" returns nothing); (d) one concept callout (blockquote) tied to plan→write→test→run naming ch01's Explore/Implement/Verify; (e) no dead Markdown links.</verify>
  <done>planning.md exists, ≥40 lines, lists the 5-task decomposition with real timings, honestly explains why the lexer/.fsproj were provided (capability boundary, not 240s), and contains Callout D linked to ch01 plan→write→test→run vocabulary.</done>
</task>

</tasks>

<verification>
- Both files exist under src/ch04-calculator/.
- `grep -ri "240" src/ch04-calculator/intro.md src/ch04-calculator/planning.md` returns nothing (no stale perf figure).
- Scaffolding disclosed in BOTH files (Lexer.fsl + calc.fsproj provided, agent wrote Parser.fsy + Program.fs).
- No precedence-bug narration.
- planning.md contains a concept↔action callout tied to ch01 plan→write→test→run.
- No Markdown links to not-yet-created files (mdbook build green is verified in 04-04, but author defensively — prose-only forward refs).
</verification>

<success_criteria>
- WALK-01 satisfied: intro explains tokenize→parse→evaluate without teaching F#.
- Honesty constraint #1 (scaffolding) and #3 (real perf, not 240s) baked into both files.
- One of the ≥3 required callouts (Callout D) lands here, tied to ch01 vocabulary.
- RUN-02 5-task decomposition narrated with real timings.
</success_criteria>

<output>
After completion, create `.planning/phases/04-worked-example-chapter/04-01-SUMMARY.md`
</output>
