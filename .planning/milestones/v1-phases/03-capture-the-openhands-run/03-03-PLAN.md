---
phase: 03-capture-the-openhands-run
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/03-capture-the-openhands-run/captured/logs/
  - .planning/phases/03-capture-the-openhands-run/captured/transcript.md
  - .planning/phases/03-capture-the-openhands-run/captured/test-output.txt
  - .planning/phases/03-capture-the-openhands-run/captured/final-source/
  - .planning/phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
autonomous: true

must_haves:
  truths:
    - "Programmatic verification confirms a genuine error-and-fix cycle exists in the captured JSONL (a failure signal followed by a later success), satisfying RUN-03 from real evidence"
    - "Programmatic + host verification confirms the final calculator evaluates 2+3*4=14, (2+3)*4=20, AND 10-3-2=5"
    - "The captured artifacts are committed to a TRACKED location: per-task JSONL logs, a human-readable transcript, the final source snapshot (.fsl/.fsy/.fs/.fsproj), and the build/test output"
    - "The live working project in oh-workdir/ remains gitignored and is NOT committed"
    - "A capture manifest ties each artifact to the requirement it evidences (RUN-01/02/03 + the 2+3*4=14 criterion) and points at the error-and-fix location for Phase 4"
  artifacts:
    - path: ".planning/phases/03-capture-the-openhands-run/captured/logs/"
      provides: "Copies of the keeper per-task JSONL logs (RUN-01 raw evidence)"
    - path: ".planning/phases/03-capture-the-openhands-run/captured/transcript.md"
      provides: "Human-readable per-task command/output transcript distilled from the JSONL"
    - path: ".planning/phases/03-capture-the-openhands-run/captured/final-source/"
      provides: "Snapshot of the agent's final calc.fsproj, Lexer.fsl, Parser.fsy, Program.fs"
    - path: ".planning/phases/03-capture-the-openhands-run/captured/test-output.txt"
      provides: "The build + three-case run output (14/20/5)"
    - path: ".planning/phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md"
      provides: "Artifact -> requirement map + error-and-fix pointer for Phase 4"
  key_links:
    - from: "captured/logs/*.jsonl"
      to: "captured/transcript.md + test-output.txt"
      via: "JSONL parser distilling commands/outputs"
      pattern: "ObservationEvent|TerminalAction"
    - from: "captured/CAPTURE-MANIFEST.md"
      to: "the error-and-fix event range in the JSONL"
      via: "documented file + event pointer for Phase 4 narration"
      pattern: "error-and-fix|RUN-03"
---

<objective>
Verify the captured run meets all Phase 3 criteria from real evidence, then assemble and commit the
captured artifacts to a tracked location so Phase 4 can write the walkthrough. The live oh-workdir/
project stays gitignored.

Purpose: Turns the raw (gitignored) capture into committed, Phase-4-ready evidence and proves RUN-01,
RUN-02, RUN-03, and the 2+3*4=14 criterion are satisfied honestly.
Output: captured/ directory (logs, transcript, final source snapshot, test output, manifest), committed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-capture-the-openhands-run/03-CONTEXT.md
@.planning/phases/03-capture-the-openhands-run/03-RESEARCH.md
@.planning/phases/03-capture-the-openhands-run/03-02-RUN-NOTES.md
@.planning/phases/03-capture-the-openhands-run/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Programmatically verify the captured run against all Phase 3 criteria</name>
  <files>
    .planning/phases/03-capture-the-openhands-run/captured/test-output.txt
  </files>
  <action>
    Using the research B3/B4/B5 parser patterns (always filter `l.strip().startswith('{')`), run
    these checks over the keeper JSONL logs in oh-workdir/ (identified from 03-02-RUN-NOTES.md):

    1. SCOPED-TASKS (RUN-02): confirm >=5 distinct per-task JSONL logs exist
       (task1-scaffold ... task5-buildtest, + task6-fix if present), each with >=1 TerminalAction.
       Confirm they share the same workdir (observation metadata working_dir under oh-workdir/calc).

    2. ERROR-AND-FIX (RUN-03): scan task5-buildtest.jsonl (and task6-fix.jsonl if present) for a
       genuine failure signal — a TerminalObservation with exit_code != 0 on a build, OR an
       observation whose text shows a wrong result (e.g. 10-3-2 -> 9), OR an AgentErrorEvent —
       FOLLOWED by a later TerminalObservation showing a successful build and the correct outputs.
       Record the file and the approximate event indices of the failure and the fix.
       If NO such cycle is found, STOP and report a gap (do NOT proceed to commit) — RUN-03 unmet.

    3. CALCULATOR CORRECTNESS (the 2+3*4=14 criterion + the other two): do a fresh HOST run as the
       authoritative check and tee it to captured/test-output.txt:
         dotnet build oh-workdir/calc  (capture "Build succeeded")
         dotnet run --project oh-workdir/calc -- "2+3*4"    => expect 14
         dotnet run --project oh-workdir/calc -- "(2+3)*4"  => expect 20
         dotnet run --project oh-workdir/calc -- "10-3-2"   => expect 5
       Assert all three match. If any mismatches, STOP and report (the run is not a keeper).

    Create the captured/ directory tree first. Write captured/test-output.txt with the build line and
    the three labeled case outputs.
  </action>
  <verify>
    captured/test-output.txt shows Build succeeded and exactly 14, 20, 5 for the three expressions.
    The error-and-fix scan prints the file + event range of a real failure->fix (non-empty).
    >=5 per-task JSONL logs confirmed sharing the oh-workdir/calc workdir.
  </verify>
  <done>
    All four Phase 3 success criteria are confirmed from real evidence: log on disk (RUN-01),
    >=5 scoped tasks (RUN-02), a real error-and-fix cycle (RUN-03), and 2+3*4=14 (+20, +5). Any failure
    halts before commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble and commit the captured artifacts (logs, transcript, final source, manifest)</name>
  <files>
    .planning/phases/03-capture-the-openhands-run/captured/logs/
    .planning/phases/03-capture-the-openhands-run/captured/transcript.md
    .planning/phases/03-capture-the-openhands-run/captured/final-source/
    .planning/phases/03-capture-the-openhands-run/captured/CAPTURE-MANIFEST.md
  </files>
  <action>
    Assemble the committed capture under captured/ (a TRACKED path; oh-workdir/ stays gitignored):

    1. captured/logs/: copy the keeper per-task JSONL logs from oh-workdir/ (task1-scaffold.jsonl
       ... task5-buildtest.jsonl, + task6-fix.jsonl if it exists). These are the raw RUN-01 evidence.

    2. captured/transcript.md: a human-readable, per-task transcript distilled from the JSONL with a
       parser (filter non-JSON lines). For each task, in order, render: a heading (task name + log
       filename + completion signal), then each agent step as: the command (action.command), a
       trimmed output (observation.content[].text, cap long outputs), and exit_code. Clearly mark the
       error-and-fix moment (the failing build/test and the subsequent correction). Keep it faithful —
       no editorializing, no idealizing; this is the honest record Phase 4 narrates from.

    3. captured/final-source/: snapshot the agent's FINAL source files from oh-workdir/calc:
       calc.fsproj, Lexer.fsl, Parser.fsy, Program.fs. Copy them verbatim (do NOT hand-edit). This is
       WALK-03's source-of-truth and proves the final Parser.fsy contains the precedence fix the agent
       arrived at.

    4. captured/CAPTURE-MANIFEST.md: map each artifact to the requirement it evidences:
         - RUN-01 -> captured/logs/*.jsonl (real captured commands+output)
         - RUN-02 -> the >=5 per-task logs (scaffold->lexer->parser->evaluator->build&test)
         - RUN-03 -> the error-and-fix cycle: name the file + event range + a 1-2 sentence honest
           description of the real error and the real fix (for Phase 4 narration)
         - 2+3*4=14 criterion -> captured/test-output.txt (with 20 and 5 too)
       Also note: which logs were keepers, total run attempts, any prompt adjustments (cross-ref
       03-02-RUN-NOTES.md), and that oh-workdir/ is the live (gitignored) project.

    Confirm oh-workdir/ stays untracked: `git -C /Users/ohama/projs/OpenHandsTests check-ignore oh-workdir`
    should report it ignored. The committed artifacts live ONLY under the phase's captured/ dir.
  </action>
  <verify>
    captured/ contains logs/ (>=5 .jsonl), transcript.md (non-empty, per-task, marks the error-and-fix),
    final-source/ (calc.fsproj + Lexer.fsl + Parser.fsy + Program.fs), test-output.txt (14/20/5), and
    CAPTURE-MANIFEST.md (maps RUN-01/02/03 + the 2+3*4=14 criterion to artifacts).
    `git check-ignore oh-workdir` confirms oh-workdir/ is ignored (not committed).
    final-source/Parser.fsy contains the precedence declarations the agent's final version has
    (evidence the bug was fixed).
  </verify>
  <done>
    Captured artifacts (logs + transcript + final source snapshot + test output + manifest) are
    assembled in the tracked captured/ dir and committed; oh-workdir/ remains gitignored; the manifest
    ties every artifact to RUN-01/02/03 and the 2+3*4=14 criterion for Phase 4.
  </done>
</task>

</tasks>

<verification>
- captured/ holds: logs/ (the keeper JSONL), transcript.md, final-source/ (4 files), test-output.txt,
  CAPTURE-MANIFEST.md.
- Programmatic checks confirm: >=5 scoped task logs sharing oh-workdir/calc (RUN-02); a genuine
  error-and-fix cycle locatable in the JSONL (RUN-03); the calculator prints 14, 20, 5 (the
  2+3*4=14 criterion + grouping + associativity), all from real evidence (RUN-01).
- oh-workdir/ confirmed gitignored; nothing from it committed except via the captured/ snapshot.
- CAPTURE-MANIFEST.md maps each requirement to its evidencing artifact and points Phase 4 at the
  error-and-fix location.
</verification>

<success_criteria>
The captured run is verified against all four Phase 3 success criteria from real evidence, and the
Phase-4-ready artifacts (logs, human-readable transcript, final source snapshot, build/test output,
manifest) are committed to the tracked captured/ dir while the live oh-workdir/ project stays
gitignored. RUN-01, RUN-02, RUN-03, and 2+3*4=14 are all satisfied honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-capture-the-openhands-run/03-03-SUMMARY.md`
</output>
