---
phase: 09-6부-chapter-publish
plan: 09-02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: true
must_haves:
  truths:
    - "mdbook build completes with zero errors and exit code 0"
    - "Zero NEW warnings appear; the only acceptable warning is the pre-existing 부록 C <char> warning"
    - "6부 renders to HTML and is reachable from the sidebar nav; all 6부 links resolve"
    - "1부–5부 + 부록 A/B/C still render correctly and are not regressed"
  artifacts:
    - path: book/ch06-rust-server/intro.html
      provides: "Rendered 6부 intro (proof the chapter built)"
    - path: book/ch06-rust-server/build-test.html
      provides: "Rendered 6부 build-test section"
  key_links:
    - from: src/SUMMARY.md
      to: book/ch06-rust-server/*.html
      via: "mdbook build resolves all 6부 entries with no broken-link warnings"
  key_links_note: "This plan modifies NO source files. If the build errors or emits a NEW warning, report it so 09-01 can be corrected — do not silently edit chapter content here."
---

<objective>
Run mdbook build and confirm zero errors and zero NEW warnings, that 6부 renders and links resolve, and that 1부–5부 + 부록 A/B/C are not regressed.

Purpose: Satisfies PUB-01 — the build gate that must pass before publishing.
Output: A verified-clean local build under book/, with 6부 HTML present.
</objective>

<context>
@.planning/ROADMAP.md
@src/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the book and confirm zero errors</name>
  <files></files>
  <action>Run `mdbook build` from /Users/ohama/projs/OpenHandsTests/ and capture full stdout+stderr. Confirm exit code 0 and zero errors. If `mdbook` is not on PATH, locate it the same way prior phases did (it was available and used in earlier phases) before failing.</action>
  <verify>mdbook build exits 0 with zero errors.</verify>
  <done>Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm zero NEW warnings</name>
  <files></files>
  <action>Inspect the captured build output for warnings. Confirm the ONLY warning present is the pre-existing 부록 C &lt;char&gt; warning (from src/appendix-c-comparison.md). Assert there are zero NEW warnings — in particular, no broken-link or missing-file warnings for the new 6부 entries added to SUMMARY.md. If any new warning appears, report it as a failure (do not edit chapter content in this plan).</action>
  <verify>Only the pre-existing &lt;char&gt; warning appears; no new warnings.</verify>
  <done>No new warnings introduced by 6부.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 6부 rendered and existing parts not regressed</name>
  <files></files>
  <action>Confirm book/ch06-rust-server/ contains intro.html, planning.html, writing.html, build-test.html, and final.html, and that the 6부 entries appear in the generated sidebar nav (e.g., grep the 6부 title / ch06-rust-server links in a generated HTML page). Spot-check that 1부–5부 and 부록 A/B/C HTML still exist and render (not regressed).</action>
  <verify>book/ch06-rust-server/*.html exist; 6부 present in sidebar; existing parts/appendices still rendered.</verify>
  <done>6부 reachable in nav; no regression to existing content.</done>
</task>

</tasks>

<verification>
- mdbook build exits 0 with zero errors.
- Only the pre-existing 부록 C <char> warning present; zero NEW warnings.
- book/ch06-rust-server/ HTML generated for all five sections; 6부 reachable in sidebar.
- 1부–5부 + 부록 A/B/C still render (no regression).
</verification>

<success_criteria>
PUB-01 satisfied: clean build (zero errors, zero new warnings), all 6부 links resolve, existing parts not regressed.
</success_criteria>

<output>
After completion, create .planning/phases/09-6부-chapter-publish/09-02-SUMMARY.md
</output>

## Reviewer Notes

In plain language: this plan touches no content — it only proves the book still builds cleanly after 6부 was added. A human reviewer should confirm `mdbook build` finished with no errors and that the only warning is the long-standing cosmetic `<char>` warning from 부록 C; any brand-new warning (especially a "broken link" or "missing file" for one of the new 6부 pages) means the SUMMARY wiring or a filename is wrong and must go back to plan 09-01. The honesty rule here is procedural: if the build is not actually clean, the executor must report that failure rather than quietly editing chapter files to make it pass.
