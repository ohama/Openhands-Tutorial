---
phase: 09-6부-chapter-publish
plan: 09-03
type: execute
wave: 3
depends_on: [09-02]
files_modified: []
autonomous: true
must_haves:
  truths:
    - "6부 + SUMMARY.md change committed and pushed to main"
    - "The GitHub Actions deploy workflow fires on the push and completes successfully"
    - "The live URL https://ohama.github.io/Openhands-Tutorial/ returns HTTP 200"
    - "6부 chapters are reachable from the sidebar nav on the live site"
    - ".github/workflows/deploy.yml is unchanged (verifiable by git diff)"
  artifacts: []
  key_links:
    - from: "push to main"
      to: "GitHub Actions deploy.yml → GitHub Pages"
      via: "push trigger runs mdbook build and deploys book/ to Pages"
  key_links_note: ".github/workflows/deploy.yml MUST NOT be modified. Do not edit any chapter content in this plan (build already verified clean in 09-02)."
---

<objective>
Commit and push the 6부 chapter + SUMMARY.md change to main, confirm the GitHub Actions deploy fires and succeeds, and verify the live site serves 6부 from the sidebar — all without modifying the deploy workflow.

Purpose: Satisfies PUB-02 — the updated book is live on GitHub Pages.
Output: 6부 live at https://ohama.github.io/Openhands-Tutorial/.
</objective>

<context>
@.planning/ROADMAP.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify deploy.yml unchanged, then commit and push to main</name>
  <files></files>
  <action>First run `git diff -- .github/workflows/deploy.yml` and confirm it shows NO changes (the workflow must stay untouched — PUB-02). Then stage the new src/ch06-rust-server/*.md files and the src/SUMMARY.md change, commit with a descriptive message (e.g., "feat(09): add 6부 Rust HTTP server chapter from captured evidence"), and push to main. End the commit message with the Co-Authored-By trailer per repo convention.</action>
  <verify>git diff shows deploy.yml unchanged; commit created with only 6부 + SUMMARY changes; push to main succeeds.</verify>
  <done>6부 pushed to main; deploy.yml untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Watch the GitHub Actions deploy run to success</name>
  <files></files>
  <action>Use the gh CLI (e.g., `gh run list` then `gh run watch <id>`) to watch the deploy workflow run triggered by the push until it concludes. Confirm it completes with status success.</action>
  <verify>The deploy workflow run concludes with status success.</verify>
  <done>Deploy run green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 6부 is live</name>
  <files></files>
  <action>After the deploy completes, verify https://ohama.github.io/Openhands-Tutorial/ returns HTTP 200, and that a 6부 page (e.g. ch06-rust-server/intro.html) returns 200 and the 6부 section appears in the live sidebar nav. Allow a brief retry window for Pages CDN propagation before concluding failure.</action>
  <verify>Live root returns 200; ch06-rust-server page returns 200 and 6부 is in the live sidebar.</verify>
  <done>6부 reachable on the live site.</done>
</task>

</tasks>

<verification>
- .github/workflows/deploy.yml unchanged (confirmed by git diff).
- 6부 + SUMMARY.md pushed to main; Actions deploy run succeeded.
- Live URL returns 200; 6부 reachable from sidebar nav.
</verification>

<success_criteria>
PUB-02 satisfied: pushed to main, deploy fired and succeeded, live URL serves 6부 from the sidebar, deploy.yml unchanged.
</success_criteria>

<output>
After completion, create .planning/phases/09-6부-chapter-publish/09-03-SUMMARY.md
</output>

## Reviewer Notes

In plain language: this plan publishes the already-verified book and must not change what gets published. The one hard rule for a reviewer to check is that `.github/workflows/deploy.yml` is byte-for-byte unchanged — `git diff` on it must be empty; the deploy pipeline is trusted infrastructure and altering it would invalidate the PUB-02 guarantee. The commit should contain only the new 6부 chapter files and the SUMMARY.md edit, nothing else. Success is observable, not asserted: the Actions run must go green and the live URL must actually return HTTP 200 with 6부 visible in the sidebar — a reviewer can confirm this by loading the page, not by trusting the log.
