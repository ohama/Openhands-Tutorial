Working directory: /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-c/scala

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

Goal: Build a Scala 3 calculator (Calc.scala) that evaluates an integer arithmetic
expression passed as a command-line argument and prints the integer result. Operators:
+ - * / where * and / bind more tightly than + and -; parentheses override
precedence; integer arithmetic only. Use ONLY the Scala standard library — no
external dependencies.

Constraints:
- scala-cli 1.14.0 + Scala 3 is available on this host (verified).
- All file creation and editing must use bash commands only (heredoc/printf/tee).
- The calculator must read exactly one command-line argument (the expression string).
- Use only the Scala standard library — no external libraries, no build.sbt,
  no scala-parser-combinators, no fastparse.

Canonical tests (must pass):
  scala-cli run Calc.scala -- "2+3*4"    → 14   (precedence: * binds tighter than +)
  scala-cli run Calc.scala -- "(2+3)*4"  → 20   (parentheses override)
  scala-cli run Calc.scala -- "10-3-2"   → 5    (left-associative subtraction)

Run everything in this one session. Do not stop until all three canonical tests pass
or you have documented why they cannot be fixed. Report the EXACT output and exit
code for each test; an honest failure is more valuable than a fabricated success.

Complete the following numbered steps in order, in this one session:

Step 1 — Scaffold:
Confirm scala-cli is available (`scala-cli version`). You will write a single file,
`Calc.scala`, using ONLY the Scala standard library — no build.sbt, no external parser
libraries (no scala-parser-combinators, no fastparse). All file creation via bash
heredoc/printf only.

Step 2 — Write Calc.scala:
Write `Calc.scala` (bash only) implementing a hand-rolled recursive-descent integer
calculator. You write ALL the code yourself — the notes below are requirements and known
failure modes, NOT source to copy.

  Requirements:
  - An `@main` entry point that reads exactly ONE command-line argument (the expression
    string) and prints the integer result.
  - A recursive-descent parser structured in precedence TIERS: an expression level for
    + and -, a term level for * and /, and a factor level for integer literals and
    parenthesized expressions. * and / must bind tighter than + and -; parentheses
    override precedence.

  Known failure modes to avoid:
  - LEFT-associativity: implement the +/- and */ levels with an ITERATIVE left-fold loop
    (accumulate left to right: acc = acc OP rhs), NOT right recursion. Right recursion
    makes 10-3-2 evaluate to 9 instead of the correct 5.
  - Multi-digit integers: your tokenizer must accumulate consecutive digits (so "10" is
    one number, not 1 and 0).
  - Scope trap: if you put parser state/helpers inside a class, keep them private and call
    them only from inside that class; reaching an instance-private member from `@main`
    (outside the instance) is a compile error.

After writing, show the file (`cat Calc.scala`).

Step 3 — Build and test:
Run the three canonical tests and report the EXACT output and exit code for each. If a
compile error occurs, read it, fix it (bash only — NOT file_editor), and re-run:
  scala-cli run Calc.scala -- "2+3*4"     (expect 14)
  scala-cli run Calc.scala -- "(2+3)*4"   (expect 20)
  scala-cli run Calc.scala -- "10-3-2"    (expect 5)
Success criterion: all three print 14 / 20 / 5 respectively. If they cannot be made to
pass, state that clearly — an honest failure is more valuable than a fabricated success.
