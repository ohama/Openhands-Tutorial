---
phase: 07-comparison-chapter-publish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "src/SUMMARY.md references the new chapter via `[부록 C: 모델 비교 — 35B vs 122B](appendix-c-comparison.md)` immediately after 부록 B."
    - "The new entry uses the same flat-bullet appendix style as 부록 A and 부록 B (no leading `-`, sits under the `---` separator)."
    - "`mdbook build` completes with exit code 0 and no ERROR-level output (PUB-01)."
    - "`book/appendix-c-comparison.html` is produced in the build output."
    - "Pre-existing chapter outputs (about.html, ch01-*, ch02-*, ch03-*, ch04-*, ch05-*, appendix-a-repro.html, appendix-b-troubleshooting.html) remain present — no regression."
    - "The new chapter is reachable via the rendered book navigation (sidebar shows the new entry)."
  artifacts:
    - path: "src/SUMMARY.md"
      provides: "Wiring entry for the new comparison appendix"
      contains: "appendix-c-comparison.md"
    - path: "book/appendix-c-comparison.html"
      provides: "Built HTML for the new chapter"
      min_lines: 50
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/appendix-c-comparison.md"
      via: "mdBook appendix entry: `[부록 C: 모델 비교 — 35B vs 122B](appendix-c-comparison.md)`"
      pattern: "appendix-c-comparison\\.md"
    - from: "book/index.html"
      to: "book/appendix-c-comparison.html"
      via: "rendered sidebar nav entry"
      pattern: "appendix-c-comparison\\.html"
---

<objective>
Wire the new `src/appendix-c-comparison.md` (created by plan 07-01) into `src/SUMMARY.md` as the third appendix entry, then run `mdbook build` to confirm a clean build with no errors and no broken links. Satisfies PUB-01.

Purpose: Without SUMMARY.md wiring, the new chapter is not reachable via book navigation and `mdbook build` does not render it. mdBook uses `create-missing = false` in `book.toml`, so this plan runs AFTER plan 07-01 (file exists first).

Output: Modified `src/SUMMARY.md` with one new appendix entry; clean `mdbook build` producing `book/appendix-c-comparison.html`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-comparison-chapter-publish/07-RESEARCH.md
@.planning/phases/07-comparison-chapter-publish/07-01-SUMMARY.md

# Files this plan touches and depends on
@src/SUMMARY.md
@book.toml
@src/appendix-c-comparison.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 부록 C into SUMMARY.md and verify clean mdbook build</name>
  <files>src/SUMMARY.md</files>
  <action>
**Step 1 — Pre-check:** Verify `src/appendix-c-comparison.md` exists (it should, from plan 07-01). If missing, STOP and report failure — do not edit SUMMARY.md against a missing file (mdBook will fail to build).

```bash
test -f src/appendix-c-comparison.md || { echo "FAIL: appendix-c-comparison.md missing — plan 07-01 incomplete"; exit 1; }
```

**Step 2 — Edit `src/SUMMARY.md`:** The current tail (lines ~37–41) reads:

```
---

[부록 A: 재현 가이드](appendix-a-repro.md)
[부록 B: 트러블슈팅](appendix-b-troubleshooting.md)
```

Append one line after the `부록 B` entry so the tail reads:

```
---

[부록 A: 재현 가이드](appendix-a-repro.md)
[부록 B: 트러블슈팅](appendix-b-troubleshooting.md)
[부록 C: 모델 비교 — 35B vs 122B](appendix-c-comparison.md)
```

Use the Edit tool with `old_string` matching the existing two-appendix block and `new_string` adding the third line. Do NOT change any other lines in SUMMARY.md — preserve 1부–5부 entries exactly.

**Step 3 — Build (delete stale target FIRST to avoid stale-build masking):** Run from project root. We delete the target HTML before invoking `mdbook build` so that a leftover `book/appendix-c-comparison.html` from any earlier attempt cannot mask a current-build failure (heavier `mdbook clean` is acceptable instead, but unnecessary — we only need to invalidate the one target):

```bash
rm -f book/appendix-c-comparison.html
mdbook build 2>&1 | tee /tmp/mdbook-build-07-02.log
```

Inspect the log for any line containing `ERROR` or `error:`. If `mdbook` exits non-zero or reports a missing file / broken link / unknown chapter, STOP and report the error verbatim — do not "fix" by mutating other files. The most likely error class is "Chapter file not found: appendix-c-comparison.md" which would mean Step 1 lied or the path is wrong.

**Step 4 — Spot-check output:**

```bash
test -f book/appendix-c-comparison.html || { echo "FAIL: HTML output missing"; exit 1; }
test -f book/appendix-a-repro.html || { echo "FAIL: regressed appendix A"; exit 1; }
test -f book/appendix-b-troubleshooting.html || { echo "FAIL: regressed appendix B"; exit 1; }
test -f book/index.html || { echo "FAIL: index missing"; exit 1; }
grep -c "appendix-c-comparison" book/index.html || echo "WARN: nav may not link new chapter"
```

The grep should match >= 1 (sidebar nav references the new chapter).

**Style/discipline notes:**
- mdBook treats entries under `---` as flat-list appendixes when written WITHOUT a leading `-`. 부록 A and 부록 B already use this pattern; the new entry must match.
- Title format: `부록 C: 모델 비교 — 35B vs 122B` (uses em-dash `—` like 부록 A/B if any consistency; check existing entries before finalizing the exact punctuation).
- Do NOT add a new `# 6부:` header or any other structural reordering — the comparison is appendix material, not a new part.

**Do NOT in this plan:**
- Do NOT modify `book.toml` (no config change needed).
- Do NOT modify any chapter file in `src/ch0X-*/`.
- Do NOT modify `src/appendix-c-comparison.md` (plan 07-01 owns its content).
- Do NOT commit (commits happen at phase end via the orchestrator workflow, not from within a plan).
  </action>
  <verify>
1. SUMMARY.md has the new entry: `grep -c "appendix-c-comparison.md" src/SUMMARY.md` == 1.
2. SUMMARY.md still has the prior appendix entries (no regression):
   - `grep -c "appendix-a-repro.md" src/SUMMARY.md` == 1
   - `grep -c "appendix-b-troubleshooting.md" src/SUMMARY.md` == 1
3. SUMMARY.md 1부–5부 entries untouched: `grep -cE "^- \\[" src/SUMMARY.md` returns the SAME count as before this plan (count was 17 prior to this plan — 2 in 1부, 5 in 2부, 3 in 3부, 5 in 4부, 2 in 5부). Verify by inspection.
4. Build clean (deterministic — clear target first so a stale file cannot mask a failed current build):
   ```bash
   rm -f book/appendix-c-comparison.html
   mdbook build 2>&1 | tee /tmp/mdbook-verify-07-02.log
   BUILD_RC=$?
   [ "$BUILD_RC" = "0" ] || { echo "FAIL: mdbook build exited $BUILD_RC"; exit 1; }
   grep -cE "^error:|^ERROR|Chapter file not found" /tmp/mdbook-verify-07-02.log
   ```
   The grep must return 0 AND `BUILD_RC` must be 0. After this block completes successfully, explicitly assert the target was produced by the current build: `test -f book/appendix-c-comparison.html && echo EXISTS` should output `EXISTS` (exit code 0 = file exists).
5. New HTML produced by the current build: `test -f book/appendix-c-comparison.html && echo EXISTS` should output `EXISTS` (exit code 0 = file exists).
6. Other appendix HTML preserved: `test -f book/appendix-a-repro.html && test -f book/appendix-b-troubleshooting.html`.
7. Sidebar wiring (nav references new chapter): `grep -c "appendix-c-comparison" book/index.html` >= 1.
  </verify>
  <done>
- `src/SUMMARY.md` contains the new `[부록 C: 모델 비교 — 35B vs 122B](appendix-c-comparison.md)` entry positioned immediately after the 부록 B entry, matching the existing flat-list appendix style (no leading `-`, sits under the `---` separator).
- All previously listed chapters and appendices still appear in `src/SUMMARY.md` unchanged.
- `mdbook build` exits 0 with no ERROR-level output.
- `book/appendix-c-comparison.html` exists.
- `book/index.html` (the sidebar) references `appendix-c-comparison`.
- No files outside `src/SUMMARY.md` were modified by this plan.
  </done>
</task>

</tasks>

<verification>
1. SUMMARY diff is minimal and correct:
   `git diff src/SUMMARY.md` shows exactly one added line containing `appendix-c-comparison.md` — no other changes.
2. Build is fully clean (re-run for certainty):
   `mdbook clean && mdbook build` exits 0; stderr does not contain `ERROR`, `error:`, `not found`, or `broken link`.
3. Full appendix set in HTML:
   `ls book/appendix-*.html` lists exactly `appendix-a-repro.html`, `appendix-b-troubleshooting.html`, `appendix-c-comparison.html`.
4. New chapter is the LAST appendix (matching SUMMARY order):
   `grep -oE "appendix-[abc]-[a-z-]+\\.html" book/index.html | tail -1` returns `appendix-c-comparison.html`.
</verification>

<success_criteria>
- PUB-01 satisfied: book builds clean with new chapter wired into SUMMARY.md, no broken links.
- Phase 7 success criterion #1 satisfied: "Comparison chapter exists as a new src/ file, wired into SUMMARY.md; mdbook build clean."
- Ready for plan 07-03 (push + verify live).
</success_criteria>

<output>
After completion, create `.planning/phases/07-comparison-chapter-publish/07-02-SUMMARY.md` recording:
- Exact diff of `src/SUMMARY.md` (one-line addition).
- `mdbook build` exit code and tail of stdout/stderr.
- Confirmation that `book/appendix-c-comparison.html` exists.
- List of files in `book/` matching `appendix-*.html` (should be 3).
- Confirmation that no other files were modified.
</output>
