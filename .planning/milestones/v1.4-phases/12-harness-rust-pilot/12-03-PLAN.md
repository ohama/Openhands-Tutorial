---
phase: 12-harness-rust-pilot
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/logs/run.jsonl
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/logs/run.jsonl
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/metrics.json
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/metrics.json
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/comparison.json
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/test-output.txt
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/test-output.txt
  - .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-self-plan.md
  - .planning/phases/12-harness-rust-pilot/captured-planning/CAPTURE-MANIFEST.md
autonomous: false

must_haves:
  truths:
    - "The mechanical source=agent honesty gate PASSES on both Rust JSONLs (the initial Arm A source=user plan-delivery MessageEvent is excluded; every ActionEvent has source=agent) (METH-03, success criterion 3)"
    - "metrics_extractor.py parsed both Rust JSONLs and wrote captured-planning/rust/arm-a/metrics.json and arm-b/metrics.json, each with honesty_gate=PASS and canonical_tests.curl_hello recorded PASS or FAIL (not null) (METH-03, success criterion 4)"
    - "A comparison.json exists for Rust (arm-a vs arm-b on the chosen metrics) and the committed JSONLs are copies of the live runs (the live runs stay in gitignored oh-workdir-planning/)"
    - "The Phase 12 CAPTURE-MANIFEST.md documents whether Arm B emitted TaskTrackerObservation events and whether token `usage` is present in the JSONL — resolving both open unknowns (success criterion 5)"
  artifacts:
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/metrics.json"
      provides: "Arm A Rust per-run metrics with honesty_gate=PASS and curl_hello pass/fail"
      contains: "honesty_gate"
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/metrics.json"
      provides: "Arm B Rust per-run metrics with honesty_gate=PASS and curl_hello pass/fail"
      contains: "honesty_gate"
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/rust/comparison.json"
      provides: "Arm A vs Arm B Rust comparison record"
      contains: "arm_a"
    - path: ".planning/phases/12-harness-rust-pilot/captured-planning/CAPTURE-MANIFEST.md"
      provides: "Phase 12 pilot manifest: study design, version block, run conditions/order, honesty-gate results, and the two resolved open unknowns"
      min_lines: 40
  key_links:
    - from: "metrics_extractor.py"
      to: "captured-planning/rust/arm-{a,b}/metrics.json"
      via: "python3 metrics_extractor.py --jsonl ... --example rust --arm ... --out ..."
      pattern: "curl_hello"
    - from: "CAPTURE-MANIFEST.md"
      to: "open unknowns (TaskTrackerObservation, usage)"
      via: "documented resolution closing the pilot gate"
      pattern: "TaskTracker"
---

<objective>
Close the Phase 12 pilot capture gate: run the mechanical source=agent honesty gate over both Rust JSONLs, run metrics_extractor.py on each to produce metrics.json, build the Rust comparison.json, extract the Arm B self-plan artifact, do fresh host re-runs of the canonical curl test, write the Phase 12 CAPTURE-MANIFEST.md (with the two open unknowns resolved), copy the live JSONLs into the committed tree, and commit the pilot artifacts.

Purpose: METH-03 + the pilot gate. Phase 13 (full study) is BLOCKED until both Rust metrics.json are committed with honesty_gate=PASS and curl_hello recorded (PASS or FAIL, not null), and the manifest resolves both open unknowns. This plan has a human-verify checkpoint because committing the pilot artifacts is the gate that authorizes the rest of the study.
Output: committed captured-planning/rust/ tree (logs, metrics.json x2, comparison.json, test-output x2, oh-self-plan.md) + captured-planning/CAPTURE-MANIFEST.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/12-harness-rust-pilot/12-01-SUMMARY.md
@.planning/phases/12-harness-rust-pilot/12-02-SUMMARY.md
@.planning/phases/12-harness-rust-pilot/12-02-RUN-NOTES.md

# Manifest structure precedent (v1.2 Rust):
@.planning/milestones/v1.2-phases/08-capture-the-35b-rust-http-server-run/captured-rust/CAPTURE-MANIFEST.md
</context>

<honesty_constraints>
- The honesty gate is BLOCKING. If ANY ActionEvent (excluding the initial source=user plan-delivery MessageEvent in Arm A) has source != agent, HALT — do not write metrics.json, do not write the manifest, do not commit. Report the offending 1-based event indices. A failed gate means the capture is not honest (Pitfall 13).
- All numbers in metrics.json and the manifest come from the real JSONLs via metrics_extractor.py — NO hand-edited metrics, NO fabricated values. A FAILED canonical test (curl != hello) is recorded honestly as FAIL.
- Manifest event citations are 1-based (JSONL line N = event #N). Add the convention header (Pitfall 8/11).
- Per-call timing in the manifest is derived from JSONL LLM-call gaps and labeled derived. The `~14–32s/call` v1 prediction must NOT appear as a measurement (Pitfall 12).
- The fresh host re-run (test-output.txt) re-runs the AGENT-WRITTEN source from the workspace; do NOT hand-edit that source to make it pass.
- Do NOT modify .github/workflows/deploy.yml.
</honesty_constraints>

<tasks>

<task type="auto">
  <name>Task 1: BLOCKING honesty gate + run metrics_extractor.py for both arms + comparison.json</name>
  <files>.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/logs/run.jsonl, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/logs/run.jsonl, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/metrics.json, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/metrics.json, .planning/phases/12-harness-rust-pilot/captured-planning/rust/comparison.json, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-self-plan.md</files>
  <action>
STEP A — BLOCKING source=agent honesty gate over BOTH Rust JSONLs (reuse the v1.3 gate; excludes MessageEvents, only checks ActionEvents):
  python3 -c "
import json, sys
bad=[]
for arm in ['arm-a','arm-b']:
    f=f'/Users/ohama/projs/OpenHandsTests/oh-workdir-planning/{arm}/rust-run.jsonl'
    for ln,l in enumerate(open(f),1):  # 1-based
        if not l.strip().startswith('{'): continue
        e=json.loads(l)
        if e.get('kind')=='ActionEvent' and e.get('source')!='agent':
            bad.append((arm,ln,e.get('source'),str((e.get('action') or {}).get('command',''))[:60]))
if bad:
    print('HONESTY GATE FAILED — ActionEvents with source != agent:')
    for b in bad: print(' ',b)
    sys.exit(2)
print('HONESTY GATE PASS — every ActionEvent has source=agent across both Rust JSONLs')
"
  echo "gate exit=$?"
  If exit != 0: HALT. Do not proceed. Report offenders. (The Arm A initial source=user MessageEvent is a MessageEvent, not an ActionEvent, so it is correctly NOT flagged.)

STEP B — Copy the live JSONLs into the committed tree (the live runs stay in gitignored oh-workdir-planning/):
  cp /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust-run.jsonl /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/logs/run.jsonl
  cp /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-b/rust-run.jsonl /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/logs/run.jsonl

STEP C — Run metrics_extractor.py for each arm against the COMMITTED-tree JSONL copies:
  cd /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot
  python3 metrics_extractor.py --jsonl captured-planning/rust/arm-a/logs/run.jsonl --example rust --arm arm-a --prompt-file captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt --out captured-planning/rust/arm-a/metrics.json
  python3 metrics_extractor.py --jsonl captured-planning/rust/arm-b/logs/run.jsonl --example rust --arm arm-b --prompt-file captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt --out captured-planning/rust/arm-b/metrics.json
  Confirm each metrics.json has honesty_gate.result=="PASS" and canonical_tests.curl_hello.pass is true OR false (NOT null). If curl_hello.pass is null, the extractor did not find the curl observation — investigate the JSONL (the agent may have used a different curl form); adjust ONLY the extractor's curl detection (not the data) and re-run. A genuine FAIL (curl_hello.pass==false) is acceptable and recorded as-is.

STEP D — Extract the Arm B self-plan artifact (PCAP-02 precursor). From the Arm B JSONL: if TaskTrackerObservation events exist (per 12-02 RUN-NOTES), extract their task list text; otherwise extract the first MessageEvent or first agent ActionEvent with tool_name=="" (its thought/text). Save verbatim to captured-planning/rust/arm-b/planning-artifact/oh-self-plan.md with a one-line header noting the source (TaskTracker vs first-agent-message fallback).

STEP E — Build comparison.json (research §Comparison Table Schema). Read both metrics.json, write captured-planning/rust/comparison.json with arm_a / arm_b blocks (terminal_actions, wall_clock_seconds, avg_llm_call_gap_seconds, error_fix_cycle_count, canonical curl_hello pass, honesty_gate) and a deltas block (a_minus_b values + interpretation "arm-a faster"/"arm-b faster"/"indeterminate"). All values pulled from the metrics.json files — no recomputation by hand.
  </action>
  <verify>cd /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot && python3 -c "import json; a=json.load(open('captured-planning/rust/arm-a/metrics.json')); b=json.load(open('captured-planning/rust/arm-b/metrics.json')); assert a['honesty_gate']['result']=='PASS' and b['honesty_gate']['result']=='PASS', 'honesty gate must be PASS'; assert a['canonical_tests']['curl_hello']['pass'] in (True,False), 'arm-a curl_hello must be PASS/FAIL not null'; assert b['canonical_tests']['curl_hello']['pass'] in (True,False), 'arm-b curl_hello must be PASS/FAIL not null'; json.load(open('captured-planning/rust/comparison.json')); print('METRICS + COMPARISON OK')"</verify>
  <done>Honesty gate PASS on both Rust JSONLs; both metrics.json written with honesty_gate=PASS and curl_hello recorded PASS/FAIL (not null); comparison.json written from the metrics files; oh-self-plan.md extracted; JSONLs copied into the committed tree.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh host re-run + write Phase 12 CAPTURE-MANIFEST.md</name>
  <files>.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/test-output.txt, .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/test-output.txt, .planning/phases/12-harness-rust-pilot/captured-planning/CAPTURE-MANIFEST.md</files>
  <action>
STEP A — Copy each arm's AGENT-WRITTEN final source into the committed final-source/ tree (no edits):
  cp -R /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-a/rust/. /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/final-source/ 2>/dev/null || true
  cp -R /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/arm-b/rust/. /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/final-source/ 2>/dev/null || true
  (Exclude the Cargo target/ build dir if large: rm -rf .../final-source/rust-server/target or final-source/target as applicable.)

STEP B — Fresh host re-run of the canonical curl test for each arm's source (re-run, do NOT hand-edit). For each arm where the source builds:
  cd into the arm's final-source rust project; cargo build 2>&1; cargo run & ; sleep 2; curl -s http://localhost:8080/ ; echo "exit=$?"; kill %1
  Capture the FULL output (build result + curl output + exit code) into the arm's test-output.txt. If the agent's source does not build or curl fails, record that honestly in test-output.txt (a FAIL re-run is valid data).

STEP C — Write captured-planning/CAPTURE-MANIFEST.md (Phase 12 pilot manifest). Model the structure on the v1.2 captured-rust/CAPTURE-MANIFEST.md. Required sections:
  - Header + 1-BASED EVENT-NUMBERING CONVENTION note ("Event numbers in this manifest are 1-based: JSONL line N = event #N").
  - Study design: the v1.4 A/B (Arm A = expert/Claude-authored plan embedded; Arm B = 35B self-plans via task tracker); FRAMING RULE stated correctly ("does an expert-authored plan help the 35B execute?" — NOT "Claude plans better"); single-session, one invocation per arm; n for this pilot (n=1 single-run observation — label it).
  - Version block: openhands --version, model (openai/qwen-35b), LLM_BASE_URL, capture date. (Run `openhands --version` to fill it.)
  - Run conditions: per-arm workspace path, pre-run workspace-empty verification, run ORDER + proxy-state strategy (from 12-02 RUN-NOTES) with clock times; arm isolation confirmation.
  - Honesty-gate result: PASS on both JSONLs (cite total ActionEvents checked per arm; note the Arm A source=user MessageEvent exclusion).
  - Per-arm summary table from the metrics.json: TerminalAction count, total events, wall-clock, derived avg LLM-call gap (labeled "derived from JSONL gaps, not the v1 ~14–32s/call prediction"), error-fix cycles, AgentErrorEvent count, curl_hello PASS/FAIL.
  - RESOLVED OPEN UNKNOWN #1: did Arm B emit TaskTrackerObservation events? (count + first 1-based index, or "no — fell back to first agent message"; note any prompt re-pilot).
  - RESOLVED OPEN UNKNOWN #2: is token `usage` present in the JSONL? YES/NO + sample, and the P3-feasible/not-feasible decision.
  - Gate status line: "PHASE 12 PILOT CAPTURE GATE: CLOSED" (only if both metrics.json have honesty_gate=PASS and curl_hello not null).
  All numbers cited from metrics.json / comparison.json / RUN-NOTES — no fabrication.
  </action>
  <verify>cd /Users/ohama/projs/OpenHandsTests/.planning/phases/12-harness-rust-pilot && test -s captured-planning/CAPTURE-MANIFEST.md && grep -q "1-based" captured-planning/CAPTURE-MANIFEST.md && grep -qi "tasktracker\|task tracker" captured-planning/CAPTURE-MANIFEST.md && grep -qi "usage" captured-planning/CAPTURE-MANIFEST.md && grep -qi "expert-authored plan" captured-planning/CAPTURE-MANIFEST.md && test -s captured-planning/rust/arm-a/test-output.txt && test -s captured-planning/rust/arm-b/test-output.txt && ! grep -q "~14–32s/call" captured-planning/CAPTURE-MANIFEST.md && echo "MANIFEST OK"</verify>
  <done>Both test-output.txt files contain the fresh host re-run (PASS or honest FAIL); CAPTURE-MANIFEST.md exists with the 1-based convention header, framing rule, version block, run conditions/order, honesty-gate PASS, per-arm metrics table (timing labeled derived), both open unknowns resolved, and the gate-status line; `~14–32s/call` does not appear as a measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Phase 12 Rust pilot capture: honesty gate PASS on both arms, both metrics.json (honesty_gate=PASS, curl_hello recorded), comparison.json, the Arm B self-plan, fresh host re-run test-output, and the CAPTURE-MANIFEST.md resolving both open unknowns. This commit is the pilot gate that unblocks Phase 13.</what-built>
  <how-to-verify>
1. Review captured-planning/CAPTURE-MANIFEST.md: confirm the framing rule ("does an expert-authored plan help the 35B execute?"), the resolved open unknowns (TaskTracker emission YES/NO; token usage YES/NO + P3 decision), and the "PHASE 12 PILOT CAPTURE GATE: CLOSED" line.
2. Spot-check one cited 1-based event number against the JSONL line in captured-planning/rust/arm-{a,b}/logs/run.jsonl.
3. Confirm both metrics.json show honesty_gate=PASS and curl_hello pass=true/false (not null) — a FAIL is acceptable and honest.
4. Confirm no run was cherry-picked (RUN-NOTES discloses any re-run with an infrastructure reason).
  </how-to-verify>
  <resume-signal>Type "approved" to commit the pilot artifacts, or describe issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the Rust pilot artifacts (closes the pilot gate)</name>
  <files>.planning/phases/12-harness-rust-pilot/captured-planning/</files>
  <action>
After checkpoint approval, commit the committed-tree artifacts (the live oh-workdir-planning/ scratch is gitignored and NOT committed). On the main branch this is fine — these are planning/capture docs, not app code.
  git -C /Users/ohama/projs/OpenHandsTests add .gitignore .planning/phases/12-harness-rust-pilot/metrics_extractor.py .planning/phases/12-harness-rust-pilot/task-prompts .planning/phases/12-harness-rust-pilot/captured-planning .planning/phases/12-harness-rust-pilot/12-*-PLAN.md .planning/phases/12-harness-rust-pilot/12-*-SUMMARY.md .planning/phases/12-harness-rust-pilot/12-02-RUN-NOTES.md .planning/ROADMAP.md .planning/STATE.md
  git -C /Users/ohama/projs/OpenHandsTests commit -m "feat(12): close Phase 12 Rust pilot capture gate

- Both arms captured live on 35B Rust HTTP server (single invocation each)
- source=agent honesty gate PASS on both JSONLs
- metrics.json (arm-a + arm-b) + comparison.json written
- CAPTURE-MANIFEST.md resolves both open unknowns (TaskTracker emission; token usage)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
  Verify the live scratch was NOT committed:
  git -C /Users/ohama/projs/OpenHandsTests status --porcelain | grep oh-workdir-planning && echo "ERROR: scratch tracked" || echo "scratch correctly gitignored"
  </action>
  <verify>git -C /Users/ohama/projs/OpenHandsTests log -1 --name-only | grep -q "captured-planning/CAPTURE-MANIFEST.md" && git -C /Users/ohama/projs/OpenHandsTests ls-files --error-unmatch .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/metrics.json >/dev/null 2>&1 && ! git -C /Users/ohama/projs/OpenHandsTests ls-files oh-workdir-planning/ | grep -q . && echo "COMMIT OK"</verify>
  <done>The pilot artifacts (metrics_extractor.py, both prompts + diff, both JSONLs, both metrics.json, comparison.json, oh-self-plan.md, both test-output.txt, CAPTURE-MANIFEST.md) are committed; the gitignored oh-workdir-planning/ scratch is NOT tracked; the Phase 12 pilot capture gate is CLOSED.</done>
</task>

</tasks>

<verification>
- Honesty gate PASS on both Rust JSONLs (every ActionEvent source=agent; Arm A source=user MessageEvent excluded).
- captured-planning/rust/arm-a/metrics.json AND arm-b/metrics.json committed, each honesty_gate=PASS and canonical_tests.curl_hello recorded PASS/FAIL (not null).
- captured-planning/rust/comparison.json committed; both logs/run.jsonl committed; oh-self-plan.md committed; both test-output.txt committed.
- captured-planning/CAPTURE-MANIFEST.md committed with: 1-based convention header, correct framing rule, version block, run conditions/order, honesty-gate PASS, derived-timing label, BOTH open unknowns resolved (TaskTracker emission + token usage/P3 decision), and "PHASE 12 PILOT CAPTURE GATE: CLOSED".
- oh-workdir-planning/ scratch NOT committed (gitignored); `~14–32s/call` not cited as a measurement; deploy.yml untouched; no hand-edited metrics or agent source.
</verification>

<success_criteria>
METH-03 met and the Phase 12 pilot capture gate CLOSED: both Rust metrics.json committed with honesty_gate=PASS and curl_hello recorded, comparison.json + manifest committed, both open unknowns resolved. Phase 13 (full F#/Scala study) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/12-harness-rust-pilot/12-03-SUMMARY.md`.
</output>
