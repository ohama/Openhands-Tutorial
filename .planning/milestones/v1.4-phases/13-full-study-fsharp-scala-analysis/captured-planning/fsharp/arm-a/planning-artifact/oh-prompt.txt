Working directory: __WORKDIR__

IMPORTANT: Create and edit ALL files using ONLY bash shell commands (printf, tee,
or `cat > FILE <<'EOF' ... EOF` with a quoted heredoc). Do NOT use the file_editor
/ str_replace tool — it errors in this setup (it requires a security_risk field
that fails validation).

Goal: Build an F# calculator using FsLex (Lexer.fsl) and FsYacc (Parser.fsy) that
evaluates an integer arithmetic expression passed as a command-line argument and
prints the integer result. Operators: + - * / where * and / bind more tightly than
+ and -; parentheses override precedence; integer arithmetic only.

Constraints:
- Use FsLexYacc 11.3.0 (already in the local NuGet cache — no network needed).
- All file creation and editing must use bash commands only (heredoc/printf/tee).
- The calculator must read exactly one command-line argument (the expression string).

Canonical tests (must pass):
  dotnet run -- "2+3*4"    → 14   (precedence: * binds tighter than +)
  dotnet run -- "(2+3)*4"  → 20   (parentheses override)
  dotnet run -- "10-3-2"   → 5    (left-associative subtraction)

Run everything in this one session. Do not stop until all three canonical tests pass
or you have documented why they cannot be fixed. Report the EXACT output and exit
code for each test; an honest failure is more valuable than a fabricated success.

Complete the following numbered steps in order, in this one session:

Step 1 — Scaffold:
Run `dotnet new console -lang F# -o calc` in the working directory to create the
project skeleton. This creates calc/ with calc.fsproj and Program.fs.
Wire calc/calc.fsproj for FsLexYacc 11.3.0 by rewriting it to include:
- An <FsYacc Include="Parser.fsy"> item with --module Parser in OtherFlags.
- An <FsLex Include="Lexer.fsl"> item with --unicode --module Lexer in OtherFlags.
- A <PackageReference Include="FsLexYacc" Version="11.3.0" /> item.
- A compile order <Compile> group listing: Parser.fsi, Parser.fs, Lexer.fs,
  Program.fs (F# compile order matters; Parser.fsi/.fs must precede Lexer.fs because
  Lexer opens Parser).
- A <Target Name="FixLineDirectives"> that runs after CallFsYacc and CallFsLex and
  strips `# 0` line directives that fsyacc emits and that the .NET 10 F# compiler
  rejects. Use sed to remove those lines from Parser.fs and Lexer.fs.
Create an empty placeholder Parser.fsy: cd calc && touch Parser.fsy
Verify: ls calc/ && cat calc/calc.fsproj
Success criterion: calc/ contains calc.fsproj, Program.fs, Parser.fsy; .fsproj is
wired for FsLexYacc 11.3.0 with the FixLineDirectives target and correct compile
order. Do not build yet.

Step 2 — Write Lexer.fsl:
After Step 1, calc/ exists with the wired .fsproj. Create calc/Lexer.fsl — the FsLex
lexer source — that tokenizes integer arithmetic expressions.
Write a lexer (FsLex syntax, NOT FsYacc syntax — FsLex does NOT use %% section
separators). The lexer must:
- Open the Parser module and FSharp.Text.Lexing.
- Define a lexing rule called `tokenize` that matches: whitespace (skip), integer
  literals (['0'-'9']+, parse to int and emit INT token), + - * / ( ) operator
  characters (each emitting the corresponding token), eof (emit EOF), and unexpected
  characters (failwith an error).
- Use token names: INT (carrying an int), PLUS, MINUS, STAR, SLASH, LPAREN, RPAREN,
  EOF.
After writing, show `cat calc/Lexer.fsl`. Do not build yet.
Success criterion: Lexer.fsl exists with FsLex rule syntax; token names match the
parser.

Step 3 — Write Parser.fsy:
After Step 2, Lexer.fsl exists. Write calc/Parser.fsy — the FsYacc grammar.
The parser must:
- Declare tokens: INT (int), PLUS, MINUS, STAR, SLASH, LPAREN, RPAREN, EOF.
- Define arithmetic grammar; semantic actions compute and return an int directly.
- Entry point `start` of type int: use `%start start` and `%type <int> start`;
  `start` parses an `expr` followed by EOF.
- Enforce: * / bind tighter than + -; all operators left-to-right associative
  (10-3-2 = 5); parentheses override.
After writing, show `cat calc/Parser.fsy`. Do not build yet.
Success criterion: Parser.fsy declares correct tokens, entry point `start`, and
enforces precedence/associativity.

Step 4 — Write Program.fs:
After Step 3, all source files exist. Rewrite calc/Program.fs — the CLI entry point.
Program.fs must:
- Read exactly one command-line argument (the expression string).
- If wrong number of args, print usage to stderr and exit nonzero.
- Open `FSharp.Text.Lexing` (NOT Microsoft.FSharp.Text.Lexing — does not exist in
  .NET 10).
- Create LexBuffer<char> from the argument using LexBuffer<char>.FromString.
- Call `Parser.start Lexer.tokenize lexbuf` to parse and evaluate.
- Print the integer result (only the result) to stdout followed by a newline.
After writing, show `cat calc/Program.fs`. Do not build yet.
Success criterion: Program.fs reads argv.[0], parses with generated modules, prints
the integer result.

Step 5 — Build and test:
After Step 4, calc/ has all four source files. Build and verify all canonical tests.
Build: cd calc && dotnet build 2>&1
If build fails, read the error, fix the relevant source file (bash heredoc/printf/tee
only — NOT file_editor), and rebuild. Repeat until build succeeds. Report each error
and what was changed to fix it.
Run all three canonical tests and report exact outputs + exit codes:
  dotnet run -- "2+3*4"
  dotnet run -- "(2+3)*4"
  dotnet run -- "10-3-2"
Required outputs: 14 / 20 / 5 respectively.
Report the EXACT output and exit code for each test. If a test prints the wrong
number, state that clearly — an honest failure is more valuable than a fabricated
success.
Success criterion: all three canonical tests print the correct integer and exit 0.
