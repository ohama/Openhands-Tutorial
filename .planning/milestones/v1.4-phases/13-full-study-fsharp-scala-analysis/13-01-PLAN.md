---
phase: 13-full-study-fsharp-scala-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-fsharp.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-scala.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-fsharp.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-scala.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/oh-prompt.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-goal-prompt.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/claude-plan.md
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/oh-prompt.txt
  - .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/planning-artifact/oh-goal-prompt.txt
autonomous: true

must_haves:
  truths:
    - "F# has a control block (goal + bash-only constraint + canonical tests 2+3*4=14 / (2+3)*4=20 / 10-3-2=5) shared verbatim by both arms; the only prompt difference is Arm A's numbered plan vs Arm B's task-tracker self-plan instruction (PCAP-01, METH-01 / Pitfall 1)"
    - "Scala has the same symmetric control-block / Arm A / Arm B structure with the same canonical tests"
    - "The F# Arm A claude-plan explicitly covers the fslex/fsyacc invocation/build steps (so Arm A is not blocked by missing build wiring) but embeds NO lexer/parser/.fsproj source — neither arm receives pre-supplied source (FAIRNESS for the OOD case)"
    - "A literal symmetry diff for each language masks only the workdir token + the variable plan/goal block and exits 0, ending PROMPT-DIFF-{lang}.txt with 'CONTROL-BLOCK SYMMETRY: PASS' (METH-01)"
    - "The per-example, per-arm, multi-run directory layout (runs/run-1..run-3) exists empty under captured-planning/ for fsharp and scala, mirroring the Phase-12 Rust layout extended for n=3"
  artifacts:
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-fsharp.txt"
      provides: "Shared F# control block (goal + bash-only IMPORTANT + canonical tests), workdir as __WORKDIR__ token"
      min_lines: 18
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-scala.txt"
      provides: "Shared Scala control block (goal + bash-only IMPORTANT + canonical tests), workdir as __WORKDIR__ token"
      min_lines: 18
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md"
      provides: "Claude's F# task decomposition (scaffold→lexer→parser→evaluator→build/test), fslex/fsyacc steps covered, NO source embedded"
      contains: "fslex"
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/claude-plan.md"
      provides: "Claude's Scala task decomposition (scaffold→write Calc.scala→build/test), NO source embedded"
      min_lines: 20
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-goal-prompt.txt"
      provides: "F# Arm B prompt = control block + the proven Phase-12 task-tracker self-plan instruction"
      min_lines: 18
  key_links:
    - from: "CONTROL-BLOCK-{lang}.txt"
      to: "arm-a/oh-prompt.txt AND arm-b/oh-goal-prompt.txt"
      via: "control block prepended verbatim to both arms; symmetry diff confirms"
      pattern: "CONTROL-BLOCK SYMMETRY: PASS"
---

<objective>
Build the F# and Scala prompt sets for the planning A/B study, reusing the proven Phase-12 symmetry-diff discipline, and scaffold the n=3 multi-run directory layout. Each language gets: a shared CONTROL-BLOCK, an Arm A prompt (control block + Claude's numbered plan), an Arm B prompt (control block + the proven task-tracker self-plan instruction), and a literal symmetry diff that PASSES. No live OpenHands runs in this plan — prep only.

Purpose: PCAP-01 / METH-01. The comparison is only valid if both arms share an identical control block and differ ONLY in the planning input. This plan finalizes the canonical goal wording (which must be frozen before any run) and proves symmetry by literal diff. The F# fairness rule is the load-bearing nuance: neither arm gets pre-supplied source (FsLex/FsYacc is OOD for the 35B; both arms may FAIL — valid data), so Arm A's plan must cover the fslex/fsyacc build invocation without embedding the lexer/parser source.
Output: task-prompts/CONTROL-BLOCK-{fsharp,scala}.txt + PROMPT-DIFF-{fsharp,scala}.txt; per-arm planning-artifact prompts under captured-planning/{fsharp,scala}/; empty runs/ dirs for n=3.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

# REUSE — the proven Phase-12 symmetry artifacts (mirror these exactly):
@.planning/phases/12-harness-rust-pilot/task-prompts/CONTROL-BLOCK-rust.txt
@.planning/phases/12-harness-rust-pilot/task-prompts/PROMPT-DIFF-rust.txt
@.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/claude-plan.md
@.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-a/planning-artifact/oh-prompt.txt
@.planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-b/planning-artifact/oh-goal-prompt.txt

# SOURCE GOALS to mechanically decompose for Arm A (reuse meaning verbatim; adapt for single-session only):
# F# (v1 FsLex/FsYacc calculator capture — scaffold/lexer/parser/evaluator/buildtest task prompts):
@.planning/milestones/v1-phases/03-capture-the-openhands-run/task-prompts/task1-scaffold.txt
# Scala (v1.3 calculator — scaffold/write-calc/buildtest task prompts):
@.planning/milestones/v1.3-phases/10-capture-the-35b-scala-calculator-run/task-prompts-scala/task2-write-calc.txt
</context>

<honesty_constraints>
- The Arm A conversion is MECHANICAL ONLY: each source-task → one numbered single-session step, meaning preserved, NO new planning detail, NO restructuring of subtasks, NO clarifying ambiguities (Pitfall 5 — converter must not become a second planner). Document the conversion rule in claude-plan.md (mirror the Rust claude-plan header).
- F# FAIRNESS (hard): neither arm receives pre-supplied lexer/parser/.fsproj source. Arm A's plan may name the fslex/fsyacc invocation and the .fsproj wiring REQUIRED to build, but must NOT paste the contents of Lexer.fsl / Parser.fsy / Program.fs / the FsLexYacc .fsproj. The 35B must write all source itself in both arms. State this honesty point explicitly inside claude-plan.md.
- Arm B prompt is UNCHANGED from the proven Phase-12 pattern: control block + "Plan your own implementation steps using the task tracker, then execute each step. Do not ask for confirmation between steps." (STATE.md: 35B self-plans via task_tracker at this phrasing — no change needed).
- The canonical goal wording is FROZEN here. Any later change to control-block wording requires re-running both arms (Pitfall: post-hoc goal changes). Do not touch it in 13-02+.
- Do NOT run openhands in this plan. Do NOT modify .github/workflows/deploy.yml. Do NOT touch existing 1부–7부 / 부록 content.
</honesty_constraints>

<canonical_tests_reference>
Both F# and Scala use the SAME canonical test set (already supported by metrics_extractor.py for examples "fsharp" and "scala"):
  - 2+3*4   → 14   (precedence: * binds tighter than +)
  - (2+3)*4 → 20   (parentheses override)
  - 10-3-2  → 5    (left-associative subtraction)

F# canonical run shape: `dotnet run -- "2+3*4"` (or the project's argument convention) prints 14, etc.
Scala canonical run shape: `scala-cli run Calc.scala -- "2+3*4"` prints 14, etc.
State the EXACT run command for each language in its control block so both arms attempt the identical test.
</canonical_tests_reference>

<tasks>

<task type="auto">
  <name>Task 1: Build the F# prompt set (control block + Arm A plan covering fslex/fsyacc + Arm B self-plan) + symmetry diff</name>
  <files>
.planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-fsharp.txt
.planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-fsharp.txt
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/oh-prompt.txt
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-goal-prompt.txt
  </files>
  <action>
STEP A — Write CONTROL-BLOCK-fsharp.txt, mirroring CONTROL-BLOCK-rust.txt structure exactly:
  - Line 1: `Working directory: __WORKDIR__` (token; substituted at run time per arm).
  - The identical bash-only IMPORTANT paragraph (copy verbatim from CONTROL-BLOCK-rust.txt — do NOT use file_editor / str_replace).
  - Goal: build an F# calculator using FsLex (Lexer.fsl) and FsYacc (Parser.fsy) that evaluates an integer arithmetic expression passed as a command-line argument and prints the integer result. Operators + - * / with * / binding tighter than + -; parentheses override; integer arithmetic only.
  - Canonical tests (must pass): give the EXACT run command and expected outputs for all three: `dotnet run -- "2+3*4"` → 14; `dotnet run -- "(2+3)*4"` → 20; `dotnet run -- "10-3-2"` → 5. (Match whatever single argument-passing convention you state — keep it identical for both arms.)
  - The same honest-failure closing paragraph as the Rust control block ("an honest failure is more valuable than a fabricated success"; "Run everything in this one session").
  NOTE: the .NET 10 + FsLexYacc 11.3.0 reality (the FixLineDirectives sed workaround, compile order) is environment knowledge — Arm A's PLAN may reference the need to wire the .fsproj for fslex/fsyacc and may mention the known line-directive workaround at the level of "you may need to strip `# 0` directives fsyacc emits", but must NOT paste the full .fsproj. Decide: keep build-environment hints in the Arm A PLAN only (NOT the shared control block), so symmetry holds. The control block stays language-goal + tests only.

STEP B — Write claude-plan.md = the MECHANICAL decomposition of the v1 F# task prompts (task1-scaffold … task5-buildtest) into single-session numbered steps. Mirror the Rust claude-plan.md header (conversion rule + "no scaffolded source" disclosure). Steps, meaning preserved:
  1. Scaffold: `dotnet new console -lang F# -o calc`; wire calc.fsproj for FsLexYacc 11.3.0 (FsYacc Include="Parser.fsy", FsLex Include="Lexer.fsl", PackageReference FsLexYacc 11.3.0, compile order Parser.fsi→Parser.fs→Lexer.fs→Program.fs, and the FixLineDirectives target to strip fsyacc `# 0` directives). DESCRIBE these as required steps — do NOT paste the .fsproj XML body as ready-to-use source; the 35B writes it.
  2. Write Lexer.fsl (fslex tokens for ints, + - * / parens). DESCRIBE; do not paste lexer source.
  3. Write Parser.fsy (fsyacc grammar with precedence/associativity). DESCRIBE; do not paste parser source.
  4. Write Program.fs (read argv[0], lex+parse, evaluate, print integer). DESCRIBE; do not paste source.
  5. Build and test: `dotnet build` (fix fslex/fsyacc/compile errors via bash-only edits, rebuild until clean), then run the three canonical tests verbatim and report exact outputs + exit codes.
  Add an explicit FAIRNESS note in the file: "No lexer/parser/.fsproj source is embedded; FsLex/FsYacc is out-of-distribution for the 35B and it may FAIL in BOTH arms — that is valid data. The ONLY difference vs Arm B is that this plan supplies the task decomposition and names the fslex/fsyacc build steps."

STEP C — Assemble oh-prompt.txt (Arm A) = CONTROL-BLOCK-fsharp.txt contents, then a literal separator line, then "Complete the following numbered steps in order, in this one session:" followed by the claude-plan steps inlined as numbered items. (Mirror the Rust arm-a/oh-prompt.txt assembly.) Keep within max_message_chars=30000 — verify `wc -c` < 30000.

STEP D — Assemble oh-goal-prompt.txt (Arm B) = CONTROL-BLOCK-fsharp.txt contents, then: "Plan your own implementation steps using the task tracker, then execute each step. Do not ask for confirmation between steps." (verbatim from the proven Phase-12 Arm B pattern).

STEP E — Write PROMPT-DIFF-fsharp.txt (mirror PROMPT-DIFF-rust.txt): document what is masked (workdir token + the variable plan/goal block), the exact sed/diff commands, the diff output, and the result. Run the diff: normalize both prompts (mask `__WORKDIR__`-substituted paths to a token, strip from the "Complete the following numbered steps" line onward in Arm A, strip from the "Plan your own" line onward in Arm B), then `diff`. It MUST exit 0 (no output). End the file with `CONTROL-BLOCK SYMMETRY: PASS`. If the diff is non-empty, fix the control block so both arms share it verbatim, then re-diff.
  </action>
  <verify>
test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/oh-prompt.txt &&
test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/planning-artifact/oh-goal-prompt.txt &&
grep -q "CONTROL-BLOCK SYMMETRY: PASS" .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-fsharp.txt &&
grep -qi "fslex" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md &&
grep -qi "fsyacc" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md &&
test $(wc -c < .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/oh-prompt.txt) -lt 30000 &&
echo "F# PROMPT SET OK" &&
echo "Confirm no embedded source: the following should find NO multi-line lexer/parser bodies" &&
! grep -qi "rule tokenize\|%token\|%left\|let main" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/planning-artifact/claude-plan.md && echo "NO EMBEDDED SOURCE OK"
  </verify>
  <done>CONTROL-BLOCK-fsharp.txt + Arm A oh-prompt.txt + Arm B oh-goal-prompt.txt + claude-plan.md all exist; Arm A plan covers fslex/fsyacc + build wiring but embeds no lexer/parser/.fsproj source; PROMPT-DIFF-fsharp.txt ends with CONTROL-BLOCK SYMMETRY: PASS; Arm A prompt < 30000 chars.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Scala prompt set (control block + Arm A plan + Arm B self-plan) + symmetry diff + scaffold n=3 layout</name>
  <files>
.planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/CONTROL-BLOCK-scala.txt
.planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-scala.txt
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/claude-plan.md
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/oh-prompt.txt
.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/planning-artifact/oh-goal-prompt.txt
  </files>
  <action>
STEP A — Write CONTROL-BLOCK-scala.txt mirroring the Rust control block: `Working directory: __WORKDIR__`; the identical bash-only IMPORTANT paragraph; goal = a Scala 3 calculator (Calc.scala) that evaluates an integer arithmetic expression passed as a command-line argument and prints the integer result (operators + - * / with * / tighter; parentheses override; integer only; std library only, no external deps); canonical tests with the EXACT run command: `scala-cli run Calc.scala -- "2+3*4"` → 14; `-- "(2+3)*4"` → 20; `-- "10-3-2"` → 5; the same honest-failure closing paragraph.

STEP B — Write claude-plan.md = mechanical decomposition of the v1.3 Scala task prompts (task1-scaffold → task2-write-calc → task3-buildtest) into single-session numbered steps, meaning preserved. Mirror the Rust claude-plan header (conversion rule + no-scaffolded-source disclosure):
  1. Scaffold: confirm scala-cli works (`scala-cli --version`); create a calc/ project dir.
  2. Write Calc.scala: parse a single argv expression, respect precedence and parentheses, integer arithmetic, std library only. DESCRIBE the requirements; do NOT paste the parser/evaluator source.
  3. Build and test: run the three canonical tests via `scala-cli run Calc.scala -- "<expr>"`; if a compile error occurs, read it, fix via bash-only edits, re-run; report exact outputs + exit codes.

STEP C — Assemble oh-prompt.txt (Arm A) = control block + "Complete the following numbered steps in order, in this one session:" + the numbered plan steps. Verify `wc -c` < 30000.

STEP D — Assemble oh-goal-prompt.txt (Arm B) = control block + the verbatim Phase-12 task-tracker self-plan instruction.

STEP E — Write PROMPT-DIFF-scala.txt and run the literal symmetry diff (same masking method as Task 1 STEP E). It MUST exit 0. End with `CONTROL-BLOCK SYMMETRY: PASS`.

STEP F — Scaffold the n=3 multi-run directory layout under captured-planning/ for BOTH languages, extending the Phase-12 Rust layout for multiple runs. For each of {fsharp, scala} × {arm-a, arm-b}, create:
  - runs/  (will hold run-1.jsonl … run-3.jsonl — created by 13-02; add a .gitkeep now)
  - final-source/ (.gitkeep — the LAST run's final source, copied in 13-03)
  Also create the planning-artifact/ dirs (already populated by Tasks 1–2 with prompts). Add .gitkeep to any empty dir so the structure commits. (metrics-run-N.json, the aggregated metrics.json, test-output.txt, comparison.json are written by 13-03 — do not create stubs.)
  </action>
  <verify>
test -s .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/oh-prompt.txt &&
grep -q "CONTROL-BLOCK SYMMETRY: PASS" .planning/phases/13-full-study-fsharp-scala-analysis/task-prompts/PROMPT-DIFF-scala.txt &&
grep -q "scala-cli run Calc.scala" .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/planning-artifact/oh-prompt.txt &&
test -d .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-a/runs &&
test -d .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/arm-b/runs &&
test -d .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-a/runs &&
test -d .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/arm-b/runs &&
echo "SCALA PROMPT SET + N=3 LAYOUT OK"
  </verify>
  <done>CONTROL-BLOCK-scala.txt + Arm A/B prompts + claude-plan.md exist; PROMPT-DIFF-scala.txt ends with CONTROL-BLOCK SYMMETRY: PASS; the runs/ + final-source/ n=3 layout exists (with .gitkeep) for fsharp and scala × arm-a and arm-b.</done>
</task>

</tasks>

<verification>
- Both CONTROL-BLOCK-{fsharp,scala}.txt exist, each with the identical bash-only IMPORTANT paragraph and the three canonical tests with an exact run command.
- Both PROMPT-DIFF-{fsharp,scala}.txt end with "CONTROL-BLOCK SYMMETRY: PASS" (literal diff exited 0).
- F# Arm A claude-plan covers fslex/fsyacc invocation + .fsproj build wiring but embeds NO lexer/parser/.fsproj source (neither arm gets pre-supplied source); the fairness note is present.
- Arm B prompts use the verbatim proven Phase-12 task-tracker self-plan instruction (no change).
- Both Arm A oh-prompt.txt files are < 30000 chars (max_message_chars).
- The n=3 directory layout (runs/ + final-source/ + planning-artifact/) exists under captured-planning/{fsharp,scala}/arm-{a,b}/.
- No openhands runs; deploy.yml untouched; existing chapters untouched.
</verification>

<success_criteria>
PCAP-01 / METH-01 prep complete: F# and Scala prompt sets built with frozen canonical goal wording, symmetric control blocks proven by literal diff (PASS), F# Arm A fair (covers fslex/fsyacc, no embedded source), and the n=3 capture layout scaffolded. Ready for 13-02 to run the full live study.
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-study-fsharp-scala-analysis/13-01-SUMMARY.md`.
</output>
