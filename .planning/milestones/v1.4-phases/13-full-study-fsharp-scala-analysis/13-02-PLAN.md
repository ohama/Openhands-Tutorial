---
phase: 13-full-study-fsharp-scala-analysis
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md
autonomous: true

must_haves:
  truths:
    - "F# Arm A and Arm B each ran live on the 35B n=3 (single openhands invocation per run, default CodeActAgent, --headless --json --yolo --override-with-envs), each producing a real non-empty JSONL with ActionEvent + ObservationEvent records (PCAP-01)"
    - "Scala Arm A and Arm B each ran live n=3, each producing a real non-empty JSONL (PCAP-01)"
    - "Rust was topped up to n=3 per arm (2 additional reps per arm beyond the Phase-12 pilot run-1) OR explicitly kept at n=1 with a labelled justification in RUN-NOTES (REPETITION POLICY)"
    - "Run order was COUNTERBALANCED per example and documented with clock times + litellm proxy state; each run's workspace was verified empty immediately before it and not touched by any other run (METH-02 / Pitfalls 3, 6)"
    - "Every run's outcome (canonical tests PASS/FAIL) is read honestly from its JSONL — including honest F# FAILs in either/both arms — and no run was re-run to get a 'better' result (Pitfall 4)"
  artifacts:
    - path: ".planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md"
      provides: "Full live-study log: per language × arm × run-N command, empty-workspace verification, counterbalanced order + proxy state + clock times, settle evidence, and the verbatim canonical-test outcome per run (incl. honest FAILs)"
      min_lines: 80
  key_links:
    - from: "openhands --headless --json invocation (per language × arm × run-N)"
      to: "captured-planning/{lang}/arm-{a,b}/runs/run-N.jsonl"
      via: "tee captures JSONL into the committed runs/ dir; judged from JSONL not exit code"
      pattern: "ActionEvent"
---

<objective>
Run the FULL planning A/B study live on the local 35B: F# both arms n=3, Scala both arms n=3, and top Rust to n=3 per arm (2 more reps each beyond the Phase-12 pilot). One openhands invocation per run, default CodeActAgent, arm-isolated gitignored workspaces, counterbalanced run order. Capture real JSONL per run directly into the committed runs/ tree. Judge every run from its JSONL, never the exit code. This is the slow live part of Phase 13.

Purpose: PCAP-01 — the comparison needs real per-run JSONL for all six (example × arm) cells at n=3 where feasible (35B runs ~1 min → feasible). F# is the OOD case: both arms may FAIL the canonical tests — that is valid, important data and must be captured honestly, never re-run to improve. The first completed run is THE run; re-runs are permitted ONLY for genuine infrastructure failures and must be disclosed.
Output: captured-planning/{fsharp,scala}/arm-{a,b}/runs/run-1.jsonl..run-3.jsonl (+ Rust topped to run-2/run-3), plus 13-02-RUN-NOTES.md. (Metrics, honesty gate, comparison, manifest are 13-03/13-04.)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/13-full-study-fsharp-scala-analysis/13-01-SUMMARY.md

# REUSE the proven Phase-12 live-run mechanics verbatim (background + poll-to-settle, judge from JSONL):
@.planning/phases/12-harness-rust-pilot/12-02-PLAN.md
@.planning/phases/12-harness-rust-pilot/12-02-RUN-NOTES.md
</context>

<honesty_constraints>
- The FIRST completed run for each (example × arm × rep) is THE run for that rep, even if the canonical tests FAIL. A FAILED canonical test is valid data — do NOT re-run to get a "better" result, and do NOT cherry-pick which reps to keep (ALL reps are kept, including failures) (Pitfall 4).
- A re-run of a given rep is permitted ONLY for a genuine INFRASTRUCTURE failure (proxy down, host timeout/crash, empty JSONL) — disclose it in RUN-NOTES with the reason; archive the failed attempt; the re-run takes that rep's slot.
- ZERO manual edits to any agent-written file in any workspace. The agent writes ALL source via its own bash commands. Do not hand-fix F#/Scala/Rust source to make a test pass (this is the explicit no-fake-RUN discipline).
- ONE openhands invocation per run — do NOT split a run into per-task invocations (Anti-Pattern 1).
- Judge every run ONLY from its JSONL (canonical-test ObservationEvent content + exit_code, FinishAction/idle settle) — NEVER from the CLI exit code (it exits 0 regardless).
- Do NOT present `~14–32s/call` as a measurement. Any per-call timing in RUN-NOTES must be derived from JSONL LLM-call gaps and labelled "derived from JSONL"; timing carries a cache/run-order caveat.
- Do NOT modify .github/workflows/deploy.yml; do NOT touch existing chapters.
</honesty_constraints>

<counterbalance_plan>
Run order is counterbalanced per example to mitigate KV-cache prefix warmth (Pitfall 3). Use this documented order (matches STATE.md v1.4 decisions: Arm-B-first for F#, Arm-A-first for Rust, Arm-B-first for Scala). The simplest fully-compliant alternative is to restart the litellm proxy between every arm; if you do that, order ceases to matter — RECORD whichever strategy you use, with clock times.
  - F#:    Arm B first, then Arm A  (per rep, or all B reps then all A reps — document which)
  - Rust:  Arm A first, then Arm B  (only the 2 top-up reps; pilot run-1 already exists)
  - Scala: Arm B first, then Arm A
Within each (example × arm), the n=3 reps are independent fresh invocations in the SAME arm workspace, but the workspace MUST be wiped to empty before each rep (each rep is a clean run). Record the empty-workspace listing before EACH rep.
</counterbalance_plan>

<run_recipe>
For one run of (LANG ∈ {fsharp,scala,rust}, ARM ∈ {arm-a,arm-b}, REP ∈ {1,2,3}):

PROMPT FILE:
  Arm A: captured-planning/<LANG>/arm-a/planning-artifact/oh-prompt.txt
  Arm B: captured-planning/<LANG>/arm-b/planning-artifact/oh-goal-prompt.txt
  (For Rust top-up, reuse the Phase-12 Rust prompts at
   .planning/phases/12-harness-rust-pilot/captured-planning/rust/arm-{a,b}/planning-artifact/.)

WORKSPACE: /Users/ohama/projs/OpenHandsTests/oh-workdir-planning/<ARM>/<LANG>   (gitignored)
  Wipe to empty before each rep:  rm -rf "$WD" && mkdir -p "$WD" && ls -la "$WD"  (paste listing into RUN-NOTES)

INVOKE (background; substitute __WORKDIR__ in the prompt to the actual workspace path):
  OPENHANDS_SUPPRESS_BANNER=1 \
    LLM_MODEL="openai/qwen-35b" \
    LLM_BASE_URL="http://127.0.0.1:4000/v1" \
    LLM_API_KEY="dummy" \
    OPENHANDS_WORK_DIR="$WD" \
    openhands --headless --json --yolo --override-with-envs \
    -t "$(sed "s#__WORKDIR__#$WD#g" <PROMPT FILE>)" \
    2>"captured-planning/<LANG>/arm-<x>/runs/run-<REP>.stderr.log" \
    | tee "captured-planning/<LANG>/arm-<x>/runs/run-<REP>.jsonl"
  (tee directly into the COMMITTED runs/ dir under the phase directory, OR into the gitignored scratch and copy in 13-03 — choose one and state it. Tee-ing into runs/ directly is simplest. Capture the PID.)

POLL to settle (reuse the Phase-12 status-check; run_in_background, poll ~60s, do NOT foreground sleep):
  Settled when FinishAction=True OR ConvError=True OR last_ts has not advanced across 3 consecutive ~60s polls (~3 min sustained idle).
  Budget: Scala/Rust ~5–15 min/run; F# ~10–30 min/run (OOD, more error-fix cycles). HARD ABORT at 60 min wall-clock with 5 min idle: kill PID, record timeout as infrastructure issue, re-run that rep.

RECORD per run in RUN-NOTES: exact command, pre-run empty-workspace listing, run order position + proxy state + clock start/settle times, the final status-check line (kinds, TerminalActions, FinishAction, ConvError, NonZeroExits, last_ts), and the verbatim canonical-test outcomes read from the JSONL (the ObservationEvent(s) whose command contains "2+3*4"/"(2+3)*4"/"10-3-2" for fsharp/scala, or "curl" for rust — paste content + exit_code; state PASS/FAIL per test honestly).
</run_recipe>

<tasks>

<task type="auto">
  <name>Task 1: F# full study — both arms n=3 (Arm B first, counterbalanced)</name>
  <files>.planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md</files>
  <action>
Run F# per the run_recipe and counterbalance_plan: Arm B first, then Arm A; 3 reps per arm = 6 F# runs total. Tee each into captured-planning/fsharp/arm-{a,b}/runs/run-{1,2,3}.jsonl. Wipe the arm workspace to empty before EACH rep and paste the listing. Record the proxy strategy (counterbalance OR proxy-restart) once at the top of the F# section with clock times.

For each run, after settle, read the canonical-test outcomes from the JSONL and record PASS/FAIL per test honestly. EXPECT possible FAILs in either/both arms — FsLex/FsYacc is OOD for the 35B; both arms may fail. Do NOT re-run a rep to improve a FAIL; a FAIL is the captured result for that rep. Only infrastructure failures justify a re-run (disclose).

Write the RUN-NOTES "## F#" section: proxy/order strategy, then per (arm × rep): command, empty-workspace listing, settle evidence + status-check line, and the three canonical-test outcomes verbatim. Summarize the F# pass/fail matrix (arm-a: rep1/2/3; arm-b: rep1/2/3).
  </action>
  <verify>
for arm in arm-a arm-b; do for r in 1 2 3; do f=.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/$arm/runs/run-$r.jsonl; test -s "$f" || { echo "MISSING $f"; exit 1; }; python3 -c "import json,sys; ev=[json.loads(l) for l in open('$f') if l.strip().startswith('{')]; assert any(e.get('kind')=='ActionEvent' for e in ev) and any(e.get('kind')=='ObservationEvent' for e in ev), 'no Action+Obs in $f'; print('$f OK', len(ev),'events')"; done; done && echo "F# 6 RUNS OK"
  </verify>
  <done>captured-planning/fsharp/arm-a/runs/run-1..3.jsonl AND arm-b/runs/run-1..3.jsonl all exist, non-empty, with Action+Observation records; each settled (or disclosed abort); RUN-NOTES F# section records command, empty-workspace verification, counterbalanced order + proxy state, settle evidence, and verbatim canonical-test outcomes (incl. honest FAILs) for all 6 runs.</done>
</task>

<task type="auto">
  <name>Task 2: Scala full study n=3 (Arm B first) + Rust top-up to n=3</name>
  <files>.planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md</files>
  <action>
PART 1 — SCALA: run per run_recipe + counterbalance (Arm B first, then Arm A); 3 reps per arm = 6 Scala runs into captured-planning/scala/arm-{a,b}/runs/run-{1,2,3}.jsonl. Wipe workspace empty before each rep. Record outcomes (these should mostly PASS — Scala is in-distribution — but capture honestly whatever happens). Write the RUN-NOTES "## Scala" section.

PART 2 — RUST TOP-UP: the Phase-12 pilot already has n=1 (run-1) per arm. Top up to n=3 by running 2 MORE reps per arm (Arm A first, then Arm B per counterbalance) using the Phase-12 Rust prompts. Decision: COPY the Phase-12 pilot run-1 JSONL into this phase's tree as run-1 so all three Rust reps live together, OR keep run-1 referenced in Phase 12 and add run-2/run-3 here — choose and document. Recommended: copy the pilot JSONL to captured-planning/rust/arm-{a,b}/runs/run-1.jsonl (labelled "(Phase-12 pilot rep)") and add run-2.jsonl + run-3.jsonl fresh, so Rust is a clean n=3 in this phase's tree. Wipe the Rust workspaces empty before each new rep. Write the RUN-NOTES "## Rust (top-up)" section, noting run-1 provenance and the 2 new reps. (If topping Rust proves infeasible for any infrastructure reason, fall back to keeping Rust at n=1 and LABEL it "(단일 실행)" explicitly in RUN-NOTES — but prefer n=3.)

Close RUN-NOTES with a "## Run-order & proxy summary" table (example × arm × rep → order position, clock time, proxy state) and an overall canonical pass/fail matrix across all examples.
  </action>
  <verify>
for arm in arm-a arm-b; do for r in 1 2 3; do f=.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/$arm/runs/run-$r.jsonl; test -s "$f" || { echo "MISSING $f"; exit 1; }; python3 -c "import json; ev=[json.loads(l) for l in open('$f') if l.strip().startswith('{')]; assert any(e.get('kind')=='ActionEvent' for e in ev), 'no Action in $f'; print('$f OK',len(ev))"; done; done && echo "SCALA 6 RUNS OK" &&
RUSTN=$(ls .planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/arm-a/runs/run-*.jsonl 2>/dev/null | wc -l | tr -d ' ') && echo "Rust arm-a reps: $RUSTN" && test "$RUSTN" -ge 1 && grep -qi "rust" .planning/phases/13-full-study-fsharp-scala-analysis/13-02-RUN-NOTES.md && echo "RUST TOP-UP RECORDED"
  </verify>
  <done>Scala arm-a/arm-b runs/run-1..3.jsonl all exist non-empty with ActionEvents; Rust topped to n=3 per arm (or kept n=1 with explicit "(단일 실행)" label disclosed); RUN-NOTES has Scala + Rust sections, the run-order/proxy summary table, and the overall canonical pass/fail matrix; all runs judged from JSONL, no cherry-picking, no manual source edits.</done>
</task>

</tasks>

<verification>
- F#: captured-planning/fsharp/arm-{a,b}/runs/run-1..3.jsonl (6 files) exist, non-empty, Action+Observation records.
- Scala: captured-planning/scala/arm-{a,b}/runs/run-1..3.jsonl (6 files) exist, non-empty.
- Rust: topped to n=3 per arm in this phase's tree (run-1 provenance documented) OR explicitly kept n=1 and labelled "(단일 실행)".
- RUN-NOTES records per-run command, pre-run empty-workspace listing, counterbalanced run order + proxy state + clock times, settle evidence, and verbatim canonical-test outcomes (incl. honest F# FAILs) for every run; plus a run-order/proxy summary table and the overall pass/fail matrix.
- ONE invocation per run; no per-task splits; no manual edits to agent files; no cherry-picking (all reps kept); outcome judged from JSONL; `~14–32s/call` not cited as a measurement; deploy.yml + chapters untouched.
</verification>

<success_criteria>
PCAP-01 capture complete: all six (example × arm) cells have real per-run JSONL at n=3 where feasible (F# + Scala fresh n=3; Rust topped to n=3 or labelled n=1), captured as single isolated invocations with counterbalanced/disclosed run order. Honest F# failures preserved. Ready for 13-03 to run metrics + honesty gate + build comparisons.
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-study-fsharp-scala-analysis/13-02-SUMMARY.md`.
</output>
