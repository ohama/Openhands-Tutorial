---
phase: 14-appendix-d-chapter-publish
plan: 14-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/appendix-d-planning-comparison.md
autonomous: true
must_haves:
  truths:
    - "src/appendix-d-planning-comparison.md exists as a Korean chapter mirroring 부록 C style (no pictograph emoji)"
    - "The opening paragraph frames the research question as '전문가가 작성한 계획이 35B의 실행에 도움이 되는가?' — NOT 'Claude plans better than the 35B'"
    - "A cross-example P1/P2 metrics table covers all six cells (F#/Rust/Scala × Arm A/Arm B) in median (min–max) format, n=3"
    - "A canonical pass/fail table reports F# 1/3 vs 0/3, Scala 3/3 vs 2/3+1 PARTIAL, Rust 3/3 vs 3/3 — mixed/inconclusive results reported honestly, not cherry-picked"
    - "Per-arm planning-artifact excerpts and a qualitative plan comparison are present for all three examples"
    - "Every cited event number matches CAPTURE-MANIFEST.md's 1-based positions; the string '~14–32s/call' never appears as a measurement; timing values carry the run-order/cache confound caveat"
  artifacts:
    - path: "src/appendix-d-planning-comparison.md"
      provides: "부록 D planning-comparison chapter, fully sourced from committed captured-planning/ artifacts"
      min_lines: 200
      contains: "전문가가 작성한 계획"
  key_links:
    - from: "src/appendix-d-planning-comparison.md (metrics table)"
      to: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/{fsharp,scala,rust}/comparison.json"
      via: "verbatim transcription of median (min–max) P1/P2 values, n=3"
      pattern: "median|min.max|중앙값"
    - from: "src/appendix-d-planning-comparison.md (event citations + framing + caveats)"
      to: ".planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md"
      via: "1-based event numbers, framing rule, timing caveat, pass/fail summary"
      pattern: "#200|#202|#204|#39|#41|#43|#25"
---

<objective>
Write `src/appendix-d-planning-comparison.md` — the new Korean 부록 D "계획 방식 비교: Claude 계획 vs OpenHands 자체 계획" chapter — verbatim from the committed Phase-13 `captured-planning/` artifacts. This plan ONLY authors the chapter file; it does NOT touch SUMMARY.md and does NOT run mdbook build (those are plan 14-02).

Purpose: Satisfies DCHAP-01 and DCHAP-02 — an honest, fully-traceable chapter reporting the A/B planning study (Arm A = Claude-authored plan embedded in prompt; Arm B = 35B self-plans via task tracker) across F#/Rust/Scala on the local 35B.
Output: `src/appendix-d-planning-comparison.md` (Korean prose, 부록 C style, no emoji), all numbers/excerpts/events traceable to committed artifacts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-appendix-d-chapter-publish/14-RESEARCH.md

# Authoritative data sources (read ALL of these in full before writing):
@.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md
@.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/PLAN-COMPARISON-QUALITATIVE.md
@.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/fsharp/comparison.json
@.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/scala/comparison.json
@.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/rust/comparison.json

# Style precedent (match exactly — Korean, no emoji, honesty callouts):
@src/appendix-c-comparison.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all committed artifacts and assemble the verified data set</name>
  <files></files>
  <action>
Read every authoritative source in FULL before writing a single line of the chapter. The chapter is a transcription job — invent nothing.

1. Read `.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/CAPTURE-MANIFEST.md` (300 lines) in full. Extract:
   - The framing rule (§"Study Design", line ~23): "Does an expert-authored plan help the 35B execute?"
   - The EVENT-NUMBERING CONVENTION (§ line ~9): JSONL line N = event #N; first line is event #1. ALL event citations come from here.
   - The Version Block (§ line ~43) and Run Conditions / Counterbalanced Order (§ line ~57) for the 실험 설계 section.
   - The Timing caveat (mandatory) (§ line ~76): timing derived from JSONL timestamps; cache-warmth/run-order confound; F#/Scala Arm B ran first (colder), Arm A second (warmer); Rust top-ups Arm A first; deltas MUST NOT be planning-quality signals; legacy `~14–32s/call` is a v1 PREDICTION not a measurement.
   - The Six-Cell Outcome Table (§ line ~91) and Overall Canonical Pass/Fail Summary (§ line ~185).
   - Planning Artifact Pointers (§ line ~237) and Task tracker confirmation (§ line ~252).
   - The per-rep canonical pass event numbers (the manifest's pass detail): F# Arm A run-1 PASS at events #200/#202/#204; Scala Arm A run-1 at #39/#41/#43; Rust Arm A run-1 at #25; F# Arm B plan = event #4 (TaskTrackerAction); Scala Arm B plan = event #6; Rust Arm B plan = event #4.

2. Read the three `comparison.json` files (fsharp/, scala/, rust/). These are AUTHORITATIVE for all P1/P2 numeric metrics. Each metric has median + min + max + n. Confirm `"n": 3` for all six cells. Transcribe exactly these fields per cell: terminal_actions_count, total_events, wall_clock_seconds, avg_llm_call_gap_seconds, min_llm_call_gap_seconds, max_llm_call_gap_seconds, error_fix_cycle_count, task_tracker_observation_count, task_tracker_action_count. Do NOT include any token/`usage` column — P3 token metrics are unavailable (no `usage` field in JSONL).

3. Read `PLAN-COMPARISON-QUALITATIVE.md` (144 lines) for the ANAL-02 qualitative analysis: per-example plan tables (F# §1, Rust §2, Scala §3) and the cross-example summary (lines ~136–144). This is the source for the 계획 아티팩트 비교 section.

4. Read the six planning-artifact files for verbatim plan excerpts:
   - fsharp/arm-a/planning-artifact/claude-plan.md (5-step Claude plan)
   - fsharp/arm-b/planning-artifact/oh-self-plan.md (5-task self-plan, event #4)
   - scala/arm-a/planning-artifact/claude-plan.md (3-step)
   - scala/arm-b/planning-artifact/oh-self-plan.md (4-task, event #6)
   - rust/arm-a/planning-artifact/claude-plan.md (3-step)
   - rust/arm-b/planning-artifact/oh-self-plan.md (4-task, event #4)

5. Read `src/appendix-c-comparison.md` to internalize the chapter style: Korean prose, blockquote callouts (`> **사용자 프롬프트**` / `> **내부 프로세스**` / `> **결과**`), 출처: lines citing artifact paths, NO pictograph emoji anywhere.

Do NOT write the chapter in this task — only gather and cross-check the data. If any comparison.json value disagrees with the CAPTURE-MANIFEST six-cell table, STOP and flag it (do not silently pick one).
  </action>
  <verify>All six comparison.json files confirmed `"n": 3` for every metric; the manifest six-cell table values match the comparison.json values; all six planning-artifact files read; framing rule, event-numbering convention, and timing caveat located in the manifest.</verify>
  <done>A verified, cross-checked data set is in hand: six-cell P1/P2 table values, canonical pass/fail per example, per-rep pass event numbers (1-based), per-arm plan excerpts, framing rule text, and the mandatory timing caveat. No discrepancies between comparison.json and CAPTURE-MANIFEST.</done>
</task>

<task type="auto">
  <name>Task 2: Write src/appendix-d-planning-comparison.md from the verified data</name>
  <files>src/appendix-d-planning-comparison.md</files>
  <action>
Write the chapter in Korean, mirroring 부록 C's style (no pictograph emoji, honesty-first callouts). Use this section structure (from RESEARCH §"Chapter Structure Recommendation"):

1. **제목 + 연구 질문 (Opening / 연구 질문)**: H1 title "부록 D: 계획 방식 비교 — Claude 계획 vs OpenHands 자체 계획". 1–2 framing paragraphs. The research question MUST be stated as: "이 연구의 핵심 질문은 '전문가가 작성한 계획이 35B의 실행에 도움이 되는가?'이다" (or equivalent). Define Arm A (프롬프트에 Claude가 작성한 번호 매긴 계획을 포함) and Arm B (목표만 주고 task tracker로 스스로 계획하게 함). Explicitly state this is NOT a claim that "Claude가 35B보다 계획을 더 잘 짠다."

2. **실험 설계 (Study design)**: A table — n=3 per cell, counterbalanced run order, single OpenHands invocation per arm, CodeActAgent, local 35B (qwen-35b via litellm proxy). Include the version block facts and run-order from CAPTURE-MANIFEST §"Version Block" + §"Run Conditions". State the prompt-symmetry control (arms share an identical control block; differ only in whether a numbered plan is supplied).

3. **계획 아티팩트 비교 (Plan artifact comparison)**: Per-example subsections (F#, Rust, Scala). For each: a short verbatim excerpt of the Claude plan (Arm A) and the self-plan (Arm B), with task/step counts, and a note on whether each decomposition matches the canonical scaffold→write→build→test shape. Cite the planning-artifact source paths and the Arm B plan event numbers (F# #4, Scala #6, Rust #4). Source these from PLAN-COMPARISON-QUALITATIVE.md + the six planning-artifact files.

4. **지표 비교 테이블 (P1/P2 metrics)**: One cross-example table covering all six cells × all metrics (terminal_actions_count, total_events, wall_clock_seconds, error_fix_cycle_count, avg/min/max_llm_call_gap_seconds, task_tracker_observation_count, task_tracker_action_count). Every value in `median (min–max)` form, labeled n=3. Immediately follow with a MANDATORY timing caveat callout: "타이밍 값은 JSONL 타임스탬프에서 도출됨" + the run-order/cache-warmth confound (F#/Scala Arm B 먼저 실행 = 콜드 캐시; Rust top-up Arm A 먼저). State timing deltas are NOT planning-quality signals. Do NOT add a token column.

5. **정규 테스트 결과 (Canonical pass/fail)**: A table — F#: Arm A 1/3 vs Arm B 0/3; Scala: Arm A 3/3 vs Arm B 2/3 + 1 PARTIAL; Rust: Arm A 3/3 vs Arm B 3/3. Define PARTIAL-PASS inline (Scala Arm B run-3: tests 2+3 ran after a compile fix; test 1 not re-run — disclose, do not report as full PASS). Cite the per-rep pass event numbers (1-based) from the manifest: F# Arm A run-1 #200/#202/#204; Scala Arm A run-1 #39/#41/#43; Rust Arm A run-1 #25. Where you reference a single specific run (e.g., "F# Arm A run-1의 단 한 번의 PASS"), add the `(단일 실행)` hedge.

6. **해석 (Interpretation)**: Honest per-example reading. F# = OOD-dominated, mixed (Arm A 1/3 shows the expert plan CAN help navigate the FsLex/FsYacc OOD task in one run, but 5/6 runs failed overall — do NOT write "Arm A is better"). Scala = both arms broadly succeed; Arm A had fewer error-fix cycles; the wall-clock gap (Arm B ~936s vs Arm A ~345s) is almost entirely the run-order/cache confound, NOT planning quality. Rust = tie (both 3/3, zero error-fix cycles, no meaningful arm difference). Overall: the expert plan helped navigate OOD in one F# run; no observable effect on in-distribution tasks. Report all three patterns; do NOT cherry-pick Scala.

7. **출처 (Sources)**: List every committed captured-planning/ artifact cited (CAPTURE-MANIFEST.md, the three comparison.json, PLAN-COMPARISON-QUALITATIVE.md, the six planning-artifact files), with their `.planning/phases/13-full-study-fsharp-scala-analysis/captured-planning/...` paths.

HONESTY RULES (non-negotiable, all from CAPTURE-MANIFEST + STATE.md):
- The string `~14–32s/call` (or `14-32`, `14–32`, `14.32`) must NEVER appear as a measurement.
- Every event number traces to CAPTURE-MANIFEST's 1-based positions — never RUN-NOTES 0-based scratchpad.
- No pictograph emoji.
- All six cells are n=3 → the table uses median (min–max); the `(단일 실행)` hedge is for prose references to a single specific run, not for the table cells.
- Mixed/inconclusive (F#) is a valid result; PARTIAL-PASS (Scala Arm B run-3) is disclosed, never reported as full PASS.
  </action>
  <verify>`test -f src/appendix-d-planning-comparison.md` and `wc -l` ≥ 200. The file contains all 7 sections, the cross-example P1/P2 table with six cells in median (min–max) form, the canonical pass/fail table, per-arm plan excerpts, and a 출처 section.</verify>
  <done>src/appendix-d-planning-comparison.md is a complete Korean chapter sourced entirely from committed artifacts, in 부록 C style with no emoji, covering framing → design → plan comparison → metrics → canonical results → interpretation → sources.</done>
</task>

<task type="auto">
  <name>Task 3: Run the inline honesty audit on the new chapter</name>
  <files></files>
  <action>
Run the RESEARCH §"Honesty Audit Checklist" against `src/appendix-d-planning-comparison.md`. This is a content audit only — SUMMARY wiring and mdbook build happen in plan 14-02.

1. `~14–32s/call` grep — MUST return no matches:
   `grep -nE "14[.–\-]32|~14|32s/call" src/appendix-d-planning-comparison.md`
   (Also eyeball for "14–32"/"14-32" with the en-dash.) If any match exists as a measurement claim, remove it.

2. Framing-sentence check — confirm the opening states the research question as "전문가가 작성한 계획이 35B의 실행에 도움이 되는가?" and does NOT claim "Claude plans better":
   `grep -n "전문가가 작성한 계획" src/appendix-d-planning-comparison.md` must match; grep for any "더 잘" / "better" claim about Claude planning and remove if present.

3. Event-number trace — every event number cited (`#200 #202 #204 #39 #41 #43 #25 #4 #6`) must match CAPTURE-MANIFEST's 1-based positions. Cross-check each citation against the manifest. If any number was taken from a 0-based source, correct it.

4. Emoji check — confirm no pictograph emoji in the file (the chapter must match 부록 C's no-emoji style).

5. n=3 / median(min–max) check — confirm the cross-example table cells use `median (min–max)` form and are labeled n=3; confirm any single-run prose reference carries `(단일 실행)`.

6. PARTIAL-PASS disclosure — confirm Scala Arm B run-3 is shown as PARTIAL-PASS (not full PASS), with the inline definition.

7. Timing caveat — confirm the metrics table is followed by the run-order/cache confound caveat and the "JSONL 타임스탬프에서 도출됨" label.

Fix any failure in place, then re-run the relevant grep to confirm.
  </action>
  <verify>grep for `~14–32s/call` returns nothing; framing grep matches; all cited event numbers verified against CAPTURE-MANIFEST 1-based positions; no emoji; PARTIAL-PASS disclosed; timing caveat present.</verify>
  <done>All nine honesty-audit items pass on src/appendix-d-planning-comparison.md. The chapter is honest, traceable, and ready to be wired into SUMMARY.md in plan 14-02.</done>
</task>

</tasks>

<verification>
- src/appendix-d-planning-comparison.md exists, ≥200 lines, Korean, no emoji.
- Cross-example P1/P2 table: six cells (F#/Rust/Scala × Arm A/B), median (min–max), n=3, no token column.
- Canonical pass/fail: F# 1/3 vs 0/3, Scala 3/3 vs 2/3+1 PARTIAL, Rust 3/3 vs 3/3 — PARTIAL-PASS disclosed.
- Framing rule present ("전문가가 작성한 계획이 35B의 실행에 도움이 되는가?"); no "Claude plans better" claim.
- Event numbers all 1-based, traceable to CAPTURE-MANIFEST.
- `~14–32s/call` absent; timing carries run-order/cache caveat.
- Every number/excerpt sourced from committed captured-planning/ artifacts; 출처 section lists them.
</verification>

<success_criteria>
DCHAP-01 + DCHAP-02 satisfied at the content level: a fully-traceable, honestly-framed 부록 D chapter file exists, with the cross-example metrics table, canonical results, plan excerpts, qualitative comparison, and honest interpretation — passing all nine honesty-audit checks. SUMMARY.md is untouched and mdbook is not yet built (those are plan 14-02).
</success_criteria>

<output>
After completion, create .planning/phases/14-appendix-d-chapter-publish/14-01-SUMMARY.md
</output>
