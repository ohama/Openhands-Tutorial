# Requirements Archive: v1.2 Rust Example

**Archived:** 2026-06-01
**Status:** ✅ SHIPPED

This is the archived requirements specification for v1.2.
For current requirements, see `.planning/REQUIREMENTS.md` (created for the next milestone via `/gsd:new-milestone`).

---

**Defined:** 2026-05-28
**Milestone:** v1.2 — Rust Example (second worked example)
**Core Value:** A reader watches the same local 35B model that built the F# calculator now build a minimal Rust HTTP server end-to-end — proving that the agentic pattern transfers across languages, with all model boundaries honestly documented.

## v1.2 Requirements

### Rust capture

- [x] **RUST-01**: A real captured 35B OpenHands run of a minimal Rust HTTP server exists on disk (per-task JSONL), with the agent attempting the work **unaided first** — no provided source for any file, including `Cargo.toml` (the agent runs `cargo new` itself). Scaffold fallback may be prepared but should only be invoked if the agent demonstrably cannot proceed, and if invoked it must be disclosed in the capture manifest. — **Complete.** `captured-rust/logs/task1-scaffold.jsonl` event #7 = agent `cargo new rust-server`; no pre-seeded source in git before the run.
- [x] **RUST-02**: The run honestly records whether 35B wrote the Rust source itself; scaffolding disclosed if used; `source=agent` on every ActionEvent; zero manual edits between tasks. — **Complete.** did-write-server-unaided YES (task2 event #13); scaffold-invoked NO; 28 ActionEvents, 0 with source≠agent.
- [x] **RUST-03**: The real outcome of `curl localhost:8080/` (`hello\n` while running from `cargo run`) is captured in a terminal observation; genuine build/borrow-checker errors and self-correction sequences captured (no fabrication). — **Complete.** curl ObservationEvent #38 `hello\nEXIT_CODE=0` exit 0; two build failures (#16 format! syntax, #28 E0382) with full fix sequences traceable; host re-run confirms HTTP/1.1 200 OK.

### 6부 chapter content

- [x] **CHAP-01**: A new 6부 chapter group is added in `src/ch06-rust-server/` and wired into `src/SUMMARY.md`, structurally parallel to 4부, using the 사용자 프롬프트 / 내부 프로세스 / 결과 callout pattern (no pictograph emoji), with concept↔action callouts to 1부/2부 concepts. — **Complete.** Five files (intro/planning/writing/build-test/final), SUMMARY 6부 section after 5부 before 부록, four 개념↔행동 callouts, zero emoji.
- [x] **CHAP-02**: Every code quote, event number, timing figure, error message, and capability claim in 6부 traces verbatim to the captured JSONL / logs / `cargo` output; no fabricated or idealized numbers; unbacked sections omitted. — **Complete (with audit correction).** Code byte-identical to source; errors verbatim. Audit caught one timing figure (v1 ~14–32s/call) presented as a measurement when it was a pre-run prediction; corrected to the derived ~5.3s/call (per 부록 C) before close-out (TD-6).

### Publish

- [x] **PUB-01**: `mdbook build` clean (no errors, no broken links); 6부 in SUMMARY.md; 1부~5부 + 부록 A/B/C not regressed; the pre-existing `<char>` warning acceptable; no NEW warnings. — **Complete.** Build exit 0, only the pre-existing 부록 C `<char>` warning, all parts render.
- [x] **PUB-02**: Re-deployed live to GitHub Pages via the existing Actions workflow on push to main; `.github/workflows/deploy.yml` NOT modified; live URL returns 200; 6부 reachable from sidebar nav. — **Complete.** Actions runs 26731890916 + 26735121737 green; ch06 pages HTTP 200; deploy.yml unchanged (git diff empty).

## Traceability (final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| RUST-01 | Phase 8 | Complete |
| RUST-02 | Phase 8 | Complete |
| RUST-03 | Phase 8 | Complete |
| CHAP-01 | Phase 9 | Complete |
| CHAP-02 | Phase 9 | Complete |
| PUB-01 | Phase 9 | Complete |
| PUB-02 | Phase 9 | Complete |

---

## Milestone Summary

**Shipped:** 7 of 7 v1.2 requirements.
**Adjusted:** CHAP-02 — one timing figure required an audit-stage correction (prediction-as-measurement → derived ~5.3s/call) to honor the "predictions never presented as measurements" rule; corrected before close-out.
**Dropped:** None.

**Deferred to later milestones (carried forward):** EXT-01 (more-language worked examples), EXT-02 (English translation), EXT-03 ("build your own minimal agent in F#" appendix), EXT-04 (local-vs-cloud comparison), EXT-05 (Rust with a framework), EXT-06 (35B-vs-122B on Rust).

---
*Archived: 2026-06-01 as part of v1.2 milestone completion.*
