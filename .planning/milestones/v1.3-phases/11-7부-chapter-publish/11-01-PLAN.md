---
phase: 11-7부-chapter-publish
plan: 11-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ch07-scala-calc/intro.md
  - src/ch07-scala-calc/planning.md
  - src/ch07-scala-calc/writing.md
  - src/ch07-scala-calc/build-test.md
  - src/ch07-scala-calc/final.md
  - src/SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "src/ch07-scala-calc/ contains five chapter files mirroring the 4부/6부 structure (intro / planning / writing / build-test / final)"
    - "src/SUMMARY.md has a 7부 section with heading '# 7부: 다른 워킹 예제 - Scala 계산기', placed AFTER the 6부 block (ending at the 완성된 Rust 서버 entry) and BEFORE the --- that precedes 부록 A/B/C, with five working entries"
    - "Every code quote, event number, timing figure, error message, and capability claim in 7부 traces verbatim to captured-scala/ evidence under .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/; sections lacking real backing are omitted, not invented"
    - "The 70-line Calc.scala is quoted byte-identical to final-source/Calc.scala (post-fix var pos version); the one genuine compile error (private member access from @main) is quoted verbatim from the manifest/task3 JSONL with the agent's own sed self-correction"
    - "A short ADT-vs-FsLex contrast note is present: 4부 needed a scaffolded FsLex .fsl DSL (94+27+16 failed TerminalActions across 3 invocations); 7부's Scala used hand-rolled recursive descent written unaided — sourced to the manifest Comparison Hook and 4부"
    - "사용자 프롬프트 / 내부 프로세스 / 결과 callouts are used WITHOUT pictograph emoji; concept↔action callouts pair agent actions with 1부/2부 concepts; event-number citations use the MANIFEST's 1-based positions; the v1 ~14–32s/call figure NEVER appears as a measurement"
  artifacts:
    - path: src/ch07-scala-calc/intro.md
      provides: "7부 예제 프로젝트 소개 (model openai/qwen-35b, run 2026-06-01, std-only Scala 3 recursive-descent calculator goal; calculator-trilogy thesis from manifest Comparison Hook)"
      min_lines: 50
    - path: src/ch07-scala-calc/planning.md
      provides: "Task decomposition (task1 scaffold / task2 write-calc unaided / task3 buildtest) with real event-count citations and 사용자 프롬프트/내부 프로세스/결과 callout"
      min_lines: 50
    - path: src/ch07-scala-calc/writing.md
      provides: "The 70-line Scala 3 calculator narrated, code quoted byte-identical from final-source/Calc.scala; scaffold-invoked NO / did-write-calc-unaided YES disclosed"
      min_lines: 60
    - path: src/ch07-scala-calc/build-test.md
      provides: "The one genuine compile error (private var pos accessed from @main) + agent's own sed fix + three canonical tests 14/20/5 + host re-run"
      min_lines: 60
    - path: src/ch07-scala-calc/final.md
      provides: "Final 70-line Calc.scala verbatim + honest timing (manifest Timing Summary) + ADT-vs-FsLex contrast + Sources/출처 bibliography"
      min_lines: 60
    - path: src/SUMMARY.md
      provides: "7부 section with five ch07-scala-calc entries"
  key_links:
    - from: src/ch07-scala-calc/build-test.md
      to: .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/logs/task3-buildtest.jsonl
      via: "verbatim compile error at events #6/#7, agent sed fix at #8/#9, canonical tests at #10/#11 (14), #12/#13 (20), #14/#15 (5)"
    - from: src/ch07-scala-calc/writing.md
      to: .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/final-source/Calc.scala
      via: "70-line calculator quoted byte-identical (post-fix var pos version)"
    - from: src/ch07-scala-calc/final.md
      to: src/ch04-calculator/intro.md
      via: "ADT-vs-FsLex contrast note referencing 4부's scaffolded FsLex DSL and the 94+27+16 failed-TerminalAction record"
    - from: src/SUMMARY.md
      to: src/ch07-scala-calc/*.md
      via: "five 7부 list entries, after the 6부 block, before the ---/부록 block"
  key_links_note: "CAPTURE-MANIFEST.md is AUTHORITATIVE for all event-number citations (1-based JSONL positions). transcript.md is a prose/ordering scaffold ONLY — never quote an event number, timing, error message, or line of code from it that is not independently present in CAPTURE-MANIFEST.md, the JSONL, or final-source. .github/workflows/deploy.yml MUST NOT be modified."
---

<objective>
Write a new 7부 chapter group, "다른 워킹 예제: Scala 계산기", documenting the Phase 10 35B Scala 3 calculator run, and wire all five sections into src/SUMMARY.md.

Purpose: Adds a third, real "다른 워킹" worked example (Scala, completing the calculator trilogy F#→Rust→Scala) built entirely from captured evidence, satisfying CHAP-01 and CHAP-02.
Output: src/ch07-scala-calc/{intro,planning,writing,build-test,final}.md + a 7부 section in src/SUMMARY.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# THE ONLY allowed evidence source (quote from these; do NOT invent).
# All under .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/ (git-tracked).
# NOTE: this directory is NOT at the repo root — use the full path below.
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/CAPTURE-MANIFEST.md
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/logs/task1-scaffold.jsonl
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/logs/task2-write-calc.jsonl
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/logs/task3-buildtest.jsonl
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/final-source/Calc.scala
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/final-source/Hello.scala
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/test-output.txt
@.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/transcript.md

# Structure + style precedent to MIRROR (the 6부 Rust chapter — same 5-file shape, just-shipped template):
@src/ch06-rust-server/intro.md
@src/ch06-rust-server/planning.md
@src/ch06-rust-server/writing.md
@src/ch06-rust-server/build-test.md
@src/ch06-rust-server/final.md

# The 4부 F# calculator chapter (SAME GOAL, different language — source for the ADT-vs-FsLex contrast note):
@src/ch04-calculator/intro.md
@src/ch04-calculator/final.md

# Wiring target + concept source for concept↔action callouts:
@src/SUMMARY.md
@src/ch01-agentic-ai/concepts.md
@src/ch02-openhands/agent-loop.md
@src/ch02-openhands/actions-observations.md
</context>

<honesty_discipline>
HARD CONSTRAINTS (carried from v1/v1.1/v1.2/Phase 10):
- Quote ONLY from the captured-scala evidence listed above. NO fabricated event numbers, timings, error messages, code, or capability claims.
- EVENT NUMBERS: cite the CAPTURE-MANIFEST.md's (1-based) event numbers — these are authoritative. Do NOT use any 0-based / +1-offset per-task scratchpad numbering. The specific manifest-cited events you will use:
  - task1: #6 (scala-cli --version → 1.14.0 / 3.8.3), #12 (writes Hello.scala with `@main def hello()`), #16/#17 (`scala-cli run Hello.scala` → SCALA_OK, exit=0).
  - task2: #6 (first Calc.scala write attempt), #30/#32/#34/#36/#38 (printf section writes completing the file), #40/#41 (`cat Calc.scala` verification, exit=0, 70-line Scala 3 calculator). did-write-calc-unaided=YES, scaffold-invoked=NO, unaided-attempts=1.
  - task3: #6/#7 (run `2+3*4` → COMPILE ERROR exit=1, `private var pos` accessed from @main), #8/#9 (agent's OWN `sed -i '' 's/private var pos = 0/var pos = 0/' Calc.scala`, exit=0), #10/#11 (`2+3*4`→`14`), #12/#13 (`(2+3)*4`→`20`), #14/#15 (`10-3-2`→`5`), all exit=0.
- TIMING: use ONLY the manifest's Timing Summary table for THIS run — task1 30.5s (4 TA, 2.1s avg call), task2 174.8s/~2m55s (20 TA, 6.5s avg call), task3 29.5s (6 TA, 2.3s avg call), total active 234.8s/~3m55s (30 TA, ~5.0s avg call). Per-call min/max are in the table. The v1 ~14–32s/call figure is a v1 PREDICTION and must NEVER be cited as a measurement of any run. If you compare to v1's 35B per-call, the derived figure per 부록 C is ~5.3s/call — label it v1, never ~14–32s. (See manifest "NOTE on the legacy ~14–32s/call figure".)
- CODE: the calculator is 70 lines. Quote final-source/Calc.scala BYTE-IDENTICAL — this is the POST-FIX state where line 10 reads `var pos = 0` (NOT `private var pos = 0`). The pre-fix `private var pos = 0` text appears ONLY when narrating the task3 compile error and the sed fix; the quoted final source has plain `var pos = 0`.
- SCALA 3 IDIOM (from manifest Scala 3 Idiom Notes): entry point `@main def calc(args: String*)`; class `class ExprParser(input: String):` (significant-indentation `:` block syntax); `if/then`, `while/do`, `peek() match { case Some('+') => ... }`; hand-rolled recursive descent parseExpression(+/-) → parseTerm(*//) → parseFactor(digits/parens); correct operator precedence AND left-associativity (`10-3-2 → (10-3)-2 = 5`, NOT the right-recursive 9). adt-style: NONE — direct Int-returning methods, NO sealed trait Expr (the ROADMAP's pre-run mention of `sealed trait Expr` is a prediction; the manifest is authoritative and records adt-style: None). scala-2-vs-3-slip: NONE. Do NOT claim a sealed-trait ADT exists.
- ADT-vs-FsLex CONTRAST NOTE (CHAP-01, required): include a short sourced note contrasting the two calculator approaches. In v1 (4부) the 35B could NOT write a valid FsLex `.fsl` lexer unaided — 3 agent invocations (94+27+16 TerminalActions) all failed and the FsLex lexer was provided as a scaffold (source: manifest Comparison Hook + src/ch04-calculator/intro.md). In v1.3 (7부) the Scala calculator used a hand-rolled recursive-descent parser written unaided on attempt 1. The honest framing: the calculator is not the hard part — the parser-generator DSL (FsLex/FsYacc) was out-of-distribution for the model, while hand-rolled recursive descent is in-distribution. NOTE the nuance: the manifest records the Scala calculator has NO sealed-trait ADT (direct Int methods), so frame the contrast as "hand-rolled recursive descent + pattern matching (unaided)" vs "FsLex/FsYacc DSL (scaffolded)", NOT as "ADT vs FsLex" implying a sealed trait that isn't there.
- SCAFFOLDING: manifest records scaffold-invoked: NO / did-write-calc-unaided: YES (attempt 1). State the agent wrote the calculator unaided. The fallback scaffold (task2-calc-scaffold.txt) was staged but never invoked — disclose this honestly; do not hide it, do not inflate it.
- VERSIONS/DATES (from manifest Run Metadata): model openai/qwen-35b (Qwen2.5-35B via litellm @127.0.0.1:4000), run 2026-06-01, OpenHands SDK v1.21.0 / CLI 1.16.0, scala-cli 1.14.0, Scala 3.8.3, JDK 17.0.19. The agent run AND the host re-run (test-output.txt) are both 2026-06-01 — do not invent a separate re-run date. No `//> using scala` directive (scala-cli default 3.8.3).
- HONESTY GATE context (optional to mention): 36/36 ActionEvents source=agent, zero manual edits, host re-run reproduced 14/20/5.
- If a section would have no real backing evidence, OMIT it rather than inventing content.
- transcript.md is a prose/ordering scaffold ONLY — never quote a value from it that is not independently in the manifest/JSONL/source.
- Korean prose throughout, matching the rest of the book.
- NO pictograph emoji anywhere in callouts (post-cleanup convention — mirror the 6부 callout style exactly: blockquote `> **사용자 프롬프트**`, `> **내부 프로세스**`, `> **결과**`, `> **개념 ↔ 행동: ...**`).
- Do NOT modify .github/workflows/deploy.yml.
</honesty_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Absorb all captured-scala evidence and the 6부/4부 structure precedents</name>
  <files>(read-only) the captured-scala files under .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/ (CAPTURE-MANIFEST.md, logs/task1-scaffold.jsonl, logs/task2-write-calc.jsonl, logs/task3-buildtest.jsonl, final-source/Calc.scala, final-source/Hello.scala, test-output.txt, transcript.md), plus src/ch06-rust-server/*.md, src/ch04-calculator/intro.md, src/ch04-calculator/final.md, src/ch01-agentic-ai/concepts.md, src/ch02-openhands/*.md, src/SUMMARY.md</files>
  <action>Read everything in &lt;context&gt;. Confirm DIRECTLY from the files (do not trust memory) and note for later tasks: (a) model + run date from CAPTURE-MANIFEST.md (openai/qwen-35b via litellm @127.0.0.1:4000, run 2026-06-01, scala-cli 1.14.0, Scala 3.8.3, JDK 17); (b) the manifest's 1-based event numbers you will cite (task1 #6/#12/#16/#17; task2 #6/#30/#32/#34/#36/#38/#40/#41; task3 #6/#7 compile error, #8/#9 sed fix, #10/#11 →14, #12/#13 →20, #14/#15 →5) — confirm each against the manifest text and the JSONL; (c) the ONE verbatim compile-error block ("variable pos cannot be accessed as a member of (parser : ExprParser) from the top-level definitions in package &lt;empty&gt;." etc.) exactly as it appears in the manifest/task3 JSONL; (d) the full 70-line final-source/Calc.scala (note line 10 is `var pos = 0`, the POST-FIX state) and Hello.scala (`@main def hello() = println("SCALA_OK")`); (e) the Timing Summary table (task1 30.5s, task2 174.8s, task3 29.5s, total 234.8s, ~5.0s avg) and the manifest's NOTE forbidding the ~14–32s/call figure as a measurement; (f) the test-output.txt host re-run (2026-06-01, 14/20/5, all exit=0); (g) the manifest Comparison Hook + 4부 intro facts for the ADT-vs-FsLex note (4부: FsLex scaffolded, 94+27+16 failed TerminalActions across 3 invocations; 7부: hand-rolled recursive descent unaided, NO sealed-trait ADT); (h) the 6부 file shape/headings and exact callout style to mirror (`> **사용자 프롬프트**` / `> **내부 프로세스**` / `> **결과**` / `> **개념 ↔ 행동: ...**`, no emoji, `출처:` lines, `Sources / 출처` section); (i) the exact 1부/2부 concept names (에이전트 루프, 액션-관찰 사이클, 도구 사용/tool calling, 자가 수정) for concept↔action callouts. Do NOT write any chapter file yet.</action>
  <verify>You can state the manifest's 1-based event numbers, the verbatim compile-error text, the timing table, and the 70-line Calc.scala (with `var pos = 0` on line 10) from the actual files (not from this plan's summary) before writing.</verify>
  <done>All evidence and precedents read and cross-checked; citation facts in hand.</done>
</task>

<task type="auto">
  <name>Task 2: Write intro.md, planning.md, and writing.md</name>
  <files>src/ch07-scala-calc/intro.md, src/ch07-scala-calc/planning.md, src/ch07-scala-calc/writing.md</files>
  <action>
intro.md (예제 프로젝트 소개): introduce the Scala 3 계산기 as a third "다른 워킹" example completing the calculator trilogy (4부 F# FsLex/FsYacc scaffolded → 6부 Rust unaided → 7부 Scala unaided). State model (openai/qwen-35b via litellm proxy), run date (2026-06-01), versions (scala-cli 1.14.0, Scala 3.8.3, JDK 17), and goal (a std-only Scala 3 command-line recursive-descent arithmetic calculator: `calc "2+3*4" → 14`, `(2+3)*4 → 20`, `10-3-2 → 5`). Show the three canonical examples (as in 4부/6부 intro). Frame the chapter's thesis from the manifest Comparison Hook — the same 35B that could NOT write an FsLex lexer unaided in v1 wrote the SAME calculator unaided in Scala — but only as the manifest states it (capability is domain-distribution, not model size). Include at least one concept↔action callout linking the example to a 1부/2부 concept (e.g., 에이전트 루프 / 자가 수정). Disclose scaffolding: scaffold-invoked NO / did-write-calc-unaided YES (the fallback was staged but never invoked). Facts sourced ONLY from CAPTURE-MANIFEST.md (+ 4부 for the trilogy reference).

planning.md (태스크 계획 단계): describe the decomposition into task1 (scaffold: verify scala-cli + trivial Hello.scala), task2 (write the calculator unaided), task3 (build/test + self-correct). Give a task table with real event counts and TerminalAction counts from the manifest (task1: SCAL-01 setup, #6/#12/#16/#17; task2: unaided write; task3: error+fix+canonical tests). Include a 사용자 프롬프트 / 내부 프로세스 / 결과 callout for the task1 scaffold cycle (scala-cli --version at #6 → 1.14.0/3.8.3; Hello.scala at #12; `scala-cli run Hello.scala` → SCALA_OK at #16/#17). Include a concept↔action callout mapping task1/task2/task3 to Explore→Implement→Verify (mirror 6부 planning.md). If the JSONL line-count vs manifest event-count differs (as in 6부), add the same honest "이벤트 수·번호 표기 주의" note clarifying you cite the manifest's 1-based numbering.

writing.md (코드 작성 단계): narrate task2 writing the 70-line Scala 3 recursive-descent calculator. Open with the scaffolding disclosure (scaffold-invoked NO / did-write-calc-unaided YES / unaided-attempts 1; fallback staged but never invoked). Quote the code BYTE-IDENTICAL from final-source/Calc.scala (70 lines, with `var pos = 0` on line 10 — the final post-fix state; note in prose that task2's on-disk version had `private var pos = 0`, fixed in task3). Add a code 해설 covering the Scala 3 idioms actually present: `@main def calc(args: String*)`, `class ExprParser(input: String):` significant-indentation, `peek() match`, `while ... do` left-associative loops, parseExpression→parseTerm→parseFactor recursive descent with correct precedence; explicitly state there is NO sealed-trait Expr ADT (direct Int-returning methods). Cite manifest event numbers (#6 first write, #30/#32/#34/#36/#38 section writes, #40/#41 verification). Include a 사용자 프롬프트 / 내부 프로세스 / 결과 callout and a concept↔action callout (도구 사용/tool calling + 액션-관찰 사이클, mirror 6부 writing.md).

All three: Korean prose, no emoji in callouts, no fabricated values, manifest 1-based event numbers only.
  </action>
  <verify>Three files exist; model/date(2026-06-01)/versions/goal match the manifest; event counts/numbers match the manifest's 1-based numbering; code in writing.md matches final-source/Calc.scala byte-identical (70 lines, `var pos = 0` on line 10); "wrote unaided" + scaffold-not-invoked stated; NO sealed-trait-ADT claim; callouts present in 6부 style without emoji.</verify>
  <done>intro/planning/writing complete and traceable.</done>
</task>

<task type="auto">
  <name>Task 3: Write build-test.md and final.md, then wire SUMMARY.md</name>
  <files>src/ch07-scala-calc/build-test.md, src/ch07-scala-calc/final.md, src/SUMMARY.md</files>
  <action>
build-test.md (빌드와 테스트 단계): narrate task3. Document the ONE genuine compile error verbatim from logs/task3-buildtest.jsonl / manifest:
  - Event #6 (TerminalAction): `scala-cli run Calc.scala -- "2+3*4" 2>&1`.
  - Event #7 (ObservationEvent, exit=1) — COMPILE ERROR, quoted verbatim: the full `[error] ./Calc.scala:5:6 ... variable pos cannot be accessed as a member of (parser : ExprParser) from the top-level definitions in package <empty>. ... private variable pos can only be accessed from class ExprParser. ... Compilation failed` block.
  - Root cause (from manifest): `pos` was declared `private var pos = 0` in `ExprParser`, but the `@main` top-level function accessed `parser.pos` directly (lines 5–6). In Scala 3, top-level definitions including `@main` cannot access private members of other classes — a clean in-distribution access-modifier mistake.
  - Event #8 (TerminalAction, source=agent) — the agent's OWN fix, verbatim: `cd .../calc && sed -i '' 's/private var pos = 0/var pos = 0/' Calc.scala`. Event #9 (exit=0): sed applied (empty output). Emphasize: the agent diagnosed the private-field cause and self-corrected with its own sed — no operator intervention.
Then document the three canonical tests (post-fix), citing manifest events:
  - #10/#11 (exit=0): `2+3*4` → `14` (event #11 verbatim content: `Compiling project (Scala 3.8.3, JVM (17))` / `Compiled project (Scala 3.8.3, JVM (17))` / `14`).
  - #12/#13 (exit=0): `(2+3)*4` → `20` (cached compilation — only result printed).
  - #14/#15 (exit=0): `10-3-2` → `5` (left-associative: (10-3)-2=5, not 9).
Cross-reference test-output.txt (host re-run 2026-06-01T06:03:25Z, scala-cli 1.14.0 / Scala 3.8.3: 14 / 20 / 5, all exit=0; host-rerun-matches-agent-capture YES). Use 사용자 프롬프트 / 내부 프로세스 / 결과 callouts for the error-and-fix cycle. Include a concept↔action callout tying the compile-error-read-and-self-correct to the 2부 액션-관찰 사이클 + 1부 자가 수정 (mirror 6부 build-test.md). No emoji.

final.md (완성된 Scala 계산기): present the complete final-source/Calc.scala (70 lines, verbatim, `var pos = 0` on line 10) and note Hello.scala (`@main def hello() = println("SCALA_OK")`). Summarize the honest outcome (compile PASS after 1 self-corrected error; 14/20/5 all pass; std-only, no external deps, no sealed-trait ADT). Give a timing summary FROM THE MANIFEST's Timing Summary table (task1 30.5s, task2 174.8s/~2m55s, task3 29.5s, total active 234.8s/~3m55s, ~5.0s avg LLM-call; per-call min/max from the table). Include the honest-labeling note: the v1 ~14–32s/call figure is a v1 PREDICTION, never a measurement (mirror 6부 final.md's "솔직한 표기 주의"); if comparing to v1's 35B per-call, use the derived ~5.3s/call (부록 C), labeled v1. Include the ADT-vs-FsLex CONTRAST NOTE (CHAP-01): 4부 needed a scaffolded FsLex `.fsl` DSL (the 35B's 3 invocations of 94+27+16 TerminalActions all failed to produce a valid `.fsl`); 7부's Scala calculator was hand-rolled recursive descent written unaided on attempt 1 — frame as "hand-rolled recursive descent + pattern matching (unaided)" vs "FsLex/FsYacc DSL (scaffolded)", honestly noting the Scala calc has NO sealed-trait ADT. State the calculator-trilogy thesis (F# scaffolded → Rust unaided → Scala unaided; capability is domain-distribution, not model size), sourced to the manifest Comparison Hook. End with a Sources / 출처 section listing the captured-scala paths actually quoted, each with the .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/ prefix: CAPTURE-MANIFEST.md, logs/task1-scaffold.jsonl, logs/task2-write-calc.jsonl, logs/task3-buildtest.jsonl, final-source/Calc.scala, final-source/Hello.scala, test-output.txt.

SUMMARY.md: insert a new section with the exact heading `# 7부: 다른 워킹 예제 - Scala 계산기` AFTER the "# 6부: 다른 워킹 예제 - Rust HTTP 서버" block (which ends with the `[완성된 Rust 서버](ch06-rust-server/final.md)` entry) and BEFORE the "---" separator that precedes the 부록 A/B/C links (so 부록 stay last). Add exactly these five entries (slugs MUST match the files created in Tasks 2/3):
  - [예제 프로젝트 소개](ch07-scala-calc/intro.md)
  - [태스크 계획 단계](ch07-scala-calc/planning.md)
  - [코드 작성 단계](ch07-scala-calc/writing.md)
  - [빌드와 테스트 단계](ch07-scala-calc/build-test.md)
  - [완성된 Scala 계산기](ch07-scala-calc/final.md)
  </action>
  <verify>The compile error is quoted verbatim with manifest event numbers (#6/#7) and the agent's own sed fix (#8/#9); the three canonical tests cite #10/#11 (14), #12/#13 (20), #14/#15 (5); test-output.txt figures match (14/20/5, 2026-06-01); final Calc.scala verbatim (70 lines, `var pos = 0`); timing matches the manifest table and the ~14–32s figure is absent as a measurement; ADT-vs-FsLex contrast note present and honest (no false sealed-trait claim); Sources lists the real .planning/phases/10-.../captured-scala/ paths; SUMMARY.md 7부 section uses the exact heading, is after 6부 and before the ---/부록 block, with the five entries.</verify>
  <done>build-test/final complete; SUMMARY wired correctly.</done>
</task>

<task type="auto">
  <name>Task 4: Honesty + convention audit</name>
  <files>(audit) src/ch07-scala-calc/*.md, src/SUMMARY.md</files>
  <action>Re-scan every newly written 7부 file against the actual captured-scala files. Confirm zero fabricated content: every code block, event number, error message, timing figure, date, version, and capability claim is traceable to the evidence (cross-check directly). Specifically verify: (1) event numbers use the MANIFEST's 1-based positions (compile error #6/#7, sed fix #8/#9, tests #10–#15) — no 0-based/off-by-one scratchpad numbering; (2) the ~14–32s/call figure does NOT appear as a measurement of any run (it may appear only as an explicitly-labeled v1 prediction being corrected, mirroring 6부); (3) the final Calc.scala is byte-identical to final-source/Calc.scala (70 lines, `var pos = 0` on line 10, NOT `private var pos = 0`); (4) the compile error text is verbatim; the agent's sed fix is the verbatim `s/private var pos = 0/var pos = 0/`; (5) no false sealed-trait-ADT claim — the ADT-vs-FsLex note is framed honestly (recursive descent unaided vs FsLex scaffolded); (6) the 94+27+16 FsLex-failure figure traces to the manifest Comparison Hook / 4부; (7) run date 2026-06-01 throughout (agent run and host re-run both 2026-06-01); (8) NO pictograph emoji in any callout, and callouts use the 6부 style (사용자 프롬프트 / 내부 프로세스 / 결과 + 개념 ↔ 행동); (9) a Sources/출처 section exists with real captured-scala/ paths; (10) .github/workflows/deploy.yml was NOT modified. Fix any drift found.</action>
  <verify>No untraceable or stale values; manifest 1-based event numbers used; ~14–32s never a measurement; final code byte-identical (var pos); both error+fix verbatim; ADT-vs-FsLex note honest; no emoji in callouts; Sources present; deploy.yml untouched.</verify>
  <done>7부 passes the honesty + convention audit.</done>
</task>

</tasks>

<verification>
- Five files exist under src/ch07-scala-calc/ (intro, planning, writing, build-test, final).
- src/SUMMARY.md has a 7부 section (heading "# 7부: 다른 워킹 예제 - Scala 계산기") after the 6부 block and before the ---/부록 block, with five entries matching the created filenames.
- The one genuine compile error is documented verbatim with manifest event numbers (#6/#7) and the agent's own sed self-correction (#8/#9); the three canonical tests cite #10–#15 with results 14/20/5.
- Final Calc.scala quoted byte-identical from final-source/Calc.scala (70 lines, post-fix `var pos = 0`).
- Timing figures come from the manifest Timing Summary; the ~14–32s/call figure never appears as a measurement (only as an explicitly-labeled v1 prediction, if at all).
- The ADT-vs-FsLex contrast note is present, sourced, and honest (no false sealed-trait claim; FsLex 94+27+16 failed-TA record cited).
- All callouts use 사용자 프롬프트 / 내부 프로세스 / 결과 with NO emoji; concept↔action callouts present.
- No fabricated content; every quoted value traceable to .planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/.
- .github/workflows/deploy.yml not modified.
</verification>

<success_criteria>
CHAP-01 and CHAP-02 satisfied: 7부 chapter exists under src/, wired into SUMMARY.md, structurally parallel to 4부/6부, no fabricated content; callouts present without emoji; concept↔action callouts to 1부/2부; ADT-vs-FsLex contrast note present; the one real compile error + self-correction documented from the JSONL; all facts (paths, manifest 1-based event numbers, dates, versions, code, timing) match the captured evidence; ~14–32s/call never cited as a measurement.
</success_criteria>

<output>
After completion, create .planning/phases/11-7부-chapter-publish/11-01-SUMMARY.md
</output>

## Reviewer Notes

In plain language: every concrete claim in 7부 must come from the real captured Scala run, not from imagination or "what a nice tutorial would say." The only sources the writer may quote live under `.planning/phases/10-capture-the-35b-scala-calculator-run/captured-scala/` — the manifest (authoritative for event numbers), the three task JSONL logs (`task1-scaffold`, `task2-write-calc`, `task3-buildtest`), the final `final-source/Calc.scala` and `final-source/Hello.scala`, and the host re-run `test-output.txt`. The `transcript.md` may guide ordering and phrasing, but no number, error message, or line of code may come from it unless that value also appears in the manifest, JSONL, or source. When reviewing, spot-check: the calculator should match `final-source/Calc.scala` character-for-character (70 lines, line 10 = `var pos = 0`, the post-fix state); the one genuine compile error (`variable pos cannot be accessed as a member of (parser : ExprParser) from the top-level` at manifest events #6/#7) and the agent's own `sed` fix (`s/private var pos = 0/var pos = 0/` at #8/#9) must appear with the actual compiler text; the three canonical tests (14/20/5) tie to events #10–#15; timing comes from the manifest table (~5.0s/call avg, 234.8s active) and the v1 `~14–32s/call` figure must be absent as a measurement (it is a v1 prediction only). The ADT-vs-FsLex contrast note is required by CHAP-01 — but it must be honest: the manifest records the Scala calculator has NO sealed-trait ADT (direct `Int` methods), so the contrast is "hand-rolled recursive descent (unaided)" vs "FsLex/FsYacc DSL (scaffolded, 94+27+16 failed TerminalActions)", not a fictional sealed trait. Callouts use 사용자 프롬프트 / 내부 프로세스 / 결과 with no pictograph emoji (mirror 6부 exactly), and the deploy workflow must not be touched in this plan.
</output>
