---
phase: 11-7부-chapter-publish
plan: 11-02
type: execute
wave: 2
depends_on: [11-01]
files_modified: []
autonomous: true
must_haves:
  truths:
    - "mdbook build completes with zero errors and exit code 0"
    - "Zero NEW warnings appear; the only acceptable warning is the pre-existing 부록 C <char> warning (TD-4)"
    - "7부 renders to HTML and is reachable from the sidebar nav; all 7부 links resolve"
    - "1부–6부 + 부록 A/B/C still render correctly and are not regressed"
  artifacts:
    - path: book/ch07-scala-calc/intro.html
      provides: "Rendered 7부 intro (proof the chapter built)"
    - path: book/ch07-scala-calc/build-test.html
      provides: "Rendered 7부 build-test section"
    - path: book/ch07-scala-calc/final.html
      provides: "Rendered 7부 final section"
  key_links:
    - from: src/SUMMARY.md
      to: book/ch07-scala-calc/*.html
      via: "mdbook build resolves all 7부 entries with no broken-link warnings"
  key_links_note: "This plan modifies NO source files. If the build errors or emits a NEW warning, report it so 11-01 can be corrected — do not silently edit chapter content here."
---

<objective>
Run mdbook build and confirm zero errors and zero NEW warnings, that 7부 renders and links resolve, and that 1부–6부 + 부록 A/B/C are not regressed.

Purpose: Satisfies PUB-01 — the build gate that must pass before publishing.
Output: A verified-clean local build under book/, with 7부 HTML present.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the book and confirm zero errors</name>
  <files></files>
  <action>Run `mdbook build` from /Users/ohama/projs/OpenHandsTests/ and capture full stdout+stderr. Confirm exit code 0 and zero errors. If `mdbook` is not on PATH, locate it the same way prior phases did (it was available and used in Phase 9 for the 6부 build) before failing.</action>
  <verify>mdbook build exits 0 with zero errors.</verify>
  <done>Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm zero NEW warnings</name>
  <files></files>
  <action>Inspect the captured build output for warnings. Confirm the ONLY warning present is the pre-existing 부록 C &lt;char&gt; warning (TD-4, from src/appendix-c-comparison.md). Assert there are zero NEW warnings — in particular, no broken-link or missing-file warnings for the new 7부 entries added to SUMMARY.md (ch07-scala-calc/intro.md, planning.md, writing.md, build-test.md, final.md). If any new warning appears, report it as a failure (do NOT edit chapter content in this plan).</action>
  <verify>Only the pre-existing &lt;char&gt; warning appears; no new warnings.</verify>
  <done>No new warnings introduced by 7부.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 7부 rendered and existing parts not regressed</name>
  <files></files>
  <action>Confirm book/ch07-scala-calc/ contains intro.html, planning.html, writing.html, build-test.html, and final.html, and that the 7부 entries appear in the generated sidebar nav (e.g., grep the 7부 title / ch07-scala-calc links in a generated HTML page). Spot-check that 1부–6부 (including book/ch06-rust-server/*.html) and 부록 A/B/C HTML still exist and render (not regressed).</action>
  <verify>book/ch07-scala-calc/*.html exist; 7부 present in sidebar; 1부–6부 + appendices still rendered.</verify>
  <done>7부 reachable in nav; no regression to existing content.</done>
</task>

</tasks>

<verification>
- mdbook build exits 0 with zero errors.
- Only the pre-existing 부록 C <char> warning present; zero NEW warnings.
- book/ch07-scala-calc/ HTML generated for all five sections; 7부 reachable in sidebar.
- 1부–6부 + 부록 A/B/C still render (no regression).
</verification>

<success_criteria>
PUB-01 satisfied: clean build (zero errors, zero new warnings), all 7부 links resolve, existing parts not regressed.
</success_criteria>

<output>
After completion, create .planning/phases/11-7부-chapter-publish/11-02-SUMMARY.md
</output>

## Reviewer Notes

In plain language: this plan touches no content — it only proves the book still builds cleanly after 7부 was added. A human reviewer should confirm `mdbook build` finished with no errors and that the only warning is the long-standing cosmetic `<char>` warning from 부록 C (TD-4); any brand-new warning (especially a "broken link" or "missing file" for one of the new 7부 pages) means the SUMMARY wiring or a filename is wrong and must go back to plan 11-01. The honesty rule here is procedural: if the build is not actually clean, the executor must report that failure rather than quietly editing chapter files to make it pass.
</output>
